# full_project SW 구조 — 세션 인계 문서 (Handoff)

> 2026-07-26 (KST) 작성. 이 작업은 원래 다른 주제(Tailscale/SSH 셋업) 세션에서 우발적으로 시작됐고, 주제 혼선을 피하기 위해 **Big-AMR 전용 새 세션에서 이어가기로** 결정됨. 본 문서는 그 인계용 상태 스냅샷이다.

## 1. 배경 / 목적

참조 프로젝트 **LGIT-C6-Cobot**(`amap@amap-1:/home/amap/T-Robotics/LGIT-C6-Cobot /docs/sw_structure/full_project/`)의 full_project SW 구조 문서 방식을, 동일 SOP(이 프로젝트 `docs/claude_guideline/sw_structure/structure.md`)로 **Big-AMR(Ford-CATL AMR retrofit)** 코드에 적용하는 작업.

- 범위 결정: **전체 시스템(13 ROS2 패키지 전부)**, 참조 full_project 수준.
- 시스템 성격: 레거시 체인 `ACS→PLC→Seer→AMR(motor)` 를 불가침으로 두고, Seer↔모터 CAN 구간에 **Black Panda(CAN relay)** 인라인 삽입 + 자율주행 PC(Jetson Orin NX/Neousys) 를 종속 주행원으로 얹는 리트로핏. 배경은 `docs/sw_structure/system-architecture/2026-06-27.md`(결정로그 D1~D17) 참조.

## 2. 이미 생성된 산출물 (KEEP — 되돌리지 않음)

**루트 정본** `docs/sw_structure/full_project/`:
- `2026-07-26.md` — ① 파일 의존 그래프(48노드/59엣지, 5 도메인 subgraph) ② 클래스 관계도(51노드/77엣지) ③ 시퀀스 3종(구동명령·안전정지·trnav 모션) ④ 연결 관계표(파일 / 클래스 / 런타임 topic·service·action, file:line 근거) ⑤ 구조 관찰 + 부록 A
- `2026-07-26-file-graph.drawio` / `-class.drawio` / `-sequence.drawio` — `drawio_validate.py` 통과(dangling 0), mermaid↔drawio 1:1(59/59·77/77·20/20)
- `README.md` — 타임라인 인덱스

**패키지 병기(이중기록)**: 코드가 있는 13개 패키지 루트 각각에 `docs/sw_structure/full_project/2026-07-26.md`(발췌) + `README.md`.

## 3. 실측 커버리지 (중요 — 다음 세션 검증 대상)

**코드 존재·실측 완료 (13패키지)**: motor_control, trnav 6종(msgs·interfaces·motion_core·qd_kinematics·motion_qd·motion_action_server), iahrs_driver·interfaces(IMU), sick_safetyscanners2·dual_laser_merger·lidar_calibration_2d·seer_lidar_tf(Lidar 2D).

**stub (빈 디렉토리, 파일 0 — 문서에 "미구현"으로 기록됨)**:
`Comm/CAN/can_relay`(Black Panda relay), `Control/Seer`, `Safety`, `Sensors/Lidar/3D`, `Control/Motion_Control/{2WS,4IS,DD}`.
→ 아키텍처 정본(2026-06-27)의 Seer·Panda·Safety PLC 체인은 **아직 코드 미구현**.

## 4. 다음 세션이 확인/이어갈 사항 (미검증 관찰 — 재확인 필요)

1. **모션↔구동 도메인 미연결(실측 주장, 검증 필요)**: trnav 액션서버는 `WheelSetArray`를 `/motion/wheel_cmd/<action>`에 발행하고 `/select_motion_source`를 client 호출하나 in-repo 에 구독자·mux 서버가 없음. motor_control 은 `/cmd_vel`(Twist) 구독. 두 도메인의 브리지(mux)가 미구현/외부인지 확인.
2. **유일 교차 토픽 `imu/data`** 외 도메인 간 연결이 정말 없는지 재확인.
3. **stub 5개** 구현 시점에 문서 갱신(신규 날짜 파일).
4. 구조 사실: 순환 의존 0(DAG), 고립 노드 3개(dual_laser_merger.cpp·tf_publisher_node.cpp·seer_lidar_tf_node.py — 외부 라이브러리만 의존).
5. **작성자-검토자 분리 원칙(OMC)**: 위 문서는 authoring 패스 산출물. 다음 세션에서 `code-reviewer`/`verifier` 등 별도 패스로 실측 정확성(file:line 대조)을 검증할 것.

## 4-bis. 검증 세션 결과 (2026-07-26, sess:36b811e1 — 위 §4 이어받음)

작성자-분리 검토 패스 완료. 상세는 `2026-07-26.md` §검증 패스 절.

- **§4-1 모션↔구동 미연결: 확정.** `src` 전역 grep — `/motion/wheel_cmd/*` in-repo 구독자 0, `/select_motion_source` in-repo 서버 0(9서버 전부 client), motor_control 은 `cmd_vel`(Twist)만·wheel_cmd/WheelSetArray 0. **mux 는 외부/미구현**.
- **§4-2 imu/data 유일 교차 토픽: 확정.** iahrs pub `imu/data`(:52) ↔ qd_action_server_base sub(:82). 그 외 도메인 간 공유 토픽 없음.
- **§4-3 stub 5(+2): 코드 0 재확인** — 7 디렉토리(`can_relay`·`Seer`·`Safety`·`Lidar/3D`·`Motion_Control/{2WS,4IS,DD}`) 미구현 유지 기록.
- **④ 표 정확도: ~178 주장 중 173 정확(≈97%).** 5건 정정 반영(IMU service `all_data_reset`·sub `imu/reset`, sick `m_msg_creator` 기반 상속, motor_control `cmd_vel`/`estop` 상대명 표기). 루트 정본 + IMU 2·motor_control 패키지 병기본 동시 갱신.

## 5. SOP 준수 체크

- `docs/claude_guideline/sw_structure/structure.md` 의 ①~⑤ 산출물 + .drawio + 이중기록 + Step 10 self-check grep 수행됨.
- 재작업/갱신 시에도 동일 SOP를 **응답 전 의무 선행 Read** 후 진행할 것(CLAUDE.md `kuks_agent_setup:sw_structure`).
