---
id: 2026-09-02-001
target: ford-catl-orin
kind: 실행
status: open
created: 2026-09-02
done_when: Big-AMR-deploy 가 이 커밋을 포함한 origin/main 으로 갱신·빌드되고, can_relay 유닛 재기동 후 ① `python3 -m pytest -q test/test_link.py -k "atomic or final_heartbeat"` 2 passed 출력 ② `systemctl status` 두 유닛 active 를 수행 기록에 남긴다
evidence: (완료 시 기입)
---

# can_relay USB 링크 안전 수정 이식분 — Orin 배포·확인

## 지시 원문

사용자(sess:336c1015, 2026-09-02): "그리고 ford_catl pc에도 이식 가능?" → 패치 선별
이식안 보고 → "승인 진행하고 remote instruction 폴더에 작업 내용과 후속 조치에 대해서
알려주세요"

## 배경

- LGIT-C6 저장소의 engage USB 재개방(LIBUSB_ERROR_BUSY) 결함 수정(2026-09-02, LGIT 커밋
  005cf76) 과정에서 외부 검토(Codex)가 잡은 경계 결함 2건이 본 저장소 `link.py` 에도
  동일하게 존재해 이식·수정했다:
  1. `acquire()` 마지막 heartbeat 가 트랜잭션 밖 — 심박 실패 시 intercept 를 무장한 채
     방치(펌웨어 fail-safe 는 켜지지 않은 상태).
  2. `open()` 비원자 — `health()` 검증 실패 시 부분 초기화 핸들이 `_panda` 로 공표됨.
- 수정·검증 내역: `code_updates/2026-09-02-can-relay-usb-link-atomic-port.md`
  (SIL red→green 2건, 전체 회귀 신규 실패 0).
- 개발 PC 에서 2026-09-02 08:2x Orin(100.92.214.74) ssh 타임아웃 실측 — 도달 불가라
  본 지시서로 이관한다. LGIT 실기와 달리 **Orin 실기(HIL) 검증은 미실시** 상태다.

## 작업 내용 (개발 PC 측, 완료)

- `src/Comm/CAN/can_relay/can_relay/link.py` — `open()` 원자화 · `acquire()` 트랜잭션화.
- `src/Comm/CAN/can_relay/test/test_link.py` — 회귀 2건 + 대역(`_FakeHandle`/`_FakePanda`).
- `src/Comm/CAN/can_relay/docs/function_table.md` — §6 부분 등재(open:394 · acquire:425).
- origin/main 커밋·push 완료(커밋 해시는 git log 의 `Session: 336c1015` trailer 로 식별).

## 후속 조치 (Orin 측, 이 지시의 수행 대상)

`docs/deployment/2026-08-16-can-relay-supervision-deploy.md` 의 절차를 따른다:

1. `~/Project/Ford-CATL-AMR/Big-AMR-deploy` 에서 `git fetch` →
   `git checkout --detach origin/main` (이 커밋 포함 확인: `git log -1 --format=%h·%s`).
2. `colcon build --packages-select trnav_msgs can_relay --symlink-install`.
3. `install_service.sh --apply` — **sudo 필요**(사용자 개입).
4. **유닛 재기동 전 운용 확인** — 가동 중 유닛의 재기동 시점은 운용자(사용자) 판단.
   재기동 후 `systemctl status` 두 유닛 active 확인.
5. SIL 확인: can_relay 패키지에서
   `python3 -m pytest -q test/test_link.py -k "atomic or final_heartbeat"` → 2 passed.
6. 추가 점검(조사): Orin 벤더 panda(`vendor/panda/python/__init__.py`)에 송신 재시도 상한
   `CAN_SEND_RETRY_MAX` 가 있는지 확인해 기록한다 — LGIT 벤더 판에는 있음(:584 실측),
   개발 PC dev 미러의 can_relay 에는 vendored panda 가 없어 실물 확인이 필요하다.

## 수행 기록

(Orin 측 수행 시 기입)
