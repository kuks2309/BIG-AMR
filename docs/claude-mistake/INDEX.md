# Claude 실수 기록 INDEX

## 메타 패턴

- **고치면서 「무엇을 고쳤는지」를 고친 자리에 적었다 — 감사 1라운드를 자초했다 (2026-08-13-001)** —
  2WS 주석 감사에서 1·2라운드 111건을 고쳐 커밋했는데, 그 정정문 상당수가 코드가 지금 무엇을
  하는지가 아니라 **내가 무엇을 고쳤는지**였다(「종전 문구 …는 2026-08-10 교체됐고」·
  「그 불일치는 실재했고(2026-08-08 확인) … 정정해 해소했다」·「… 는 이 저장소에 없다
  (git 이력에도 0건)」·「In-repo trace: …code_updates.md」). 사용자 지적은
  「**기능만 넣기 이력은 넣지마시길**」 · 「**코드의 주석은 이력을 넣지 않습니다. 대신 code update
  문서에서 만드는 것이 주석으로 인한 오류를 줄일 수 있습니다**」 였다.
  ⚠ 주목할 점 — **지식 공백이 아니었다.** 「주석은 현재 동작을 적는 곳」은 알고 있었고, 이력이 갈
  곳(`docs/*_code_updates.md`)을 **같은 커밋에서 내가 쓰고 있었다.** 그런데도 같은 내용을 주석에
  중복해 넣었다 — **감사 근거를 남기려는 의도가 원칙을 이겼다.** 대가는 즉시 나왔다: 3라운드
  라인 단위 재독(16,174줄) 69건 중 **22건이 1·2라운드가 심은 이력**이라 그 라운드의 **약 1/3 이
  자초한 것**이었고, 심어 놓은 날짜 하나는 **그 자리에서 이미 틀려 있었다**(「2026-08-06 결정」 ↔
  실제 커밋 `e0064d5` **2026-08-05**). **낡음에서 주석을 구하려고 넣은 문장이 새로운 낡음이 됐다.**
  → **닫은 방법**: 이력 22건 전량 제거·`code_updates` 로 이관(잔존 스캔 0건), `debt-071`(지능)에
  **판정 지점 한 줄**을 고정 — 「이 문장이 1년 뒤에도 참인가」가 아니면 주석이 아니라 code_updates
  소관이고, **부재 사실은 서술하지 않고 인용을 삭제한다.** 상위 문제는 `debt-070`(이해)로 분리
  등록 — 이번 감사가 찾은 주석↔코드 불일치가 **180건(≈90줄당 1건)**인데 **빌드·테스트가 하나도
  검출하지 못한다**(67 tests PASS 상태에서 전부 살아 있었다). **검사기는 아직 없다 — 설치 전까지
  이 부채는 수동 준수로만 지켜진다.** 형제 QD 스택에도 같은 잔재가 있으나 **여기서 감사하지 않는다**
  — LGIT 검증 후 이식 예정이므로 통제점은 **이식 시점 게이트**(이식본이 검사기를 통과하기 전 머지 금지)다.

- **보유 원자료를 조사 후보에 넣지 않는다 — 「없다」 계열의 **네 번째**이자, 이번엔 방향이 반대
  (2026-08-07-002)** — Seer 포트 동시연결 한도를 정해야 하는데 동봉 PDF(v1.2.1)는 **1**,
  최신 문서(v1.4.2)는 **5** 라 갈렸다. 나는 **보수값 1 을 코드에 박고**, 미확정을 debt-043 으로
  등록하고, **벤더 문의 항목 7개를 복붙용으로 정리해 사용자에게 올렸다**("문서 PDF 하나만 받아도
  4개가 해결된다"는 조언까지 덧붙였다). 사용자 지적 한 줄 —
  「**amap-server에 63g하드에 소스코드가 잇는데 구지 물어봐야 하는지?**」
  30분 만에 전부 나왔다: 한도는 `robot.param`(SQLite) `NetProtocol` 테이블에 **19204·19301=10,
  나머지=5**, 초과 거동은 `libNetProtocol.so` 문자열 + 실기 실측으로 **거부형**(기존 연결 생존,
  `ret_code 61001`). 그 하드의 `product.version.h` 는 **실기와 같은 `3.4.5.22`** 였다.
  ⚠ 주목할 점 — **문제 설정 자체가 틀렸다.** "판본을 알아야 한도를 안다"가 전제였는데, 한도는
  판본이 정하는 상수가 아니라 **런타임 파라미터**(`minValue` 1 ~ `maxValue` 20)였고
  **API 1400 으로 실기에 직접 물어볼 수 있었다** — 내가 그날 짠 라이브러리로 한 줄이면 되는 일이었다.
  앞선 세 건(2026-08-03-002 · 2026-08-05-001 · 2026-08-06-004)은 「문서에 없으니 없다」였고
  이번은 「문서가 애매하니 벤더에 묻자」로 **방향만 반대, 공백은 동일**하다. 특히
  **2026-08-06-004 와 같은 장비·같은 하드**였고 그때 만든 래퍼 `Tools/seer_re/amap_server.sh` 와
  메모리 2건이 이미 있었는데 열지 않았다 — 그 하드를 「측위 리버스 자산」으로 태그해 둬서
  「API 포트 설정」이라는 검색 키에 걸리지 않았다.
  그리고 **틀린 사실 위에 강제 장치를 얹었다** — `EXCLUSIVE_PORTS` 를 한도에서 `n <= 1` 로
  파생시켰기 때문에, 값을 실측(5)으로 고치는 순간 집합이 **빈 집합**이 되어 게이트가 조용히
  사라질 뻔했다.
  → **닫은 방법**(「원본을 보라」를 또 적지 않는다): ① `Tools/seer_re/seer_param.sh` — 실기 1400 과
  원본 하드 `robot.param` 을 **양쪽 다** 조회해 나란히 찍는 실행 파일(30초). ② 코드가 상수를
  진실로 주장하지 않게 — `SeerApi.get_max_connections()` 가 로봇에 묻고, `OBSERVED_MAX_CONNECTIONS`
  는 docstring 에서 스스로 「정본 아님」을 선언(돌연변이 `A13` 이 상수 우회를 차단).
  ③ 파생 게이트 → **명시 게이트**(`GUARDED_PORTS`), 근거도 「동시연결 1」(반증됨) → 「지령 중재」
  (반증 불가)로 교체(돌연변이 `P2` 가 파생 회귀를 차단).

- **강제 장치가 있는데 「배포」와 「데이터」가 없어 빈 통과한다 (2026-08-06-003)** —
  coding SOP §2(함수표 선독)·§3(공개 API ADR)·§5(자기승인 금지)·§6(표 갱신)을 전부
  건너뛰고 구현·커밋·푸시했다. 사용자 지적은 「**규칙을 매번 어기네요**」였다.
  ⚠ 주목할 점 — **주의력 문제가 아니었다.** 차단용 훅 `coding-inventory-gate.py` 는
  **존재하고 `.claude/settings.json` 에 등록까지 돼 있다.** 그런데 두 겹으로 무력했다:
  ① **배포** — 가이드라인 **8개 번들이 전부 git 미추적**이라 작업 워크트리
  (`/tmp/2ws-geom`)에 `coding/` 자체가 없다. `coding.md:16` 자신이 「활성화 게이트: 본
  파일이 그 경로에 없으면 본 룰 **비활성**」이라 규정한다.
  ② **데이터** — `coding.md:53` 이 「표가 아예 없으면 **통과**가 기본값」이라 규정하는데
  저장소 전체 함수표가 **1개**(`mcl2d_core`)뿐이라 대상 파일에서 게이트가 무조건 통과했다.
  같은 형태의 선례가 있다 — `session_workflow` 훅도 미추적이라 워크트리에 상속되지 않아
  **심볼릭 링크로 우회**했는데(메모리 `biguamr-session-workflow-hooks`), 그 대응이
  `coding/` 에는 적용되지 않았다. **훅을 만든 것과 훅이 도달하는 것은 다르다.**
  → **부분 닫음**: 오늘 고친 2개 모듈의 함수표·전역변수표를 생성(이중 기록)해 ②를
  그 범위에서 해소했고, 미등재 범위를 `docs/sw_structure/function_table.md` 에 **명시적으로
  나열**해 「어디가 아직 빈 통과인지」를 숨기지 않게 했다.
  **①(번들 git 추적 여부)은 사용자 결정 대기 — 닫히기 전까지 다음 워크트리 세션에서 재발한다.**

- **도구가 보증하는 범위를 주장의 범위로 부풀린다 — 「없다」 일반화의 **네 번째**이자
  「시험 추가 = 검출」의 재발이 한 작업에서 동시에 (2026-08-06-002)** —
  Tongyi CANopen 프로토콜 정본을 쓰고 검사기(174 항목)와 돌연변이(15건)를 붙여
  **「통과 174 · 불일치 0 · 모든 주장이 원자료·매뉴얼과 일치한다」**·**「돌연변이 15건 전부 검출 —
  검사기는 실제로 오류를 잡는다」**로 보고했다. 사용자가 적대적 감사 3인을 지시하자 그 상태에서
  **치명 13건**이 나왔다. 부풀림은 세 겹이었다:
  ① **부재 주장 3건**(`0x4670`·DI5/DI8·§6.17 이 「V7.0 에 없다」)을 **V7.0 을 조회하지 않고** 적었다 —
  셋 다 V7.0 page 135·33·190 에 있다. ② EDS 「통신 오브젝트 무변경」의 근거로 실은 스니펫은
  **섹션 이름 집합 차이만** 출력해 공통 섹션 내부를 볼 수 없었다 — 실제로는 DefaultValue 18건이
  바뀌고 **TPDO2 에 온도 매핑이 추가**됐다. ③ `counts/°` 57,344 를 **[실측]** 으로 등급 매겼으나
  스윕 도구가 `deg × 57344` 로 **지령한 상수를 되읽은 순환**이었다(잔차 1~5 counts 가 그 증거인데
  그것을 정밀도의 근거로 제시했다).
  ⚠ 주목할 점 — **검사기 라벨이 검사 범위를 대신 말했다.** `verify_eds()` 는 `0x14xx`~`0x1Axx` 를
  배제해 놓고 라벨은 「CiA402·**통신** 오브젝트 무변경」이었다. 그리고 돌연변이 15건이 전부
  **숫자 한 자리 변조**뿐이라, 감사관이 **숫자를 그대로 둔 채 서술만 23곳 뒤집은 사본**
  (little-endian→big-endian, **「주변 확보 필수」→「불필요」**, 폴트 이름 맞바꿈, 회전 부호 반전)을
  넣자 **통과 174 · 불일치 0 으로 한 건도 줄지 않았다.**
  원자료 쪽도 마찬가지였다 — 주 캡처의 **12.65 s 전노드 두절**(무응답 3,389건)을 놓친 채
  「두절 국면 없음」·「dropout 미관측」이라 적었다. **요청 121,721 과 응답 118,333 을 같은 표에
  나란히 적어 놓고 왜 다른지 묻지 않았다.**
  → **닫은 방법**: `verify_doc_claims.py` 에 **부재 주장 검사**(반대편 판본 전문 조회) ·
  **전량 대조**(`doc_values()` — `claim()` 의 다중 등장 사각 차단) · **인용 페이지 커버리지 강제**
  (예외 목록 폐지) · **다중 캡처 대조**를 넣고, 돌연변이를 **25건**(서술·등급·EDS 변조 포함)으로 넓혔다.
  **도구 출력 문구를 「인쇄 수치·인용 좌표·부재 주장만 검증됨 / 서술·해석·안전 문구는 미검증」으로
  바꾸고 같은 경고를 문서 §0-3 에 박았다.** 「검증됐다」는 이제 이 범위 안에서만 말한다.

- **기록이 있는데 코드만 보고 판정한다 — 한 세션에 3회 (2026-08-05-001)** —
  「크랩 wrap 임계 115° > can_relay 클램프 90°」를 **High 결함**으로 올리고, 신설 검사기가
  그것을 **매 실행 WARN 으로 재노출**하게 만들었다. 실제로는 ±90° 가 **유일해(canonical)
  구속**이고 전용 ADR(`2026-07-26-qd-ik-pm90-unique-solution.md`)이 있었다 — 등가해
  `(θ,+v) ≡ (θ∓180°,−v)` 를 반원으로 묶지 않으면 지령이 비결정·chattering 이 된다.
  내 「두 한계를 통일하라」는 권고는 **그 구속을 깨자는 제안**이었다.
  ⚠ 주목할 점 — **근거를 손에 쥐고도 못 봤다.** `foil_a082.yaml:162-166` 의
  「이중해를 없앤다**(합의)**」 블록은 같은 세션에서 내가 열어 다른 값을 인용까지 한 파일이다.
  두 수치를 비교(115 > 90)하는 산술로 판정을 끝내고 **왜 그 값인지**를 찾지 않았다.
  같은 형태가 이 세션에서만 3회 — ① `/select_motion_source` 를 브링업 수동 단계로 오판
  (액션 서버가 자동 호출) ② `homing_enabled` 를 「결정 필요」로 제기(2026-08-04 사용자 결정
  기록 존재) ③ 본 건. 2026-08-03-002 의 **거울상**이다 — 그때는 문서만 보고 실측을 안 봤고,
  이번엔 코드만 보고 문서를 안 봤다.
  → **닫은 방법**: 지시를 추가하지 않고 **판정 지점에 근거를 붙였다** —
  `Tools/motion_chain_check/check_chain_contract.py` `check_steer_limits` 의 WARN 을 정보
  표시로 바꾸고 docstring 에 ADR·config·크랩 주석 3곳을 인용했다. 체인 작업마다 실행되는
  검사기라 다음 사람은 같은 지점에서 **결함이 아니라는 근거를 먼저 본다.**
- **「없다」의 근거 범위를 넘겨 일반화한다 — 세 번째 (2026-08-05-001)** —
  조향 정지 명령을 만들 때 본 것은 ① 저장소 `docs/` grep ② Seer 마스터 캡처뿐이었고,
  캡처에 `0x03`·`0x05` 가 0회인 것을 **「이 드라이브에는 정지 명령이 없다」**로 읽었다.
  참은 **「Seer 가 그 명령을 쓰지 않는다」**였다. **벤더 매뉴얼(Handbook V7.0 §위치 모드 PP
  `:8467-8469`)에 `6040→0x03 Pause`·`0x05 Stop` 이 명시돼 있었고**, 상류 `TR_Nav_ros2_ws`
  (`can_open.hpp:36-37,468-469`)가 그대로 쓰고 있었다. 둘 다 조사 후보에 없었다.
  ⚠ 주목할 점 — **순서가 최악이었다.** 없는 것을 만들고(08-01) → 근거를 못 찾고(08-03) →
  그 실패를 부채·실수로 기록하고(`-002`·`-003`, debt-040·041) → 그 위에 안전 게이트를 덧대는
  작업(08-05)까지 했다. **매뉴얼 한 절이면 전부 불필요했다.** 소모 4일.
  2026-08-03-002(「`docs/` 에 없다 → 근거가 없다」)와 **대상만 바뀐 같은 형태**이고,
  그 기록이 이 §메타 패턴에 주입된 상태에서 재발했다 — 「주입만으로는 막히지 않는다」의 재확인.
  → **재발 방지 미정(사용자 결정 대기).** 「조사하라」를 또 적는 것은 닫힘이 아니라고 보고
  ① 하드웨어 지령 신설 시 매뉴얼 인용을 `checks/` 로 강제 ② 상류 저장소 로컬 상주 두 안을 올렸다.

- **시험을 *추가한 것*과 시험이 *검출하는 것*을 같게 취급한다 (2026-08-04-001)** —
  원본 GUI 결함 11건을 고치고 「각 항목에 회귀를 붙였다」고 보고했다. 근거는 **`111 passed`
  한 줄**이었다. 사용자가 「기존 can relay ui 수정한 것 검증부터」라고 하여 11건을 하나씩
  수정 전으로 되돌려 보니, Medium ⑤의 회귀는 **핸들러를 직접 호출**해서 배선
  (`_loop` 의 `poll_died.emit()`)을 지나가지 않았다 — 그 한 줄을 지워도 **111개 전부 통과**.
  ⚠ 주목할 점 — **통과 숫자가 개별 항목의 커버리지 부재를 가린다.** 2026-08-03-001 의
  「1차 측정이 튼튼할수록 파생 집계의 무검증이 가려진다」와 같은 형태로, 여기서는 합계가
  가림막이었다. 같은 날 `can_relay/ui` 에는 돌연변이 확인을 적용했는데 원본에는 적용하지 않았다.
  → **닫은 방법**: `Tools/amr_test_gui/mutation_check.py` — 11건을 수정 전으로 되돌려
  전체 시험을 돌리고, 되돌렸는데 통과하면(미검출) `exit 1`. 앵커 불일치도 `exit 1`.
  **「회귀로 고정했다」는 이 도구 출력으로 말한다 — 통과 숫자로 말하지 않는다.**
  부수 소득 2건: ① 상수를 지렛대로 쓴 돌연변이는 시험이 그 상수를 monkeypatch 하면 **무력**
  (미검출 ≠ 공백 — 지렛대 오류와 구분해야 한다) ② 새로 쓴 시험이 공유 위젯 `lab_status` 를
  Seer 스레드가 덮어써 **전체 실행에서만** 깨졌다(단독은 통과) — 판정 근거를 토글로 교체.

- **조사 범위를 「문서와 매뉴얼」로 한정한다 — 저장소의 실측 원자료를 후보에 넣지 않는다 (2026-08-03-002)** —
  「`halt_steer` 는 어디서 만든 명령인가」에 `docs/` 전수 grep + 벤더 Handbook 대조까지 하고
  **「벤더 문서에 없는 우회 기법 · 근거가 어디에도 기록돼 있지 않다」로 `debt/registry.md` 에 등록**했다.
  사용자가 「**can log 에 이 명령이 있는지?**」라고 묻자 열어 본 `Log/homing_capture_220350.jsonl`
  (마스터 Seer 253,510 프레임)은 정반대였다 — 마스터가 **같은 조합을 12,928회**, 그중 **95.9 %가
  「목표 ≈ 현재 실측」**, 그리고 **Halt(bit8) 0회**. 우리 기법은 **마스터 동작 그대로**였다.
  ⚠ 주목할 점 — **grep 결과는 참이었는데 결론이 거짓이었다.** 「`docs/` 에 없다」와 「근거가 없다」는
  다른 명제인데 후자로 적었다. 하루 전 2026-08-03-001 의 「**부정 판정의 범위를 넘겨 일반화**」와 같은
  형태를 **하루 만에 반복**했고, 이번에도 **registry**(다른 세션이 인용하는 자산)에 박았다.
  같은 세션에서 사용자가 두 번 같은 것을 지적했다 — 「**오늘 오후에 확정한 실험 값도 참조할 것 제발**」
  → 「**can log 에 이 명령이 있는지?**」. 둘 다 「저장소에 이미 있는 원자료를 보라」였다.
  → **닫은 방법**: `Tools/docking_field_kit/master_command_census.py` — 캡처에서 ① 마스터가 쓰는 객체
  ② controlword 분포와 **Halt 사용 여부** ③ 「목표 ≈ 실측」 비율 ④ **이동 중 목표 변경 건수**를 낸다.
  ④가 0 이면 「이동 중 정지」 주장을 하지 말라고 출력이 직접 안내한다.
  **「우리가 만든 명령인가」는 이 도구 출력으로 답한다 — 문서 grep 으로 판정하지 않는다.**


- **팬아웃이 오류를 증폭한다 — 기준선 1줄이 42개 문서가 된다 (2026-08-03-001)** —
  호밍 10회 실측(10/10)은 원자료로 확인했으나, 거기서 **파생한 집계**(「13회 연속 성공」·
  「`0x6064`=0 은 1회 관측」)는 세지 않고 기억으로 썼다. 그 기준선을 **에이전트 14명에게 배포**해
  42개 문서를 정정시켰고, 틀린 파생 수치가 **70곳**에 사실로 박혔다. 같은 배포에 들어간
  「물리 직진 앵커 부재」 오주장은 **27개 파일**로 퍼졌고, 그걸 근거로 `debt-036` 우선순위까지 낮췄다.
  ⚠ 주목할 점 — **1차 측정이 튼튼할수록 파생 집계의 무검증이 가려진다.** 「10/10」이 원자료로
  검증됐다는 사실이 같은 표에 있던 「13회」에까지 신뢰를 빌려줬다. 읽는 쪽(에이전트)에는
  둘을 구분할 수단이 없었다.
  또 하나 — **부정 판정의 범위를 넘겨 일반화**했다. 「(a) 육안 확인은 counts 역산에 기여 못 한다」는
  참인데 「(b) 물리 직진이 미확인이다」로 확대했다. **(b) 는 이미 사용자 육안 확인으로 성립**해 있었다.
  `2026-07-28-012`(「검증했는데 **대상**이 틀린 것」)의 변형 — 이번엔 **무효화의 대상**이 틀렸다.
  → **닫은 방법**: `Tools/docking_field_kit/verify_homing_claims.py` — `Log/` 원자료를 재계산해
  문서 수치와 대조하고 불일치 시 `exit 1`. 회차 집계·최장 연속·WAIT **체류**(절대시각과 분리)·
  정착 σ(모/표본 병기)·**`0x6064`=0 의 bit15 층화**를 낸다. `--docs` 는 과대주장·「1회 관측」 잔존·
  **「앵커 부재」 오주장**을 잡는다. **팬아웃 캠페인의 기준선은 이 도구 출력으로 만든다 — 기억으로 쓰지 않는다.**
  사용자 지적이 결정적이었다: 「**10번 호밍 명령 보내서 몇번 실패하는지 보면 끝나는데**」
  「**의심되면 실행해서 검증하면 되는데**」 — 5분 실측이 하루치 가설을 종결시켰다.

- **하류만 조사하고 상류를 조사하지 않는다 — `context-missing` 4연속
  (2026-07-27-004 → 2026-07-28-010 → 2026-07-31-004)** — 세 건 다 "조사 범위를 지목된 것으로
  한정" 이 뿌리이고, 마지막 건에서 **비용이 가장 컸다**: 신설 ROS2 드라이버 `can_relay` 의 상위
  인터페이스를 `cmd_vel` 로 정했는데 저장소 모션 스택(QD/2WS)의 실제 명령 계약은
  `/motor/low_cmd`(`MotorCmdArray`)여서 **체인의 어느 노드와도 연결되지 않았다.** 부작용으로
  축별 동일각 게이트가 들어가 **선회가 원천 차단**됐다(최소 선회반경 68.8 m, 액션 서버 9종 중
  6종 무력). QD 스택은 그 ADR **3일 전**에 이미 저장소에 있었다.
  ⚠ 주목할 점 — **하류 검증이 철저할수록 상류 미조사가 가려진다.** 그 ADR 은 실측 캡처
  253,510 프레임 바이트 대조·안전요건 S1~S10 을 84건 회귀로 고정했고, 그 철저함이 "검증된
  설계"라는 인상을 만들었다. 정작 §Verification 항목은 **전부 자기 노드 내부**였고 다른 노드와의
  연결을 확인한 항목이 **0**이었다.
  더 뼈아픈 것은 **2026-07-28-010 의 재발 방지가 메모리에 이미 적혀 있었다**는 점이다
  (「기능 도입 검토 시 사용자 저장소를 먼저 조사한다」). 그 다음 날 같은 조사가 또 생략됐다 —
  §메타 패턴이 이미 결론지은 **「주입만으로는 막히지 않는다」**(2026-07-28-005)의 재확인.
  → **이번 대응은 「조사하라」는 지시를 또 적지 않고, 체인 계약을 *값으로* 메모리에 박았다**
  (`biguamr-motion-motor-chain.md` — 토픽·메시지·QoS·motor_id·부재 노드 목록). 조사 행위를
  요구하는 대신 조사 결과를 상주시키는 쪽으로 바꾼 것이며, 이 방식이 재발을 막는지는
  **다음 신규 노드 설계에서 검증된다**(미검증).

- **뒤집는 절차는 작동하는데, 기동 조건이 여전히 「사용자 지시」다 (2026-07-29-003)** —
  10인 감사 결론 **5건이 적대적 심문 3인에게 뒤집혔고**(브링업 부재 = 거짓 / 조향 홈 기준계 =
  논리 반증 / NaN 방향 = 미입증 / guard forward 조건 = 누락 / 「중복이니 확장」 = 반박),
  뒤집힌 정정본 위에 코드를 지었기에 **오보고가 사용자에게 도달하지 않았다.**
  2026-07-28-011·012·013 과 다른 점은 여기다 — 그때는 사후에 뒤집혔고 이번엔 같은 턴 안에서
  뒤집혔다. 같은 점은 **둘 다 사용자가 "적대적 검증"을 지시해야만 그 라운드가 돌았다**는 것이다.
  → **교훈: 반대심문은 산출물이 아니라 절차다. 지시가 없어도 돌아야 한다.**
  이 항목은 **미채택**(사용자 결정) → `docs/debt/registry.md` **debt-020** 으로 추적한다.
  덧붙여 같은 세션에서 **부정형 단정이 또 재발**했다(신설 패키지 docstring·yaml 주석).
  INDEX §메타 패턴의 해당 항목이 SessionStart 로 **눈앞에 주입된 상태였다** —
  2026-07-28-005 의 결론("주입만으로는 막히지 않는다")이 그대로 재확인됐다.
  **닫은 방법**: `review-claim-lint.py` 에 **S6**(검증 명령 없는 절대형 부정) 추가 +
  검사 대상을 **소스 주석·docstring 까지 확대**(review.md VERSION 1.4.0). 도입 직후 전수
  검사가 *같은 세션에 내가 쓴 코드* 3곳을 잡았고 그 주장이 실제로 과장이었다
  ("호밍은 멈출 수 없다" → 펌웨어에 취소 경로 실재, `safety_seer_gate.h:307-309`).
  근거 인정 범위는 리뷰 SOP 룰 8 과 같다(도구 호출·결과 **또는** `파일:줄` 인용) —
  전자만 인정하면 기존 통과 산출물에 오탐 3건이 생김을 실측 확인했다.

- **자기 산출물 감사가 자기 결론을 뒤집는다 (2026-07-28-011·012·013)** — 코드리뷰 산출물을
  10명이 line-by-line 재감사하자 **내가 세운 High 결함 1건이 반증**되고(011: 배포 *파일* md5 만
  보고 "실기에 안 올라갔다" 확정 → 실기 조회 결과 정반대), **부정형 단정 3건**이 조건 생략으로
  뒤집혔으며(012), **인벤토리 24행 발췌·재현 불가 검증**이 드러났다(013).
  세 건의 공통 뿌리는 `verify-skip` 이지만 형태가 새롭다 — **"검증을 아예 안 한 것"이 아니라
  "검증했는데 대상이 틀린 것"**이다: 파일을 보고 장치를 말했고(011), 2차 자료 수치를 옮기며
  "실측" 라벨을 붙였고(012), 임시 사본을 검증하고 저장소를 통과시켰다(013).
  → **교훈: 주장의 *대상*과 검증의 *대상*이 같은지 먼저 확인한다.**
  강제: `docs/claude_guideline/code_review/checks/review-claim-lint.py` (S1 인벤토리 결번 ·
  S2 severity 분포 · S3 dangling 참조 · S4 검증기 하드코딩 경로 · S5 장치 조회 없는 `[동작]` 확정).
  이 저장소 최초의 **실행 가능·회귀 시험된** 리뷰 게이트다.
- **사용자 개입이 매번 결정적이었다** — "10명 투입 line by line", "중복 함수 중복 변수도 모두
  검토했는지?", "문서로만 하지말고 실제 구동을 해 보면 됨" 세 지시가 각각 결함군 하나씩을 열었다.
  특히 마지막 지시가 없었으면 011 의 오보고가 정본으로 남았을 것이다.

- **정정 작업 자체가 오염된다 (2026-07-28-006)** — 오류를 고치려고 만든 정오표 v1 이 **정확한
  Handbook 인용을 「오진」으로 규정**했다. 원인은 2차 자료(검증 에이전트 산문)를 1차 자료처럼
  사용한 것 — `external_reference/handling.md` §3 이 금지하는 형태다. 사용자가 "10명이 재검토"·
  "적대적 검증을 해야 합니다" 를 지시하지 않았으면 잘못된 정오표가 정본으로 남아 **정확한 인용을
  되돌리는 데** 쓰였을 것이다. **교훈: 정정문에는 원본보다 높은 수준의 원문 대조를 요구한다.**
- **표준을 「안전」을 이유로 이탈 (2026-07-28-004)** — 리밋 백오프·조향 0° 복귀·E-STOP 을 각각
  "안전상" 이라며 뺐다. 셋 다 사용자가 바로잡았고 셋 다 업계 표준이었다. 참조 구현(TR_Nav)과
  실측 캡처를 **이미 읽은 뒤에도** 기준선으로 삼지 않고 내 판단을 먼저 세웠다.
  **표준 시퀀스는 그 자체가 검증된 안전 절차이고, 일부만 떼면 검증 안 된 새 상태를 만든다.**
- **`verify-skip` 의 시간 축 변형 (2026-07-28-008)** — 기존 3건은 "검증 전 확정" 이었으나 본 건은
  "**검증했으나 시효 지난 값을 확정**" 이다. 시변 상태(프로세스·장치 점유)를 1회 판독으로 고정했고
  그 사이 실기 관측 기회를 놓쳤다.
- **부정형 단정이 또 재발 (2026-07-28-005)** — 아래 2026-07-27-003 항목이 SessionStart 훅으로
  **주입되어 눈앞에 있었는데도** "우리가 못 막습니다" 를 인용 없이 단정했다. 실제로는 차단 기계
  (`seer_gate_fwd_hook` 의 Seer write drop)가 이미 존재했고 약 20줄로 해결됐다.
  **주입만으로는 막히지 않는다 — 강제 검사가 필요하다는 증거.**
- **자기 도구 출력 무검증 (2026-07-28-001)** — 같은 세션에서 만든 로그 생산자·소비자의 필드 의미
  (증분 vs 누적)를 대조하지 않아 "정지 구간 4~8건" 을 확정 보고했으나 실제는 0건. 같은 로그에 독립
  교차검증 지표(뷰어 자체 WARN 0건)가 이미 있었다. **로그 파생 수치는 독립 지표와 대조 후 보고**가
  결론. closed — 필드 개명(`frames_delta`)·0 판정 교체·회귀 테스트로 코드에 고정.
- **`verify-skip` 3연속 (2026-07-26-001 → 2026-07-27-001 → 2026-07-27-002)** — 모두 "검증·대조
  전에 확정형으로 보고" 가 뿌리다. 심각도가 소프트 오보고(emulate 정지값) → 문서 추측(freeze 객체)
  → **실장비 물리 손상**(node4 137° 갇힘) 순으로 상승했다. 세 건 다 규칙은 설치·인지돼 있었고
  advisory 였다는 점이 공통 — 강제 메커니즘 부재가 원인.
- **부정형 단정도 같은 뿌리 (2026-07-27-003)** — type 은 `mistake`/`wrong-assumption` 이지만
  실패 형태는 위와 같다: 자유도를 소진하지 않고 "두 실측은 모순" 이라는 구조적 결론을 확정형으로
  서술했다. **"된다" 만 조심하고 "안 된다·불가능하다" 에는 같은 기준을 적용하지 않은 것**이 핵심 공백.
- **부분 진전 (2026-07-27) → ⚠ 철회 (2026-07-28)** — `Tools/amr_test_gui/amr_test_gui/ramp.py` 가
  2026-07-27-002 의 "하드웨어 지령 전 정본대조·램프 강제" 요구를 **GUI 경로에 한해** 코드로
  강제한다고 적었으나, **그 파일은 2026-07-28 에 삭제됐다**(구 GUI 일괄 폐기).
  단계 램프는 시스템의 방식이 아니라 사용자가 세운 시험 절차였고, 실기 캡처가 이를 반증했다
  (2026-07-28-003 · `Log/homing_capture_220350.jsonl` — Seer 도 최종 절대 목표를 반복 송신).
  **현재 남은 강제는 `Tools/amr_test_gui/gui.py` 의 ±90° 하드 클램프뿐**(`_steer_to`·`_steer_axis`).
  저장소 전역 강제(hook·공용 지령 래퍼)는 여전히 미설치.

## 미해결 항목

| id | type | category | 제목 | owner | 등록일 |
|----|------|----------|------|-------|--------|
| 2026-08-08-002 | rule-violation | overclaim | `--ignore` 로 제외하고 돌린 시험을 「311 passed / 0 failed」로 전체인 것처럼 보고(실측 364 passed / 4 failed) + 회귀 0건인데 「검증」 선언 | claude | 2026-08-08 |
| 2026-08-09-001 | rule-violation | overclaim | 규격 결정용 `turn` 오차 자료를 원자료 아닌 **내 요약 문장**에서 재인용해 표본 2개 누락(n=4 로 보고, 실제 6) + 한 문장 안에서 부호 규약 혼용(`|실측|−|지령|` 과 `실측−지령`). 누락된 −0.35° 는 제시 범위 밖이었고, spin 규격 0.40° 적용 시 6개 중 2개 탈락이 자료에 안 보였다 | user | 2026-08-09 |
| 2026-08-10-001 | rule-violation | overclaim | 센서 3값이 갈리자 mcl2d 를 「고장」으로 단정 — 실은 액션 자기보고와 외부 IMU 가 **같은 센서를 읽는 두 창구**(한 표를 두 번 셈)이고 독립 계통은 mcl2d 쪽이었다. 게다가 못 믿는다던 그 값으로 되짚기를 지령해 로봇이 22° 더 돎. 같은 형태 3건 동반 (발산 오진·맞는 구역만 골라 복귀 선언·원인 2회 연속 단정) | user | 2026-08-10 |
| 2026-08-08-001 | rule-violation | overclaim | 「두 경로 다 실패 ⇒ 소프트웨어 배제」로 하드웨어 고장·물리 점검을 요구 — 두 「경로」는 같은 `RelayBackend` 인스턴스였고 실제 원인은 그 공유 구간 | claude | 2026-08-08 |
| 2026-08-06-004 | rule-violation | verify-skip | 내 시험 입력을 검증하지 않고 「mpc_reverse 제어 결함」으로 단정해 공유 기록에 커밋 — 재현성과 그럴듯한 인과가 가림막 | claude | 2026-08-06 |
| 2026-08-06-003 | rule-violation | verify-skip | coding SOP §2·§3·§5·§6 을 건너뛰고 자기승인으로 커밋·푸시 — 강제 훅은 있으나 **워크트리 미배포 + 표 부재로 빈 통과** | user | 2026-08-06 |
| 2026-08-05-001 | rule-violation | tech-debt-shortcut | 매뉴얼에 있는 정지 명령(6040→0x03/0x05)을 안 찾고 없는 명령을 만들어 4일 소모 | user | 2026-08-05 |
| 2026-08-02-001 | rule-violation | verify-skip | 교차검증 도구 역산 **부호**를 1점 측정으로 가정 → 실기 오방향 지령 · 「설정 호출 0건」 거짓 단정 | claude | 2026-08-02 |
| 2026-08-01-001 | rule-violation | scope-creep | 이미 후속 합의된 캘리브레이션을 세 턴 연속 현재 블로커로 되살림(내 기록 미독) | user | 2026-08-01 |
| 2026-07-31-003 | rule-violation | verify-skip | SOP 읽기 전 git 선택지 작성 → 세션 브랜치를 「HEAD 이동」으로 오설명, 게이트 정정 후에도 override | user | 2026-07-31 |
| 2026-07-29-002 | rule-violation | verify-skip | 프로세스 상태 미확인 + `ps %CPU` 오독 → 오염된 수치 2회 보고, `kill -9` 로 카메라 파이프라인 붕괴 | claude | 2026-07-29 |
| 2026-07-29-001 | rule-violation | verify-skip | 세션 실수 전수(부정형 과단정·근거없는 단정·추측코드 누적·자기도구 무검증·과잉 자기정정) | claude | 2026-07-29 |
| 2026-07-28-014 | rule-violation | verify-skip | 화면 한 번 안 보고 UI 결함을 네 번 오진 | claude | 2026-07-28 |
| 2026-07-28-009 | rule-violation | verify-skip | 공유 워킹트리에서 `checkout -b` 로 20개 세션 HEAD 이동 | claude | 2026-07-28 |
| 2026-07-28-008 | rule-violation | verify-skip | 이미 종료된 GUI 를 「점유 중」으로 stale 보고 | claude | 2026-07-28 |
| 2026-07-28-005 | rule-violation | verify-skip | 「Seer 호밍은 못 막는다」 검증 없는 부정형 단정 | claude | 2026-07-28 |
| 2026-07-28-004 | mistake | wrong-assumption | 표준 동작(백오프·0° 복귀·E-STOP)을 근거 없이 생략 | claude | 2026-07-28 |
| 2026-07-27-002 | rule-violation | verify-skip | 검증 안 된 조향지령으로 node4 물리 손상 | claude | 2026-07-27 |
| 2026-07-27-001 | rule-violation | verify-skip | freeze 대상 객체를 문서 아닌 추측으로 작성 | claude | 2026-07-27 |
| 2026-07-26-001 | rule-violation | verify-skip | emulate 가 "정지값 송신" 한다는 미검증 완료보고 | claude | 2026-07-26 |

## 카테고리 × 사건 매트릭스

| category | 사건 id 목록 | 건수 |
|----------|-------------|------|
| verify-skip (rule-violation) | 2026-07-26-001, 2026-07-27-001, 2026-07-27-002, 2026-07-28-001, 2026-07-28-005, 2026-07-28-006, 2026-07-28-008, 2026-07-28-009, **2026-07-28-011**, **2026-07-28-012**, **2026-07-28-013**, **2026-07-28-014**, **2026-07-29-002**, **2026-07-29-003**, **2026-07-31-003**, **2026-08-02-001**, **2026-08-03-001**, **2026-08-04-001**, **2026-08-06-003**, **2026-08-06-004**, **2026-08-07-001** | 21 |
| wrong-assumption (mistake) | 2026-07-27-003, 2026-07-28-004, 2026-07-28-007, **2026-07-31-001** | 4 |
| context-missing (mistake) | 2026-07-27-004, 2026-07-28-010, **2026-07-31-004**, **2026-08-05-001**, **2026-08-06-005** , **2026-08-07-002** | 6 |
| context-missing (mistake) | 2026-07-27-004, 2026-07-28-010, **2026-07-31-004**, **2026-08-03-002** | 4 |
| tech-debt-shortcut (rule-violation) | **2026-07-31-002** | 1 |
| scope-creep (rule-violation) | **2026-08-01-001** | 1 |

- [2026-08-10-002](2026-08-10-002_verified-then-edited-then-committed.md) — 검증 후 편집하고 재검증 없이 커밋. yaml 키가 갈라져 파싱이 깨진 채 병합됐다. 없는 키는 기본값으로 조용히 채워지므로 **회귀가 초록으로 통과할 수 있는** 형태다. SIL 회귀에 YAML 사전점검을 넣어 닫았다.

- [2026-08-10-003](2026-08-10-003_unread-then-framed-as-reversal.md) — 읽지 않고 「배선 불일치」로 단정했다가, 읽은 뒤 「중요한 반전」이라고 포장. 설계 의도는 docstring·런타임 경고·RUNBOOK 세 곳에 이미 있었다. **2026-08-05-001 의 재발이자 악화** — 못 본 것을 넘어 내 착오를 코드의 의외성으로 전가했다.

- [2026-08-10-004](2026-08-10-004_registered-debt-that-records-already-answered.md) — 나흘 전 실험기록(`issues_and_fixes.md` 2026-08-06)에 이미 측정·자기정정까지 끝난 내용을 「미확인 부채」로 등록. **같은 날 세 번째** — 조회 대신 머릿속 모델로 판정하는 형태. 부채 등록 전 `issues_and_fixes`·`code_review` grep 을 규칙으로. 부수: 커밋 메시지가 하지 않은 일을 주장(`git show --stat` 확인 누락).

- [2026-08-10-005](2026-08-10-005_speculation-driven-code-change.md) — **관측된 고장이 아니라 추정 위에서 실측 기반 설계를 바꿨다.** `−8` 을 「진전 기반」으로 재작업했는데 근거인 `max_steer_deg 82°` 오탐은 실기 미실행이었고, 임계 5 s 는 실측 근거와 함께 이미 기록된 결정이었다(같은 측정으로 뒤집음). 되돌렸다. **규칙: 바꾸기 전에 「이 변경이 막는 고장이 실기에서 관측된 적 있는가」를 묻는다 — 없으면 고치지 않고 「관측 0·추정」으로 등록한다.**

- [2026-08-10-006](2026-08-10-006_unmonitored-first-drive-with-warned-pose-source.md) — **충돌 직전 사고.** 노드가 「Seer 에 의존한다」고 경고한 측위원을 mcl2d 가 돌고 있는데도 붙였고, 실기 검증 0 건인 `mpc` 첫 주행을 **독립 감시 없이** 돌렸다. Seer 측위 상실로 자세가 얼자 액션은 진행 0 으로 보고하며 60초간 지령을 계속 내 약 6.7 m 개루프 주행. **신선도 ≠ 살아있음** — stamp 만 갱신되는 동결 자세를 모든 가드가 통과시켰다. 값-정지 감시(STUCK) 신설·SIL 재현·감시형 이동 도구로 닫음.
