---
id: 2026-07-29-001
type: rule-violation
category: verify-skip
status: open
reflected_assets: ["docs/debt/registry.md#debt-014"]
---

> ## ❌ 통합 재정정 2026-08-03 17:30 — 아래 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> 원문은 삭제하지 않는다. 재검증: `python3 Tools/docking_field_kit/verify_homing_claims.py --docs`
> (`Log/` 원자료 재계산 — 문서를 근거로 문서를 고치지 않는다)
>
> - **E1** 「13회 연속 성공」 → **12회**. 2026-08-03 은 **시도 13 / 성공 12 / 실패 1**(09:58 `ERR_TIMEOUT`).
>   15:33 실행의 `baseline` 은 호밍이 아니라 **레지스터 스냅샷**이다 — 회차로 세지 말 것.
>   ⚠ **「10회 연속 10/10」 자체는 유효**하다.
> - **E2** 「`0x6064`=0 은 1회 관측·재현 안 됨」 → **[거짓] 재현된다.** `0x6041` bit15=1 인 채로 0 이 나온
>   캡처가 **6개**다. ⚠ **인과는 양쪽 미판정** — 0 이 관측된 직후 호밍이 성공한 회차가 있으므로
>   「0 이면 호밍 실패」도 성립하지 않는다. **「호밍 중(bit15=0)의 0」은 별개의 유효 관측**이다.
> - **E3** 「WAIT 31.7 s」 → WAIT(state 4) **체류 31.30 s**. 31.7 s 는 개시~RESTORE **절대시각**.
> - **E4** 「35.0 s」 → 15:33 10회 **평균 35.07 · 중앙 35.05**(34.99~35.16, 폭 0.17).
> - **E5** 「counts/° = 57,344」 → node3 **57,344.0** / node4 **57,344.3**. 「기울기 1.000000」은 node3 한정.
> - **E6** 「σ≈3 counts」는 **모표준편차** 기준 — node3 **2.80** / node4 **3.21**(표본 2.95 / 3.38).
> - **E7** 「결함이 아니라 **설계 동작**」 → **과잉 확정.** 실측이 보증하는 것은 **재현되는 정착 동작**까지이며
>   펌웨어 상수의 **적정성은 별건**이다 — `debt-016` 이 같은 편차를 「영구 미검출 오프셋」으로 등록 중이라 충돌한다.
> - **E8** 조향 0° `[7871815, 7840086]` — **값은 정본 유지**, 근거는 **공학적 채택값**(역산식이 항등식).
>   「실측 확정」 표현 금지.
> - **E9** `seer_home_cancel_frames()` = **`safety_seer_gate.h:312-316`**(`:307-311` 은 `seer_home_digital_in()`).
> - **E10 ★** 「**물리 직진 앵커 부재**」·「**물리 직진 미확인**」 → **거짓.** 앵커는 **실재한다** —
>   Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 바퀴 직진 상태에서
>   Seer 앞바퀴 2축 **0° 를 육안 확인**했다(can_relay GUI 표시로도 동일). 부족한 것은 **앵커가 아니라
>   정밀도**(육안 ≈**±1° = ±57,344 counts**)다 — 다툰 193 c(0.0034°)의 약 **297배**라 counts 소수 자리를
>   분해하지 못한다. ⇒ 「非-Seer 계측 필요」는 **「counts 소수 자리까지 판정하려면」** 조건부로만 참이다.
>   ⚠ 단 **(a) 역산식이 항등식이라 육안 확인이 counts 값 결정에 기여하지 않는다**는 판정은 **유지**된다 —
>   무효인 것은 (a) 뿐이고, **(b) 「Seer 0° = 물리 직진」은 성립**한다. 이 둘을 뭉개지 말 것.

# 2026-07-29 (KST) — 이 세션 실수 전수 기록 (부정형 과단정 재발 + 근거없는 단정 + 추측 코드 누적 + 과잉 자기정정)

> 사용자 지시 "실수는 모두 기록하도록"(08:28)에 따라 이 세션(2026-07-28 밤~2026-07-29 아침, sess 17558ff1)의 실수를 뿌리별로 전수 열거한다.

> ## 2026-08-03 15:40 호밍 10회 실측 반영 — A-1 은 ✅ **유효**, 8번(E)의 사유는 ❌ **폐기**
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> 원자료: `Log/home_experiment_260803_153319_summary.json` · `Log/home_experiment_260803_153319.jsonl`
> 실행: `Tools/docking_field_kit/orin_home_experiment.py --repeat 10` (15:33~15:40, **10/10 성공**, 35.0 s)
>
> - ✅ **A-1 의 사실 부분은 실측으로 재확인됐다.** 호밍 진행 구간(`0x6041` bit15 = 0, WAIT **31.2~31.4 s**)
>   에서 `0x6064` 가 **0** 인 것이 **10회 모두 재현**됐다. 원본 CAN 로그의 SDO 응답(`0x583`/`0x584`,
>   index `0x6064`)을 디코드한 결과 bit15=0 표본 중 `0x6064`==0 이 **node3 32,243 / 32,243 (100 %)**,
>   **node4 32,167 / 32,175 (99.98 %)**.
>   ⇒ 「`0x6064` 는 호밍 중 0 이다」는 참이고, **실수는 거기서 「각도 표시 불가」를 도출한 비약**이라는
>   본 entry 의 판정은 그대로 성립한다. §무엇이 잘못이었나 의 "debt-007 §① 3,080/3,080" 서술도 유효하다.
> - ❌ **8번(E)의 「confounded」 사유는 폐기됐다** — 「조향이 이미 홈이라 재호밍이 실질 무동작이었다」는
>   설명이 반증됐다. 10회 중 **runs 2~10 은 전부 정착값(= 이미 홈)에서 시작했는데도** 매회 31 s 를
>   이동해 −리밋(DI `0x01`→`0x09`)에 도달하고 `DONE` 했다. 「축이 이미 홈이면 무동작 즉시 완료」는
>   저장소 전체에서 **인용 금지 주장**이다(§0-4).
>   ⇒ 다만 8번의 **결론**(자기정정 "문서가 틀렸다" 가 틀렸고 문서가 맞았다)은 위 ✅ 로 **재확인**된다.
>   틀린 것은 그 결론에 붙인 **사유**뿐이다 — 정정문에 원문 대조를 요구한다는 교훈은 그대로 남는다.
> - ⚠ **debt-014 후보 객체 `0x60FB:03 slAbsAngle` 은 아직 판정되지 않았다.** 2026-08-03 **정지** 상태
>   실측에서 `slAbsAngle` 은 `0x6064` 와 **같은 값**을 반환했다(assets §15-1: node3 7,871,810 /
>   node4 7,840,052). **호밍 중 값은 이번에도 측정되지 않았다** — 10회 로그의 `0x60FB` SDO 는 개시
>   쓰기 20건(10 run × 2 축)뿐이고 읽기 0건이다. ⇒ 「독립 각도원이다/아니다」 어느 쪽도 단정 금지.
>   debt-014 는 그대로 열려 있다.
> - ❌ 함께 폐기(인용 금지): 「`Switch on disabled` 라 호밍 불가」·「`0x6098`=0」(실측 **1**)·
>   「RstStart 고착」·「`0x6064`=0 은 전원 사이클 래치」·「CAN↔Seer 독립 교차검증」
>   (Seer 1005·1040 **둘 다 `0x6064` 유래**)·「+0.334° / +0.332°」(→ **+0.331°**).

## 무엇을 했는가

### A. 부정형("불가/없음") 과단정 — 근거 소진 전 (뿌리: verify-skip, 2026-07-27-003·2026-07-28-005 재발)

1. **호밍 중 각도 표시 불가** — "호밍 중 유효한 다른 위치 객체가 필요하다"며 사실상 불가로 서술. 사용자가 07:04 "이상한 소리 하지말고 TR_Nav 는 UI 로 호밍 중 각도 잘 표시됨", 07:50 "엔코더 값을 읽으면 되는데", 08:03 "status 읽기에 각도를 안 읽는지?" 로 3회 반증. **0x6064 는 이미 탐침에서 OK 로 값을 읽었고**(7,871,810/7,840,091), 매뉴얼 `0x60FB.3`(slAbsAngle 절대각) 을 **탐침 목록에서 빠뜨린 채** "불가" 로 점프.
2. **relay 실제 상태 못 읽음 / 상태변수 없음** — S5 를 두고 21:44~23:24 "못 읽는다 / 상태변수 어떻게 보나 / 하드웨어에 없음(회로도 필요)" 을 근거 없이 단정 후 철회.
3. **TR_Nav 방식 Tongyi 에 불가** — "0x2106 이 Tongyi 에 없으니 각도 표시 불가" 로 함의 확대. 손에 든 0x6064 OK 와 미탐침 0x60FB.3 을 두고 낸 오판. (요약표에 "PARTIAL(사실 맞고 함의만 틀림)" 로 적어 **자기 오판을 완곡화**한 것도 잘못 — 사용자 08:27 지적.)

### B. 긍정형 근거없는 단정 / 사양 임의 발명 (뿌리: verify-skip / 2026-07-28-004형)

4. **"PC 가 죽는 상황은 대개 모터 구동 중"** (21:26) — 빈도 데이터 없이 단정. 사용자 "헛소리 하지말고" 후 철회.
5. **S4′ 규칙을 근거 없이 발명** (21:24~) — S4 는 이미 가이드 §2 에 있던 규칙("relay off 전 모터 정지")인데 "S4′" 라는 새 규칙을 내가 만든 것처럼 제안. 사용자 "s4는 내가 준 지식사항이잖아" 지적.

### C. 추측 코드 누적 (뿌리: 검증 없는 구현 직행)

6. **판다에 쓸모없는 코드를 추측으로 쌓음** — S5 를 좇아 SBU ADC 반환 엔드포인트(0xfc)를 넣고 플래시했다가 무용 판명, 이어 ODR 되읽기까지 얹음. 사용자 "코드가 점점 이상해지잖아"·"레지스터 쓰면 당연히 바뀌는데 뭐하자는 건지" 지적 후 전량 되돌리고 원본(12dd1138) 복구.

### D. 자기 도구 출력 무검증 보고 (뿌리: 2026-07-28-001형)

7. **S4 "성립" 을 오염된 자동판정으로 보고** — `orin_gap_measure`/`s4_verify` 의 자동판정이 상시 Seer 트래픽(target velocity 0)에 오염됐는데 그대로 "성립" 신호로 읽었다가, bus 태그로 걸러 정정. 판다 발원 정지프레임이 에코로 node1 1건만 잡힌 것을 측정한계 vs 실결함 못 가림(→ debt-013).

### E. 과잉 자기정정 (뿌리: 2026-07-28-006형, 정정문이 또 미검증)

8. **"0x6064 는 호밍 중 실제값, 문서가 틀렸다"** — A-1 반증을 받아 호밍 실기 판독 후 재단정. 그러나 그 판독은 **조향이 이미 홈**(직전 GUI 호밍)이라 재호밍 실질 무동작이었던 **confounded** 결과. `0x6064` 가 실제 호밍 이동 중 0 인 것은 참이며 debt-007 §①(3,080/3,080 샘플)에 이미 확정 — 문서가 맞았다.
   - ❌ **정정 2026-08-03 15:40 (사유)**: 위의 **「이미 홈이라 재호밍 실질 무동작」이라는 사유는 폐기**한다.
     2026-08-03 호밍 10회 실측에서 **runs 2~10 은 전부 정착값(= 이미 홈)에서 시작했는데도** 매회
     WAIT 31.2~31.4 s 를 이동해 −리밋(DI `0x01`→`0x09`)에 도달하고 `DONE` 했다 — 즉 「이미 홈이면
     무동작」이 아니다. ⇒ 그 판독이 confounded 였다면 원인은 **다른 것**이며, 현재 자료로는 판정하지 않는다.
   - ✅ **유지(재확인)**: 「`0x6064` 가 호밍 이동 중 0 이다 — 문서가 맞았다」는 **결론은 그대로**다.
     2026-08-03 10회 원본 CAN 로그 디코드에서 bit15=0 구간의 `0x6064`==0 이 node3 **32,243/32,243**,
     node4 **32,167/32,175 (99.98 %)**. 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0,
     `Log/home_experiment_260803_153319.jsonl`.

## 무엇이 잘못이었나

- 루트 `CLAUDE.md` reverse_engineering §6(대조 전 확정 금지), `external_reference/handling.md` §8(강한 단정어는 primary source 인용 시만) 위반 — 부정형 단정을 인용·검증 없이 냈다.
- 사실관계로도 틀렸다(적대적 감사 `wf_cf5aa79d` 확인):
  - **호밍 중 각도**: `0x6064` 가 호밍 중(0x6041 bit15=0) 0 인 것은 참이며 debt-007 §①(3,080/3,080 샘플)에 이미 확정. 실수는 "0x6064=0" 이 아니라 거기서 "표시 불가" 를 도출한 **비약**이다. Handbook V7.0 에 `0x60FB.3 slAbsAngle`(32bit 절대각, 9656줄)·기본 TPDO4(0x60FB.02+0x6064, 8055줄)가 있어 각도 경로가 존재한다.
  - **자기정정**: confounded 판독으로 "문서가 틀렸다" 고 한 것은 근거가 아니다 — 이동이 없었으니 0 이 안 나온 것뿐.
    - ❌ **정정 2026-08-03 15:40**: 「이동이 없었으니」라는 **설명은 폐기**한다 — 이미 홈에서 시작한
      호밍도 31 s 를 이동한다(10회 중 runs 2~10, 리밋 도달 10/10). **"문서가 틀렸다" 는 자기정정이
      틀렸다는 판정 자체는 유지**되며, 이제는 더 강한 근거가 있다 — 2026-08-03 10회 실측에서
      bit15=0 구간의 `0x6064` 가 **0** 임이 32,243/32,243 (node3) · 32,167/32,175 (node4) 로 재현됐다.
      근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-1 · §0-4.
- 뿌리는 이미 등재된 것과 동일: 2026-07-27-003(구조적 모순 확정), 2026-07-28-005(부정형 단정 무검증), INDEX §메타 "'된다'만 조심하고 '안 된다·불가능하다'엔 같은 기준 미적용". SessionStart 훅 주입에도 재발.

## 사용자 지적

> "이상한 소리 하지말고 … TR_Nav 에서는 ui로 호밍중에 각도 잘 표됨" (07:04)
> "엔코더 값을 읽으면 되는데" (07:50)
> "자꾸 근거없는 이야기를 지어내지 말고" (21:56)
> "릴레이 상태 읽기는 안하는 것으로 했는데" (23:24)

## 원인 분석

- 다중 뿌리지만 **공통은 "확정 전 보고"** 다. A(부정형 과단정)=2026-07-27-003·2026-07-28-005, B(근거없는 단정·사양 발명)=2026-07-28-004, C(검증없는 구현 직행), D(자기도구 출력 무검증)=2026-07-28-001, E(정정문이 또 미검증)=2026-07-28-006 — 전부 기존 등재 유형의 한 세션 내 재발.
- 부정형("못 한다")은 "보수적 판단" 처럼 느껴져 자기검열을 통과하나 실제로는 작업을 중단시켜 비용이 크다 — 2026-07-28-005 와 동일.
- 이번엔 새 변형이 하나 추가됐다: **반증을 받은 뒤의 정정이 또 다른 미검증 단정**이 됐다(confounded 실기 1회로 문서 반증). 정정문에는 원본보다 높은 대조를 요구한다는 2026-07-28-006 교훈이 여기도 적용됐어야 했다.
- 강제 메커니즘 부재 — `review-claim-lint.py` 는 [동작] 긍정 확정만 검사(S5), 부정형 단정·과잉정정은 미검사. `checks/` 자체 미설치.

## 재발 방지

- **자산 반영(reflected)**: `docs/debt/registry.md` 에 debt-014(호밍 중 조향각 표시 — 유효 객체 미확정) 등록. 0x6063 이 그 객체라고 단정 금지(Handbook grep 0건).
  - ⚠ **2026-08-03 15:40 현재도 미해결**: 호밍 10회 실측은 `0x6041`·`0x6000:01`·`0x603F`·`0x6064` 만
    폴했고 `0x60FB` 는 개시 쓰기 20건뿐(읽기 0건) — **호밍 중 `0x60FB:03 slAbsAngle` 은 여전히 미측정**이다.
    정지 상태에서는 `slAbsAngle` = `0x6064` 로 같은 값이 나왔다(assets §15-1).
    ⇒ 유효 객체 판정에는 **호밍 중 `0x60FB:03` 폴 1회**가 필요하다. debt-014 는 열린 상태로 유지한다.
- **필요하나 이번 세션 미설치(그래서 open)**: 응답의 `불가능`·`없다`·`못 한다`·`must-not` 류 부정형 단정에 근거 인용 동반을 요구하는 검사(2026-07-27-003·2026-07-28-005 와 동일 미해결 요구). 부정형뿐 아니라 "정정문"에도 원문 대조를 요구하는 확장 필요.
- S5(relay 상태 판독)는 사용자 결정으로 **미채택**(물리 read-back 부재·명령 되읽기는 순환) — 미해결 부채 아님, 결정으로 종결.

**owner**: claude
