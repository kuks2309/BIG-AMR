---
id: 2026-07-28-004
type: mistake
category: wrong-assumption
status: open
reflected_assets:
  - Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h (호밍 = 원점 경유 → 조향 0° 복귀 전 사이클을 상태기계로 구현: SEER_HOME_GOZERO/GOZERO_W)
  # ❌ 정정 2026-08-03 (라벨): 위 줄의 "조향 0° 복귀" 는 정확히는 **「호밍 후 정착값」 복귀**다.
  #   safety_seer_gate.h 의 SEER_HOME_ZERO_N3/N4 = 7882020 / 7859062 는 **GOZERO 상수**이며
  #   조향 0°(= 7871815 / 7840086, 실측 역산 7871816 / 7840087)에서 +0.178° / +0.331° 벗어나 있다.
  #   ⇒ 상태기계 구현 자체는 유효, 상수의 **이름·설명만** 0° 가 아님을 반영해야 한다.
  #   근거: docs/homing/2026-08-03-can-relay-homing-assets.md §10 (특히 §10-6 말미 주의).
  # ✅ 확정 2026-08-03 15:40: 위 라벨 정정이 호밍 10회 연속 실측으로 확정됐다 — 정착값은
  #   node3 7,882,021 / node4 7,859,065 (σ≈3c), 0° 대비 +0.178° / +0.331°, 10/10 재현.
  #   상태기계(GOZERO/GOZERO_W → DONE)도 10/10 정상 동작했다. 정본: 같은 문서 §0.
  - .claude memory biguamr-steer-homing-architecture (호밍 정의를 "원점에 머무는 것이 아님" 으로 명문화)
---

> ## ❌ 통합 재정정 2026-08-03 17:30 — 아래 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> 원문은 삭제하지 않는다. 재검증: `python3 Tools/docking_field_kit/verify_homing_claims.py --docs`
> (`Log/` 원자료 재계산 — 문서를 근거로 문서를 고치지 않는다)
>
> - **E1** 「13회 연속 성공」 → **12회**. 2026-08-03 은 **시도 13 / 성공 12 / 실패 1**(09:58 `ERR_TIMEOUT`).
>   15:33 실행의 `baseline` 은 호밍이 아니라 **레지스터 스냅샷**이다 — 회차로 세지 말 것.
>   ⚠ **「10회 연속 10/10」 자체는 유효**하다.
> - **E2** 「`0x6064`=0 은 1회 관측·재현 안 됨」 → **[거짓] 재현된다.** `0x6041` bit15=1 인 채로 0 이 나온
>   캡처가 **6개**다. ⚠ **인과는 양쪽 미판정** — 0 이 관측된 직후 호밍이 성공한 회차가 있으므로
>   「0 이면 호밍 실패」도 성립하지 않는다. **「호밍 중(bit15=0)의 0」은 별개의 유효 관측**이다.
> - **E3** 「WAIT 31.7 s」 → WAIT(state 4) **체류 31.30 s**. 31.7 s 는 개시~RESTORE **절대시각**.
> - **E4** 「35.0 s」 → 15:33 10회 **평균 35.07 · 중앙 35.05**(34.99~35.16, 폭 0.17).
> - **E5** 「counts/° = 57,344」 → node3 **57,344.0** / node4 **57,344.3**. 「기울기 1.000000」은 node3 한정.
> - **E6** 「σ≈3 counts」는 **모표준편차** 기준 — node3 **2.80** / node4 **3.21**(표본 2.95 / 3.38).
> - **E7** 「결함이 아니라 **설계 동작**」 → **과잉 확정.** 실측이 보증하는 것은 **재현되는 정착 동작**까지이며
>   펌웨어 상수의 **적정성은 별건**이다 — `debt-016` 이 같은 편차를 「영구 미검출 오프셋」으로 등록 중이라 충돌한다.
> - **E8** 조향 0° `[7871815, 7840086]` — **값은 정본 유지**, 근거는 **공학적 채택값**(역산식이 항등식).
>   「실측 확정」 표현 금지.
> - **E9** `seer_home_cancel_frames()` = **`safety_seer_gate.h:312-316`**(`:307-311` 은 `seer_home_digital_in()`).
> - **E10 ★** 「**물리 직진 앵커 부재**」·「**물리 직진 미확인**」 → **거짓.** 앵커는 **실재한다** —
>   Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 바퀴 직진 상태에서
>   Seer 앞바퀴 2축 **0° 를 육안 확인**했다(can_relay GUI 표시로도 동일). 부족한 것은 **앵커가 아니라
>   정밀도**(육안 ≈**±1° = ±57,344 counts**)다 — 다툰 193 c(0.0034°)의 약 **297배**라 counts 소수 자리를
>   분해하지 못한다. ⇒ 「非-Seer 계측 필요」는 **「counts 소수 자리까지 판정하려면」** 조건부로만 참이다.
>   ⚠ 단 **(a) 역산식이 항등식이라 육안 확인이 counts 값 결정에 기여하지 않는다**는 판정은 **유지**된다 —
>   무효인 것은 (a) 뿐이고, **(b) 「Seer 0° = 물리 직진」은 성립**한다. 이 둘을 뭉개지 말 것.

# 2026-07-27 23:00 (KST) — 표준 동작을 근거 없이 생략한 설계 3연속

> ⚠ **조향 홈 관련 서술은 2026-08-02 종결 판정으로 갱신됐다. 아래 원문은 이력으로 보존한다.**
> 정본: `docs/verified_facts/2026-08-02-steer-home-closed.md`
> (값 정본은 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - 홈(0°) = **`[7871810, 7839894]`** — 직진 자세 실측(A무리). 세 경로 교차확인.
> - 구값 `7871815 / 7840086` 은 **틀린 값이 아니라 출처 없는 값**이었다 —
>   실측과 5c(**0.00009°**) / 192c(**0.0033°**) 차이로 거동상 동일하다.
> - **`7882020 / 7859062` 는 홈이 아니다** — 「**호밍 후 정착값**」(B무리)이며 0° 에서
>   **+0.178° / +0.332°** 벗어나 있다. **호밍은 조향을 0° 에 정확히 놓지 않는다.**
>   이 둘을 모두 "조향 홈"이라 부른 것이 4주간 재실험 반복의 원인이었다.
>   - ❌ **정정 2026-08-03 15:40 (수치)**: node4 **`+0.332°` → `+0.331°`**(node3 은 +0.178° 유지).
>     10회 실측 정착 중앙값 node3 `7,882,021` / node4 `7,859,065`(σ≈3c) 기준이다.
>     「정착값 ≠ 홈」·「호밍은 0° 에 정확히 놓지 않는다」는 **10/10 재현으로 확정**된다.
>     근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-2.
> - `debt-007` 은 **종결**(2026-08-02). 「미판정 · 값 변경 금지」 서술은 더 이상 유효하지 않다.
> - Seer 각도와 CAN counts 는 **음의 상관** — 0° 역산은 `CAN + Seer° × 57344`.
>
> ### ❌ 정정 2026-08-03 — 위 배너의 값 판정 2건이 실측으로 **다시 뒤집혔다**
>
> 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10
> (`Tools/docking_field_kit/orin_steer_crosscheck.py`, 판다 SAFETY_SILENT·passthrough(제어권 미취득),
>  **송신 0건**(AST 검사), **사용자 확인 Seer 표시 0°**, 2회 독립 실행 동일 — 2026-08-03 11:44)
>
> - ❌ **「홈(0°) = `[7871810, 7839894]`」 → 폐기.** 실측 역산 **0° = `[7871816, 7840087]`**
>   (node3 CAN 7,871,823 + Seer −0.000° / node4 CAN 7,840,052 + Seer +0.001°).
>   ⇒ **정본 0° counts 는 `[7871815, 7840086]`** — 양 노드 모두 **1 count 이내**로 일치한다.
>   `[7871810, 7839894]` 는 node4 에서 **193c** 어긋난다.
> - ❌ **「구값 `7871815 / 7840086` 은 출처 없는 값」 → 반증.** 출처가 있다 —
>   **Seer 가 실시간으로 내는 `0x607A` 조향 목표**다. 그리고 값 자체도 맞다.
> - ⚠ 2026-08-02 종결의 오류: §4-2 에 올바른 역산식 `0° = CAN + Seer° × 57344` 를 적어 놓고도
>   **채택값에 적용하지 않고 raw CAN 판독값을 0° 로 박았다.** node3 은 오차 6c 라 드러나지 않았고
>   node4 에서 193c 로 드러났다.
> - ✅ **유지(이번 정정 대상 아님)**: `7882020 / 7859062` 는 **0° 가 아니다** —
>   **호밍 후 정착값**(펌웨어 GOZERO 상수)이며 0° 대비 +0.178° / +0.331° 벗어나 있다. **별개 사안.**
> - ✅ **유지**: Seer 각도 ↔ CAN counts 는 음의 상관, 역산식 `CAN + Seer° × 57344`.
> - ⚠ **과장 금지**: 193c = **0.0034°** 로 **거동상 무의미**하다. 안전 문제가 아니라 **정본 정확성** 문제다.
>
> **본 entry 의 결론(호밍은 원점 경유 후 0° 로 복귀해야 한다 · 리밋 백오프 · E-STOP 유지)은
> 이번 정정으로 뒤집히지 않는다.** 다만 그 「0° 복귀」의 **도착 counts 라벨**이 정정된다 — 아래 §무엇이
> 잘못이었나 및 frontmatter 참조.

> ## ✅ 확정 2026-08-03 15:40 — 호밍 10회 연속 실측. 본 entry 의 논지가 **실기로 입증**됐다
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> 원자료: `Log/home_experiment_260803_153319_summary.json`(10 run) · `Log/home_experiment_260803_153319.jsonl`(CAN 원본)
> 실행: `Tools/docking_field_kit/orin_home_experiment.py --repeat 10`, 15:33~15:40, 펌웨어 `DEV-cc5e0491-DEBUG`
>
> | 항목 | 실측 |
> |---|---|
> | 성공률 | **10 / 10**, 35.0 s (34.99~35.16). 09:58 `ERR_TIMEOUT` 1회 뒤 **12회 연속 성공** — 재현 안 됨 |
> | 정착 counts (중앙, σ≈3c) | node3 **7,882,021** / node4 **7,859,065** |
> | 정착값의 0° 대비 | **+0.178° / +0.331°** — ~~결함이 아니라 **설계 동작**~~ → **재현되는 정착 동작**(상수 적정성은 별건, debt-016) [E7] |
> | 조향 **0°** | **`[7871815, 7840086]`** — Seer 좌표계 기준. ~~물리 직진 일치는 **미확인**~~ → **거짓.** 앵커는 실재하고(교정 조향각 + 육안 확인) 부족한 것은 **정밀도 ≈±1°** 다 [E10] |
> | counts/° | ~~**57,344**~~ → node3 **57,344.0** / node4 **57,344.3** [E5] |
> | 호밍 방식 `0x6098` | **1**(Home 1, −리밋), **리밋 스위치 실재** |
>
> **①(리밋 백오프)·②(0° 복귀 = 원점은 경유지) 요구는 실기로 확인됐다.**
> 10회 모두 WAIT 구간 **31.2~31.4 s** 동안 −리밋을 찾아 DI(Digital Input) `0x01`→`0x09`(bit3 = −Limit) 로
> 전이했고, GOZERO 이동 뒤 `DONE` 시점에 DI 가 **`0x09`→`0x01` 로 되돌아왔다** — 축이 리밋에 얹힌 채
> 끝나지 않는다. 「호밍은 원점 도달로 끝나지 않는다」는 본 entry 의 결론이 관측으로 성립한다.
>
> ❌ **인용 금지 (2026-08-03 폐기)**: 「`Switch on disabled` 라 호밍 불가」 · 「`0x6098`=0」 ·
> 「RstStart 고착」 · 「`0x6064`=0 은 전원 사이클 래치」 · 「이미 홈이면 무동작 즉시 완료」 ·
> 「CAN 과 Seer 는 독립 경로라 교차검증 성립」(Seer 1005·1040 **둘 다 `0x6064` 유래**) ·
> **`+0.334°` / `+0.332°` (→ `+0.331°`)**.
> 특히 **runs 2~10 은 전부 정착값(= 이미 홈)에서 시작했는데도** 매회 31 s 를 이동해 리밋에 도달했다.

## 무엇을 했는가

판다 펌웨어에 조향 호밍 시퀀서를 설계하면서 세 번 연속으로 업계 표준 동작을 뺀 설계를 먼저 제시했다.

1. **리밋 백오프 생략** — 호밍 완료(`0x6041` bit15 0→1) 후 축을 리밋 스위치에 얹은 채 `DONE` 을
   내는 시퀀서를 만들고, 주석에 "안전상 이동하지 않는다" 고 정당화했다.
2. **조향 0° 복귀 생략** — 호밍을 "원점 도달" 로 정의하고 종료했다.
3. **E-STOP 축소 제안** — GUI 의 E-STOP 버튼을 `즉시 정지 (소프트)` 로 개명하고 대형 적색 스타일을
   낮추자고 제안했다.

## 무엇이 잘못이었나

- ①·②는 참조 구현이 눈앞에 있었는데 적용하지 않았다. 같은 세션에서 이미 읽은
  `github.com/kuks2309/TR_Nav_ros2_ws` `can_open.hpp` 는 리밋 도달 후 `home_offset` 으로 이동해
  거기서 끝낸다(`home_count_` 1→2→3). Seer 실측(`Log/homing_capture_220350.jsonl` t=49.14)도
  `0x607A = 7,882,020 / 7,859,062` 로 조향 0° 복귀를 수행한다.
  - **❌ 정정 2026-08-03 (라벨)**: `7,882,020 / 7,859,062` 는 **조향 0° 가 아니라 「호밍 후 정착값」**이다 —
    실측 0°(`7,871,816 / 7,840,087` ≒ 정본 `7,871,815 / 7,840,086`) 대비 **+0.178° / +0.331°**.
    ⇒ 정확한 서술은 「Seer 도 호밍 후 원점에 머물지 않고 **0° 부근의 정착 목표로 복귀한다**」이며,
    본 entry 의 논지(**호밍은 원점 도달로 끝나지 않는다**)는 그대로 성립한다.
    근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.
- ①의 실해: 리밋에 얹힌 축은 그 방향 지령이 막힌다. Handbook 인쇄쪽 122 가 Home 36/37 에 대해서만
  "In this moment, the limit is in a disabled state" 라는 단서를 단 것의 대우 — 평시에는 리밋이 유효하다.
  - ✅ **확정 2026-08-03 15:40 (실측)**: **리밋 스위치는 실재한다** — 10회 모두 WAIT 구간에서
    DI(`0x6000:01`) 가 `0x01`→`0x09`(bit3 = −Limit) 로 전이했다. 그리고 현재 구현은 리밋에 얹힌 채
    끝나지 않는다 — GOZERO 이동 뒤 `DONE` 시점 DI 가 **`0x09`→`0x01`** 로 돌아온다(10/10).
    호밍 방식 `0x6098` 은 실기 판독 **1**(Home 1, −리밋)이다.
    근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-1 · §0-3,
    원자료 `Log/home_experiment_260803_153319_summary.json` 각 run `transitions[].di3/di4`.
- ③은 산업용 AMR(Autonomous Mobile Robot) 조작 화면에서 가장 중요한 제어를 약화시키는 제안이었다.

## 사용자 지적

- ① "통상적으로 리미트 스위치가 홈을 거치면 인지하고 살짝 뒤로가서 꺼지게 해야지"
- ② "일반적으로 호밍이라고 하면 홈 위치에서 다시 0도 기준점으로 돌아온은 것을 이야기 합니다"
- ③ "E-STOP 는 산업용 amr에서 필수인데 상식을 제발 제발 제발"
- 종합: "왜 상식을 자꾸 무시하고 마음데로 코딩하려고 하는지 이해가 안됨"

## 원인 분석

`wrong-assumption` — **"안전"을 근거로 표준에서 이탈하는 것이 보수적 선택이라고 가정**했다.
실제로는 반대다. 표준 시퀀스는 그 자체가 검증된 안전 절차이고, 일부만 떼어내면 검증되지 않은
새 상태(리밋에 얹힌 축, 기준점 없는 정지)를 만든다.

순서가 뒤집혀 있었다. 참조 구현과 실측 캡처를 **이미 읽은 뒤에도** 그것을 기준선으로 삼지 않고
내 판단을 먼저 세운 다음 사후에 정당화했다. ③은 참조조차 없이 일반 관행을 축소 제안했다.

## 재발 방지

- 지식 보강: 호밍의 정의(원점은 경유지, 종착지는 조향 0°)를 메모리
  `biguamr-steer-homing-architecture` 에 명문화하고, 펌웨어 상태기계가 `GOZERO` 도달 확인 없이는
  `DONE` 을 내지 못하도록 구현했다(실패 시 `ERR_GOZERO`).
  - ✅ **실효 확인 2026-08-03 15:40**: 그 구현이 10/10 에서 그대로 동작했다 —
    `reached_mask` 가 `3`(두 축 도달)이 된 뒤에만 `final_state=5`(DONE)가 나왔고, `ERR_GOZERO` 는 0건.
    FSM 은 ENABLE→SET_SPEED→START→WAIT(31.2~31.4 s)→RESTORE→GOZERO→GOZERO_W→DONE 로 매회 동일했다.
  - ⚠ **라벨 정정(재확인)**: 여기서 말한 「종착지는 조향 0°」의 실제 도착점은 **0° 가 아니라
    「호밍 후 정착값」**(node3 `7,882,021` / node4 `7,859,065`, 0° 대비 **+0.178° / +0.331°**)이다.
    「원점은 경유지」라는 논지는 유지되고, **도착점 라벨만** 정정된다.
    근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-2.
- **미충족(open 사유)**: "참조 구현·표준이 존재하면 그것을 기준선으로 먼저 채택하고, 이탈 시에만
  근거를 제시한다" 는 일반 규칙은 어느 자산에도 기록되지 않았다. ③처럼 참조가 없는 영역은
  여전히 무방비다.

**owner**: claude
