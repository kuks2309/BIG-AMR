---
id: 2026-07-27-002
type: rule-violation
category: verify-skip
status: open
reflected_assets:
  # ⚠ 2026-07-27 감사: 첫 항목은 이 기록 자기 자신이라 '자산 반영' 증거가 되지 못한다(순환 참조).
  #   대조군 2026-07-27-003:6-11 · 2026-07-27-004:6-7 은 외부 자산만 나열한다. 항목은 지우지 않고 표기만 남긴다.
  - docs/claude-mistake/2026-07-27-002_node4-unverified-command-damage.md (하드웨어 지령 검증 원칙)  # ⚠ 자산 아님(자기 기록) — 2026-07-27 확인
  - .claude memory biguamr-motor-node4-sign-crab (실측 부호·crab 정합)  # ⚠ STEER_HOME 은 미판정 모순(debt-007) — 말미 §감사
  # ❌ 정정 2026-08-03: 위 줄의 「STEER_HOME 은 미판정 모순」은 해소됐다 — 이 메모리가 적은
  #   STEER_HOME=[7871815, 7840086] 이 실측 0°([7871816, 7840087])와 1c 이내로 **맞았다**.
  #   (docs/homing/2026-08-03-can-relay-homing-assets.md §10) 줄은 이력 보존을 위해 지우지 않는다.
  # ⚠ 2026-07-28: 아래 두 자산은 구 GUI 폐기로 **삭제됐다** (docs/adr/2026-07-28-old-gui-removal.md).
  #   ⇒ 본 기록의 재발방지 반영은 현재 **±90° 클램프만 남고 단계 램프·FAULT 래치는 대체 없이 사라진 상태**다.
  #   대체 요구는 debt-010(FAULT 래치 부재)·debt-011(테스트 0건)로 등록. 항목은 이력 보존을 위해 지우지 않는다.
  - Tools/amr_test_gui/amr_test_gui/ramp.py (2026-07-27 신설 — GUI 경로 한정 강제 인터록, 부분 충족)  # ⚠ 삭제됨(2026-07-28)
  - Tools/amr_test_gui/test/test_ramp.py (급점프·범위이탈·FAULT 래치 회귀 16건)  # ⚠ 삭제됨(2026-07-28)
  - Tools/amr_test_gui/gui.py `_steer_to`·`_steer_axis` (2026-07-28 — ±90° 하드 클램프만 계승)
---

> ## ❌ 통합 재정정 2026-08-03 17:30 — 아래 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> 원문은 삭제하지 않는다. 재검증: `python3 Tools/docking_field_kit/verify_homing_claims.py --docs`
> (`Log/` 원자료 재계산 — 문서를 근거로 문서를 고치지 않는다)
>
> - **E1** 「13회 연속 성공」 → **12회**. 2026-08-03 은 **시도 13 / 성공 12 / 실패 1**(09:58 `ERR_TIMEOUT`).
>   15:33 실행의 `baseline` 은 호밍이 아니라 **레지스터 스냅샷**이다 — 회차로 세지 말 것.
>   ⚠ **「10회 연속 10/10」 자체는 유효**하다.
> - **E2** 「`0x6064`=0 은 1회 관측·재현 안 됨」 → **[거짓] 재현된다.** `0x6041` bit15=1 인 채로 0 이 나온
>   캡처가 **6개**다. ⚠ **인과는 양쪽 미판정** — 0 이 관측된 직후 호밍이 성공한 회차가 있으므로
>   「0 이면 호밍 실패」도 성립하지 않는다. **「호밍 중(bit15=0)의 0」은 별개의 유효 관측**이다.
> - **E3** 「WAIT 31.7 s」 → WAIT(state 4) **체류 31.30 s**. 31.7 s 는 개시~RESTORE **절대시각**.
> - **E4** 「35.0 s」 → 15:33 10회 **평균 35.07 · 중앙 35.05**(34.99~35.16, 폭 0.17).
> - **E5** 「counts/° = 57,344」 → node3 **57,344.0** / node4 **57,344.3**. 「기울기 1.000000」은 node3 한정.
> - **E6** 「σ≈3 counts」는 **모표준편차** 기준 — node3 **2.80** / node4 **3.21**(표본 2.95 / 3.38).
> - **E7** 「결함이 아니라 **설계 동작**」 → **과잉 확정.** 실측이 보증하는 것은 **재현되는 정착 동작**까지이며
>   펌웨어 상수의 **적정성은 별건**이다 — `debt-016` 이 같은 편차를 「영구 미검출 오프셋」으로 등록 중이라 충돌한다.
> - **E8** 조향 0° `[7871815, 7840086]` — **값은 정본 유지**, 근거는 **공학적 채택값**(역산식이 항등식).
>   「실측 확정」 표현 금지.
> - **E9** `seer_home_cancel_frames()` = **`safety_seer_gate.h:312-316`**(`:307-311` 은 `seer_home_digital_in()`).
> - **E10 ★** 「**물리 직진 앵커 부재**」·「**물리 직진 미확인**」 → **거짓.** 앵커는 **실재한다** —
>   Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 바퀴 직진 상태에서
>   Seer 앞바퀴 2축 **0° 를 육안 확인**했다(can_relay GUI 표시로도 동일). 부족한 것은 **앵커가 아니라
>   정밀도**(육안 ≈**±1° = ±57,344 counts**)다 — 다툰 193 c(0.0034°)의 약 **297배**라 counts 소수 자리를
>   분해하지 못한다. ⇒ 「非-Seer 계측 필요」는 **「counts 소수 자리까지 판정하려면」** 조건부로만 참이다.
>   ⚠ 단 **(a) 역산식이 항등식이라 육안 확인이 counts 값 결정에 기여하지 않는다**는 판정은 **유지**된다 —
>   무효인 것은 (a) 뿐이고, **(b) 「Seer 0° = 물리 직진」은 성립**한다. 이 둘을 뭉개지 말 것.

> ## ★★ 확정 2026-08-03 15:40 — 호밍 실험 종결 (10회 연속 실측). 아래 모든 배너·정정은 이 절에 종속된다.
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (`orin_home_experiment.py --repeat 10`, 15:33~15:40, 접지 상태, 펌웨어 `DEV-cc5e0491-DEBUG`)
>
> | 확정 사실 | 값 |
> |---|---|
> | 호밍 성공률 | **10 / 10** (전부 `DONE`), 소요 **35.0 s**(WAIT 31.7 s) |
> | 호밍 후 정착 counts (10회 중앙, σ≈3c) | node3 **7,882,021** / node4 **7,859,065** |
> | 정착값의 0° 대비 편차 | **+0.178° / +0.331°** — 결함이 아니라 **설계 동작**(펌웨어 GOZERO 목표) |
> | 조향 **0°** | **`[7871815, 7840086]`** — **Seer 좌표계 기준.** ~~물리 직진 일치는 **미확인**~~ → **❌ 재정정 2026-08-03 17:30**: 물리 직진은 **확인됨**(Seer 교정각 + 사용자 육안, **≈±1°**). 부족한 것은 **counts 소수 자리**(193 c = 0.0034°) 판정 — 아래 17:30 블록 |
> | counts/° | **57,344** |
> | 호밍 방식 `0x6098` | **1**(Home 1, −리밋), **리밋 스위치 실재**(10회 모두 DI bit3 전이) |
>
> ❌ **인용 금지 (2026-08-03 폐기)**: 「`Switch on disabled` 라 호밍 불가」 · 「`0x6098`=0」 ·
> 「RstStart 고착」 · 「`0x6064`=0 은 전원 사이클 래치」 · 「이미 홈이면 즉시 완료」 ·
> 「CAN 과 Seer 는 독립 경로라 교차검증 성립」(1005·1040 **둘 다 `0x6064` 유래**) ·
> **`+0.334°` / `+0.332°` (→ `+0.331°`)**.
>
> ⇒ 「호밍이 안 된다」는 **현재 사실이 아니다** (09:58 `ERR_TIMEOUT` 1회 후 **12회 연속 성공**).
> ⚠ 본 entry 의 **핵심 결론(검증 없는 하드웨어 지령 금지)은 이번 확정과 무관하며 그대로 유효**하다.
> 이번에 소멸한 것은 「값이 미판정이다」라는 **근거**뿐이고, 절차 위반이라는 **사실**은 남는다.

> ## ❌ 재정정 2026-08-03 17:30 — 「물리 직진 앵커 부재」는 **오주장**이다
>
> ⚠ **박제된 원문(:78 이하 본문·§감사 A/B)은 한 글자도 고치지 않는다.** 여기서 고치는 것은
> **2026-08-03 15:40 에 내가 덧붙인 정정본의 라벨**뿐이다. 대상: 위 표의 「물리 직진 일치는 미확인」 ·
> §감사 A 「판정에 필요한 측정」에 붙인 「❌ 정정 2026-08-03 15:40」(「실제로 필요한 것은 非-Seer 물리 계측」).
>
> 그 정정본은 **서로 다른 두 질문을 하나로 뭉갰다.**
>
> - **(a) 육안 확인이 「조향 0° = 몇 counts 인가」 역산에 기여하나? → 아니오.**
>   역산식 `0° = CAN + Seer°×57344` 는 **항등식**이라 자세와 무관하게 같은 값을 돌려준다.
>   ⇒ 「측정 1(판다 read ↔ Seer 1040 동시 취득)은 반드시 일치하며 아무것도 판정하지 못한다」는
>   **유효하며 그대로 유지한다 — 뒤집지 않는다.**
> - **(b) 「Seer 0° 표시 = 바퀴가 물리적으로 직진」이 성립하나? → 예.**
>   Seer 1005/1040 이 내는 조향각은 EasyDRIVE `steerOffset` 으로 **교정된** 각이고, 사용자가
>   **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다(can_relay GUI 표시로도 동일 확인).
>
> ⇒ **(b) 를 (a) 에 딸려 무효화한 것이 오류다.** 「물리 직진 앵커 부재」·「물리 직진 앵커를 만들지 못했다」·
> 「非-Seer 앵커가 (무조건) 필요하다」는 **거짓 — 인용 금지.**
>
> **정확한 서술 (이대로 쓸 것)**
> - **물리 직진 앵커는 실재한다** — Seer 교정 조향각 + 사용자 육안 확인.
> - **정밀도 ≈ ±1°(= ±57,344 counts)** — 「바퀴가 직진이다」에는 충분, **counts 소수 자리**
>   (다툰 **193 c = 0.0034°**, ±1° 의 **1/297**) 판정에는 불충분.
> - `[7871815, 7840086]` 이 **공학적 채택값**인 사유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
> - **「非-Seer 물리 계측(수평계·레이저·정렬 지그)이 필요하다」는 「counts 소수 자리까지 판정하려면」
>   이라는 조건 하에서만 참**이다 — 「직진 여부」 판정에는 필요하지 않다.
>
> ⚠ 본 entry 의 **핵심 결론(검증 없는 하드웨어 지령 금지)** 은 여기서도 **불변**이다.
> 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §11-1 말미
> (「물리 정렬 오차의 상한은 여전히 **육안 한계 ≈±1°(±57,344 c)** 로, 이번에 좁힌 193 c 의 **약 297배**」).

> ⚠ **조향 홈 관련 서술 정정 — 정본은 아래 한 곳이다. 원문은 이력으로 보존한다.**
> **정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0** (호밍 **10회 연속 실측**, 2026-08-03 15:33~15:40)
> (값 정본은 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - 조향 0° = **`[7871815, 7840086]`** — ⚠ **Seer 좌표계 기준**이며 **물리적 직진은 미확인**이다.
> - **`7882020 / 7859062` 는 0° 가 아니다** — 「**호밍 후 정착값**」이며 0° 에서
>   **+0.178° / +0.331°** 벗어나 있다. 호밍 10회 실측 정착값은 node3 **7,882,021**(σ≈2.8c) ·
>   node4 **7,859,065**(σ≈3.2c) 로 σ≈3 counts 에 재현된다 ⇒ **결함이 아니라 설계 동작**이다.
> - `counts/°` = **57,344**(지령각→CAN 기울기 실측 1.000000) · `0x6098` 호밍 방식 = **1**(−리밋) ·
>   리밋 스위치 **실재** · 호밍 성공률 **10/10**, 소요 **35.0 s**.
>
> **❌ 2026-08-02 판정(`docs/verified_facts/2026-08-02-steer-home-closed.md`)은 폐기됐다 — 인용 금지:**
> - ~~홈 = `[7871810, 7839894]`~~ → **틀렸다.** node4 가 193c 어긋난 raw 판독값이었다.
> - ~~구값 `7871815 / 7840086` 은 「출처 없는 값」~~ → **반증됐다.** 출처는 **Seer 가 실시간으로 내는
>   `0x607A` 조향 목표**이며, 그 값이 1 count 이내로 맞았다.
> - ~~「CAN ↔ Seer 독립 교차확인」~~ → **성립하지 않는다.** Seer 1040 은 판다가 엿듣는 **바로 그
>   `0x6064` 의 아핀 변환**이다(기울기 ×57,344 = **1.000001**). 같은 프레임을 두 번 읽은 것이라
>   역산 `0° = CAN + Seer°×57344` 는 **항등식**이고 자세와 무관하게 같은 값을 낸다.
>   그 측정이 확정한 것은 **Seer 내부 조향 영점**이지 물리적 0° 가 아니다.
> - `debt-007` 은 종결이나, **홈 상수 부채 id 는 계보마다 다르다** — `origin/main` 이 정본:
>   홈 상수 하드코딩 = **debt-026** · can_relay 이름 충돌 = **debt-025** · 구동축 브링업 = **debt-027**.
>
> ### ❌ 정정 2026-08-03 — 위 배너의 값 판정 2건이 실측으로 **다시 뒤집혔다**
>
> 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10
> (`Tools/docking_field_kit/orin_steer_crosscheck.py`, 판다 SAFETY_SILENT·passthrough(제어권 미취득),
>  **송신 0건**(AST 검사), **사용자 확인 Seer 표시 0°**, 2회 독립 실행 동일 — 2026-08-03 11:44)
>
> - ❌ **「홈(0°) = `[7871810, 7839894]`」 → 폐기.** 실측 역산 **0° = `[7871816, 7840087]`**
>   (node3 CAN 7,871,823 + Seer −0.000° / node4 CAN 7,840,052 + Seer +0.001°).
>   ⇒ **정본 0° counts 는 `[7871815, 7840086]`** — 양 노드 모두 **1 count 이내**로 일치한다.
>   `[7871810, 7839894]` 는 node4 에서 **193c** 어긋난다.
> - ❌ **「구값 `7871815 / 7840086` 은 출처 없는 값」 → 반증.** 출처가 있다 —
>   **Seer 가 실시간으로 내는 `0x607A` 조향 목표**다. 그리고 값 자체도 맞다.
> - ⚠ 2026-08-02 종결의 오류: §4-2 에 올바른 역산식 `0° = CAN + Seer° × 57344` 를 적어 놓고도
>   **채택값에 적용하지 않고 raw CAN 판독값을 0° 로 박았다.** node3 은 오차 6c 라 드러나지 않았고
>   node4 에서 193c 로 드러났다.
> - ✅ **유지(이번 정정 대상 아님)**: `7882020 / 7859062` 는 **0° 가 아니다** —
>   **호밍 후 정착값**(펌웨어 GOZERO 상수)이며 0° 대비 +0.178° / +0.331° 벗어나 있다. **별개 사안.**
> - ✅ **유지**: Seer 각도 ↔ CAN counts 는 음의 상관, 역산식 `CAN + Seer° × 57344`.
> - ⚠ **과장 금지**: 193c = **0.0034°** 로 **거동상 무의미**하다. 안전 문제가 아니라 **정본 정확성** 문제다.

# 2026-07-27 18:30 (KST) — 검증 안 된 조향지령으로 node4 물리 손상 + 세션 반복 추측

## 무엇을 했는가
CAN relay PC 제어 검증 중, node4(RearSteer)에 **부호/target을 실측·정본과 대조하지 않은
조향 지령**(홈에서 큰 절대위치 점프)을 보냈다. 그 결과 node4가 **물리적으로 137°(정상
±90° 범위 밖)로 밀려 갇혔고**, 사용자가 CAN 직결 원점 호밍으로 복구해야 했다.
또한 세션 전반에서 "emulate로 충분(새 펌웨어 불필요)", "node4 무반응", "steer_sign=−1이
정답" 등을 **실측·물리 확인 전에 확정**으로 보고했다가 모두 뒤집혔다(실제: +90°·−90° 둘 다
정상, PC crab 왼쪽 이동 IMU 실증).

## 무엇이 잘못이었나
- `docs/claude_guideline/coding/coding.md` §2(사전조사, :L38-40)·§5(verify, :L71) 위반 —
  하드웨어 작동 지령(0x607A 위치)을 정본(config `steer_home_counts`·`kin_steer_sign`,
  protocol.py) 대조·단계검증 없이 송신.
- `docs/claude_guideline/reverse_engineering/principle.md` §6 위반 — [동작] 주장을
  배포자산/실측 대조 전 "확정"으로 반복 보고.
- 루트 CLAUDE.md failure_mode_guards "No fake completion" 위반 — 검증 전 완료/성공 단정.
- 결과: **실장비 물리 손상**(node4 범위이탈 갇힘) = 지식자산 오류를 넘어선 실피해.

## 사용자 지적
- "지금 심각한 실수를 하는 것입니다. 코딩은 추측에 의해서 하면 되는 것이 아닌데"
- "왜 분석 데이터를 활용하지 않고 마음대로 명령을 만들어서 보내는지"
- "너가 명령을 이상하게 줘서 바퀴가 이상하게 돌아가서 다시 원점으로 잡을것임"
- "개새끼야 seer 가 움직이는데 무슨 물리적 점검을 하라는 것인지"
- "지금 90도 아니고 0도인데" / (정정) "90도 확인"

## 원인 분석
규칙(coding §2/§5, reverse_engineering §6, no-fake-completion)은 설치·인지되어 있었으나
advisory였고, **하드웨어를 실제 작동시키는 지령**에 대해 "실측·정본 대조·단계 램프·실readback
교차확인"을 강제하는 게이트가 없었다. 데이터 그라운딩을 건너뛴 과신이 반복 원인이며,
2026-07-26-001·2026-07-27-001과 동일 뿌리(verify-skip)의 3번째 발현. 특히 이번엔
소프트 오보고를 넘어 **물리 손상**으로 이어져 심각도가 최고.

## 재발 방지
- **하드웨어 작동 지령 필수 절차**(강화): ① 정본(config/protocol) 상수·부호를 먼저 인용 →
  ② 저각/단계 램프(예 ±30→±60→±90)로 각 단계 실측 추종 확인 → ③ 이탈 시 즉시 home·중단 →
  ④ 단일 read를 물리상태 진실로 신뢰 금지(Seer 권위/물리 육안과 교차). 전범위 직접 점프 금지.
- (지식) 메모리 `biguamr-motor-node4-sign-crab` 신설 — 실측 확정: STEER_HOME=[7871815,7840086],
  홈에서 단계 램프면 ±90° 둘 다 정상, node4 137° 갇힘은 급점프/범위이탈이 원인,
  crab = 조향 90° + 구동부호로 좌/우(양수+90°=왼쪽, IMU 실증), `kin_steer_sign` 단정 금지.
  - ⚠ **[2026-07-27 감사] 위 줄의 "실측 확정: STEER_HOME=…" 은 무조건이 아니다 — 미판정 모순(debt-007).**
    이 값은 (b) 가설이 참이면 **기동 전(호밍 전)에만 유효**하다. 말미 §감사 A 참조.
    - ⚠ **정정 (2026-07-27 22:03 실기 캡처)**: 근거 (b)「호밍 후 위치 기준 0 재설정」은 기각 방향이나,
      **결론(무비판 신뢰 금지)은 유지**된다. 실측상 7,871,815 / 7,840,086 은 **호밍 *전*** 조향 목표이고
      호밍 *후* 정착값은 **7,882,020 / 7,859,062**(전환 t≈49.14)다
      (`Log/homing_capture_220350.jsonl`). 상세는 말미 §감사 A 의 ⚠ 정정 항목 참조.
      - **❌ 정정 2026-08-03**: 위 두 값의 **라벨**을 확정한다 —
        **7,871,815 / 7,840,086 = 조향 0°(직진)** 이고(실측 역산 7,871,816 / 7,840,087 과 1c 이내),
        **7,882,020 / 7,859,062 = 「호밍 후 정착값」이며 0° 가 아니다**(0° 대비 +0.178° / +0.331°,
        펌웨어 GOZERO 상수). 즉 :69 의 「실측 확정: STEER_HOME=[7871815, 7840086]」은
        **결과적으로 옳았고**, 이를 「미판정 모순」으로 되돌린 2026-07-27 감사와
        「출처 없는 값」으로 폐기한 2026-08-02 종결이 둘 다 **틀렸다.**
        근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.
        ⚠ 다만 :69 가 그 시점에 **근거를 인용하지 않은 채 "실측 확정"이라 쓴 것** 자체는 여전히
        절차 위반이다 — 값이 맞았다는 사실이 검증 생략을 정당화하지 않는다.
  - ⚠ **[2026-07-27 감사] 위 줄의 "137° 갇힘은 급점프/범위이탈이 원인" 은 미확정 가설이다** —
    본 기록 안에 이 인과를 뒷받침하는 측정·캡처 인용이 없다. 말미 §감사 B 참조.
- (부분 충족, 2026-07-27) `Tools/amr_test_gui/amr_test_gui/ramp.py` `SteerRamp` 신설 —
  ⚠ **[2026-07-27 감사] 아래 대책은 "급점프/범위이탈이 원인"이라는 _가정된 원인_ 에 대한 대책이다.**
  대안 가설(§감사 B, 기준계 불일치)이 참이면 이 경로를 막지 못할 수 있다. 대책 자체는 유효하므로 유지한다 —
  위 ①~④ 절차를 **코드로 강제**한다: ±90° 하드 클램프(범위이탈 지령 생성 불가) · ≤30° 단계 전진 ·
  단계별 실측 추종 확인 후에만 다음 단계 · 기한 내 미추종 시 FAULT 래치(지령 홈 0° 강제·구동 금지,
  해제는 운전자 명시 `reset()`). 램프 입력 실측은 조향축 중 **최악 편차**를 쓰므로 한 축만 뒤처져도
  전진하지 않는다. 회귀 테스트 `test_ramp.py` 16건이 이 속성들을 고정한다(137° 지령을 넣어도
  지령이 90°를 못 넘음을 검증).
- **(추가 2026-08-03 17:30) 「근거가 무효다」를 선언할 때 무효 범위를 질문 단위로 명시한다.**
  이번 문서에서 「역산이 항등식 ⇒ 육안 확인은 counts 역산에 기여 0」(참)에 딸려
  「Seer 0° 표시 = 물리 직진」(참)까지 무효화해 **「물리 직진 앵커 부재」라는 거짓을 만들어냈다.**
  ⇒ 규칙: ① 무효화 선언은 **「어느 질문에 대해 무효인가」**를 한 줄로 적는다.
  ② 계측 앵커를 「없다」고 쓰기 전에 **「없다」인지 「정밀도가 부족하다」인지**를 구분하고,
  후자면 **필요 분해능(이번 건: 193 c = 0.0034° vs 육안 ±1°)** 을 숫자로 병기한다.
  ③ 「비-Seer 계측이 필요하다」류 요구에는 반드시 **판정 대상(무엇을 소수 몇 자리까지)** 조건을 붙인다.
- (잔존 gap) **GUI 경로에 한정된 강제다.** 필드 스크립트(`Tools/docking_field_kit/*.py`)나 임시
  스크립트로 0x607A 를 직접 송신하면 램프를 우회한다. 저장소 전역 강제(hook 또는 공용 지령 래퍼)는
  여전히 미설치 — 이 때문에 `status: open` 유지.

**owner**: claude

## 2026-07-27 감사 (append — 위 서술은 이력 보존을 위해 지우지 않는다)

> 인용 줄번호는 2026-07-27 감사 시점 기준이다. 같은 내용을 예전 줄번호(`tongyi_amr.yaml:20-32`,
> `design-inputs.md:56,81`)로 인용하는 문서가 다수 있으므로, 줄이 어긋나면 인용문 문자열로 찾을 것.

### A. STEER_HOME=[7871815, 7840086] 은 "실측 확정"이 아니라 **미판정 모순**이다 (debt-007)

> **❌ 정정 2026-08-03 — 이 §A 의 결론은 뒤집혔다.**
> `[7871815, 7840086]` 은 **조향 0°(직진) counts 로 맞다** — 2026-08-03 11:44 실측 역산
> `[7871816, 7840087]` 과 **양 노드 1 count 이내**(`orin_steer_crosscheck.py`, 송신 0건,
> 사용자 확인 Seer 표시 0°, 2회 독립 실행 동일. `docs/homing/2026-08-03-can-relay-homing-assets.md` §10).
> `debt-007` 은 2026-08-02 에 종결됐고, 이번 실측으로 **채택값만 `[7871810, 7839894]` → `[7871815, 7840086]`**
> 로 되돌려진다(node4 193c = 0.0034° 차, **거동상 무의미**).
> 아래 §A 본문·감사 서술은 이력 보존을 위해 그대로 둔다.

- 정본 config 는 같은 값에 대해 미판정 모순을 명시한다 —
  `src/Actuators/motor_control/config/tongyi_amr.yaml:91`
  `steer_home_counts: [7871815, 7840086]  # ⚠ debt-007 판정 전까지 무비판 신뢰 금지`,
  같은 파일 `:79-89` 「⚠ 미해소 모순 (2026-07-27 관측, debt-007) … Seer 가 이미 기동·호밍을 마치고
  (1040 position≈0·calib=True) 육안으로도 바퀴가 직진인 상태인데 **판다 read 0x6064 는 ≈0** 이었다.
  설계대로라면 그 시점 raw 는 ≈7.87M 이어야 한다. → 둘 중 하나가 틀렸다: (a) 판다의 조향 0x6064 read 가
  오염됐다 (b) 호밍 후 드라이브가 위치 기준을 0 으로 재설정한다(= 아래 값은 기동 전에만 유효) …
  미판정 상태이므로 어느 쪽으로도 값을 고치지 않는다」.
- 설계 근거도 값이 **절대 목표(기동 전 유효)** 임을 전제한다 —
  `docs/ros2_driver/2026-07-09-design-inputs.md:78` 「부팅 직후 **전 노드 0x6064 ≈ 0** … 홈값 아님, 정상」,
  `:82` 「0에서 홈까지 3.3 s 물리 스윙(+137.3° = config steerOffset) … **홈 상수는 전원 사이클 불변**
  (07-07/07-08 두 전원 세션 일치)」, 그리고 `:83` 「⚠ 2026-07-27 재개방 — 「홈 상수는 전원 사이클 불변」은
  **미판정 모순이다(반증 확정 아님)**」, `:139` 「0 기준점이 절대 엔코더 기준인지 … **미확인** — 2회 전원
  사이클 관측뿐」.
- ⚠ **정정 (2026-07-27 22:03 실기 캡처)** — 위 두 bullet 이 인용한 전제 중 아래 3건은 실측과 어긋난다
  (원문 보존, 값·코드 변경 없음. `Log/homing_capture_220350.jsonl`, 253,510 프레임 수동청취).
  - **(b)「호밍 후 위치 기준 0 재설정」은 기각 방향**이다. 호밍 완료 후 조향 목표·정착값은 0 이 아니라
    **node3 7,882,020 / node4 7,859,062**(0x607A t=179.993/179.9955 까지 유지)이고, 호밍 전 값
    7,871,815 / 7,840,086(t=0.0346/0.0347)에서 **t≈49.14 에 전환**된다. `tongyi_amr.yaml:85-86` 의
    관측(판다 read 0x6064 ≈ 0)에는 **제3 후보 (c) 관측 시점이 호밍 진행 중이었다** 를 추가해야 한다 —
    호밍 구간(`0x60FB.4=1` write t=17.9252/17.9257 이후 ~ 완료)에서 조향 `0x6064` 는 두 노드 모두
    **정확히 0** 을 보고한다. ⇒ 위 「판정에 필요한 측정」에 「read 시각이 호밍 구간인지」 확인을 추가할 것.
  - **`design-inputs.md:78` 「부팅 직후 전 노드 0x6064 ≈ 0 … 정상」은 무조건 참이 아니다.** 본 캡처는
    시작 시점(t=0~5.16 s) 조향 `0x6064` 가 node3 7,882,014 / node4 7,859,058 (= 137.4513° / 137.0511°)
    로 **홈 부근**이었다. 「0x6064≈0 = 콜드 부팅」 분기 근거는 미판정으로 되돌린다.
  - **`design-inputs.md:82` 의 「홈 상수는 전원 사이클 불변」·「+137.3° = config steerOffset」도 단일값이
    아니다.** EasyDRIVE `steerOffset` 은 node3 **138.000** / node4 **137.250** 으로 노드별로 다르고,
    실측 정착값 7,882,020 / 7,859,062 는 **+137.45° / +137.05°**(57344 counts/°)에 해당한다.
  - ⇒ §A 의 결론(「STEER_HOME 을 무조건 신뢰하지 말 것」, debt-007 미판정 유지)은 **그대로 유효**하나,
    그 근거는 (b) 가 아니라 **「7,871,815/7,840,086 은 호밍 *전* 값이고 호밍 *후* 정착값은 다르다」**로
    정정한다. 어느 쪽이 물리 직진인지는 여전히 미판정이므로 값은 변경하지 않는다.
  - **❌ 정정 2026-08-03**: 위 줄의 「**어느 쪽이 물리 직진인지 미판정**」은 **판정됐다** —
    물리 직진(조향 0°)은 **7,871,815 / 7,840,086** 쪽이다(실측 역산 7,871,816 / 7,840,087, 1c 이내).
    **7,882,020 / 7,859,062 는 호밍 후 정착값이며 0° 가 아니다**(+0.178° / +0.331°).
    따라서 §A 결론 「STEER_HOME 을 무조건 신뢰하지 말 것」의 **근거는 소멸**했다
    (`docs/homing/2026-08-03-can-relay-homing-assets.md` §10).
- 즉 본 기록의 :52 (「실측 확정: STEER_HOME=…」, 감사 전 원본 :50) 은 **조건(기동 전/호밍 전 유효
  가능성)을 누락한 채 "실측 확정"만 남겨** 후속 작업자가
  검증 대상에서 제외할 수 있는 서술이다. 여기서 어느 쪽이 참인지는 **판정하지 않는다**(값 변경 없음).
- **판정에 필요한 측정**:
  1. Seer 호밍 완료·바퀴 육안 직진 상태에서 조향 노드 `0x6064` 를 (i) 판다 read 와
     (ii) Seer 1040 position 으로 **동시 취득**해 절댓값 일치 여부 확인
     (구동 노드는 이미 양쪽 일치가 관측됐다 — `tongyi_amr.yaml:87`).
  2. 조향 **임의 자세에서 전원 재투입** 후 `0x6064` 재현성 확인
     (= `design-inputs.md:139` 가 「미확인」으로 남겨둔 바로 그 측정).

  > ### ❌ 정정 2026-08-03 15:40 — 위 **측정 1 은 교차검증이 되지 못한다** (원문 보존)
  >
  > 「판다 read `0x6064`」와 「Seer 1040 position」은 **독립 경로가 아니다.**
  > - **1040 = `0x6064` 의 아핀 변환** — 기울기 × 57,344 = **1.000001**(스윕 실측).
  > - **1005(`steer_angles`) 도 같은 엔코더 유래** — `0x6064` × **1.000130**(Seer 130 ppm 스케일 보정).
  > - 판다는 Seer 가 받는 `0x581~0x584` 응답을 수동청취할 뿐이므로 **같은 프레임을 두 번 읽는다.**
  >
  > ⇒ 측정 1 은 반드시 일치하며 **아무것도 판정하지 못한다.** 「세 경로 교차확인」·「독립 실행 일치」류
  > 서술도 같은 이유로 무효다. 실제로 필요한 것은 **非-Seer 물리 계측**(수평계·레이저·기구 정렬 지그)이며,
  > 이것이 없으므로 `[7871815, 7840086]` 은 **「Seer 좌표계 0°」까지만 확정**이고 **물리 직진은 미확인**이다.
  > 측정 2(전원 재투입 재현성)는 여전히 유효한 미측정 항목이다.
  > 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-3 · §0-4 · §11-1 · §12-2 · §14-3.
  >
  > ### ❌ 재정정 2026-08-03 17:30 — 바로 위 3~4행의 「非-Seer 계측 필요 / 물리 직진 미확인」에 **조건이 빠졌다**
  >
  > - **유지**: 「측정 1 은 항등식이라 아무것도 판정하지 못한다」 · 「세 경로 교차확인·독립 실행 일치는 무효」 ·
  >   「측정 2(전원 재투입 재현성)는 유효한 미측정 항목」 — **셋 다 그대로 옳다.**
  > - **정정**: **물리 직진 앵커는 실재한다.** Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된**
  >   조향각이고, 사용자가 **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다
  >   (can_relay GUI 표시로도 동일 확인). 「앵커 부재」는 **거짓**이다.
  > - **非-Seer 물리 계측이 필요한 대상은 「counts 소수 자리」이지 「직진 여부」가 아니다.**
  >   기존 앵커의 **정밀도 ≈±1°(±57,344 c)** 는 「바퀴가 직진이다」에는 충분하고,
  >   다툰 **193 c = 0.0034°**(±1° 의 **1/297**) 판정에는 불충분하다.
  > - ⇒ `[7871815, 7840086]` 이 **공학적 채택값**인 사유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
  > 근거: 같은 문서 **§11-1 말미**(「물리 정렬 오차의 상한은 여전히 육안 한계 ≈±1°(±57,344 c) 로,
  > 이번에 좁힌 193 c 의 약 297배」).
- 판정 전까지 `allow_homing_motion` 게이트를 끄지 말 것(`tongyi_amr.yaml:89-90` 「사용 규칙(판정
  전까지) … 그 게이트가 이 불일치를 잡아내는 방어선이다」).
  - ⚠ **정정 (2026-07-27)** — 이 게이트의 **근거 서술이 바뀌었다**(게이트 존폐는 여기서 판단하지 않으며,
    설정·코드 변경 없음). 종전 근거였던 「콜드부팅 137° 조향 스윙 = 위험한 미지 거동」은 **사실이 아니다** —
    호밍 완료 후 원점(음의 리밋)에서 조향 0° 로 복귀하는 **설계된 정상 이동**이다
    (목표 7,882,020 / 7,859,062 = +137.45° / +137.05°, `Log/homing_capture_220350.jsonl`;
     Seer 실측 시퀀스 `0x6040=0x86` → `0x6099=2500` → `0x60FB.4=1`(RstStart) → 탐색 ~29 s →
     `0x6041` bit15 0→1 → `0x6060=1` → `0x6081/0x6083/0x6084` → `0x607A`=조향 0° → `0x6040=0x3F`,
     복귀 약 3 s) [Handbook V7.0 §6.9, page 171 — `0x60FB.4` = RstStart, "0-Reset off, 1-Reset on"].
    ⇒ 게이트를 유지하려면 근거를 「debt-007(홈 counts 전/후 불일치) 미판정」으로 다시 세워야 한다.
    - **❌ 정정 2026-08-03 (라벨)**: 위 문단이 `7,882,020 / 7,859,062` 를 「**조향 0° 로 복귀**」의 목표라 부른 것은
      **부정확**하다. 그것은 **「호밍 후 정착값」**(펌웨어 GOZERO 상수)이며 **조향 0° 가 아니다** —
      실측 0°(`7,871,816 / 7,840,087`) 대비 **+0.178° / +0.331°** 벗어나 있다.
      정확한 서술은 「호밍 원점에서 **0° 부근의 정착 목표**로 약 137° 복귀한다」이다.
      마지막 문장의 「debt-007 미판정」도 소멸했다(debt-007 종결 2026-08-02, 값 확정 2026-08-03).
      근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.

### B. ":53 「137° 갇힘은 급점프/범위이탈이 원인」" 은 **미확정 가설**이다 (감사 전 원본 :51)

- 본 기록 어디에도 이 인과를 뒷받침하는 측정·캡처 인용이 없다(:17-19 「무엇을 했는가」는 결과 서술,
  :41-46 「원인 분석」은 「검증 절차 부재」라는 **절차적** 원인만 다룬다).
- 배제되지 않은 대안 가설: **기준계 불일치**. `tongyi_amr.yaml:85-86` 의 (b)「호밍 후 드라이브가 위치
  기준을 0 으로 재설정한다(= 아래 값은 기동 전에만 유효)」가 참이면, 홈 counts(7.87M)를 **절대 목표로
  송신하는 것 자체**가 137.3° 스윙(`design-inputs.md:82`)을 유발한다. 이 경우 원인은 "급점프"가 아니라
  "기준계 불일치"이며, :55-60 의 ±90° 클램프·단계 램프는 이 경로를 막지 못할 수 있다.
  - ⚠ **정정 (2026-07-27 22:03 실기 캡처)** — 위 문단의 **근거였던 (b) 는 기각 방향**이고,
    '기준계 불일치' 가설 자체는 **다른 형태로 유지·구체화**된다(원문 보존, 값·코드 변경 없음).
    1. **(b) 기각 방향**: 호밍 완료 후 Seer 가 정착시킨 조향 목표는 0 이 아니라
       **node3 7,882,020 / node4 7,859,062** 이며 t=179.99 까지 그대로 유지된다
       (`Log/homing_capture_220350.jsonl`, 0x607A 최종값 t=179.993/179.9955). 호밍 전 값은
       7,871,815 / 7,840,086(t=0.0346/0.0347)으로, 전환 시각은 t≈49.14 다. 즉 호밍이 위치 기준을
       0 으로 재설정하지 않는다. `tongyi_amr.yaml:85-86` 관측(판다 read 0x6064 ≈ 0)은 **(c) 관측
       시점이 호밍 진행 중이었다** 로 설명된다 — 호밍 구간(0x60FB.4=1 write t=17.9252/17.9257 이후
       ~ 완료)에서 조향 0x6064 는 두 노드 모두 정확히 0 을 보고한다.
    2. **137° 는 이상 거동이 아니다**: 호밍 완료 후 원점(음의 리밋)에서 조향 0° 로 복귀하는
       **설계된 이동**이다. 목표 7,882,020 / 7,859,062 = **+137.45° / +137.05°**(57344 counts/°)이며
       EasyDRIVE `steerOffset` 138.000 / 137.250 과 대응한다. 따라서 "원인 미상", "이상 스윙",
       "Home 36/37 기계 하드스톱" 류 해석은 **오류**다. (조향축 리밋 스위치 실재 · 방식 **Home 1**
       (음의 리밋 트리거), 전 노드 `0x6098 = 1` 실기 판독 [Handbook V7.0 §4.6, page 116])
       - ✅ **재확인 2026-08-03 15:40** — 이 괄호 안 3건은 **10회 실측으로 확정**됐다:
         `0x6098` = **1**(Home 1, −리밋), **리밋 스위치 실재**(호밍 10회 전부 DI `0x01`→`0x09`, bit3 = −Limit),
         호밍 **10/10 성공**(35.0 s, WAIT 31.7 s). ⇒ 「`0x6098`=0 이라 호밍 비활성」·「`Switch on disabled` 라
         호밍 불가」·「RstStart 고착」·「이미 홈이면 즉시 완료 → 타임아웃」은 **전부 폐기**(인용 금지).
         근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-1 · §0-4.
    3. **가설의 잔존 형태**: 호밍 원점(0 counts) ↔ 직진(조향 0°) 자세가 **+137.45°/+137.05°**
       떨어져 있으므로, **0 counts 부근을 조향 목표로 송신하면 바퀴는 직진 대비 약 137° 자세로 간다.**
       사고 시 갇힌 '137°' 가 이 호밍 원점 자세였을 가능성이 있다
       (⚠ **추론** — 사고 당시 실제 송신 `0x607A` 로그 미회수, 확정 금지).
       ⇒ ±90° 클램프·단계 램프가 이 경로를 막지 못할 수 있다는 원문의 지적은 **유효하게 유지**된다.
    4. **❌ 정정 2026-08-03 (라벨)**: 위 2·3 항이 `7,882,020 / 7,859,062` 를 「조향 0° 로 복귀」·
       「직진(조향 0°) 자세」로 부른 것은 **부정확**하다 — 그것은 **「호밍 후 정착값」**(GOZERO 상수)이고
       **조향 0° 는 `7,871,815 / 7,840,086`**(실측 역산 `7,871,816 / 7,840,087`, 1c 이내)이다.
       두 무리는 **+0.178° / +0.331°** 떨어져 있다. 다만 3항의 각도 계산(호밍 원점 ↔ 직진 ≈ +137°)과
       결론(클램프·램프가 이 경로를 막지 못할 수 있다)은 **그대로 유효**하다 —
       0.334° 는 137° 논지에 영향이 없다. 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.
       - ❌ **정정 2026-08-03 15:40 (수치)**: 위 줄의 **`0.334°` 는 틀린 값이다 → `+0.331°`**(node4).
         node3 은 **+0.178°**. 10회 실측 정착 중앙값 node3 `7,882,021` / node4 `7,859,065` 기준이다
         (σ ≈ 3 counts). 「137° 논지에 영향 없다」는 **결론 자체는 유지**된다.
         같은 이유로 문서 전반의 `+0.332°` 표기도 `+0.331°` 로 읽을 것.
         근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-2 · §0-4.
- **판정에 필요한 측정**: 호밍 완료 직후 조향 `0x6064` 를 판다·Seer 1040 양쪽에서 동시 취득해 홈 counts와
  대조(=§A 측정 1과 동일). 그리고 사고 당시 실제 송신된 `0x607A` 목표값·직전 `0x6064` 로그의 회수.
  - ❌ **정정 2026-08-03 15:40**: 앞 문장(=§A 측정 1)은 **폐기**한다 — 판다 read 와 Seer 1040 은
    독립 경로가 아니라 **같은 `0x6064` 의 아핀 변환**이라 항상 일치하며 아무것도 판정하지 못한다
    (§A 측정 1 옆 정정 블록 참조). **뒤 문장(사고 당시 `0x607A`·`0x6064` 로그 회수)만 유효한
    미측정 항목으로 남는다.** 이 로그가 없는 한 §B 의 137° 원인은 계속 **미확정 가설**이다.
    호밍 후 정착 자세 자체는 이제 확정됐다 — 10회 중앙 node3 `7,882,021` / node4 `7,859,065`
    (0° 대비 +0.178° / +0.331°). 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-2 · §0-4.
- `SteerRamp` 대책(:59-66, 감사 전 원본 :53-58)은 **가정된 원인에 대한 대책**으로 표기해 유지한다
  (삭제하지 않음). 대안 가설이 참으로
  판명되면 "지령 기준계(절대/상대) 확인 게이트"가 추가로 필요하다.

- 본 감사에서 값·상수는 일절 변경하지 않았다(서술·경고만).
