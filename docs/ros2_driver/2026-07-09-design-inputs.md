# ROS2 CAN 모터 구동 — 설계 입력 (기존 분석 통합)

- 작성: 2026-07-09 (KST) · sess c64c0e35
- 목적: **ROS2 노드가 Seer 마스터를 대신해 CAN 으로 Tongyi AMR 4축 서보를 직접 구동**하기 위한 설계 입력.
  `experiements/`(실측 CAN 분석)와 `References/motor_configuration/`(EasyDRIVE config)의 기존 분석을 통합 — 신규 실측 없음.
- 검증 등급: **✓** 1차 source/실측 직접 · **⚠** 미확정(구동 전 확인 필요)

> ### ⚠ 2026-07-27 감사 — 이 문서의 ✓ 등급 대부분은 현재 **재대조 불가**
>
> 본 문서가 근거로 인용한 `experiements/`·`experiences/` 하위 자산이 이 저장소에 **존재하지 않는다**.
> 확인 방법·결과(2026-07-27):
> - 저장소 루트에 `experiements/`·`experiences/` 디렉터리 자체가 없다(`ls` 결과 `experiments/` 만 존재).
> - git 전체 이력에도 없다 — `git log --all -- '*tongyi_250k_startup*' '*linux-session-log*' '*maneuver-analysis*' '*drive-analysis*'` 무결과, 전 커밋 tree name grep 무결과.
> - 별도 저장소 `/home/nvidia/Project/Ford-CATL-AMR/T-Driver-Analysis/` 에서도 `.asc` 캡처·session-log·maneuver/drive analysis 는 0건.
>
> 재대조 불가 인용: :5(`experiements/`), :16(2026-07-07-tongyi-drive-analysis.md), :37(2026-07-07-tongyi-maneuver-analysis.md), :49(linux-session-log), :51(tongyi_250k_startup_20260708_103448_can0.asc).
> 유일하게 실재가 확인된 인용은 [motor-config-analysis.md](../../References/motor_configuration/motor-config-analysis.md)·[tongyi-motor-protocol-tables.md](../../References/Tongyi-Motor-Controller/docs/tongyi-motor-protocol-tables.md) 두 건과,
> 다른 저장소에 있는 `T-Driver-Analysis/experiements/2026-07-08_pcan-relay-linux-handoff/_extracted/pcan_relay_test/07_drive_gui.py`(:49) 이다.
>
> **따라서 위 자산에 근거한 ✓ 는 "원 세션의 주장"으로만 읽어야 하며, 독립 재검증 전까지 검증 대상에서 제외하지 말 것.**
> (원 등급 표기는 이력 보존을 위해 지우지 않는다. 캡처에서 도출된 수치도 변경하지 않는다 — 실측 없이 변경 불가.)
> 후속 조치: 원본을 회수해 경로를 실경로로 정정하거나, 회수 불가가 확정되면 해당 항목을 ✓ → ⚠(재대조 불가)로 재표기.
>
> ### ⚠ 줄번호 이동 주의 (2026-07-27 감사로 본문이 길어짐)
> 다른 문서들이 이 파일을 **감사 전 판(revision)** 기준 줄번호로 인용하고 있다(예: `docs/verified_facts/2026-07-27.md:196` 의 「design-inputs.md**:56,81**」, `config/tongyi_amr.yaml:92` 의 「:56」, `controller.py` 의 「:56, :81」).
> 감사 주석이 append 되면서 줄번호가 밀렸으므로, **줄번호가 아니라 아래 대응표로 찾을 것**(원문은 한 줄도 삭제하지 않았다):
>
> | 감사 전 | 현재 | 내용 |
> | --- | --- | --- |
> | :5 | :5 | `experiements/` 인용 |
> | :16 | :53 | 버스 부하 「여유 작음」 |
> | :37 | :74 | maneuver-analysis 인용 |
> | :47 | :84 | §5 제목 「실차 재생 성공」 |
> | :49 | :96 | 07_drive_gui.py + linux-session-log |
> | :51 | :98 | 기동 캡처 `.asc` 인용 |
> | :56 | :103 | 「0→홈 137.3° 스윙 · 홈 상수 전원 사이클 불변」 |
> | :57 | :120 | 「enable 누락 시 HALT」 |
> | :73 | :136 | §6 콜드/웜 분기 브링업 |
> | :74 | :147 | §6 폴링 예산 |
> | :81 | :160 | §7 첫 행(조향 절대원점 불변성) |
> | :87 | :166 | §7 「DirectDriver 실차 미검증」 |
>
> 이후 편집으로 또 밀릴 수 있으니, 인용은 가급적 줄번호 대신 **절 번호 + 원문 문구**로 할 것.

## 1. 버스·노드 (✓ 실측 3회 이상 교차검증)

| 항목 | 값 | 근거 |
| --- | --- | --- |
| 물리 | 250 kbps classic CAN, 11-bit ID | [tongyi-motor-protocol-tables.md §1](../../References/Tongyi-Motor-Controller/docs/tongyi-motor-protocol-tables.md) |
| 프로토콜 | CANopen CiA301/402, **SDO 폴링 전용**(PDO·SYNC·EMCY·NMT-Start 미사용, Pre-operational 유지) | 상동 §11 |
| 노드 | 1=FrontWalk(구동·PV) · 2=RearWalk(구동·PV) · 3=FrontSteer(위치) · 4=RearSteer(위치) | [motor-config-analysis.md](../../References/motor_configuration/motor-config-analysis.md) |
| 기구 | 듀얼 스티어 AGV, 휠베이스 1.200 m, 휠반경 0.125 m, 구동감속 32, 조향감속 315, maxRPM 3000, 엔코더 16384선 | 상동 |
| 버스 부하 | Seer 폴링 ~1600 fps 에서 75~80 % → **여유 작음, 폴링 예산 관리 필수** | [2026-07-07-tongyi-drive-analysis.md](../../experiements/can-decode/2026-07-07-tongyi-drive-analysis.md) |

## 2. 런타임 오브젝트 8종 (✓)

쓰기: `0x6040`(Controlword: 구동 **0x86** 기동 1회 / 조향 **0x3F** setpoint 마다), `0x60FF`(구동 목표속도, **0.1 rpm-모터**), `0x607A`(조향 목표위치, counts)
읽기: `0x6064`(위치, 최고빈도), `0x6041`(Status ⚠벤더편차), `0x603F`(Error, 전구간 0), `0x6078`(전류 INT16, **0.01 A** — Handbook Appendix I p195), `0x6000.1`(DI)

SDO 프레임: `0x600+N` 요청 / `0x580+N` 응답, expedited(0x23/0x2B/0x2F 쓰기, 0x40 읽기). 왕복 ~1 ms.

## 3. 스케일 (✓ config+실측 상호 확정)

| 축 | 환산 | 검증 |
| --- | --- | --- |
| 조향 각도 | **57,344 counts/°** (= 16384×4×315/360) | 실측 홈↔90° Δ=+5,160,960 = 정확히 90.00° |
| 구동 속도 | cmd[0.1rpm] = v[m/s] × **24,447** (= ÷(2π·0.125)×60×32×10) | ±2445=0.10 m/s, ±24447=1.00 m/s, 최대 1.23 m/s |
| 구동 위치 | **2,670,177 counts/m** | 실운전 Δ−24.07M ≈ −9.0 m 일치 |

부호 관례(실측): **전진 = N1·N2 모두 음(−)**. 조향 홈 = N3 7,871,815 / N4 7,840,086 (⚠ §7 참조).

> ⚠ **정정 (2026-07-27)** — 위 「조향 홈」 counts 는 **호밍 이전** 값이다(원문은 이력 보존을 위해 남긴다).
> 호밍 완료 후 조향 **0°(직진)** 로 지령·정착되는 값은 **N3 7,882,020 / N4 7,859,062**(= +137.45° / +137.05°, 57,344 counts/°)이며,
> 이것이 EasyDRIVE `steerOffset` 138.000 / 137.250 과 대응하는 정본이다. 차이는 N3 +10,205 counts(+0.178°) / N4 +18,976 counts(+0.331°).
> ⇒ 단일 '홈 상수' 로 인용하지 말고 항상 **(호밍 전 / 호밍 후)** 조건을 병기할 것. 값 변경은 debt-007 판정 후.
> 근거: `Log/homing_capture_220350.jsonl` t≈49.14 (`0x607A` 전환), 드라이브 파라미터 직접 판독.

## 4. 검증된 운동학 모델 (✓ 3회 캡처 일치)

[2026-07-07-tongyi-maneuver-analysis.md](../../experiements/can-decode/2026-07-07-tongyi-maneuver-analysis.md)

| 조향 자세 | 구동 동일방향 | 구동 역방향 |
| --- | --- | --- |
| 홈(정면) | 직진(−)/후진(+) | (미관측) |
| 90°(홈+5.16M) | 좌/우 크랩 | 좌/우 제자리회전(+미세토우 ±7.6k) |

- 조향은 **국면 전환 시 1회 세팅 후 유지**(이벤트성), 구동은 ~48 Hz 연속 지령.
- Seer 는 홈/90° **2상태만** 사용 — 중간 조향각 연속 스워브는 미관측(⚠ §7).

## 5. 검증된 구동 레시피 (✓ 실차 재생 성공 + DirectDriver selftest)

> ⚠ **2026-07-27 미판정 — 제목의 '실차 재생 성공'은 같은 문서 내부와 상충한다.**
> - 성공 주장: 본 절 제목(:47)과 :57 「실차 재생 실패→enable 접합 후 성공으로 확인」.
> - 반대 기록: :87 「DirectDriver 실차 미검증 | selftest(vcan)만 통과, **실차 T4 미실시**」.
>
> 어느 범위까지 실차에서 돌았는지 이 문서만으로는 가릴 수 없다. 판정 근거인 :49 의 `linux-session-log` 는
> 본 저장소·git 이력·T-Driver-Analysis 어디에서도 회수되지 않았다(문서 상단 감사 노트 참조).
> 같은 절의 기동 캡처 인용(:51 `..._can0.asc`)도 재대조 불가다.
> **실차 재검증 전까지 §5 를 '검증됨'으로 인용하지 말 것.** (제목의 ✓ 는 이력 보존을 위해 지우지 않는다.)
> 판정에 필요한 것: 어느 시퀀스가 실차에서 실행됐는지 기록된 원 세션 로그의 회수, 또는 잭업 상태 재실행 기록.

[relay 킷 07_drive_gui.py DirectDriver](../../experiements/2026-07-08_pcan-relay-linux-handoff/_extracted/pcan_relay_test/07_drive_gui.py) + 실차 재생([linux-session-log](../../experiements/2026-07-08_linux-session-log.md))

1. **콜드 스타트 브링업** (✓ 기동 캡처 [tongyi_250k_startup_20260708_103448_can0.asc](../../experiences/can-decode/captures/tongyi_250k_startup_20260708_103448_can0.asc) 전수 디코드, 2026-07-09 본 세션):
   - 부팅 직후 **전 노드 0x6064 ≈ 0** (N3=0, N4=−39 — 홈값 아님, 정상)
     - ⚠ **정정 (2026-07-27 실기 검증) — 「부팅 직후 0x6064≈0 이 정상」은 성립하지 않는다**(원문은 이력 보존을 위해 남긴다).
       실기 캡처에서 전원 인가 후 **호밍 전** 조향 `0x6064` 는 N3 **7,871,818** / N4 **7,840,084**(≈137°)로 **0 이 아니었다**.
       `0x6064` 가 정확히 0 이 되는 구간은 **호밍 진행 중뿐**이다 — `0x60FB.04=1`(RstStart) write 직후부터 호밍 완료(`0x6041` bit15 0→1)까지 약 31 s 동안 두 조향 노드 모두 0 을 반환한다(실위치 아님).
       ⇒ 「`0x6064`≈0 = 콜드 부팅」 판정식은 **폐기**한다. 0 은 '부팅 상태'가 아니라 '호밍 진행 중'의 표식이다.
       근거: `Log/homing_capture_220350.jsonl` — t=0.034(7,871,818 / 7,840,084) · t=17.925(`0x60FB.4=1`) · t=18.0~49.4(pos ≡ 0) · t=49.08(bit15 0→1)
   - 구동축(N1·2, t≈45.7s): `0x6040=0x86` → `0x60FF=0` → `0x100D=1` → `0x100C=500` → `0x6060=3`(PV)
   - 조향축 1차(t≈45.7s): `0x6040=0x86` → `0x6099=2500` → `0x60FB.04=1`(벤더 오브젝트, 의미 ⚠)
     - ⚠ **정정 (2026-07-27) — 「벤더 오브젝트, 의미 ⚠」 해소.** 이 3연속이 곧 **호밍 개시 시퀀스**다(원문은 이력 보존을 위해 남긴다).
       - `0x6099` = **Homing Speeds**(0.1 r/min 단위 ⇒ 2500 = 250 rpm) [Handbook V7.0 §6.9, page 171]
       - `0x60FB.04` = **RstStart(호밍 개시 트리거)** — "0-Reset off, 1-Reset on" [Handbook V7.0 §6.9, page 171; Appendix I, page 196].
         '절대 엔코더 관련'이 아니다(절대위치 계열은 같은 인덱스의 sub2 / sub3). ⚠ 이 write 는 **모터를 물리적으로 움직인다** — 무심코 재현하지 말 것.
       - `0x6098`(Homing method)은 이 시퀀스에서 **기록되지 않는다** → 드라이브 저장값을 쓴다.
         실기 판독 결과 **전 노드 `0x6098 = 1`** = **Home 1(음(−)의 리밋 트리거 신호)** 이며, 조향축에 **리밋 스위치가 실재**한다(Handbook 기본 RstMode 도 1).
         ⚠ Home 35 로 덮어쓰면 RstMode 가 0(호밍 꺼짐)으로 리셋된다 [Handbook V7.0 §4.6, page 122] — method 를 쓰지 말 것.
       - **구동축(N1·2)은 호밍하지 않는다** — `0x6099` / `0x60FB.4` 는 조향 노드에만 나간다(구동륜은 기계적 원점 없음).
       - 실측 재현: `Log/homing_capture_220350.jsonl` — node3/4 `0x6099=2500` t=17.918 / 17.919 → `0x60FB.4=1` t=17.925 / 17.926 → `0x6041` bit15 = 0 최초 관측 t=17.956 / 17.957 (⚠ 전이 시각 아님 — 직전 폴 t=5.138, 확정 구간 (5.138, 17.956])
       - **★ 대기 필수**: 호밍 실행에 약 31 s. `0x6041` bit15 가 1→0 으로 떨어졌다가 다시 1 이 될 때까지 `0x607A` 를 쓰지 말 것.
         이 구간 `0x6064` 는 실위치가 아니라 0 을 반환한다.
   - 조향축 2차(t≈76.8s): `0x6040=0x86` → `0x100D=1` → `0x100C=500` → `0x6060=1`(PP) → `0x6081=30000` → `0x6083=250` → `0x6084=250` → **`0x607A=홈상수(7,871,815/7,840,086)` + `0x6040=0x3F`** 48 Hz 반복
     - ⚠ **정정 (2026-07-27)** — ① 1차와 2차 사이 약 31 s 공백은 **드라이브가 호밍을 실행하는 구간**이다. 두 버스트를 연속 송신할 수 없으며, 2차는 호밍 완료(`0x6041` bit15 0→1) 이후에만 유효하다.
       ② 재개 후 `0x607A` 목표는 **N3 7,882,020 / N4 7,859,062**(= 조향 0°, 직진)다 — 본 줄이 적은 7,871,815 / 7,840,086 은 **호밍 이전** 값이다.
       실측 Seer 시퀀스 전문: `0x6040=0x86` → `0x6099=2500` → `0x60FB.4=1` → (탐색 ~29 s, 음의 리밋 물림) → `0x6041` bit15 0→1 → `0x6060=1`(PP) → `0x6081=30000` / `0x6083=250` / `0x6084=250` → `0x607A=조향 0°` → `0x6040=0x3F`.
       근거: `Log/homing_capture_220350.jsonl` — t=17.92(트리거) · t=47.02~49.42(`0x6000.1` bit3 = −Limit) · t=49.08(bit15 0→1) · t≈49.14(`0x607A` 전환)
   - → 조향이 **0에서 홈까지 3.3 s 물리 스윙(+137.3° = config steerOffset)**, bit10 Target-reached 로 완료 확인. **홈 상수는 전원 사이클 불변**(07-07/07-08 두 전원 세션 일치). bit15(Home attained)는 스윙 전에 이미 1 — 절대 기준 인지 상태에서의 절대위치 이동.
     - ⚠ **2026-07-27 재개방 — 「홈 상수는 전원 사이클 불변」은 미판정 모순이다(반증 확정 아님).**
       관측: 전원 재인가 후 조향 `0x6064` 가 ≈0 인데 바퀴는 물리적으로 직진이었다(Seer 1040 `position≈0`·`calib=True`·육안 0° 3중 확인)
       — `docs/verified_facts/2026-07-27.md:185-213` §B-1, `Tools/amr_test_gui/amr_test_gui/controller.py:59-75`.
       그 §B-1 표(:196)는 **본 문서 :56,81 을 모순의 한 축으로 명시 인용**하고, :205 에서 「조향 노드에서만 두 소스가 7.87M counts(=137°) 어긋난다」고 기록한다.
       `src/Actuators/motor_control/config/tongyi_amr.yaml:91-94` 도 같은 모순을 **debt-007** 로 등록했다.
       두 가설이 아직 갈리지 않았다 — (a) 제어권 보유 중 판다의 조향 `0x6064` read 오염, (b) 호밍 후 드라이브가 위치 기준을 재설정(=본 상수는 **기동 전에만** 유효).
       ※ controller.py 는 한때 이를 「실측으로 반증」이라 단정했으나 2026-07-27 스스로 철회했다(controller.py:63-71) — 「부팅 직후 `0x6064`≈0」은 본 문서 :52 가 **정상으로 예측한 값**이기 때문이다. 따라서 현재 지위는 **반증이 아니라 미판정**이다.
       **판정에 필요한 측정**: intercept **off** 상태에서 판다로 조향 `0x6064` 다중 read + 동시각 Seer 1040 `encoder` 대조(부호만 다르고 절댓값 일치 → (a) 확정 / 계속 어긋남 → (b) 확정).
       판정 전까지 상수값(7,871,815 / 7,840,086)은 실측 없이 **변경 금지**이며, 무비판 신뢰도 금지.
       ⚠ **정정 (2026-07-27 실기 검증) — 가설 (c) 추가, (b) 약화.** 지정 측정(intercept off 상태 판다 수동청취)이 수행됐다.
         · 조향 `0x6064` 가 정확히 0 으로 보고되는 구간은 **호밍 진행 중뿐**이다 — `0x60FB.4=1`(t=17.925)부터 호밍 완료(bit15 0→1, t=49.08)까지 두 노드 모두 0.
         · 호밍 **전** 정지값 7,871,818 / 7,840,084 와 호밍 **후** 정착값 7,882,001 / 7,859,058 이 0.18° / 0.33° 이내로 재현된다
           ⇒ (b)「호밍이 위치 기준을 재설정(좌표계 재정의)」은 관측과 맞지 않는다.
         · **(c) 관측 시점이 호밍 진행 중이었다** 를 가설에 추가하고, 판정 측정에 「read 시각이 호밍 구간(bit15=0)인지」 확인을 포함할 것.
         근거: `Log/homing_capture_220350.jsonl`
     - ⚠ **2026-07-27 서술 정정 — 「+137.3° = config steerOffset」은 출처가 없다**(counts 상수는 변경하지 않는다).
       두 조향축의 홈 상수는 서로 다르므로 137.3° 단일 값으로 쓸 수 없다. 본 문서 :29 의 57,344 counts/° 로 환산하면
       **N3 7,871,815 counts = +137.27°**, **N4 7,840,086 counts = +136.72°** 로 0.55°(31,729 counts) 차이가 난다(두 상수는 :33 에 병기).
       (환산 검산: 7,871,815 ÷ 57,344 = 137.2736 · 7,840,086 ÷ 57,344 = 136.7202 · 31,729 ÷ 57,344 = 0.5533)
       또한 인용된 config 원본은 정밀값을 주지 않는다 — `References/motor_configuration/motor-config-analysis.md:12` 는 node4 행에 「steerOffset **~137~138°**」 근사 범위만 기재하고,
       :41 에서 「조향 절대 원점(steerOffset 137~138°) ↔ 0x6064 절대값 대응은 **별도 확인 권장**」이라며 미확정으로 남긴다.
       → 「= config steerOffset」이라는 **등치는 미확인**이고, 「137.3」의 소수점 정밀도도 근거가 없다.
     - ⚠ **정정 (2026-07-27 실기 검증) — 본 줄의 호밍 서술 3건 정정**(원문·기존 주석은 이력 보존을 위해 남긴다).
       ① **137° 스윙은 이상거동·원인 미상이 아니다.** 호밍 완료 후 **원점(음의 리밋)에서 조향 0°(직진)로 복귀하는 설계된 이동**이다.
          목표 counts 는 **N3 7,882,020 / N4 7,859,062**(= +137.45° / +137.05°, 57,344 counts/°)이고 EasyDRIVE `steerOffset` **138.000 / 137.250** 과 대응한다.
          조향축에는 **리밋 스위치가 실재**하며 방식은 **Home 1(음의 리밋 트리거)** 이다 — 전 노드 `0x6098 = 1` 실기 판독.
          ⇒ 「원인 미상」·「이상 스윙」·「Home 36/37 기계 하드스톱」류 서술은 **전부 오류**다.
          ⚠ 호밍은 원점에 **머무는 것이 아니다** — 리밋에 얹힌 채 두면 그 방향 지령이 막힌다. 실측에서 −Limit 비트는 0° 복귀 이동 중 해제된다(t≈49.42).
       ② **「홈 상수는 전원 사이클 불변」 철회.** 전원 사이클 비교 이전에 **한 사이클 내부에서** 목표가 이동한다 —
          호밍 전 7,871,815 / 7,840,086 → 호밍 후 7,882,020 / 7,859,062 (Δ +10,205 / +18,976 counts = +0.178° / +0.331°, 전환 t≈49.14).
          비트 단위 불변은 성립하지 않는다. config `steer_home_counts` 는 **호밍 전** 값이다(값 변경은 debt-007 판정 후).
       ③ **bit15 = 「절대 기준 인지 상태」가 아니다.** bit15 는 호밍 트리거(`0x60FB.4=1`)로 1→0 이 되었다가(0 최초 관측 t=17.956, 전이 확정 구간 (5.138, 17.956]) 호밍 완료 시 0→1 로 **복원**되고(t=49.08), 스윙은 그 직후 시작한다.
          즉 스윙 시점의 bit15=1 은 **직전 호밍이 성공했다**는 뜻일 뿐이다. 1차·2차는 별개 시퀀스가 아니라 「호밍 개시 → 완료 대기 → 조향 0° 로 절대이동」 한 상태머신의 3단계다.
       ④ 바로 위 「+137.3° = config steerOffset 은 출처가 없다」 주석의 「인용된 config 원본은 정밀값을 주지 않는다」는 **파생문서에만 해당**한다 — 원본 EasyDRIVE 파라미터는 node3 `steerOffset 138.000` / node4 `137.250` 로 정밀값을 준다(축마다 다르므로 「137.3」 단일값이 근거 없다는 결론 자체는 유지).
       근거: `Log/homing_capture_220350.jsonl`(판다 수동청취 253,510 프레임) · 드라이브 파라미터 직접 판독(`0x6098`) · [Handbook V7.0 §6.9, page 171] · [Handbook V7.0 §4.6, page 122]
   - ★ enable(0x86) 누락 시 속도 지령을 받아도 HALT 유지(실차 재생 실패→enable 접합 후 성공으로 확인)
2. **지령 루프 50 Hz**: N1·2 `0x60FF=vel`, N3·4 `0x607A=pos` (조향 변경 시 `0x6040=0x3F` 재기록)
3. **Node Guarding RTR 20 Hz**: `0x700+N` RTR — **누락 시 GuardTime 500ms×1 초과로 드라이브 HALT** (모터 전원은 유지)
4. **정지**: 속도 0 기록(E-stop = step-cut, Seer 도 동일 방식). 전원 차단은 `0x6040=0x05`

## 6. ROS2 아키텍처 (사용자 지시 2026-07-09 반영)

- **패키지 위치** `src/Motor_Control/` (사용자 지정) · **패키지명 `motor_control`** (ament 는 소문자 요구, 폴더명과 무관) · ament_python, rclpy + python-can(socketcan). `ros2_canopen` 은 NMT-Operational/PDO 전제라 이 버스의 검증 안 된 경로(비채택 사유 기록).
- **★ 휠 모듈 세트 추상화 (사용자 설계 지시)** — 구동 방식별 커버리지를 위해 **모듈 지령 = {구동속도 v[m/s], 조향각 θ[rad]|None} 한 세트**로 통일:
  - `Kinematics` 계층(Twist → ModuleCommand[]): **DualSteer**(본 AMR, Phase 1 이산 모드 / Phase 2 연속 스워브), **DiffDrive**(θ=None 고정, v_l/v_r 차동 — DD 타입 커버), 이후 Ackermann 등 확장
  - `Backend` 계층(ModuleCommand[] → 버스): `TongyiSdoBackend`(SDO 폴링 마스터: 브링업·enable·guarding·50Hz 지령·피드백). DD 타입 하드웨어는 backend 만 교체
  - 조향축 없는(θ=None) 모듈은 backend 가 조향 지령을 생략 → 동일 노드/토픽 인터페이스로 dual-steer·DD 겸용
- **노드** `motor_control_node`:
  - 구독 `/cmd_vel`(Twist): Phase 1 은 검증 모델 그대로 **모드 매핑**(vx→직진/후진, vy→크랩, wz→스핀; 지배 성분 1개 선택) — 국면(홈↔90°) 전환 시 **정지 → 조향 완료 대기(0x6064 허용오차) → 구동** 상태기계(Seer 실측과 동일하게 정지 중 전환)
  - 발행 `/odom`+`/joint_states`(0x6064 폴링, 2.67M counts/m·57,344 counts/°), `/diagnostics`(0x6041/0x603F/0x6078 저빈도)
  - `/estop`(std_msgs/Bool): 즉시 vel=0 래치
- **안전장치(필수)**: cmd_vel 워치독(200 ms 무수신 → vel 0), 속도 한계(초기 0.2 m/s), 가드 RTR 20 Hz 독립 유지, **콜드/웜 분기 브링업** — 시작 시 0x6064 읽어 pos≈0(콜드)이면 §5-1 시퀀스 재현(**137° 물리 스윙 발생 — 주변 확보 필수**), pos≈홈(웜)이면 enable 만
  - ⚠ **2026-07-27 조건 보강 — `pos≈0` 은 콜드의 충분조건이 아니다.** 위 판정식이 성립하려면 「`0x6064`≈0 이면 바퀴도 물리적으로 홈 자세가 아니다」가 참이어야 하나, 그 전제가 흔들렸다:
    Seer 호밍 완료 후에도 `0x6064`≈0 인데 바퀴는 물리적 0°(직진)였다는 관측이 있다(`Tools/amr_test_gui/amr_test_gui/controller.py:59-75`, `docs/verified_facts/2026-07-27.md` §B-1 — 위 :56 주석의 미판정 모순과 같은 건).
    이 상태에서 상수 홈을 절대 목표로 쓰면 0° 지령이 7.87M counts = **137° 스윙**이 된다.
  - ⚠ **본 줄의 경고는 '주변 확보'(공간)뿐이고, 콜드 오판정·급점프 위험이 누락돼 있다.** 실제 피해가 발생했다 —
    `docs/claude-mistake/2026-07-27-002_node4-unverified-command-damage.md:17-18` 「node4 가 물리적으로 **137°(정상 ±90° 범위 밖)로 밀려 갇혔고**, 사용자가 CAN 직결 원점 호밍으로 복구」, 원인은 :51 「급점프/범위이탈」.
  - **따라서 (값·시퀀스는 그대로 두되) 다음 조건을 필수로 한다:**
    ① 콜드 판정은 `0x6064` 단독으로 하지 말고 **운전자 육안(바퀴 자세) 또는 Seer 1040 `position`/`calib` 교차확인**과 반드시 병행한다(단일 read 를 물리상태의 진실로 신뢰 금지 — mistake 2026-07-27-002:49).
    ② 0→홈 이동은 **단발 절대 점프 금지**. 단계 램프(≤30°/단계, 단계별 실측 추종 확인, 미추종 시 FAULT 래치)로만 수행한다 — `Tools/amr_test_gui/amr_test_gui/ramp.py` (:29-31 ±90° 하드 클램프, :47 `step_deg`).
       ※ 현행 안전 자산은 실측 검증 범위 **±90°** 로 하드 클램프하므로, 본 줄이 규정한 0→137° 단발 절대 점프는 그 자산과 정면 충돌한다.
    ③ `allow_homing_motion` 게이트(`config/tongyi_amr.yaml`)를 **끄지 말 것**(기본 false 유지).
  - ⚠ **정정 (2026-07-27 실기 검증) — 위 분기·경고의 전제 정정**(게이트·램프·교차확인 요구는 그대로 유지).
    - **`pos≈0` 은 콜드의 표식이 아니라 「호밍 진행 중」의 표식이다.** 실기 캡처에서 전원 인가 후 호밍 전 `0x6064` 는 7,871,818 / 7,840,084(≈137°)였고, 정확히 0 인 구간은 `0x60FB.4=1`(t=17.925) 이후 호밍 완료(bit15 0→1, t=49.08)까지뿐이었다.
      ⇒ 「`0x6064`≈0 = 콜드」 판정식은 **폐기**. 콜드/웜 판정은 `0x6041` bit15 + 육안(바퀴 자세) 또는 Seer 1040 `position`/`calib` 교차확인으로 대체할 것.
    - **137° 스윙은 「위험한 미지 거동」이 아니다** — 호밍 원점(음의 리밋)에서 **조향 0°(직진)로 복귀하는 설계된 정상 이동**이다(목표 N3 7,882,020 / N4 7,859,062).
      조향축에는 **리밋 스위치가 실재**하고 방식은 **Home 1**(전 노드 `0x6098 = 1` 실기 판독).
    - ⇒ `allow_homing_motion` 게이트의 **근거 서술**(= 「콜드부팅 스윙 = 위험한 미지 거동」)이 바뀌었을 뿐이며, **게이트 존폐 판단은 본 정정의 범위가 아니다.**
      스윙이 정상 거동이라는 것과 그것을 안전하게 수행하는 것은 별개이므로 ①의 교차확인·②의 단계 램프/FAULT 래치는 그대로 유효하다.
      또한 게이트가 스윙을 막는 것이 아니다 — 스윙을 트리거하는 것은 init 시퀀스의 `0x6099` + `0x60FB.4=1` 이다.
    근거: `Log/homing_capture_220350.jsonl` · 드라이브 `0x6098` 직접 판독 · [Handbook V7.0 §6.9, page 171]
- **폴링 예산**: 지령 50 Hz×4 + 가드 20 Hz×4 + 피드백(위치 50 Hz×4, 진단 2 Hz) ≈ 500~600 fps ≪ Seer 1600 fps → 부하 여유 충분
  - ⚠ **2026-07-27 정정(주파수 파라미터 50/20/2 Hz 는 변경하지 않음) — 위 합계는 요청 편도만 계수한 값이다.**
    SDO 폴 1회는 버스에 **2 프레임**을 싣는다(:23 「`0x600+N` 요청 / `0x580+N` 응답 … 왕복 ~1 ms」). Node Guarding 도 RTR+응답 **2 프레임**이다(`References/Tongyi-Motor-Controller/docs/tongyi-motor-protocol-tables.md:64-65` 「요청 … **RTR** 프레임 / 응답 `0x700+N` DLC1」).
    제시된 합계 200(지령)+80(가드)+200(피드백)+8 ≈ 488 은 편도 계수이며, 응답을 포함하면 **≈1,000 fps(추정)** 로 약 2배다.
    비교 대상 「Seer 1600 fps」는 버스 실측 프레임 수(:16, tables:245-252 「총 ~1600 fps(버스부하 ~75 %)」)여서 **단위 기준이 서로 달랐다**.
    → 실제 버스부하는 실측 전 **미확정**이므로 「부하 여유 충분」 단정을 보류하고, :16 의 「**여유 작음, 폴링 예산 관리 필수**」 경고를 우선한다.
    판정에 필요한 측정: 드라이버 구동 중 `can1` 실 프레임률(`canbusload can1@250000`) 실측.
- **하드웨어**: PCAN-USB Pro FD(can1=모터 버스), **Seer 마스터 분리 후 단독 마스터**(이중 마스터 금지). USB 포트 불량 이력 주의([[pcan-profd-listen-only-linux]])

## 7. 미확정(⚠) — 구동 전 확인 항목

| 항목 | 리스크 | 확인 방법 |
| --- | --- | --- |
| ~~조향 절대원점 전원 사이클 불변성~~ **⟦2026-07-27 재개방⟧** | **✓ 해소(2026-07-09)** — 기동 캡처 전수 디코드: 부팅 시 0x6064≈0 이 정상, 홈 상수(7.87M/7.84M)는 전원 사이클 불변 절대 목표(steerOffset 137.3°), 매 기동 시 브링업 스윙 필요(§5-1) <br> **⟦2026-07-27 재개방⟧ 이 '해소' 판정은 철회한다 — 미판정으로 되돌린다**(취소선·✓ 표기는 이력 보존을 위해 남긴다). 근거: `docs/verified_facts/2026-07-27.md:196` 이 **바로 이 줄(:56,81)** 을 §B「미판정 — 모순이 있고 아직 못 가린 것」표의 한 축으로 인용하고 :205 에서 「조향 노드에서만 두 소스가 7.87M counts(=137°) 어긋난다」고 기록. `config/tongyi_amr.yaml:91-94` 에 **debt-007** 로 등록됨. 또한 **바로 아래 행**은 같은 2회 관측을 근거로 「미확인」이라 판정하여, 동일 근거 위에서 상반된 결론이 병존한다(2회 관측으로 '불변 해소' 선언 vs 동일 2회 관측으로 '미확인'). <br> **판정 전까지 `allow_homing_motion` 게이트를 끄지 말 것.** 어느 쪽이 참인지 여기서 단정하지 않는다. <br> **⟦2026-07-27 실기 부분판정⟧** 지정 측정(intercept off 판다 수동청취)을 수행했다 — `Log/homing_capture_220350.jsonl`: 호밍 전 `0x6064` 7,871,818/7,840,084 · 호밍 후 정착 7,882,001/7,859,058 · `0x6064`=0 은 **호밍 진행 중 한정**. ⇒ ① 「부팅 시 `0x6064`≈0 이 정상」 **철회**, ② 「홈 상수 전원 사이클 불변」 **철회**(한 세션 내에서도 호밍 전 7,871,815/7,840,086 → 호밍 후 7,882,020/7,859,062, Δ +0.178°/+0.331°), ③ 「steerOffset 137.3°」 단일값 **철회**(EasyDRIVE 실값 node3 138.000 / node4 137.250). 게이트는 계속 유지하되 **게이트가 137° 스윙을 막는다는 뜻은 아님**을 병기한다 — 스윙 트리거는 init 의 `0x6099`+`0x60FB.4=1` 이다. | ~~콜드/웜 분기 게이트로 구현(§6)~~ → **미해결**: intercept off 상태 판다 `0x6064` 다중 read + 동시각 Seer 1040 `encoder` 대조(:56 주석 참조). 콜드 판정은 육안/Seer 교차확인 병행(:73) <br> ⟦2026-07-27⟧ intercept **off** 측정은 완료(위 부분판정). 잔여: intercept **보유** 상태 재현 |
| ~~브링업 137° 스윙의 물리 의미~~ **⟦2026-07-27 해소⟧** | ~~0 기준점이 절대 엔코더 기준인지(어느 자세에서 꺼도 재현) 미확인 — 2회 전원 사이클 관측뿐~~ <br> **✓ 해소** — 절대 엔코더 기준이 아니다. 조향축에 **리밋 스위치가 실재**하며 방식은 **Home 1(음(−)의 리밋 트리거)** 이다(전 노드 `0x6098 = 1` 실기 판독, Handbook 기본 RstMode 도 1). 호밍이 그 리밋 원점에서 `0x6064` 를 0 으로 재설정하고, **137° 스윙은 「호밍 원점 → 조향 0°(직진)」 복귀라는 설계된 이동**이다 — 목표 N3 7,882,020 / N4 7,859,062 (= +137.45° / +137.05°, EasyDRIVE `steerOffset` 138.000 / 137.250 대응). 「이상 스윙」·「Home 36/37 기계 하드스톱」 서술은 오류. | ~~조향 임의 자세에서 전원 재투입 후 0x6064 확인~~ <br> 완료: `Log/homing_capture_220350.jsonl`(수동청취 253,510 프레임) + `0x6098` 직접 판독 + [Handbook V7.0 §6.9 page 171 / §4.6 page 122] |
| ~~0x60FB.04=1 (기동 시 벤더 오브젝트)~~ **⟦2026-07-27 해소⟧** | ~~의미 미상(절대 엔코더 관련 추정)~~ <br> **✓ 해소** — `0x60FB` sub 4 = **RstStart(호밍 개시 트리거)**, "0-Reset off, 1-Reset on". 벤더 미상 오브젝트가 아니라 매뉴얼 명시 항목이다. '절대 엔코더 관련' 추정은 **오답** — 절대위치 계열은 같은 인덱스의 sub2 / sub3 이고 sub4 는 별개다. ⚠ 이 write 는 **모터를 물리적으로 움직인다**(보내면 매 기동 재호밍) — 안전 설계에 반영할 것. 참고: `0x6098`(method)은 Seer 도 우리도 쓰지 않으며 드라이브 저장값(=1, Home 1)을 쓴다. | 완료: [Handbook V7.0 §6.9, page 171](표 1행) + [Appendix I, page 196] 원문 대조. 실기 확증 `Log/homing_capture_220350.jsonl` — `0x60FB.4=1` t=17.925 → `0x6041` bit15 1→0 t=17.956 → −Limit(`0x6000.1` bit3) t=47.02 → bit15 0→1 t=49.08 |
| 중간 조향각(0~90° 외) 지령 가능성 | Phase 2 연속 스워브 전제 | 잭업(바퀴 공중) 상태에서 소각도 시험 |
| Statusword 벤더 편차 | 표준 상태 판정 불가 → fault 감지는 0x603F+전류로 | Handbook §6.6.2 대조 완료, 판정 로직은 실측 기반 |
| 전류 0.01A 단위 실측 대조 | 과부하 감지 임계 설정 | 구동 중 0x6078 vs 정격 비교 |
| DirectDriver 실차 미검증 | selftest(vcan)만 통과, 실차 T4 미실시 | ROS2 노드 첫 실차 시험 = 정지·안전구역·E-stop 상비 |
| Seer SDO 타임아웃 | 릴레이 병용 시 지연 예산 | Seer 설정 파일(입수 대기) |

## 8. 다음 단계 (제안)

1. `src/Motor_Control/` 에 `motor_control` 패키지 구현(사용자 지시 확정) — coding SOP(ADR·함수표) 경유
2. vcan 자가시험 → 잭업 실차: enable→가드→저속(±1000=0.04 m/s) 직진 → §7 항목 소거
3. 지면 시험: 직진/후진 → 크랩/스핀 → cmd_vel 원격 조종
4. Phase 2: 중간 조향각 검증 후 연속 스워브 IK + odom 정밀화
