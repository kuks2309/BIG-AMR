# 조향 홈(steer home) — 종결 판정 (2026-08-02)

> # ❌ 재정정 2026-08-03 17:00 — **아래 15:40 블록의 오류를 다시 정정한다. 이 블록이 최우선이다.**
>
> 15:40 정정 캠페인이 기준선으로 쓴 수치 중 **8건이 원자료와 어긋난다.** 아래는 원자료
> (`Log/*.json*` · 펌웨어 헤더 · `foil_a082.yaml`)를 직접 파싱해 재계산한 값이다.
> **문서를 근거로 문서를 고치지 않았다.** 15:40 블록 원문은 삭제하지 않고 그대로 둔다.
>
> | # | 15:40 서술 | 재정정 (원자료 재계산) | 원자료 |
> |---|---|---|---|
> | E1 | 「그 뒤 **12회 연속 성공**」 | **12회 연속 성공** (오늘 시도 13 / 성공 12 / 실패 1) | 아래 ① |
> | E2 | 「`0x6064`=0 은 **1회 관측·재현 안 됨**」 | **재현된다** — 09:19 · 09:58 · 10:08 · (리부팅 후) 11:38 · 14:43 캡처에서 관측 | 아래 ② |
> | E3 | (WAIT 구간 표기) | WAIT(state 4) **체류 31.30 s**. **31.7 s** 는 t=0→RESTORE 진입 **절대 시각** | 아래 ③ |
> | E4 | 소요 「**35.0 s**」 | **평균 35.07 s · 중앙 35.05 s** (범위 34.99~35.16, 폭 0.17 은 정확) | 아래 ③ |
> | E5 | 「counts/° **57,344**」 | **node3 57,344.0 / node4 57,344.3** (단일값 아님) | 아래 ④ |
> | E6 | 「σ 2.8 / 3.2 c」 | 값은 맞으나 **모표준편차 기준**임을 명시해야 함(표본 σ 는 2.95 / 3.38) | 아래 ⑤ |
> | E7 | 「**결함이 아니라 설계 동작이다**」 | **과잉 확정** → 「**재현되는 정착 동작**(상수 적정성은 별건, debt-016)」으로 완화 | 아래 ⑤ |
> | E8 | 조향 0° 의 **근거** | 값 `[7871815, 7840086]` 은 정본이라 **유지**. 근거는 **「공학적 채택값」**까지만 | 아래 ⑥ |
>
> ---
>
> **① 오늘(2026-08-03) 호밍 시도 = 13, 성공 = 12, 실패 = 1** — 전수 파싱 결과
>
> | 시각 | 파일 | 결과 |
> |---|---|---|
> | 09:58 | `Log/home_experiment_260803_095815_summary.json` | `final_state` **6 = ERR_TIMEOUT**, `elapsed` 120.26 s — **유일한 실패** |
> | 14:46 | `Log/homing_edge_260803_144602.json` | `accepted` true, `final_state` **5 = DONE**, 37.0 s |
> | 15:25 | `Log/homing_edge_260803_152520.json` | `accepted` true, `final_state` **5 = DONE**, 34.91 s |
> | 15:33 | `Log/home_experiment_260803_153319_summary.json` | `runs` **10건 전부** `accepted` true · `final_state` 5 |
>
> ⇒ 성공 12 = **15:33 런 10회 + 14:46 + 15:25**. **13 은 어디서 왔나** —
> 15:33 요약의 `baseline` 항목을 성공 1건으로 더한 것이다. 그러나
> **`baseline` 은 호밍이 아니라 레지스터 스냅샷**이다:
> `Tools/docking_field_kit/orin_home_experiment.py:390` `base = snapshot(rig, "baseline")`
> (같은 파일 :59-61 `STATE` 맵에서 5=DONE · 6=ERR_TIMEOUT).
> ⚠ **「10회 연속 10/10」 자체는 유효**하다 — 바꾸지 않았다.
> ※ `Log/homing_edge_260803_144305 · 144507 · 144508 · 144510 · 144544 · 152943.json` 6건은
> `fsm_trace` · `sw_trace` 가 **모두 0건**이라 호밍 개시 기록이 없어 시도로 세지 않았다
> (중단 사유는 **미확인**).
>
> **② `0x6064` = 0 은 재현된다** — SDO 응답(`0x583`/`0x584`, cs `0x43`, index `0x6064:00`) 전수 집계
>
> | 캡처 | node3 (0 / 전체) | node4 (0 / 전체) |
> |---|---|---|
> | 09:19 `home_experiment_260803_091956.jsonl` | **50 / 50** | **50 / 50** |
> | 09:58 `home_experiment_260803_095815.jsonl` | **12,220 / 12,220** | **12,211 / 12,211** |
> | 10:08 `seer_homing_260803_100813.jsonl` | **10,327 / 10,327** | **10,327 / 10,327** |
> | 11:38 `probe_113805.jsonl` (리부팅 직후) | 97 / 1,300 | 96 / 1,299 |
> | 14:43 `homing_edge_260803_144305_can.jsonl` | 2 / 74 | 2 / 68 |
>
> 10:08 캡처는 같은 구간 `0x6041` 이 **37968(bit15=1) 258표본/노드 전량**이다 —
> 즉 **호밍 중이 아닌데도 `0x6064`=0 이 전량**이었다.
> ⇒ 「단 1회 관측·재현 안 됨」 서술은 **철회**한다.
> ⚠ **인과는 양쪽 다 미판정**이다. `0x6064`=0 이 관측된 14:43 직후의 14:46 호밍은 성공했으므로
> 「`0x6064`=0 이 호밍을 막는다」도 성립하지 않는다.
> ⚠ **「호밍 중(bit15=0)이면 `0x6064`=0」은 유효한 관측이다** — 15:33 런 원본에서
> node3 bit15=0 구간 **32,243 / 32,243 (100%)** 로 재현됐다. 폐기된 것은
> **「전원 사이클로만 풀리는 래치」주장뿐**이다. 이 둘을 섞어 유효 서술을 폐기 처리하지 말 것.
>
> **③ 소요·구간 시각** — `Log/home_experiment_260803_153319_summary.json` 10런
>
> - `elapsed` = 35.16 / 35.05 / 35.04 / 35.02 / 35.15 / 35.11 / 35.01 / 35.02 / 35.13 / 34.99
>   → **평균 35.068 · 중앙 35.045 · 범위 34.99~35.16(폭 0.17)** · 모σ 0.060 · 표본σ 0.063
> - **WAIT(state 4) 체류** = 다음 전이(state 8 RESTORE)까지의 차 =
>   31.32/31.21/31.30/31.29/31.31/31.38/31.28/31.28/31.29/31.36 → **평균 31.30 s**
> - **RESTORE(state 8) 진입 절대 시각**(t=0 기준) 평균 **31.69 s** ← 「31.7 s」의 정체
> ⇒ 「WAIT **31.7 s**」는 **체류 시간이 아니라 절대 시각**을 쓴 것이다. 체류는 **31.30 s**.
>
> **④ counts/° 는 노드마다 다르다** — `Log/steer_two_phase_260803_131305.jsonl` phase A
> 5점(−5·−2.5·0·+2.5·+5°) 지령각→CAN 최소제곱 기울기:
> **node3 = 57,344.000 (×1.0000000) · node4 = 57,344.280 (×1.0000049)**.
> ⇒ 「57,344 (기울기 1.000000)」은 **node3 값을 단일값처럼 쓴 것**이다.
> (참고: 11점 스윕 `Log/steer_sweep_1005_260803_130543.jsonl` 은 node3 57,343.52 · node4 57,343.97
> 로 또 다르다 — **소수점 이하는 측정 세트에 따라 흔들린다.** 정수 57,344 로 쓰는 것은 무방하나
> 「실측 확정 57,344.0」이라는 표현은 node4 에 대해 사실이 아니다.)
>
> **⑤ 정착값 편차 — σ 기준과 판정 수위**
>
> - 10회 `post` 값: node3 = 7,882,021 **×8** / 7,882,014 **×2** · node4 = 7,859,065 **×8** / 7,859,058 **×2**
> - 펌웨어 상수 대비 편차: node3 **+1(8회) / −6(2회)** · node4 **+3(8회) / −4(2회)**
>   (상수: `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h:212` `SEER_HOME_ZERO_N3 7882020`,
>   `:213` `SEER_HOME_ZERO_N4 7859062`)
> - σ: **모표준편차 2.800 / 3.208** ← 문서의 2.8 / 3.2 는 이 기준. **표본표준편차는 2.951 / 3.381**.
>   ⇒ σ 를 인용할 때 **기준(모/표본)을 반드시 병기**할 것.
> - ⇒ **E7.** 실측이 보증하는 것은 **「펌웨어 상수 `SEER_HOME_ZERO_N3/N4` 에 최빈 +1 / +3 counts,
>   최대 편차 6 counts 로 재현성 있게 정착한다」**까지다. **상수의 적정성은 실측 밖**이고,
>   `docs/debt/registry.md` 의 **debt-016** 이 같은 편차를 「영구 미검출 오프셋」으로 등록 중이라
>   **「결함이 아니다」와 정면 충돌**한다. ⇒ **「재현되는 정착 동작(상수 적정성은 별건, debt-016)」**
>   으로 읽을 것. 「결함이 아니라 설계 동작이다」는 **과잉 확정이므로 인용 금지**.
>
> **⑥ 조향 0° — 값은 유지, 근거만 낮춘다**
>
> - 값 `[7871815, 7840086]` 은 **정본**이다(`src/Comm/CAN/can_relay/config/machine/foil_a082.yaml:134`
>   `steer_home_counts`). **바꾸지 않는다.**
> - 그러나 근거 두 개가 모두 증거력이 없다:
>   - ① 「1040 역산 2회 재현」 → **같은 15:40 캠페인이 항등식이라고 스스로 무효화**했다(§B).
>   - ② 「Seer 실시간 `0x607A` 연속 관측」 → **선택 인용**이다. `Log/homing_capture_220350.jsonl`
>     의 Seer→드라이브 `0x607A:00` 다운로드 전수 집계(노드당 n=6,464):
>     **node3 7,882,020 = 6,319회(97.8%) vs 7,871,815 = 145회(2.2%)**,
>     **node4 7,859,062 = 6,319회(97.8%) vs 7,840,086 = 145회(2.2%)**.
>     Seer 가 압도적으로 많이 쓴 값은 **채택값이 아니라 GOZERO 상수 쪽**이다.
> - ⇒ `[7871815, 7840086]` 은 **「공학적 채택값」**으로만 서술할 것.
>   **「실측 확정」·「0° 실측」 표현 금지.**
>
> **⑦ 펌웨어 함수 위치 정정** — `seer_home_cancel_frames()` 는
> `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h` **:312-316** 이다.
> **:307-311 은 `seer_home_digital_in()`** 이다(다른 함수).

> # ❌❌❌ 정정 2026-08-03 15:40 — **최상위 정정. 아래 모든 블록·본문에 우선한다.**
>
> **새 정본**: [`docs/homing/2026-08-03-can-relay-homing-assets.md` §0](../homing/2026-08-03-can-relay-homing-assets.md)
> — 호밍 **10회 연속 실측**(2026-08-03 15:33~15:40)으로 확정된 절이다.
> 본 문서와 어긋나면 **그쪽이 이긴다.**
>
> ### A. 확정된 실측 (가설 없음)
>
> | 항목 | 확정값 | 근거 |
> |---|---|---|
> | **호밍 성공률** | **10 / 10** (전부 `DONE`), 소요 **35.0 s**(편차 0.17 s) | `orin_home_experiment.py --repeat 10`, 접지, 펌웨어 `DEV-cc5e0491-DEBUG` (§0-1) |
> | **호밍 후 정착값** | node3 **7,882,021** (σ≈2.8 c) · node4 **7,859,065** (σ≈3.2 c) | 10회 (§0-2) |
> | **정착값 ↔ 조향 0° 간격** | **+0.178°** (node3) / **+0.331°** (node4) | 위 정착값 − `[7871815, 7840086]` |
> | **조향 0°** | **`[7871815, 7840086]`** | 2026-08-03 11:44 정정. ⚠ **Seer 좌표계 기준** — 물리 직진 여부는 **미확인** |
> | **counts/°** | **57,344** | 지령각→CAN 기울기 실측 **1.000000** (§14-1) |
> | `0x6098` 호밍 방식 | **1** (Home 1, −리밋) | 실기 판독 (§15-1) |
> | 리밋 스위치 | **실재** | 10회 모두 DI `0x01`→`0x09`(bit3 = −Limit) |
>
> > ❌ **재정정 2026-08-03 17:00 (위 표 3행):**
> > · 소요 **35.0 s** → **평균 35.07 · 중앙 35.05 s**(범위 34.99~35.16, 폭 0.17 은 정확) **[E4]**
> > · σ≈2.8 / 3.2 c 는 **모표준편차** 기준(표본 σ 2.95 / 3.38) — 기준을 병기할 것 **[E6]**
> > · counts/° **57,344** → **node3 57,344.0 / node4 57,344.3**(단일값 아님) **[E5]**
> > · 조향 0° `[7871815, 7840086]` 은 **값만 유지**, 라벨은 **「공학적 채택값」**까지 **[E8]**
>
> > ❌ **재정정 2026-08-03 17:30 (위 표 「조향 0°」행 비고):**
> > 「물리 직진 여부는 **미확인**」 → **미확인이 아니다.** 사용자가 **바퀴 직진 상태에서
> > Seer 앞바퀴 2축 0° 를 육안 확인**했고(2026-08-02 · 2026-08-03, `can_relay` GUI 동일),
> > Seer 값은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이다 ⇒ **물리 직진 앵커는 실재한다.**
> > 정확한 비고: **「Seer 좌표계 기준 — 물리 직진 앵커는 실재하나 정밀도가 육안 한계 ≈±1°
> > (±57,344 c)라 counts 소수 자리(193 c = 0.0034°)는 분해하지 못함」.**
>
> - 2026-08-03 09:58 의 `ERR_TIMEOUT` **1회**가 유일한 실패이고 그 뒤 **12회 연속 성공**했다. **재현되지 않는다.**
>   > ❌ **재정정 2026-08-03 17:00: 「13회」는 거짓 — 「12회 연속 성공」이다. [E1]**
>   > 오늘 시도 **13** / 성공 **12** / 실패 **1**. 성공 12 = 15:33 런 10회 + 14:46 + 15:25.
>   > 15:33 요약의 `baseline` 은 **호밍이 아니라 레지스터 스냅샷**이다
>   > (`Tools/docking_field_kit/orin_home_experiment.py:390` `snapshot()`).
>   > **「10회 연속 10/10」 자체는 유효** — 그대로 둔다.
> - **이 문서가 「무리 B = 호밍 후 정착값」이라 부른 것이 10회 실측으로 확정됐다** — 값도, 재현성도.
>   σ ≈ 3 counts ≈ 0.00005° 로 매번 같은 자리에 선다. ⇒ **결함이 아니라 설계 동작이다.**
>   > ❌ **재정정 2026-08-03 17:00: 「결함이 아니라 설계 동작」은 과잉 확정. [E7]**
>   > 실측이 보증하는 것은 **「펌웨어 상수 `SEER_HOME_ZERO_N3/N4`(`safety_seer_gate.h:212-213`)에
>   > 최빈 +1 / +3 counts(최대 6 c)로 재현성 있게 정착한다」**까지다.
>   > **상수의 적정성은 실측 밖**이며 **debt-016** 이 같은 편차를 「영구 미검출 오프셋」으로
>   > 등록 중이라 충돌한다. ⇒ **「재현되는 정착 동작(상수 적정성은 별건, debt-016)」**로 읽을 것.
>
> ### B. ★★ 이 문서 §4-2 의 역산 근거가 무너졌다 (가장 중요)
>
> ```
> 0° = CAN_0x6064 + Seer각도 × 57,344      ← 이것은 항등식이다
> ```
>
> Seer API **1040** 각도는 판다가 엿듣는 **바로 그 `0x6064` 의 아핀 변환**이다
> (기울기 × 57,344 = **1.000001**, 7자리 일치 — `…assets.md` §12-2 · §14-2).
> 따라서 `0°_est = CAN + (C₀ − CAN) = C₀` 로 **CAN 항이 소거되고, 자세와 무관하게 같은 값**이 나온다.
> 실제로 **0.049°(2,825 counts) 떨어진 자세**의 08-02 로그를 넣어도 **소수 4자리까지 같은 값**이 나왔다.
> Seer **1005** 도 마찬가지로 `0x6064` 유래다(gain **1.000130**, §14-3) — 독립 앵커가 아니다.
>
> ⇒ **이 역산이 확정한 것은 「Seer 내부 조향 영점 상수」이지 물리적 0° 가 아니다.**
> ⇒ 아래 근거들은 **전부 증거력이 없다 — 인용 금지**:
>   - 「판다 SAFETY_SILENT passthrough 라 **CAN 과 Seer 가 독립 경로**」 → **거짓**(같은 프레임을 두 번 읽음)
>   - 「**2회 독립 실행이 counts 단위까지 동일**」 → **동어반복**(정지 상태를 결정론적 경로로 두 번 읽음, 정보량 0)
>   - 「**사용자 Seer 표시 0° 확인 = 이상적 조건**」 → 추정량이 **자세 불변**이므로 **결과 기여 0**
>   - 「구값이 실측과 **1 count 이내**」 → `C₀` 가 비정수(node3 …815.899 / node4 …086.684)라 `floor`/`round` 차이일 뿐
> ⇒ 값 `[7871815, 7840086]` **채택 자체는 유효**하다(내비게이션이 Seer 좌표계를 쓰므로 공학적으로 옳다).
>   **무효인 것은 근거와 「물리 0° 실측 확정」이라는 라벨뿐이다.**
>
> > ❌ **재정정 2026-08-03 17:30 — 위 §B 는 (a) 한정이다. (b) 까지 무효화하지 말 것.**
> > **(a)** 「육안 확인이 **counts 역산 결과**에 기여하는가」 → **아니오.** 역산식이 항등식이라
> > 자세 불변이다. **위 §B 의 판정은 전부 유효하며 뒤집지 않는다.**
> > **(b)** 「**Seer 0° 표시 = 바퀴가 물리적으로 직진**인가」 → **예. 성립한다.**
> > Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가
> > **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다(2026-08-02 · 2026-08-03,
> > `can_relay` GUI 표시로도 동일 확인).
> > ⇒ 따라서 **「물리 직진 앵커가 없다」는 거짓이다 — 앵커는 실재한다.**
> > 앵커의 한계는 **정밀도**뿐이다: **육안 한계 ≈±1°(±57,344 counts)** 로,
> > 「바퀴가 직진이다」를 세우기에는 충분하나 **다툰 193 counts(0.0034°)는 분해하지 못한다**(약 297배).
> > ⇒ 값 `[7871815, 7840086]` 이 **공학적 채택값**인 이유는 **「앵커가 없어서」가 아니라
> > 「앵커 정밀도가 그 차이를 분해하지 못해서」**다.
>
> ### C. 폐기 — 인용 금지 목록
>
> | 폐기된 주장 | 반증 |
> |---|---|
> | 「`Switch on disabled` 라 호밍 불가」 | 07-27 성공 로그가 **같은 statusword** 로 성공 (§11-2) |
> | 「`0x6098` = 0 이라 호밍 비활성」 | 실측 **1** (§15-1) |
> | 「RstStart 가 1 로 고착돼 에지 불가」 | 실측 **0** (§15-1) |
> | 「`0x6064`=0 은 전원 사이클로만 풀리는 래치」 | 07-27 캡처가 사이클 없이 **3,115회** 관측 (§11-2) |
> | 「축이 이미 홈이면 무동작 즉시 완료 → 타임아웃」 | **홈 상태(`--offset 0`)에서도 −리밋까지 주행해 DONE** (§17-1) |
> | 「CAN ↔ Seer 독립 교차검증」 | 1040 은 `0x6064` 의 아핀 변환 (§11-1 · §12-2 · §14-3) |
> | **`+0.334°` / `+0.332°`** | **`+0.331°`** 로 통일 (10회 실측 정착값 기준) |
>
> ### D. 우선권 선언 무효화 — **조향 0° 항목에 한해**
>
> 아래 원문의 「저장소의 다른 문서가 홈에 대해 다르게 말하면 **이 문서가 이긴다**」는 선언은
> **`steer_home_counts` / 조향 0° 항목에 한해 효력이 없다.** 이 문서는 그 항목에서 값도 근거도 틀렸다.
> **새 정본 포인터**: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (값 자체의 정본은 여전히 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`).
>
> ### E. 이 문서를 삭제하지 않는 이유
>
> 원문·이전 정정 블록 모두 **역사 기록으로 보존**한다. 취소선과 `❌ 정정 2026-08-03 15:40:` 표시를
> 삭제 대신 **인접 배치**했다. 배너만 달고 본문을 방치하는 것이 이 저장소의 반복 실패 패턴이므로
> **§1 · §2 · §3 · §4-2 · §6 본문 각 지점에 인라인 정정을 달았다.**

---

> # ❌❌ 정정 2026-08-03 (11:44) — 이 문서의 §1 채택값은 **틀렸다. 폐기한다.**
>
> ⚠ **이 블록은 위 15:40 최상위 정정에 종속된다.** 아래 블록이 제시하는 **근거**
> (「독립 경로」·「2회 독립 실행 동일」·「사용자 육안 0° 확인」·「1 count 이내 일치」)는
> **전부 무효화**됐다(위 §B). **값 `[7871815, 7840086]` 채택만 유효하다.**
>
> **이 문서의 §1 채택값 `steer_home_counts = [7871810, 7839894]` 은 0° 가 아니라
> 「Seer 가 0° 를 가리키지 않던 자세에서 읽은 raw CAN 판독값」이었다.**
>
> ### 원인 — 자기 §4-2 의 역산식을 자기 채택값에 적용하지 않았다
>
> 이 문서는 §4-2 에서 0° 역산식을 **스스로 유도해 적어 놓고도**
>
> ```
> 0° = CAN_0x6064 + Seer각도 × 57,344
> ```
>
> §1 채택 시 그 식을 적용하지 않고 `0x6064` **raw 판독값을 그대로 0° 로 박았다.**
> 그래서 측정 당시 Seer 각도만큼의 오차가 그대로 정본에 실렸다.
>
> | 노드 | 측정 시 Seer 각도 | 역산 보정량 | 결과 |
> | --- | --- | --- | --- |
> | node3 | +0.0001° | 6c | 오차가 작아 **드러나지 않음** |
> | node4 | +0.0035° | 193c | 오차 **193c 가 그대로 정본에 실림** |
>
> 같은 문서 §2 의 3차 확인(2026-08-02 19:42)은 역산을 **적용해서** node4 0° = 7,840,085c
> 를 얻고도 §6 에서 「계통 편차 가능성(미판정)」으로 넘겼다 — **그 값이 정답이었다.**
>
> ### 새 정본값 — 2026-08-03 11:44 실측 확정
>
> ```
> steer_home_counts = [7871815, 7840086]      # node3(front) / node4(rear)
> ```
>
> 측정 조건 (`Tools/docking_field_kit/orin_steer_crosscheck.py`):
> - ~~판다 `SAFETY_SILENT` · passthrough — **제어권 미취득이라 CAN 과 Seer 가 독립 경로**~~
>   > ❌ **정정 2026-08-03 15:40: 거짓.** Seer 1040 은 판다가 엿듣는 **바로 그 `0x6064` 의
>   > 아핀 변환**이다(gain ×57,344 = 1.000001). **같은 프레임을 두 번 읽은 것**이며 독립 경로가 아니다.
> - AST(Abstract Syntax Tree, 추상 구문 트리) 구문 검사로 도구의 **송신 API 호출 0건** 확인
>   > ✅ 이 항목만 유효 — 송신 0건은 사실이다(다만 그것이 독립성을 만들지는 않는다).
> - ~~**사용자 육안 확인: Seer 표시 앞바퀴 2축 모두 0°**~~
>   > ❌ **정정 2026-08-03 15:40:** 추정량이 **자세 불변**이므로 이 확인은 **결과에 기여 0**이다.
>   > 관측 사실로는 남지만 **근거로 인용 금지.**
>   > > ❌ **재정정 2026-08-03 17:30 — 「인용 금지」는 (a) counts 역산 근거로서만이다.**
>   > > 이 육안 확인은 **(b) 「바퀴가 물리적으로 직진이다」의 앵커로는 유효**하다
>   > > — Seer 표시는 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자는
>   > > **바퀴 직진 상태에서** 앞바퀴 2축 0° 를 확인했다(`can_relay` GUI 동일).
>   > > 취소선은 **counts 역산 근거 목록에서의 삭제**로만 읽을 것.
> - ~~2회 독립 실행이 **counts 단위까지 동일**~~
>   > ❌ **정정 2026-08-03 15:40: 동어반복.** 정지 상태를 결정론적 파이프라인으로 두 번 읽은 것 — **정보량 0.**
>
> | 노드 | CAN `0x6064` | Seer | ~~역산 0°~~ → **Seer 영점 상수** | 구값과 차 |
> | --- | --- | --- | --- | --- |
> | node3 | 7,871,823c (σ=3, n=3110) | −0.000° | **7,871,816c** | **1c** |
> | node4 | 7,840,052c (σ=2, n=3110) | +0.001° | **7,840,087c** | **1c** |
>
> > ❌ **정정 2026-08-03 15:40 — 이 표의 4열은 「역산 0°」가 아니다.**
> > 역산식이 항등식이라 이 열이 돌려주는 것은 **Seer 내부 조향 영점 상수**
> > (node3 7,871,815.90 · node4 7,840,086.68)이며 **CAN 판독값과 무관하게 같은 값**이 나온다.
> > 5열의 **「1c」도 증거가 아니다** — `C₀` 가 비정수라 `floor`/`round` 차이일 뿐이고,
> > 두 노드 모두 `+1` 인 것은 잡음이 아니라 **계통**이다.
> > 또한 이 표의 Seer 각도 3자리 표기로는 자기 식을 재현할 수 없다(node4 22c 어긋남).
> > 실제 값은 **−0.000124° / +0.000605°** 다.
>
> 산출물: `Log/steer_xcheck_reboot_0deg.jsonl` · `Log/steer_xcheck_reboot_0deg_confirm.jsonl`
>
> ⇒ **이 문서가 「폐기」를 선언했던 `[7871815, 7840086]` 이 양 노드 모두 1 count(0.000017°)
> 이내로 맞다.** node4 0° 역산 3회 대조 — 7,840,095(08-01 역산) · 7,840,085(08-02 3차) ·
> 7,840,087(08-03 ×2). **이 문서가 채택한 7,839,894 만 홀로 떨어져 있었다.**
>
> > ❌ **정정 2026-08-03 15:40 — 위 「3회 대조」는 독립 대조가 아니다.**
> > 세 값 모두 **같은 항등식**을 통과한 결과라 자세와 무관하게 수렴한다 — 수렴 자체가 증거가 아니다.
> > **결론(값 채택)은 유효하나 근거는 이것이 아니다**: 유효한 근거는 「Seer 가 실시간 `0x607A` 로
> > 이 값을 지령한다」와 「내비게이션이 Seer 좌표계를 쓰므로 그 좌표에 맞춘다」는 **공학적 결정**이다.
> > **물리적 직진 여부는 이 대조로 전혀 확정되지 않는다.**
> > > ⚠ **범위 명시 2026-08-03 17:30:** 이 문장은 **「이 대조로는」**이라는 한정으로만 읽을 것.
> > > 물리적 직진은 **다른 경로(Seer 교정 조향각 + 사용자 육안 확인)로 확정돼 있다** —
> > > 「물리 직진 앵커가 없다」는 뜻이 **아니다**(6-B′ 재정정 참조).
> > > ❌ **재정정 2026-08-03 17:00: 「Seer 가 `0x607A` 로 이 값을 지령한다」는 선택 인용이다. [E8]**
> > > `Log/homing_capture_220350.jsonl` 의 `0x607A:00` 다운로드 전수(노드당 n=6,464):
> > > node3 **7,871,815 = 145회(2.2%)** vs **7,882,020 = 6,319회(97.8%)**,
> > > node4 **7,840,086 = 145회(2.2%)** vs **7,859,062 = 6,319회(97.8%)**.
> > > Seer 가 대부분 지령한 값은 **채택값이 아니라 GOZERO 상수** 쪽이다.
> > > ⇒ 남는 근거는 **「내비게이션이 Seer 좌표계를 쓴다」는 공학적 결정**뿐이다.
> > > 값 `[7871815, 7840086]` 은 **유지**하되 **「공학적 채택값」**으로만 서술하고
> > > **「실측 확정」 표현은 쓰지 말 것.**
>
> ### 「구값은 출처가 없는 값」 판정을 **철회한다**
>
> §1 의 「구값 `7871815 / 7840086` 은 … **출처가 없었을 뿐**」이라는 판정은 **반증됐다.**
> 출처는 **Seer 가 실시간으로 내는 `0x607A` 조향 목표**다 — 2026-08-03 passthrough 캡처에서
> 연속 관측됐다. 구값은 출처 불명이 아니라 **Seer 지령에서 온 값**이었다.
>
> ### 우선권 선언을 **이 항목에 한해 무효화한다**
>
> 아래 원문의 「저장소의 다른 문서가 홈에 대해 다르게 말하면 **이 문서가 이긴다**」는
> 선언은 **`steer_home_counts` 값에 한해 효력이 없다.** 이 문서는 그 항목에서 틀렸다.
>
> **새 정본 포인터:**
> - 근거: [`docs/homing/2026-08-03-can-relay-homing-assets.md` §10](../homing/2026-08-03-can-relay-homing-assets.md)
> - 값: `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`
>   (= `[7871815, 7840086]`, 2026-08-03 갱신 완료)
>
> ### ⚠ 과대 해석 금지
>
> - 차이 193c = **0.0034°** 로 **거동상 무의미하다.** 안전 문제가 **아니고**,
>   **정본 정확성** 문제다. "위험했다"는 식의 서술은 사실이 아니다.
> - `7882020 / 7859062` 는 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)이며 0° 가 아니다.
>   **별개 사안이고 이번 정정 대상이 아니다** — §3 의 무리 B 판정은 그대로 유효하다.
>   단 **0° 로부터의 거리는 파생값이라 같이 바뀐다**: 정정된 0° 기준으로
>   **+0.178° (node3) / +0.331° (node4)** 다.
>   널리 인용되는 **`+0.334°` 는 폐기값 `7,839,894` 로 계산된 잔재**이므로 쓰지 말 것(§3 참조).
>   > ✅ **확정 2026-08-03 15:40 — 실측이 이 상수를 추인했다.** 호밍 10회 정착 실측은
>   > node3 **7,882,021** (σ 2.8 c) · node4 **7,859,065** (σ 3.2 c) 로 상수와 **1~3 counts** 차다.
>   > 간격도 **+0.178° / +0.331°** 로 재현됐다. 「추정 GOZERO 목표」가 아니라 **실측 정착값**이다.
>   > > ❌ **재정정 2026-08-03 17:00 (원자료 재계산). [E6][E7]**
>   > > 10회 `post` 실측은 node3 7,882,021 **×8** / 7,882,014 **×2**, node4 7,859,065 **×8** / 7,859,058 **×2**
>   > > 이며 상수 대비 편차는 node3 **+1 / −6**, node4 **+3 / −4** 다 — **「1~3 counts 차」는 최빈값만
>   > > 본 것**이고 최대 편차는 **6 counts** 다. σ 2.8 / 3.2 는 **모표준편차**(표본 2.95 / 3.38).
>   > > 「상수를 추인했다」는 **재현성**까지만이고 **상수의 적정성은 미판정**이다(debt-016 과 충돌).
> - 코드·설정은 이미 전부 정정됐다(6개 파일 + 테스트 픽스처, 회귀 **319건 통과**).
>
> ### 이 문서를 삭제하지 않는 이유
>
> 원문은 **역사 기록으로 보존한다.** 아래 본문의 취소선·`❌ 정정` 표시는 삭제 대신
> 병기한 것이다. 이 문서의 실패 방식(§4-2 의 올바른 식이 §1 채택에 닿지 않음) 자체가
> 재발 방지 자료다 — **최상단 배너만으로는 부족해서** 본문 각 지점에 인라인 정정을 달았다.

---

> **이 문서가 조향 홈에 관한 정본 판정이다.** 저장소의 다른 문서가 홈에 대해 다르게
> 말하면 **이 문서가 이긴다.** 값 자체의 정본은 코드 쪽 캘리브레이션 파일 하나다:
> `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`.
>
> ❌ **정정 2026-08-03:** 위 우선권 선언은 **`steer_home_counts` 값에 한해 무효**다
> (최상단 정정 블록 참조). 「값의 정본은 `foil_a082.yaml`」이라는 부분은 **여전히 유효**하며,
> 그 파일의 현재 값은 `[7871815, 7840086]` 이다 — **이 문서 §1 이 적은 값이 아니다.**
>
> ❌ **정정 2026-08-03 15:40 — 무효 범위를 넓힌다.** 우선권 선언은 **조향 0° 항목 전체**
> (값 + 그 값을 확정했다는 **근거**)에 대해 효력이 없다. 이 문서의 역산 근거는 **항등식**이라
> 물리적 0° 를 확정하지 못한다. **해당 항목의 정본은
> `docs/homing/2026-08-03-can-relay-homing-assets.md` §0** 이다.
> 이 문서가 여전히 이기는 범위는 **무리 A/B 구분이라는 통찰과 §5 구조 변경**뿐이다.

---

## 1. 판정  ❌ **전면 정정됨 (2026-08-03) — 아래 원문은 폐기값이다**

> ### ❌ 정정 2026-08-03: 이 절의 채택값은 **0° 가 아니라 raw 판독값**이었다
>
> 아래 코드블록의 `[7871810, 7839894]` 은 **Seer 가 0° 를 가리키지 않던 자세에서 읽은
> `0x6064` raw 값**이다. 이 문서 **§4-2 의 역산식 `0° = CAN + Seer각도 × 57,344` 를
> 여기에 적용하지 않은 것**이 원인이다(node3 보정 6c / node4 보정 193c 누락).
>
> **현 정본 (2026-08-03 11:44 ~~실측 확정~~ → **채택 확정**, `foil_a082.yaml` 반영 완료):**
> ```
> steer_home_counts = [7871815, 7840086]      # node3(front) / node4(rear)
> ```
> ~~실측 역산 0°: node3 **7,871,816c** · node4 **7,840,087c** — 위 값과 **각각 1c(0.000017°)**.~~
> ~~근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.~~
>
> > ❌ **정정 2026-08-03 15:40 — 근거 교체.** §10 의 역산은 **항등식**이라
> > 「실측 역산 0°」가 아니라 **Seer 내부 영점 상수**를 돌려준다(최상단 §B).
> > **「1c 이내」도 증거가 아니다**(`C₀` 비정수의 반올림 차).
> > **현재 유효한 근거는 이것뿐이다**: Seer 가 실시간 `0x607A` 로 이 값을 지령하고,
> > 내비게이션이 Seer 좌표계를 쓰므로 **그 좌표에 맞춘다**는 공학적 결정.
> > **근거 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-3** (§10 아님).
> > ⚠ 따라서 이 값은 **「Seer 좌표계 기준 0°」**로만 확정이며 ~~**물리적 직진은 미확인**~~이다.
> > > ❌ **재정정 2026-08-03 17:30: 「물리적 직진은 미확인」은 거짓.**
> > > 사용자가 **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했고(2026-08-02 ·
> > > 2026-08-03, `can_relay` GUI 동일), Seer 값은 EasyDRIVE `steerOffset` 으로 **교정된**
> > > 조향각이다 ⇒ **물리 직진 앵커는 실재한다.** 미확인인 것은 직진 여부가 아니라
> > > **counts 소수 자리**다 — 앵커 정밀도가 **육안 한계 ≈±1°(±57,344 c)** 라 다툰
> > > **193 c(0.0034°)** 를 분해하지 못한다(약 297배).
> > > 「Seer 좌표계 기준 0°」라는 라벨 자체는 **유지**한다.
> > > ❌ **재정정 2026-08-03 17:00: 「Seer 가 `0x607A` 로 이 값을 지령」은 근거의 절반뿐이다. [E8]**
> > > 같은 캡처에서 Seer 는 이 값을 **2.2%(145/6,464)** 만 지령했고 **97.8%(6,319/6,464)** 는
> > > GOZERO 상수(7,882,020 / 7,859,062)를 지령했다 — **선택 인용**이다.
> > > ⇒ 남는 근거는 **좌표계 정합이라는 공학적 결정**뿐. 값 `[7871815, 7840086]` 은 정본
> > > (`foil_a082.yaml:134`)이므로 **유지**하되 라벨은 **「공학적 채택값」**까지만 쓸 것.
>
> 차이 193c = **0.0034°** — **거동 무영향.** 안전이 아니라 정본 정확성 문제다.

원문 (폐기, 역사 기록으로 보존):

```
❌ steer_home_counts = [7871810, 7839894]      # node3(front) / node4(rear)   ← 폐기 2026-08-03
```

> ❌ **정정 2026-08-03 — 아래 「출처가 없었을 뿐」 판정은 철회한다.**
> 구값 `7871815 / 7840086` 의 출처는 **Seer 가 실시간으로 내는 `0x607A` 조향 목표**다
> (2026-08-03 passthrough 캡처에서 연속 관측). 출처 불명이 아니었다.
> 또한 「구값이 실측과 다르다」는 전제 자체가 뒤집혔다 — **구값이 실측 0° 와 1c 이내로
> 일치**하고, 이 절이 채택한 `7839894` 쪽이 193c 어긋나 있었다.
> 다만 **"거동상 동일"이라는 결론만은 여전히 옳다** — 0.0034° 는 무의미한 차이다.

~~**구값 `7871815 / 7840086` 은 틀린 값이 아니었다 — 출처가 없었을 뿐이다.**~~
~~실측과의 차이는 각각 **5 counts(0.00009°)** · **192 counts(0.0033°)** 로, 이 기체의~~
~~조향 분해능 아래이며 거동상 동일하다. 4주간 "어느 값이 맞는가"로 다툰 대상이~~
~~실제로는 0.0001° 차이였다 — **아무도 빼보지 않았기 때문이다.**~~

## 2. ❌ 근거 — ~~같은 자세(직진)를 intercept off 로 읽은 세 경로가 일치~~

**→ 정정 2026-08-03: 「일치」가 아니었다 — 세 번째 경로는 확인값이 아니라 보정량이었다.**

> ❌ **정정 2026-08-03 — 이 표의 해석이 틀렸다.**
> 3행은 「일치하는 세 경로」가 아니다. **1·2행은 `0x6064` raw 판독값이고 3행은 그 시점의
> Seer 각도**다. 3행은 1·2행을 **확인하는** 값이 아니라 1·2행에서 **빼야 할 보정량**이었다
> (§4-2 의 `0° = CAN + Seer각도 × 57,344`). node4 의 `+0.0035°` = **193c** 가 바로
> 2행의 `7,839,894` 를 0° 로 채택할 수 없게 만드는 값이었는데, 이 절은 그것을 「일치」로 읽었다.
>
> **역산을 적용한 실측 (2026-08-03 11:44, 판다 SAFETY_SILENT passthrough · 제어권 미취득 ·
> 사용자 육안으로 Seer 앞바퀴 2축 0° 확인 · 2회 독립 실행 counts 단위까지 동일):**
>
> > ❌ **정정 2026-08-03 15:40 — 위 괄호 안 조건 4개 중 3개가 무효다.**
> > 「제어권 미취득이라 독립 경로」·「사용자 육안 0° 확인」·「2회 독립 실행 동일」은
> > 각각 **거짓 · 기여 0 · 동어반복**이다(최상단 §B). 유효한 것은 **송신 0건**뿐이다.
>
> | 경로 | node3 | node4 | 조건 |
> | --- | --- | --- | --- |
> | 판다 passthrough `0x6064` | 7,871,823 (σ=3, n=3110) | 7,840,052 (σ=2, n=3110) | 2026-08-03 11:44 · 송신 0건(AST 검사) |
> | Seer 각도 | −0.000° | +0.001° | 동시각 — ⚠ **같은 `0x6064` 의 아핀 변환**(독립 채널 아님) |
> | ~~**역산 0°**~~ → **Seer 영점 상수** | **7,871,816** | **7,840,087** | ❌ `0° = CAN + Seer × 57,344` 는 **항등식** — CAN 이 소거된다 |
>
> > ❌ **정정 2026-08-03 15:40 — 이 표는 「3경로 대조」가 아니라 사실상 1경로다.**
> > 1행(CAN)과 2행(Seer 1040)이 **같은 프레임**이고, 3행은 그 둘의 항등적 재조합이라
> > **자세와 무관하게 상수**다. 0.049° 떨어진 08-02 자세를 넣어도 소수 4자리까지 같은 값이 나온다.
> > ⇒ **이 표로 물리적 0° 를 말할 수 없다.**
>
> 산출물: `Log/steer_xcheck_reboot_0deg.jsonl` · `Log/steer_xcheck_reboot_0deg_confirm.jsonl`

원문 (보존 — 취소선·❌ 표시와 `➕` 행만 2026-08-03 에 덧붙였고 수치는 지우지 않았다):

| 경로 | node3 | node4 | 조건 |
| --- | --- | --- | --- |
| 판다 수동청취 콜드부팅 캡처 `0x6064` | **7,871,818** | **7,840,084** | 2026-07-27 22:03 · 노드당 16,811 샘플 (`docs/verified_facts/2026-07-27.md` §A-8) |
| ❌ 판다 SILENT 수동청취 `0x6064` | ~~**7,871,810**~~ | ~~**7,839,894**~~ | 2026-08-01 · CAN 프레임 송신 0건 · 제어권 미취득 — **raw 값이며 0° 아님** |
| Seer API 1040 `motor_info.position` | **+0.0001°** | **+0.0035°** | 2026-08-01 · 위와 동시각 — **보정량이지 확인값이 아님** |
| ➕ 위 두 행을 §4-2 로 역산 *(2026-08-03 추가)* | 7,871,816 | **7,840,095** | ← **이 줄이 §1 채택값이 됐어야 했다** |

4일·전원 인가 실험·펌웨어 플래시 8회를 사이에 두고 편차가 node3 **8c(0.00014°)** ·
node4 **190c(0.0033°)** 에 머물렀다 → **절대 엔코더는 전원 사이클을 넘어 재현된다.**

> ✅ **이 결론(절대 엔코더의 전원 사이클 재현성)은 정정 후에도 유효하다.** 2026-08-03
> 재부팅 후 실측이 2026-08-01 역산과 node4 8c 이내로 재현됐다.

3차 확인(2026-08-02 19:42, 자세가 달라진 상태에서 0° 역산): node3 7,871,796c ·
node4 7,840,085c — 위 값과 각각 14c / 191c.

> ❌ **정정 2026-08-03:** 이 3차 확인의 node4 값 **7,840,085c 가 정답이었다.**
> 「위 값과 191c 차이」라고 적어 **채택값 쪽을 기준으로 삼은 것**이 오류다 — 기준이 뒤집혀
> 있었다. 이 값은 08-03 실측 역산 7,840,087c 와 **2c** 차이다.
> 이 문서는 §6 에서 이것을 「계통 편차 가능성(미판정)」으로 넘겼다.

## 3. ⚠ 반복의 진짜 원인 — 두 물리량이 같은 이름을 썼다

4주간 기록된 모든 값은 **두 무리로만** 갈린다. 각 무리 안에서는 0.0003° 이내로 재현된다.

| 무리 | 뜻 | node3 | node4 | 관측 |
| --- | --- | --- | --- | --- |
| **A** ❌ | ~~**바퀴가 직진일 때의 counts = 진짜 0°**~~ → **Seer 좌표계 0°** (물리 직진 미확인) | ~~7,871,810 ± 8c~~ → **7,871,815** | ~~7,839,894 ± 192c~~ → **7,840,086** | 3회 → 08-03 **채택 확정**(근거는 항등식 아님, 최상단 §B) |
| **B** ✅ | **호밍이 끝나고 드라이브가 멈춘 counts** | ~~7,882,020 ± 19c~~ → **7,882,021** (σ 2.8c) | ~~7,859,062 ± 7c~~ → **7,859,065** (σ 3.2c) | ~~4회 / 3회~~ → **10회 연속 실측 확정 (08-03 15:33~15:40)** |
| | **A↔B 간격** | **+0.178°** (10,205c) | ~~+0.332°~~ → **+0.331°** (18,976c) | **10회 실측으로 재현 확정** |

> ✅ **확정 2026-08-03 15:40 — 무리 B 가 실측으로 못박혔다.**
> 이 문서가 「호밍 후 정착값」이라 명명한 것이 **호밍 10회 연속(전부 DONE, 35.0 s ± 0.17 s)**
> 실측으로 **값·재현성 모두 확정**됐다. σ ≈ 3 counts ≈ **0.00005°** — 매번 같은 자리에 선다.
> ⇒ **이 편차는 결함이 아니라 설계 동작이다.** 펌웨어 `SEER_HOME_ZERO_N3/N4`(7882020 / 7859062)가
> 그 목표이고 실측이 **1~3 counts** 로 일치한다.
> ⚠ 반면 **무리 A 의 「진짜 0°」라는 라벨은 과대**하다 — 확정된 것은 **Seer 좌표계 0°** 뿐이다.
>
> > ❌ **재정정 2026-08-03 17:30 (위 표 무리 A 행의 「물리 직진 미확인」).**
> > **물리 직진 앵커는 실재한다** — Seer 의 **교정된**(EasyDRIVE `steerOffset`) 조향각 +
> > 사용자 육안 확인(바퀴 직진 상태에서 앞바퀴 2축 0°, 2026-08-02 · 2026-08-03,
> > `can_relay` GUI 동일). 「미확인」이 아니라 **정밀도 한계 ≈±1°(±57,344 c)** 로 적을 것 —
> > 무리 A ↔ 무리 B 간격(0.178° / 0.331°)은 이 한계보다 작으므로 **육안 앵커로는
> > 두 무리를 구분하지 못한다.** 무리 A/B 구분 자체는 CAN 실측으로 서 있으며 유효하다.
>
> > ❌ **재정정 2026-08-03 17:00. [E4][E6][E7]**
> > · 「35.0 s ± 0.17 s」 → **평균 35.07 · 중앙 35.05 s**, 범위 34.99~35.16(폭 0.17).
> > · 「1~3 counts 로 일치」 → 최빈 **+1 / +3**, 실제 편차 집합은 node3 **{+1×8, −6×2}** ·
> >   node4 **{+3×8, −4×2}** — **최대 6 counts**. σ 2.8 / 3.2 는 **모표준편차**.
> > · 「결함이 아니라 설계 동작이다」 → **과잉 확정.** 실측이 보증하는 것은
> >   **「상수에 재현성 있게 정착한다」**까지이고 **상수의 적정성은 별건**이다(**debt-016** 이
> >   같은 편차를 「영구 미검출 오프셋」으로 등록 중 — 충돌).
> >   ⇒ **「재현되는 정착 동작(상수 적정성은 별건, debt-016)」**로 읽을 것.

> ❌ **정정 2026-08-03 — 무리 A 의 대표값만 정정.**
> A 의 `± 192c` 는 잡음 폭이 아니라 **역산을 안 한 데서 온 계통 오차**였다.
> 역산을 적용하면 node4 A 는 7,840,08x 로 **± 10c 안에 모인다.**
> **무리 A/B 를 구분한다는 §3 의 핵심 통찰 자체는 정정 대상이 아니며 그대로 유효하다.**
> `7882020 / 7859062`(무리 B)는 **펌웨어 GOZERO 상수**이고 0° 가 아니라는 판정도
> **그대로 유효 — 이번 정정과 별개 사안이다.**
>
> ⚠ **파생 주의 — node4 A↔B 간격은 재계산된다.** 이 간격은 A 에 의존하므로 A 가 바뀌면 같이 바뀐다:
> `(7,859,062 − 7,840,086) / 57,344 = **+0.3309°**`. 즉 **+0.331°** 이다.
> 저장소 곳곳에 도는 **`+0.334°` 는 폐기값 `7,839,894` 로 계산된 수치**다
> (`(7,859,062 − 7,839,894)/57,344 = 0.3343°`) — **정정 대상 잔재이므로 인용하지 말 것.**
> node3 은 A 변화가 5c 뿐이라 **+0.178° 그대로**다.

⇒ **호밍은 조향을 0° 에 정확히 놓지 않는다.** B 는 펌웨어 GOZERO 목표이지 0° 가 아니다.
✅ (**이 결론은 유효.** 정정된 A 기준으로도 간격은 +0.178° / +0.331° 로, 여전히 0° 가 아니다.)

**둘 다 "조향 홈"이라 불린 것이 재실험 반복의 원인이다.** 호밍하고 잰 사람과 안 하고
잰 사람이 만나면 매번 "값이 다르다"가 됐고, 실제로는 둘 다 옳았다.
**앞으로 B 를 "홈" 이라 부르지 않는다 — "호밍 후 정착값" 으로만 쓴다.**

## 4. 함께 해소된 것

### 4-1. 「판다 read ≈0 vs Seer −7,871,810」 137° 모순 (debt-007 §B-1)

> ℹ **2026-08-03 주석:** 제목의 `−7,871,810` 은 폐기된 §1 채택값에서 온 표기다(현 정본은
> `7,871,815`). **차이 5c = 0.00009° 로 137° 모순 분석에는 아무 영향이 없다** —
> 이 절의 결론(원인 후보 (a) 기각 · bit15 가설 유력)은 **정정 없이 그대로 유효하다.**
> 값 표기만 참고로 남긴다.

- 원인 후보 **(a) 「제어권 보유 중 판다 read 오염」 → 기각.**
  `emulate`(`= cover || pc_authority`)는 **bus 라우팅만** 바꾼다 — bus0 의 Seer SDO
  요청을 캐시 응답으로 대신하고, bus2 의 `0x600~0x604`·`0x581~0x584`·`0x701~0x704` 를
  드롭해 Seer 에게 숨긴다. **PC 자신의 수신 경로는 건드리지 않는다.**
  근거: `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h:164-193` (2026-08-02 직접 확인)
- 남는 유력 후보는 **`0x6041` bit15=0 구간에서 `0x6064` 가 0 으로 고정**되는 동작이다
  (2026-07-27 캡처 3,080/3,080 샘플 실증). 다만 당시 statusword 로그가 없어
  **확정이 아니다** — *유력 후보*로 남긴다.
- 실무상 이 잔여 불확실성은 닫혀 있다: `can_relay` 가 `safety.position_trustworthy()` 로
  bit15=0 구간의 `0x6064` 를 상위에 흘리지 않는다.

### 4-2. Seer 각도 ↔ CAN counts 는 **음의 상관**이다  ❌ **이 절의 「역산」 지위가 무너졌다 — 항등식이다**

> ❌❌ **정정 2026-08-03 15:40 — 이 절이 이 문서에서 가장 크게 오해된 부분이다.**
>
> 아래 식 `0° = CAN + Seer각도 × 57,344` 는 **역산이 아니라 항등식**이다.
> Seer 1040 각도가 **같은 `0x6064` 의 아핀 변환**(기울기 × 57,344 = **1.000001**, 7자리 일치)이라
> `0°_est = CAN + (C₀ − CAN) = C₀` 로 **CAN 항이 소거된다.**
> ⇒ **어떤 자세에서 측정해도 같은 값**이 나온다. 실증: 0.049°(2,825 counts) 떨어진 08-02 자세의
> 로그를 넣어도 **소수 4자리까지 같은 값**이 나왔다.
> Seer 1005 도 `0x6064` 유래다(gain **1.000130**) — 두 채널 어느 쪽도 독립 앵커가 아니다.
>
> **⇒ 이 식이 확정하는 것은 「Seer 내부 조향 영점 상수」**
> (node3 7,871,815.90 · node4 7,840,086.68) **이지 물리적 0° 가 아니다.**
>
> **그래서 아래 「부호 검증」 논증도 지위가 바뀐다**: `+` 가 맞다는 것은
> 실제로 **조향을 90° 움직여 얻은 실측**(CAN `+` ↔ Seer `−`, `…assets.md` §14-2)으로 확정됐다.
> 아래의 「`−` 로 하면 5,616c 어긋나고 `+` 로 하면 20c 로 수렴」은 **자기 일관성 확인**일 뿐이다.
>
> ~~⚠ **이 절 자체는 정확하다. 문제는 이 절이 §1 에 닿지 않은 것이다.**~~
> ~~아래 역산식은 2026-08-03 실측으로 **재확인됐다.** 이 문서의 오류는 식이 틀린 게 아니라,~~
> ~~**이 식을 §1 채택값에 적용하지 않고 raw 판독값을 그대로 0° 로 박은 것**이다.~~
> ~~이 절을 §1 에 적용했다면 node4 정본은 7,840,095c 가 됐을 것이고~~
> ~~실측 7,840,087c 와 8c(0.00014°) 이내로 맞았다.~~
> > (위 4줄은 2026-08-03 11:44 시점 판단으로, 같은 날 15:40 에 **항등식 판정으로 무효화**됐다.
> >  「식을 §1 에 적용했다면 맞았을 것」은 결과적으로 옳지만 — 항등식이라 **어차피 같은 값이 나왔을 뿐**이고,
> >  그것이 물리 0° 를 맞혔다는 뜻은 아니다.)

하루 간격 2점 회귀(둘 다 SILENT 수동청취):

```
node3  ΔCAN −2,818c ↔ ΔSeer +0.0488°   → 기울기 −1.732e-5 °/c
node4  ΔCAN   +191c ↔ ΔSeer −0.0035°   → 기울기 −1.832e-5 °/c
|기울기| × 57,344 = 0.993 / 1.051 ≈ 1   → 스케일은 맞고 부호만 반대
```

⇒ 0° 역산은 **`0° = CAN + Seer각도 × 57,344`** 다. `−` 로 하면 두 측정의 0° 추정이
node3 에서 5,616c(0.098°) 어긋나고, `+` 로 하면 20c(0.0003°) 로 수렴한다.
⚠ node4 의 기울기는 ΔSeer 가 0.0035° 뿐이라 **부호만 확인, 스케일 정밀도는 낮다.**

> ❌ **정정 2026-08-03 15:40 — 위 「20c 로 수렴」은 증거가 아니다.**
> 항등식이므로 **부호만 맞으면 자세와 무관하게 수렴하는 것이 당연하다.** 수렴은 부호를 가릴 뿐,
> 값의 물리적 정확성에 대해서는 아무 말도 하지 않는다.
> ✅ **부호 `+` 는 별도 실측으로 확정됐다** — 조향을 실제로 크랩 90° 움직였을 때
> CAN **+5,160,957c (= +90.0000°)** ↔ Seer **−90.000126°** (`…assets.md` §14-2).
> ✅ **스케일도 확정됐다** — 지령각→CAN 기울기 실측 **57,344.0 counts/° (배율 1.000000)**
> (§14-1). 위 「node4 스케일 정밀도 낮음」 유보는 **해소**됐다.
>
> > ❌ **재정정 2026-08-03 17:00: 「57,344.0 (1.000000)」은 node3 값이다. [E5]**
> > `Log/steer_two_phase_260803_131305.jsonl` phase A 5점 최소제곱 기울기 재계산:
> > **node3 57,344.000 (×1.0000000)** · **node4 57,344.280 (×1.0000049)**.
> > 11점 스윕(`Log/steer_sweep_1005_260803_130543.jsonl`)은 또 다르다(57,343.52 / 57,343.97).
> > ⇒ 정수 **57,344** 사용은 무방하나 **「57,344.0 으로 실측 확정」은 node4 에 대해 사실이 아니다.**

> ~~✅ **2026-08-03 검증:** 이 식을 08-03 실측에 적용하면 node3 7,871,816c · node4 7,840,087c 로,~~
> ~~정본 `[7871815, 7840086]` 과 **양 노드 1c** 이내다. **식은 옳다.**~~
> ~~**이 식을 §1 에 적용하지 않은 것이 이 문서의 유일한 실질 오류다.**~~
>
> ❌ **정정 2026-08-03 15:40:** 「1c 이내」는 `C₀` 비정수의 반올림 차이이고(두 노드 모두 `+1` = 계통),
> 「식은 옳다」는 **항등식이 옳다**는 말에 지나지 않는다.
> **「유일한 실질 오류」라는 진단도 틀렸다** — 더 큰 오류는 **항등적 재조합을
> 「독립 경로 교차검증」으로 제시한 것**이다. 값은 살아남았지만 **근거는 무너졌다.**

## 5. 구조 변경 — 값이 다시 흩어지지 않게

- **정본 1곳**: `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml`
- **코드 기본값 제거**: `safety.DEFAULT_STEER_HOME = {}`. 값이 없으면 조향 지령을
  **거부**하고 노드도 기동하지 않는다 — 「조용한 오판」보다 정지를 택한다
- ❌ ~~실행 자산 7곳의 구값을 실측값으로 갱신하고, 각 파일 주석이 위 정본을 가리키게 했다~~
  > **정정 2026-08-03:** 이 갱신은 **구값 → 폐기값 `[7871810, 7839894]`** 로의 갱신이었다.
  > 2026-08-03 에 **되돌렸다** — 실행 자산은 현재 `[7871815, 7840086]` 을 쓴다
  > (6개 파일 + 테스트 픽스처 정정, ~~회귀 **319건 통과**~~).
  > > ❌ **정정 2026-08-03 15:40: 「회귀 319건 통과」는 값의 근거가 아니다.**
  > > 변이 시험 결과 **정본을 쓰레기 값으로 바꿔도 319건이 전부 통과**했다 —
  > > 테스트가 YAML 을 로드하지 않고 픽스처가 하드코딩이었다(`…assets.md` §11-3 F1).
  > > 08-02~08-03 사이 **정본과 픽스처가 서로 모순인 채 319/319 green** 이었다.
  > > 이후 `test_steer_home_sync.py`(11건) 신설로 실제로 잡히게 됐다(§11-4).
  > **정본 1곳 원칙과 코드 기본값 제거(위 두 항목)는 그대로 유효하다** — 이번 정정으로
  > 값 하나만 고치면 전부 반영됐다는 점이 오히려 그 구조의 효과를 입증했다.
- 회귀 3건이 고정: `test_code_has_no_builtin_steer_home` ·
  `test_steer_refused_when_home_not_configured` · `test_steer_refused_when_home_is_none_and_no_default`

## 6. 아직 열려 있는 것 (이 종결에 포함되지 않음) — **2026-08-03 15:40 전면 갱신**

> 이 절은 하루에 두 번 갱신됐다. **15:40 판(6-A′/6-B′/6-C′)이 최신이고 우선한다.**
> 그 아래 11:44 판(6-A/6-B/6-C)은 **역사 기록으로 보존**한다.

### 6-A′. 2026-08-03 **15:40 기준 — 닫힌 것** (실측만)

- ✅ **호밍 동작 — 닫힘. 호밍은 정상 동작한다.** `--repeat 10` 이 **10/10 DONE**,
  소요 **35.0 s**(편차 0.17 s), 10회 모두 DI `0x01`→`0x09`(−Limit) 도달.
  09:58 `ERR_TIMEOUT` **1회** 뒤 **12회 연속 성공**, **재현 안 됨**.
  ⇒ 아래 6-C 의 「호밍 실행 자체는 여전히 미해결」은 **닫혔다.**
  > ❌ **재정정 2026-08-03 17:00. [E1][E4]** 「12회 연속 성공」 → **12회 연속 성공**
  > (오늘 시도 13 / 성공 12 / 실패 1 — 15:33 요약의 `baseline` 은 호밍이 아니라
  > 레지스터 스냅샷 `orin_home_experiment.py:390`). 「35.0 s」 → **평균 35.07 · 중앙 35.05 s**.
  > **「10/10 DONE」은 유효.**
- ✅ **무리 B(호밍 후 정착값) 재현성 — 닫힘.** node3 **7,882,021**(σ 2.8 c) ·
  node4 **7,859,065**(σ 3.2 c), **10회**. σ ≈ 0.00005°. 펌웨어 GOZERO 상수와 **1~3 counts**.
  ⇒ **설계 동작으로 확정**. 「호밍이 0° 에 놓지 않는다」는 §3 결론이 실측으로 못박혔다.
  > ❌ **재정정 2026-08-03 17:00. [E6][E7]** σ 2.8 / 3.2 는 **모표준편차**(표본 2.95 / 3.38).
  > 상수 대비 편차는 node3 **+1×8 / −6×2**, node4 **+3×8 / −4×2** — **최대 6 counts**.
  > 「설계 동작으로 확정」 → **「재현되는 정착 동작(상수 적정성은 별건, debt-016)」**로 완화.
  > 닫힌 것은 **재현성**뿐이고 **상수 적정성은 열려 있다.**
- ✅ **A↔B 간격 — 닫힘.** **+0.178° / +0.331°** 재현. `+0.334°`·`+0.332°` 는 **폐기값**.
- ✅ **`counts/°` — 닫힘.** 지령각→CAN 기울기 실측 **57,344.0 counts/° (배율 1.000000)**.
  §4-2 의 「node4 스케일 정밀도 낮음」 유보 **해소**.
  > ❌ **재정정 2026-08-03 17:00. [E5]** **node3 57,344.0 / node4 57,344.3**
  > (`Log/steer_two_phase_260803_131305.jsonl` phase A 최소제곱: 57,344.000 / 57,344.280).
  > **단일 「57,344.0」은 node3 값**이다.
- ✅ **`0x6098` 호밍 방식 — 닫힘.** 실측 **1**(Home 1, −리밋). 「0 이라 비활성」 **폐기**.
- ✅ **리밋 스위치 실재 — 닫힘.** 10회 모두 DI bit3 전이 관측.
- ✅ **「축이 이미 홈이면 무동작 즉시 완료」 가설 — 닫힘(반증).**
  `--offset 0`(홈 상태)에서도 축이 **−리밋까지 주행**해 DONE(§17-1).
- ❌ **6-A 의 「node4 +191c 편차가 닫혔다」는 판정 — 결론은 유지, 근거는 무효.**
  편차가 사라진 것은 사실이나, 그것을 보인 「역산」이 **항등식**이라 대조로서의 힘이 없다.
  값 채택은 유효, **증명 논거로 인용 금지**.
- ❌ **6-A 의 「Seer 육안 0° 확인으로 부분 폐쇄」 — 무효.**
  추정량이 **자세 불변**이라 육안 확인은 **결과에 기여 0**이다. 닫힌 것이 없다.
  > ❌ **재정정 2026-08-03 17:30 — 위 문장은 두 질문을 하나로 뭉갰다. 갈라 읽을 것.**
  > **(a) 육안 확인이 「조향 0° = 몇 counts 인가」 역산에 기여하나? → 아니오.**
  > 역산식 `0° = CAN + Seer°×57,344` 는 **항등식**(1040 = `0x6064` 의 아핀 변환)이라
  > 바퀴를 어느 자세에 두고 실행해도 같은 값이 나온다. **이 판정은 유효하며 그대로 유지한다.**
  > **(b) 「Seer 0° 표시 = 바퀴가 물리적으로 직진」이 성립하나? → 예. 성립한다.**
  > Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가
  > **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다
  > (2026-08-02 · 2026-08-03, `can_relay` GUI 표시로도 동일 확인).
  > ⇒ **(b) 를 (a) 에 딸려 무효화한 것이 오류다.** 위 문장은 **(a) 한정**으로만 읽고,
  > 「물리 직진 앵커가 없다」는 뜻으로 인용하지 말 것. 앵커에 대해서는 아래 6-B′ 재정정 참조.

### 6-B′. 15:40 기준 **여전히 열려 있는 것**

- ❌ ~~**물리적 직진 앵커가 없다**~~ — 가장 큰 미결. 현 정본 `[7871815, 7840086]` 은
  **Seer 좌표계 0°** 로만 확정이다. Seer 1040(gain 1.000001)도 1005(gain 1.000130)도
  **둘 다 `0x6064` 유래**라 ~~**非-Seer 앵커가 하나도 없다.**~~
  물리 정렬 오차 상한은 여전히 **육안 한계 ≈ ±1°(±57,344 c)** — 이번에 다툰 193 c 의 **약 297배**다.
  > ❌ **재정정 2026-08-03 17:30 — 「물리 직진 앵커가 없다」는 거짓이다. 앵커는 실재한다.**
  > **물리 직진 앵커는 실재한다** — Seer 의 **교정된**(EasyDRIVE `steerOffset`) 조향각 +
  > **사용자 육안 확인**(바퀴 직진 상태에서 Seer 앞바퀴 2축 0°, 2026-08-02 · 2026-08-03,
  > `can_relay` GUI 표시로도 동일 확인). 「앵커를 만들지 못했다 / 非-Seer 앵커가 필요하다」는
  > 서술은 **폐기**한다.
  > **남는 것은 앵커의 유무가 아니라 정밀도다** — 앵커 정밀도는 **육안 한계 ≈ ±1°(±57,344 counts)**
  > 이며, 이 수치 자체는 위 원문 그대로 유효하다.
  > ⇒ 갈라 적을 것:
  > · 앵커는 **「바퀴가 직진이다」를 세우기에 충분**하다.
  > · 앵커는 **counts 정본의 소수 자리(다툰 193 counts = 0.0034°)를 판정할 정밀도가 없다**
  >   — ±1° 는 그 **약 297배**다.
  > ⇒ counts 정본 `[7871815, 7840086]` 이 **공학적 채택값**인 이유는 **「앵커가 없어서」가 아니라
  > 「앵커 정밀도가 그 차이를 분해하지 못해서」**다.
  > ⇒ 「非-Seer 계측(휠 평면 직접 계측 0.052° / iAHRS yaw 0.012°)이 **필요하다**」는
  > **「counts 소수 자리까지 판정하려면」**이라는 조건에서만 참이다 — 무조건 필요한 것이 아니다.
  > ⚠ 「Seer 1040·1005 가 둘 다 `0x6064` 유래이므로 서로 독립 교차검증이 아니다」는 판정은
  > **그대로 유효**하다 — 그것은 **두 Seer 채널 사이의 독립성** 문제이지 물리 앵커의 유무가 아니다.
- **`0x6064` = 0 래치** — 09:58 실패의 유일한 차이점이자 유력 원인(debt-036).
  **1회 관측 후 재현 안 됨.** ⚠ 「전원 사이클로만 풀린다」는 설명은 **폐기**됐다
  (07-27 캡처가 사이클 없이 3,115회 관측 — `…assets.md` §11-2).
  > ❌ **재정정 2026-08-03 17:00: 「1회 관측 후 재현 안 됨」은 거짓 — 재현된다. [E2]**
  > `0x6064:00` SDO 응답 전수 집계(0 / 전체): 09:19 **50/50 · 50/50** · 09:58 **12,220/12,220 ·
  > 12,211/12,211** · 10:08 **10,327/10,327 · 10,327/10,327**(같은 구간 `0x6041`=37968 bit15=1
  > 258표본/노드 **전량**) · 11:38 리부팅 **이후**에도 `probe_113805.jsonl` 97/1,300 · 96/1,299,
  > 14:43 `homing_edge_260803_144305_can.jsonl` 2/74 · 2/68.
  > ⚠ **인과는 양쪽 다 미판정.** 0 이 관측된 14:43 직후의 14:46 호밍이 성공했으므로
  > 「`0x6064`=0 이 호밍을 막는다」도 성립하지 않는다.
  > ⚠ **「호밍 중(bit15=0)이면 `0x6064`=0」은 유효**하다(15:33 런 node3 **32,243/32,243**).
  > 폐기된 것은 **「전원 사이클 래치」주장뿐**이며, 이 유효 관측까지 폐기하지 말 것.
- 2026-08-02 19:42 캡처 첫 ~5초의 **+12.9° 이동 주체 미확인**(node3 7,126,954 → 7,868,999).
  (2026-08-03 재측정에서는 σ=2~3c 로 정지 상태였다.)
- §4-1 의 **`0x6041` bit15=0 ↔ `0x6064`=0** 상관은 여전히 *유력 후보*이며 **미확정**이다.
  (07-27 캡처 16,811 표본에서 상관 99.9%.)

### 6-C′. 15:40 에 **새로 열린** 것

- **호밍이 왜 조향을 0° 가 아닌 곳에 놓는지는 「설계 그렇다」까지만 확정됐다.**
  펌웨어 GOZERO 상수를 **0° 로 바꿀 것인지**는 미결정 — `SEER_HOME_ZERO_N3/N4`
  (7882020 / 7859062) 는 여전히 **무리 B** 값이다.
- **`SEER_HOME_ZERO_TOL = 57344`(1.0°) 의 진짜 문제** — 도달 판정 허용오차가 1.0° 라
  최종 오차가 **최악 1.18°**(0.178° + 1.0°)까지 벌어져도 `DONE` 이 난다.
- **09:58 `ERR_TIMEOUT` 은 미해결로 남았다** — 원인 후보는 `0x6064` 래치 하나지만
  **래치가 지금 풀려 있어 재현 불가**하다. 진단은 「재시도로 충분」에서 멈춘다.

<details><summary>11:44 판 §6 (보존 — 위 15:40 판이 우선)</summary>

### 6-A. 2026-08-03 에 **닫힌** 것

- ✅ **~~node4 의 +191c 계통 편차(미판정)~~ → 닫힘. 편차가 아니라 이 문서의 오류였다.**
  아래 원문이 「node4 0° 역산이 세 측정 모두 정본값보다 +191c 로 나온다」고 적었는데,
  **세 번 다 같은 방향으로 나온 이유는 역산 쪽이 맞고 §1 채택값이 틀렸기 때문**이다.
  기준이 뒤집혀 있었다. 정본을 `7840086` 으로 바로잡자 편차가 **1c** 로 사라졌다.
  > 교훈: 「세 측정이 모두 정본에서 같은 방향으로 벗어난다」는 관측은
  > **측정 계통 편차보다 먼저 정본 오류를 의심해야 한다.**
- ✅ **~~「Seer 의 0°」육안 미확인~~ → 부분적으로 닫힘.**
  2026-08-03 11:44 측정 시 **사용자가 Seer 표시로 앞바퀴 2축 모두 0° 임을 확인**했다.
  다만 이는 **Seer 표시 기준 0°** 확인이며, **Seer 의 0° 가 물리적 직진과 같은지는
  여전히 미확인**이다(아래 6-B 로 이월).
  > ❌ **15:40 정정: 이 항목은 무효** — 육안 확인은 결과에 기여 0이다(6-A′).
  > > ❌ **재정정 2026-08-03 17:30 — 15:40 의 「무효」는 (a) 한정이다.**
  > > 육안 확인이 **counts 역산 결과에 기여 0**인 것은 맞다(항등식·자세 불변 — 유지).
  > > 그러나 **「Seer 의 0° 가 물리적 직진과 같은지 미확인」이라는 이 항목의 유보는
  > > 이미 해소됐다** — Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고,
  > > 사용자가 **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다
  > > (2026-08-02 · 2026-08-03, `can_relay` GUI 표시로도 동일 확인).
  > > ⇒ **물리 직진 앵커는 실재한다.** 「부분적으로 닫힘」이 아니라 **(b) 는 닫혔다.**
  > > 남은 것은 앵커의 **정밀도**(육안 한계 ≈±1°)뿐이다(6-B′ 재정정 참조).

### 6-B. 여전히 열려 있는 것

- **「Seer 의 0°」와 물리적 직진(기구학적 중립)이 같은지는 미확인이다.** Seer 도 자기
  캘리브레이션을 거친 값을 준다. 조향 중립 산출은 **사용자가 별도 진행**(2026-08-02 지시).
  → 즉 현 정본 `[7871815, 7840086]` 은 **「Seer 0° 기준의 0°」**로서 확정된 것이고,
  물리 직진 기준으로는 아직 미확정이다. 이 구분을 흐리지 말 것.
  > ✅ **15:40: 유효하며 오히려 강화됐다** — 1005 도 `0x6064` 유래로 확인돼(6-B′)
  > **非-Seer 앵커가 전무**하다는 것이 확정됐다.
  > > ❌ **재정정 2026-08-03 17:30 — 「非-Seer 앵커가 전무」에서 「물리 직진 앵커 부재」를
  > > 도출한 것이 오류다.** 1005·1040 이 둘 다 `0x6064` 유래라는 사실은 **두 Seer 채널이
  > > 서로 독립 교차검증이 아니다**를 뜻할 뿐이며, 이 판정은 **그대로 유효**하다.
  > > 그러나 Seer 값은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가
  > > **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다(2026-08-02 · 2026-08-03,
  > > `can_relay` GUI 표시로도 동일 확인) — ⇒ **물리 직진 앵커는 실재한다.**
  > > 위 원문의 「물리 직진 기준으로는 아직 미확정」은 **counts 소수 자리에 한해서만** 참이다:
  > > 앵커 정밀도가 **육안 한계 ≈±1°(±57,344 c)** 라 다툰 **193 c(0.0034°)** 를 분해하지 못한다
  > > (약 297배). **앵커의 유무와 앵커의 정밀도를 구분할 것.**
- 2026-08-02 19:42 캡처 첫 ~5초에 조향이 실제로 **+12.9° 움직인 구간**이 있다
  (node3 7,126,954 → 7,868,999). 교차검증 도구는 송신하지 않으므로 우리가 아니다 —
  **움직인 주체 미확인**. (2026-08-03 재측정에서는 σ=2~3c 로 정지 상태였다.)
- §4-1 의 **`0x6041` bit15=0 구간 `0x6064`=0 고정**은 여전히 *유력 후보*이며 미확정이다.
  ~~단 2026-08-03 에 이 현상이 **전원 사이클로 풀린다**는 것이 관측됐다~~
  ~~(`docs/homing/2026-08-03-can-relay-homing-assets.md` §9-3).~~
  > ❌ **정정 2026-08-03 15:40: 「전원 사이클로 풀린다」는 폐기**됐다 —
  > 07-27 성공 캡처에서 전원 사이클 없이 `0x6064`=0 이 **3,115회** 나타났다(§11-2).

### 6-C. 2026-08-03 에 **새로 열린** 것

- **왜 `0x607A`(Seer 조향 목표)를 0° 근거로 쓰지 않았는가.** 구값의 출처가 `0x607A` 였다는
  사실이 08-03 에야 확인됐다. Seer 가 매 주기 내는 목표값은 별도 실측 없이도 얻을 수 있는
  0° 근거인데 4주간 쓰이지 않았다 — 관측 자산 정리 필요.
  > ⚠ **15:40 유보:** `0x607A` 는 **출처**로는 유효하나 **0° 근거로는 불충분**하다 —
  > 180 s 캡처에서 Seer 는 `7,882,020 / 7,859,062` 를 **97.8%** 지령했고
  > 다른 로그에선 `13,040,392`(+90° crab)도 낸다. **어느 순간이 직진인지는 외부 정보 없이 결정되지 않는다.**
- ~~**호밍 실행 자체는 여전히 미해결**이다(2026-08-03 호밍 1회차 `ERR_TIMEOUT`, 드라이브가~~
  ~~`Switch on disabled` 를 벗어나지 못함). 홈 **값**이 확정된 것과 호밍 **동작**이 되는 것은~~
  ~~별개다 — `docs/homing/2026-08-03-can-relay-homing-assets.md` §8 참조.~~
  > ❌ **정정 2026-08-03 15:40: 전부 폐기.** ① 호밍은 **10/10 성공**한다(6-A′).
  > ② 「`Switch on disabled` 를 벗어나지 못해 실패」라는 진단은 **반증**됐다 —
  > 07-27 성공 런이 **같은 statusword** 에서 성공했다(§11-2). §8 은 인용 금지.

</details>

<details><summary>원문 (보존)</summary>

- **「Seer 의 0°」와 물리적 직진이 같은지는 육안 미확인이다.** Seer 도 자기
  캘리브레이션을 거친 값을 준다. 조향 중립 산출은 **사용자가 별도 진행**(2026-08-02 지시).
  > ❌ **재정정 2026-08-03 17:30: 「육안 미확인」은 더 이상 사실이 아니다.**
  > 사용자가 **바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인**했다
  > (2026-08-02 · 2026-08-03, `can_relay` GUI 표시로도 동일 확인). Seer 값이
  > **자기 캘리브레이션(EasyDRIVE `steerOffset`)을 거친 값**이라는 서술은 맞지만,
  > 그것이 **교정된** 조향각이라는 뜻이므로 앵커를 무효화하지 않는다.
  > ⇒ **물리 직진 앵커는 실재하며**, 한계는 **정밀도(육안 ≈±1°)** 뿐이다(6-B′ 재정정).
- ❌ node4 의 0° 역산이 세 측정 모두 정본값보다 **+191c(0.0033°)** 로 나온다. 같은 방향으로
  세 번 나왔으므로 잡음이 아니라 계통 편차일 수 있다(**미판정**). 거동 영향은 없다.
- 2026-08-02 19:42 캡처 첫 ~5초에 조향이 실제로 **+12.9° 움직인 구간**이 있다
  (node3 7,126,954 → 7,868,999). 교차검증 도구는 송신하지 않으므로 우리가 아니다 —
  **움직인 주체 미확인**.

</details>

## 7. ⚠ debt id 주의 (문서를 따라갈 때)

홈 상수 부채의 id 가 **계보마다 다르다** — 참조를 따라가다 엉뚱한 항목이 나오면 이 때문이다.

| 부채 | origin/main | 로컬 main 계보 |
| --- | --- | --- |
| 홈 상수 하드코딩 계승 | **debt-026** | debt-016 |
| can_relay 패키지명 충돌 | **debt-025** | debt-015 |
| 구동축 브링업 미검증 | **debt-027** | debt-017 |

`origin/main` 이 선점(먼저 push)이므로 **origin 번호가 정본**이다.
