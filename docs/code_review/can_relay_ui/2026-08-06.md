# can_relay/ui — 2026-08-06 델타: 기체 선택(machine)·주행 스케일 YAML화 (사후 표 갱신)

> **성격**: 2026-08-04 최초 인벤토리에 대한 **함수·전역 표 델타 갱신** — 전면 재리뷰 아님,
> severity 재평가 없음. 작성 세션: LGIT 개발 PC sess:02319e57 (원격 이식 작업).
>
> ⚠ **경위 정직 고지 — 08-04 와 동일 위반의 재발.** 이 변경도 coding SOP §2(계획 전 함수표
> read/생성)·§6(작업 후 표 갱신)를 건너뛰고 코드부터 썼고, 사용자 재지적(2026-08-06 「코딩
> 규칙에 따라서 코드 분석후에 함수 변수 테이블을 만들고 읽고 수정하고 다시 업데이트 하나요?」)
> 으로 **사후 작성**한다. mistake: `docs/claude-mistake/2026-08-06-003`.

## 코드 버전

| 파일 | md5(앞 8) | 줄수 | 비고 |
| --- | --- | --- | --- |
| `ui/backend_direct.py` | `f2910261` | 520 | ⚠ 08-04(`85d0da1c`/401줄) 이후 **타 세션 WIP 변경이 섞여 있음** — 본 문서는 이 세션 변경분만 기술 |
| `Tools/can_relay_gui/can_relay_gui.py` | `53f4c617` | 48 | 신규 (Tools 독립 런처) |
| `config/machine/lgit_moma_qd.yaml` | `8d484729` | 49 | 신규 (QD 기체 — `steer_home_counts` 미실측 빈 값) |

변경 근거: 사용자 지시 2026-08-06 「Tools 폴더에 can relay gui 도 이식해서 주행 설정에 맞도록」
「Seer API 조향값을 기준으로」. Big-AMR 거동 무변경(기본 기체 foil_a082 경로 동일값 검증).

## 함수표 델타 (08-04 §3 전수 115개 기준)

| # | 함수 | 입력 | 출력 | 기능 | 위치 |
| --- | --- | --- | --- | --- | --- |
| 신규 | `_machine_yaml_path` | — | `str` | env `CAN_RELAY_MACHINE`(기본 foil_a082) 기반 machine YAML 경로 | backend_direct.py:78 |
| 신규 | `_load_drive_scale` | — | `(float,float,int,float,str)` | 주행 스케일 4종(counts/°·units/mmps·상한·조향한계) 정본 YAML 로드. 기본기체 실패 시 코드 사본 폴백, **비기본 기체 실패 시 SystemExit**(fail-loud) | backend_direct.py:120 |
| 변경 | `_load_steer_home` | — | `(dict,str)` | ① 경로가 고정 foil → `_machine_yaml_path()` ② `steer_home_counts` 빈 값 검출 시 ValueError(Seer 크로스체크 안내) ③ 비기본 기체 실패 시 SystemExit — foil 사본 오적용 차단 | backend_direct.py:83 |

`Tools/can_relay_gui/can_relay_gui.py`: 함수 0 (모듈 스크립트) — `--machine` pre-parse → env 설정
→ sys.path 주입 → `can_relay.ui.gui_node.main` 위임. UI 복제 없음.

## 전역 변수 델타 (08-04 §4 G1~G14 기준)

| 이름 | 위치 | 성격 |
| --- | --- | --- |
| `_MACHINE` (신규) | backend_direct.py:74 | env `CAN_RELAY_MACHINE`, 기본 `"foil_a082"` — import 시 1회 고정 |
| `_MACHINE_IS_DEFAULT` (신규) | :75 | 폴백 허용 여부 (기본기체만 true) |
| `COUNTS_PER_DEG`·`VEL_PER_MMPS`·`VEL_MAX_UNITS`·`STEER_LIMIT_DEG` (의미 변경) | :47-50(사본) → :150-151(오버라이드) | 리터럴 확정값 → **코드 사본(폴백)**, 정본은 machine YAML — `_load_drive_scale()` 이 덮음 |
| `DRIVE_SCALE_SOURCE` (신규) | :151 | 스케일 출처 문자열 (`STEER_HOME_SOURCE` 와 동형) |

## 검증 (스모크, 로컬 amap-1 + 원격 orin 동일 결과)

1. 기본(foil): 스케일 `57344.0/24.447/4889/90.0`·홈 `{3:7871815, 4:7840086}` — **변경 전과 동일**
2. `--machine lgit_moma_qd`(홈 미실측): SystemExit + Seer 크로스체크 안내 메시지 — 의도된 기동 거부
3. QD 임시홈 주입 테스트: 스케일 `48332.8/23.8732/4775/90.0` 적용·`steer_counts` QD 기울기 반영 확인 후 원복
4. 존재하지 않는 기체명: SystemExit (FileNotFoundError 명시)

Verdict: 본 문서는 표 갱신 델타 — 리뷰 판정 없음 (never-self-approve, 판정은 외부 리뷰 몫).
