# line_vision 인벤토리 (계획 단계 — 신규 파일)

> 루트 정본. 패키지 병기본: `src/AI/line_vision/docs/code_review/ai-line-vision/2026-08-13.md`
> 구조·설계: `docs/sw_structure/ai-line-vision/2026-08-13.md`
> ADR: `docs/adr/2026-08-13-line-following-ai-port.md`

coding.md §2 「신규 파일 → 설계할 함수·전역변수를 계획 단계에서 표로 작성」에 따른
**사전 인벤토리**다. 결함 평가(severity)는 포함하지 않는다 — 구현 후 별도 리뷰 패스 소관.

## 목적

바닥 라인을 YOLOv8-seg 로 분할하고 중심선을 피팅해 횡오차·기울기를 `/line/error`
(`ai_msgs/LineError`)로 발행한다. 주행 제어는 하지 않는다.

## 의존성

| 대상 | 의존 | 비고 |
| --- | --- | --- |
| `line_vision` | `rclpy` · `sensor_msgs` · `ai_msgs` · `cv2` · `numpy` · `ultralytics` | 신규 설치 0 — `yolo_detector` 와 동일 버전 |
| `centerline.py` | `cv2` · `numpy` 뿐 | ROS·ultralytics 무의존 (단위 테스트 대상) |
| 카메라 | `usb_cam_publisher` 토픽 구독만 | 장치 직접 open 금지 (카메라 HAL 규약) |

## 함수표

### `centerline.py` (공개)

| 함수 | 인자 | 반환 | 책임 | 부수효과 | 위치 |
| --- | --- | --- | --- | --- | --- |
| `CenterLine` | (dataclass) | — | 직선 표현: `valid`·`vx`·`vy`·`x0`·`y0`·`p1`·`p2` | 없음 | centerline.py:21-31 |
| `fit_centerline` | `mask: np.ndarray(uint8)` | `CenterLine` | 스캔라인별 전경 무게중심 → `cv2.fitLine(DIST_L2)` 직선 피팅. 주방향은 bbox 종횡비로 판정 | 없음 | centerline.py:33-90 |
| `line_error` | `line`·`img_w`·`img_h`·`control_row_ratio` | `(offset, angle)` | 기준행 정규화 횡오차 `[-1,1]` 과 수직 기준 기울기[rad] | 없음 | centerline.py:93-112 |

### `line_seg_node.py`

| 함수/메서드 | 책임 | 부수효과 | 위치 |
| --- | --- | --- | --- |
| `sensor_qos` | 발행자와 호환되는 BEST_EFFORT/KEEP_LAST QoS 생성 | 없음 | line_seg_node.py:43-50 |
| `camera_for_direction` | `direction`·전후 카메라 이름 → 구독할 카메라 논리명 (순수) | 없음 | line_seg_node.py:53-64 |
| `LineSegNode.__init__` | 파라미터 선언·모델 지연 로드·발행자 2개·구독 1개 | GPU 모델 적재 | line_seg_node.py:70-106 |
| `LineSegNode._subscribe_camera` | 현재 방향의 카메라로 구독 (재)생성 | 기존 구독 파기 | line_seg_node.py:108-124 |
| `LineSegNode._on_param_change` | `direction` 변경 콜백 → 구독 전환 | 구독 교체 | line_seg_node.py:126-135 |
| `LineSegNode._decode` | CompressedImage → BGR ndarray (+`flip_180`) | 없음 | line_seg_node.py:137-149 |
| `LineSegNode._infer_best_mask` | 최고 신뢰도 마스크를 원본 해상도로 확대 | GPU 추론 | line_seg_node.py:151-163 |
| `LineSegNode._on_image` | 디코드 → 추론 → 중심선 → `LineError` 발행 | 토픽 발행 | line_seg_node.py:165-193 |
| `LineSegNode._publish_debug_image` | 마스크·중심선·제어점 오버레이 발행 | 토픽 발행 | line_seg_node.py:195-226 |
| `main` | 노드 수명주기 | rclpy init/shutdown | line_seg_node.py:228-238 |

### `line_seg.launch.py`

| 함수 | 책임 | 부수효과 | 위치 |
| --- | --- | --- | --- |
| `_find_shared_config` | 공용 로스터 경로 탐색 (`CAMERA_CONFIG` → 상위 디렉터리 탐색) | 없음 | line_seg.launch.py:27-41 |
| `_roster_names` | 로스터에 등재된 카메라 이름 집합 | 파일 읽기 | line_seg.launch.py:44-51 |
| `_camera` | 로스터에 있으면 preferred, 없으면 fallback | 없음 | line_seg.launch.py:54-56 |
| `generate_launch_description` | 런치 인자 선언 + 노드 1개 구성 | 없음 | line_seg.launch.py:59-91 |

### 패키징 파일 (함수 없음 — 등재만)

| 파일 | 내용 | 위치 |
| --- | --- | --- |
| `test_centerline.py` | `centerline.py` 단위테스트 — 부호 규약·주방향 판정·경계 케이스 | test_centerline.py:1-123 |
| `line_seg_params.yaml` | 노드 파라미터 기본값(가중치 경로·임계·방향·카메라·전송·flip) | line_seg_params.yaml:1-24 |
| `setup.py` | ament_python 패키징. `data_files` 로 `launch/line_seg.launch.py`·`config/line_seg_params.yaml` 설치, 진입점 `line_seg = line_vision.line_seg_node:main` | setup.py:1-26 |
| `__init__.py` | 빈 파일 — 파이썬 패키지 표식뿐. re-export 없음 | __init__.py:1 |

## 전역변수·상수표

| 이름 | 값 | 위치 | 근거 |
| --- | --- | --- | --- |
| `_EPS` | 1e-6 | centerline.py:17 | 0 나눗셈 방어 (원본 `kEps` 동일) |
| `DEFAULT_MODEL_PATH` | `/home/nvidia/models/line_seg_v1.pt` | line_seg_node.py:29 | `yolo_detector` 의 `/home/nvidia/models/` 관례 |
| `DEFAULT_FORWARD_CAMERA` | `cam_f` | line_seg_node.py:32 | 로스터 전면 카메라 논리명 |
| `DEFAULT_REVERSE_CAMERA` | `cam_r` | line_seg_node.py:33 | 로스터 후면 카메라 논리명 |
| `SUBSCRIPTION_DEPTH` | 1 | line_seg_node.py:35 | 최신 프레임만 사용 — 밀린 프레임은 추론 지연만 늘린다 |
| `_MASK_COLOR` | `(0,255,255)` | line_seg_node.py:38 | 디버그 오버레이 마스크 색 (BGR) |
| `_LINE_COLOR` | `(0,255,0)` | line_seg_node.py:39 | 디버그 오버레이 중심선 색 (BGR) |
| `_POINT_COLOR` | `(0,0,255)` | line_seg_node.py:40 | 디버그 오버레이 제어점 색 (BGR) |

가변 전역은 없다 — 상태는 전부 `LineSegNode` 인스턴스 속성이다.

## 중복 확인 (coding.md §2)

| 후보 | 기존 유사물 | 판정 |
| --- | --- | --- |
| 이미지 구독·디코드 | `yolo_detector/detector_node.py`, `dataset_collector/collector_node.py` | 같은 **규약**을 따르되 코드 공유 안 함 — 세 노드가 각자 콜백 형태가 달라 추출 이득이 없다 |
| 데이터 수집 | `dataset_collector` | 원본 `data_collector` 는 **이식하지 않는다**(기존 것이 상위 호환) |
| 카메라 퍼블리셔 | `usb_cam_publisher` | 원본 `tr_camera` **이식하지 않는다**(HAL 우회·장치 이중 open) |
| `LineError` 메시지 | `ai_msgs` | 신규 인터페이스 패키지 만들지 않고 `ai_msgs` 에 추가 |

## 검증 (구현 직후)

| 항목 | 결과 |
| --- | --- |
| 단위 테스트 | **17 passed** (`pytest src/AI/line_vision/test/test_centerline.py`) |
| colcon 빌드 | `ai_msgs` · `line_vision` 2패키지 Finished, 오류 0 |
| `LineError` 인터페이스 | `ros2 interface show ai_msgs/msg/LineError` — 6필드 정상 노출 |
| 런치 로스터 유도 | 로스터 6대(`cam_f`·`cam_lf`·`cam_lr`·`cam_r`·`cam_rf`·`cam_rr`) 발견, forward→`cam_f` / reverse→`cam_r` |
| 합성 프레임 end-to-end (ROS_DOMAIN_ID=77) | 발행 140 → `/line/error` 104, `detected=True` 104·`False` 0, conf 최대 0.95 |
| offset 부호 | 합성 라인 x=340→`-0.470`, x=925→`+0.445` (기대 −0.469/+0.445) — **부호·정규화 일치** |
| 방향 전환 | `ros2 param set direction reverse` 후 `cam_r` 수신 59건·`camera` 필드 `cam_r`, 같은 시각 `cam_f` 입력은 **0건**(구 구독 정상 파기) |

**미검증**: 실카메라 영상·실라인·실주행. 합성 프레임은 배선과 부호 규약만 증명한다.
`flip_180` 의 실제 필요 여부(장착 자세)는 실기 확인 전까지 알 수 없다.

**최종 verdict 미확정** — coding.md §5 헌법(never-self-approve)에 따라 저자가 `✅` 를 찍을 수
없다. `code_review` 패스 또는 사람 리뷰가 필요하다.
