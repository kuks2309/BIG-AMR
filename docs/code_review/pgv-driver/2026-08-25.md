# pgv_driver 인벤토리 등재 (angle_offset_deg 작업용)

> 권위본은 [pgv_driver/docs/function_table.md](../../../src/Sensors/Positioning/pgv_ros2/pgv_driver/docs/function_table.md) —
> 본 문서는 게이트 인식 경로(docs/code_review/**)로의 등재 사본이며, 아래 표는 권위본과 동일 내용이다.

## 함수표 — pgv_driver_node

| 함수 | 시그니처 | 용도 | 위치 |
| --- | --- | --- | --- |
| `PgvDriver::PgvDriver` | `PgvDriver()` | 파라미터 선언·시리얼 개방·pub/srv/timer 구성 | pgv_driver_node.cpp:39 |
| `PgvDriver::serialOpen` | `bool serialOpen()` | termios 8-E-1 개방(보레이트 매핑, 미지원 값 거부) | pgv_driver_node.cpp:95 |
| `PgvDriver::transact` | `bool transact(const uint8_t *req, size_t req_len, uint8_t *rsp, size_t rsp_len)` | 입력 flush → 요청 write → 마감시한 내 수신(mutex 직렬화), 실패 시 fd 폐기·재개방 | pgv_driver_node.cpp:146 |
| `PgvDriver::pollTimer` | `void pollTimer()` | 위치 조회 폴링 → 파싱·주소 대조 → `pgv/position` 발행 | pgv_driver_node.cpp:197 |
| `PgvDriver::toMsg` | `pgv_interfaces::msg::PgvPosition toMsg(const pgv_protocol::PositionFrame &f)` | 프레임 → 메시지(해상도 스케일 적용, ERR 시 위치 무효) | pgv_driver_node.cpp:230 |
| `PgvDriver::onSetDirection` | `void onSetDirection(req, rsp)` | `pgv/set_direction` 서비스 | pgv_driver_node.cpp:269 |
| `PgvDriver::onSetColor` | `void onSetColor(req, rsp)` | `pgv/set_color` 서비스 | pgv_driver_node.cpp:299 |
| `main` | `int main(int argc, char **argv)` | rclcpp 스핀 | pgv_driver_node.cpp:347 |

## 전역변수표 (파라미터 포함, 발췌)

| 이름 | 타입 | 값/기본 | 위치 | 누가 바꾸나 |
| --- | --- | --- | --- | --- |
| `kBaudMap` | `const std::map<int, speed_t>` | 38400/57600/115200/230400 | pgv_driver_node.cpp:28 | 불변 |
| (파라미터) `position_resolution_mm` | double | 0.1 | pgv_driver_node.cpp:48 | 런치/CLI — 장치 설정 일치 필수 |
| (파라미터) `angle_resolution_deg` | double | 0.1 | pgv_driver_node.cpp:49 | 런치/CLI |
| (파라미터) `angle_offset_deg` | double | 0.0 | pgv_driver_node.cpp:50 | 런치/CLI — 설치 고정각(예: 45.3°)을 0° 로 정렬, angle_raw 미적용 (이번 작업 추가분) |
| (파라미터) `frame_rotation_deg` | double | 0.0 | pgv_driver_node.cpp:54 | 런치/CLI — 장치 축의 로봇 프레임 내 yaw(운용 −90). 발행 x/y 를 REP-103 으로 회전, raw 미적용 (이번 작업 추가분) |
| (파라미터) `frame_id` | string | `pgv_link` | pgv_driver_node.cpp | 런치/CLI |
