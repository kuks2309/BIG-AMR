# translate_forward/reverse 액션 서버 함수표 (압축판)

> 대상: `src/Control/Motion_Control/2WS/trnav_2ws_action_server/src/translate_forward/translate_forward_action_server.cpp`
> (+ 대칭 구조의 `translate_reverse/translate_reverse_action_server.cpp`).
> 패키지 로컬 권위본(`…/trnav_2ws_action_server/docs/function_table.md`)이 "translate 는
> 미작성"으로 백로그 처리한 파일 — 본 표는 2026-08-28 TransientGuard 불감대 배관
> 수정에 필요한 인터페이스 수준만 담는 압축판이다. 전체 정밀표는 백로그 유지.

## 함수표 — translate_forward_action_server.cpp

| 함수/블록 | 위치 | 용도 |
| --- | --- | --- |
| 파라미터 적재(생성자부) | translate_forward_action_server.cpp:52-130 | 기하·PathController(Stanley) 게인·TransientGuard(vy/ω 레이트, gate 3°, err_max 10°, **deadband 신설 배관**)·LocalizationMonitor·execute 손잡이 safeParam 적재. 런타임 콜백은 K_stanley 등 일부만 live-set 허용(범위검사) |
| `onGoal` 수락 검사 | translate_forward_action_server.cpp:250-280 | max_linear_speed>0 등 goal 유효성, dist 산출 |
| execute 제어 루프 | translate_forward_action_server.cpp:494-871 | 사다리꼴 프로파일(exit_speed 지원) → projection 도달 판정(coarse/fine 분기) → Stanley 조향 → 조향 레이트 제한 → 조향수렴 스케일(3~30°) → TransientGuard drive_scale → 바퀴 지령 발행 |
| 도달·종료부 | translate_forward_action_server.cpp:873-935 | exit_speed≤0 만 정지 지령, Phase 4 조향 복귀(hold_steer=False 시, v=0 반복 발행), finish_success/abort |

## 함수표 — translate_reverse_action_server.cpp (전진과 대칭 구조)

| 함수/블록 | 위치 | 용도 |
| --- | --- | --- |
| 파라미터 적재(생성자부) | translate_reverse_action_server.cpp:60-120 | 전진과 동일 접두(`transient_*`·`translate_*`) 파라미터 적재 — TransientGuard deadband 배관 동반 |
| execute 제어 루프·종료부 | translate_reverse_action_server.cpp:200-900 | 후진 방향 프로파일·Stanley·가드 체인(전진과 대칭) |

## 전역/상수

멤버-파라미터 저장 위주(모듈 전역 0). TransientGuard 주입 파라미터 접두
`transient_*`, Stanley 접두 `translate_*` — 정본 값은 config/translate_*_params.yaml.
