# seer_tcp_ip 인벤토리 (2026-08-23 · C++ 재구현)

> **이 문서는 인벤토리(코딩 SOP §2 선행 산출물)다 — severity 평가·리뷰 verdict 는 담지 않는다.**
> 결함 평가는 별도 code_review 패스 소관이며, 저자는 자기 코드에 verdict 를 찍지 않는다.
>
> 대상: `src/Comm/seer_tcp_ip/` (ament_cmake, C++17)
> 정본은 이 파일(모듈 로컬), 루트 집계는 `docs/code_review/seer_tcp_ip/2026-08-23.md`.
> 직전 판(Python): `2026-08-07.md` — 설계는 그대로이고 **언어만** 바뀌었다.
> ADR: `2026-08-18-seer-tcp-ip-cpp-rewrite.md`(언어) · `2026-08-07-seer-api-tcp-hal.md`(배치·정책)

## 0. 편호·요청 형태의 근거 (코드 주석에 두지 않는다)

주석은 현재 동작만 담는다 — 어디서 알았는지는 여기 적는다.

| 근거 | 무엇을 주는가 |
|---|---|
| `References/Seer-Driver/robokit_tcp_api.md` 편호 표 | 편호와 뜻. **4005·4006·1060·1040·1302·1500·3066·4010·6004 가 없다** |
| `T-Robot_seer_gui/seer_core/client.py` (로컬 485줄) | 위 편호의 **요청 JSON 형태까지**. 문서에 없는 편호의 정본. 재측위 상태값(1·2·3)도 여기 |
| lgit `LGIT_C6_MoMa/src/Comm/seer_api_comm` | 같은 로봇을 쓰는 C++ 클라이언트(8편호). **「2002 를 보냈다 ≠ 성공」** 규칙의 출처 |
| 실기(192.168.44.82) 조회 | 응답 키·한도·1302 확장자 규칙 — §6 |

**근거가 없는 편호는 감싸지 않는다.** 3055/3056 은 필드명이 없어 저수준 `Json` 만 받고,
2022 는 문서(스캔 시작)와 참조 클라이언트(지도 전환)가 어긋나 **감싸지 않았다**(debt-110).

⚠ **`openLoopMove` 의 `durationMs` 는 dead-man 타이머다.** 그 시간 안에 새 지령이 없으면 로봇이
스스로 선다(`0` = 무한). 기본값을 두지 않아 호출자가 정지 시간을 반드시 고르게 한다.

⚠ **재측위는 「보냈다」가 성공이 아니다.** 성공은 1021 의 `reloc_status == 1` 이며, 상태 3 은
계산 완료·**미확정**이라 2003 이 필요하다. 상태 3 은 Robokit 3.4.6.1800 미만에서만 나오는데
**이 기체(rbk 3.4.5.22)가 거기 해당한다.**

## 1. 목적

Seer(SRC) 컨트롤러와의 Robokit TCP/IP API 통신을 단일 지점으로 모은다. `rclcpp` 에 의존하지 않는
순수 C++17 라이브러리라 노드·단독 실행파일·시험이 같은 코드를 쓴다.

## 2. 함수표

### `ports.hpp` / `ports.cpp` — 포트 정책

| 위치 | 심볼 | 용도 | 비고 |
|---|---|---|---|
| `ports.hpp:20-27` | `kRobod`·`kState`·`kCtrl`·`kTask`·`kConfig`·`kKernel`·`kOther`·`kPush` | 포트 상수 | 공식 SDK 이름 유지 |
| `ports.hpp:30` | `kMinRequestIntervalMs` = 100 | 요청 간 최소 간격 | 과빈번 시 로봇이 연결 정리 |
| `ports.hpp:33` | `kResponseTypeOffset` = 10000 | 응답 편호 오프셋 | 오류 응답은 이 규칙을 안 따른다 |
| `ports.hpp:37` | `kConnectionLimitRetCode` = 61001 | 한도 초과 코드 | 거부 응답 편호 = 포트 번호 |
| `ports.hpp:40` | `kControlPreemptedRetCode` = 40020 | 제어권 없음 코드 | 4005 를 먼저 잡으라는 신호 |
| `ports.hpp:43` / `ports.cpp:8-20` | `maxConnectionParam()` | 포트 → 한도 파라미터 이름 | API 1400 조회용 |
| `ports.hpp:46` / `ports.cpp:22-28` | `observedMaxConnections()` | 관측 한도 | **정본 아님** — 판정은 `getMaxConnections()` |
| `ports.hpp:52` / `ports.cpp:30-40` | `guardedPorts()` | 직결 차단 포트 집합 | **명시 집합** — 한도에서 파생하면 비어 게이트가 사라진다 |
| `ports.hpp:55` / `ports.cpp:42-45` | `isGuarded` | 지령·설정 포트 판정 | 근거는 연결 수가 아니라 **중재** |
| `ports.hpp:58` / `ports.cpp:47-51` | `observedMaxConnectionsFor` | 관측 한도 조회 | 미관측 포트는 −1 |

### `transport.hpp` / `transport.cpp` — 전송 계층

| 위치 | 심볼 | 용도 | 예외 |
|---|---|---|---|
| `transport.hpp:33-62` | `ProtocolError`·`GuardedPortError`·`ConnectionLimitError`·`TransportError` | 오류 4종 | `ConnectionLimitError` 는 `ProtocolError` 의 하위 — 기존 호출자를 안 깬다 |
| `transport.hpp:64-66` | `kSync`·`kVersion`·`kHeadLen` | 16B 헤더 상수 | — |
| `transport.hpp:70,73` / `transport.cpp:130-160` | `pack` | 요청 프레임 생성 | 빈 객체는 본문 없이 length=0 |
| `transport.hpp:83` / `transport.cpp:162-181` | `unpackHead` | 응답 헤더 해석 | `ProtocolError`(길이·sync) |
| `transport.hpp:86-97` | `IByteStream` | 바이트 스트림 추상 | 시험이 소켓 없이 전 계층을 지난다 |
| `transport.hpp:99` / `transport.cpp:50-125` | `makeTcpStream` | POSIX TCP 구현 | `TransportError` |
| `transport.hpp:108-` | `Transport` | 포트 1개 1문1답 연결 | 스레드 안전 아님 |
| `transport.cpp:200-212` | `.connect` | lazy 연결 | **실패 시 스트림을 남기지 않는다** — 남기면 다음 요청이 죽은 서술자에 send 한다 |
| `transport.cpp:214-222` | `.close` | 소켓 해제 | — |
| `transport.hpp:121-122` / `transport.cpp:316-346` | `.request` | 편호 요청 → `Json` | 위 4종 |
| `transport.hpp:125` / `transport.cpp:309-314` | `.requestRaw` | 편호 요청 → (bytes, 편호) | 맵 본문 무결성용 |
| `transport.cpp:229-241` | `.throttle` | 최소 간격 대기 | — |
| `transport.cpp:243-257` | `.recvExact` | 정확히 n 바이트 | `TransportError`(조기 종료) |
| `transport.cpp:259-280` | `.raiseConnectionLimitIfThat` | 한도 거부 서명 판별 | `ConnectionLimitError` |

### `api.hpp` / `api.cpp` — 편호 바인딩 (50편호)

| 위치 | 심볼 | 용도 |
|---|---|---|
| `api.hpp:22-79` | `api::k*` | 편호 상수 50종 |
| `api.hpp:84-` | `SeerApi` | 포트별 연결 보유 + 이름 호출 |
| `api.cpp:38-58` | `.transport` | 포트 전송 획득 — **게이트 강제 지점** |
| `api.cpp:71-88` | `.raiseOnRetCode` | `ret_code`≠0 → 예외. 비객체 응답 가드 |
| `api.cpp:100-121` | 조회 22종 | 1000·1002·1003·1004·1005·1006·1007·1008·1010·1011·1012·1013·1021·1022·1060·1100·1101·1102·1111·1300·1500 |
| `api.cpp:123-137` | `.getLasers`·`.getSlamStatus`·`.getMotorInfo`·`.getStations` | 배열/바디 있는 조회 |
| `api.cpp:139-156` | `.getParam`·`.getMaxConnections` | 1400 — **한도는 상수가 아니라 로봇에 묻는다** |
| `api.cpp:158-196` | `.getMapMd5` | 1302 — `.smap` 자동 부착, 반환 키는 호출자 형태, **all-or-nothing** |
| `api.cpp:199-227` | `.downloadMap`·`.raiseIfErrorPayload` | 4011 — 원문 바이트. 무결성 대조는 호출자 몫 |
| `api.cpp:229-236` | `.seizeControl`·`.releaseControl` | **4005/4006** — 설정 포트(게이트 대상) |
| `api.cpp:238-249` | `.setMode`·`.setParams`·`.restoreFactoryParams`·`.clearFatal`·`.uploadMap` | 4000·4100·4101·4102·4300·4010 |
| `api.cpp:252-275` | `.stop`·`.openLoopMove`·`.calibrateGyro`·`.relocate`·`.relocateWith`·`.confirmLocation` | 2000~2010 |
| `api.cpp:278-300` | `.goTarget`·`.goTargetList`·`.goPoint`·`.patrol`·`.translate`·`.turn`·`.pauseTask`·`.resumeTask`·`.cancelTask` | 3001~3066 |
| `api.cpp:288-302` | `.softEstop`·`.speaker`·`.setDo` | 6000~6004 |

### `control.hpp` / `control.cpp` — 제어권·dead-man·재측위 (동작)

편호 래퍼가 아니라 **동작**이라 별 파일이다. **스레드를 만들지 않는다** — 호출자가 `tick()` 을 부른다.

| 위치 | 심볼 | 용도 | 예외 |
|---|---|---|---|
| `control.hpp:23-27` | `ControlError` | 제어권 상태 불일치 | — |
| `control.hpp:36-` | `ControlSession` | 획득→사용→정지→반납 RAII | — |
| `control.cpp:34-47` | 생성자 | `nickName` 필수(빈 값 거부) + 즉시 획득 | `std::invalid_argument` |
| `control.cpp:49-59` | 소멸자 | 반납. **던지지 않는다** | — |
| `control.cpp:61-78` | `.acquire` | 소유자 기록 후 4005 | `ControlError`(이중 획득) |
| `control.cpp:80-101` | `.release` | 정지 후 4006. **정지 실패에도 반납 시도** | 정지 예외 전파 |
| `control.hpp:73-` | `JogKeepalive` | dead-man 주기 재송신 | — |
| `control.cpp:103-125` | 생성자 | **`interval < duration` 불변식 검증** | `std::invalid_argument` |
| `control.cpp:138-165` | `.setVelocity`·`.due`·`.tick` | 다음 tick 반영 / 시점 판정 / 1회 송신 | — |
| `control.cpp:167-173` | `.stop` | 2000 + 속도 0 + 스케줄 리셋 | — |
| `control.hpp:111-121` | `reloc::kSuccess·kRelocing·kCompleted` | 1·2·3 | — |
| `control.hpp:133` / `control.cpp:175-224` | `relocateAndConfirm` | 2002 → 1021 폴링 → 필요 시 2003 | `ControlError`(미확정) |
| `control.hpp:139` / `control.cpp:226-230` | `preemptedByControl` | 40020 판별 | — |
| `control.hpp:142` / `control.cpp:232-240` | `describeOwner` | 1060 한 줄 요약 | — |

### `tools/` — 실행파일 2종 (조회 포트만 쓴다)

| 파일 | 용도 |
|---|---|
| `tools/read_lidar_install.cpp` | 라이다 장착 pose 조회 → `LIDARS` 에 붙일 텍스트 출력 |
| `tools/seer_param.cpp` | 파라미터 조회. 인자 없으면 포트 한도 6종 |
| `tools/control_probe.cpp` | **제어권 경로 실기 확인** — 1060→4005→1060→2000→4006→1060. 로봇을 움직이지 않는다. 안전 가드 2개(타 소유자 보유·주행 중)는 `--force` 로만 우회 |
| `tools/param_probe.cpp` | **설정 쓰기 경로 실기 확인** — 1400→4100→1400→4100 원복→1400. 로봇을 움직이지 않는다. 쓰기는 **허용 목록**(`NetProtocol.RobotNote`) 밖이면 거부하고, 쓰기 뒤 실패하면 원래 값을 출력한다. 종료 코드로 실패 지점을 구분(2 목록 밖·3 비문자열·4 미반영·5 원복 실패) |

### `test/` — 자체 CHECK 하니스 (gtest 미도입)

| 파일 | 내용 |
|---|---|
| `test/harness.hpp` | `CHECK`·`CHECK_EQ`·`CHECK_THROWS_MSG`·`CHECK_NOTHROW`. **`assert` 를 쓰지 않는다** — 기본 빌드가 Release(`-DNDEBUG`)라 `assert` 는 사라진다 |
| `test/harness_selftest.cpp` | **매크로가 실패를 실제로 검출하는지 시험한다.** 이것이 통과하지 않으면 나머지 통과 숫자는 의미가 없다 |
| `test/fake_stream.hpp` | 바이트 스트림 대역. `read` 를 조각내 부분 수신 경로를 반드시 지나게 한다 |
| `test/test_transport.cpp` | 44건 — 프레임·헤더·부분수신·대조·스로틀·한도거부·연결수명 |
| `test/test_api.cpp` | 178건 — 편호 리터럴 고정·**포트 배선**·게이트·1302·ret_code 경계·**4102 전체 초기화 가드**·**3055/3056 부호 규약** |
| `test/test_control.cpp` | 36건 — 제어권 짝·dead-man 불변식·**재측위 확정** |

⚠ **시험 `Rig` 의 멤버 선언 순서가 계약이다** — 멤버는 선언의 역순으로 파괴되므로 `api` 를
마지막에 둬야 `SeerApi` 소멸자가 살아 있는 스트림을 본다. 반대로 두면 세그폴트다(실제로 밟았다).

## 3. 전역변수표

가변 전역 **0개**. 모듈 수준 이름은 전부 `inline constexpr` 상수이거나 함수 지역 `static const`
(`ports.cpp` 의 3개 테이블 — 최초 호출 시 1회 생성되고 이후 불변).

| 이름 | 위치 | 가변 | 누가 바꾸나 |
|---|---|---|---|
| `ports::k*` (포트·코드·간격) | `ports.hpp:20-40` | 불변 | 아무도 |
| `maxConnectionParam`·`observedMaxConnections`·`guardedPorts` 의 `static const` | `ports.cpp:10,24,32` | 불변 | 아무도 |
| `kSync`·`kVersion`·`kHeadLen` | `transport.hpp:64-66` | 불변 | 아무도 |
| `api::k*` (편호 50종) | `api.hpp:22-79` | 불변 | 아무도 |
| `reloc::k*` | `control.hpp:114-119` | 불변 | 아무도 |

⚠ `observedMaxConnections` 는 **코드에서는 불변이지만 로봇에서는 가변**이다(런타임 파라미터
1~20). 상수를 진실로 쓰지 말고 판정이 필요하면 `SeerApi::getMaxConnections()` 로 묻는다.

인스턴스 상태(전역 아님): `Transport::{stream_, seq_, lastRequestAt_}` ·
`SeerApi::transports_` · `ControlSession::{held_, previousOwner_}` ·
`JogKeepalive::{vx_,vy_,w_,lastSentAt_,sentCount_}`.

## 4. 의존성

| Tier | 대상 | 버전/제약 | 부재 시 동작 | 근거 |
|---|---|---|---|---|
| 빌드 | `ament_cmake` | — | 빌드 불가 | `package.xml:22` |
| 빌드 | `nlohmann_json` | 헤더 온리, MIT | 빌드 불가 | `package.xml:23`, `CMakeLists.txt:14` |
| 런타임 필수 | Seer 컨트롤러 TCP 도달 | 무선 전용(`wlan0`) | 모든 호출이 `TransportError` 로 실패, 노드는 재시도 | `docs/network/seer_network_access.md` |
| 런타임 선택 | 없음 | — | — | — |

**`rclcpp` 의존 0** — 라이브러리는 ROS 를 모른다. `seer_lidar_tf` 만 rclcpp 를 쓴다.
OpenSSL 은 **의도적으로 넣지 않았다**(ADR `2026-08-18-seer-tcp-ip-cpp-rewrite.md` §Decision 2).

## 5. 소비자

| 소비자 | 쓰는 API | 포트 |
|---|---|---|
| `src/Sensors/Lidar/2D/seer_lidar_tf` (ament_cmake 노드) | `call(1009)` | 19204 |
| `seer_tcp_ip/tools/read_lidar_install` | `getLasers()` | 19204 |
| `seer_tcp_ip/tools/seer_param` | `getParam()` | 19204 |
| `Tools/seer_re/seer_param.sh` | 위 실행파일 호출 | 19204 |

전부 조회 포트라 broker 없이 라이브러리 직결이 정책상 허용된다.

## 6. 실기 확인 범위 (192.168.44.82 Foil_A082, rbk 3.4.5.22)

**조회 경로만 확인했다.** 쓰기 API 는 로봇을 움직이거나 설정을 바꾸므로 승인 없이 호출하지 않았다.

| 확인 | 결과 |
|---|---|
| `read_lidar_install` | FrontLiDAR·RearLiDAR install_info 출력 — Python 판과 같은 값 |
| `seer_param` | 한도 6종 — 19204·19301=**10**, 19205/06/07/10=**5** (`value == defaultValue`, 범위 1~20) |
| `seer_lidar_tf_node` | `base_footprint -> [scan_front, scan_rear]` TF 발행 |
| 실패 경로 | 도달불가 IP 에서 재시도 반복, 크래시·헛돎 없음 |
| **게이트** | `seer_port:=19205` 로 기동 시 `GuardedPortError` 로 차단 — 끝단에서 작동 확인 |
| **제어권 경로**(쓰기 첫 확인) | `control_probe` — `1060`(사전 `locked=false`) → `4005` → `1060`(`nick_name='big-amr-control-probe'` **내 이름 확인**) → `2000` → `4006` → `1060`(사후 `locked=false`). `ControlSession` 계약이 실기에서 성립 |
| **설정 계열 4종** | `param_probe` + 실기 확인 — **4100** setparams 왕복 PASS(`RobotNote` 쓰기가 1400 되읽기에 반영, 원복 확인) · **4101** saveparams **디스크 저장** PASS(되읽기 확인 후 `""` 로 원복) · **4102** 지정 대상 공장값 복원 `ret_code 0` · **4300** clearfatal `ret_code 0`(1050 fatal 0건 상태에서 호출) |
| ⚠ 4102 는 「재적재」가 아니다 | 벤더 원문 설명이 `重载参数, 恢复参数的出厂默认值` — **공장 기본값 복원**이다. 본문은 JSON **배열** `[{plugin, params:[…]}]` 이고 **빈 배열은 전 플러그인 전 파라미터 초기화**다. 우리 구현은 본문 없이 보내 `ret_code 60002 "data is not JSON array"` 로 거부됐다 — 타입 하나 차이로 전체 초기화를 부를 수 있었다. `restoreFactoryParams()` 로 교체하고 빈 배열·객체·`plugin` 누락을 **송신 전에** 거부한다 |
| ⚠ 편호 오류가 여기서 잡혔다 | 최초 시험은 4001 을 보냈고 로봇이 `{"ret_code":0}` 을 돌려줬는데 값은 그대로였다. 원인은 **편호 오류** — 되읽기 왕복이 없었으면 「썼다」로 통과했을 것이다. `debt-126`·`debt-095` |

⚠ **미확인(단위 시험만)**: 2001·2002·2003·2010·3001·3002·3003·3050·3051·3052·3055·3056·3066·
4000·4010·4011·6000·6001·6004 및 `JogKeepalive`·`relocateAndConfirm`.
**이것은 「우리 라이브러리 경로가 미호출」이라는 뜻이지 「그 API 가 미검증」이 아니다** —
`2010`·`2000` 은 `Tools/seer_jog`·`Tools/amr_test_gui`·`Tools/docking_field_kit` 이 실기에서 이미 썼다(debt-111).
저장소 전체에서 **송신 코드 0건**인 것은 내비 태스크(3050·3051·3052·3066)다 — 우리가 부른 적이 없다.
다만 **전제 조건은 이미 충족돼 있다**(2026-09-02 실기 조회): 지도 `260709_test` 로드 완료(1022 `loadmap_status=1`) · 스테이션 **LM1·LM2** 실재(1301) · 측위 성공(1021 `reloc_status=1`).
의미와 인자는 `docs/seer/2026-08-08-seer-path-control.md` 가 벤더 원문 대조로 확정해 뒀다 — 특히 **`3052` 는 평동이 아니라 순찰**이라는 정정이 그 문서에 있다.
**4005·4006·2000·1060 과 `ControlSession`, 설정 계열 4종(4100·4101·4102·4300)은 실기 확인됐다.**
「응답했다」와 「반영됐다」를 같은 칸에 넣지 않는다 — 이번에 그 구분이 편호 오류를 잡아냈다.
