# can_relay — 판다 릴레이 경유 모터 구동 ROS2 드라이버

> ⚠ **조향 홈 관련 서술 정정 — 정본은 아래 한 곳이다. 원문은 이력으로 보존한다.**
> **정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0** (호밍 **10회 연속 실측**, 2026-08-03 15:33~15:40)
> (값 정본은 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - 조향 0° = **`[7871815, 7840086]`** — ⚠ **Seer 좌표계 기준**이며 **물리적 직진은 미확인**이다.
> - **`7882020 / 7859062` 는 0° 가 아니다** — 「**호밍 후 정착값**」이며 0° 에서
>   **+0.178° / +0.331°** 벗어나 있다. 호밍 10회 실측 정착값은 node3 **7,882,021**(σ≈2.8c) ·
>   node4 **7,859,065**(σ≈3.2c) 로 σ≈3 counts 에 재현된다 ⇒ **결함이 아니라 설계 동작**이다.
> - `counts/°` = **57,344**(지령각→CAN 기울기 실측 1.000000) · `0x6098` 호밍 방식 = **1**(−리밋) ·
>   리밋 스위치 **실재** · 호밍 성공률 **10/10**, 소요 **35.0 s**.
>
> **❌ 2026-08-02 판정(`docs/verified_facts/2026-08-02-steer-home-closed.md`)은 폐기됐다 — 인용 금지:**
> - ~~홈 = `[7871810, 7839894]`~~ → **틀렸다.** node4 가 193c 어긋난 raw 판독값이었다.
> - ~~구값 `7871815 / 7840086` 은 「출처 없는 값」~~ → **반증됐다.** 출처는 **Seer 가 실시간으로 내는
>   `0x607A` 조향 목표**이며, 그 값이 1 count 이내로 맞았다.
> - ~~「CAN ↔ Seer 독립 교차확인」~~ → **성립하지 않는다.** Seer 1040 은 판다가 엿듣는 **바로 그
>   `0x6064` 의 아핀 변환**이다(기울기 ×57,344 = **1.000001**). 같은 프레임을 두 번 읽은 것이라
>   역산 `0° = CAN + Seer°×57344` 는 **항등식**이고 자세와 무관하게 같은 값을 낸다.
>   그 측정이 확정한 것은 **Seer 내부 조향 영점**이지 물리적 0° 가 아니다.
> - `debt-007` 은 종결이나, **홈 상수 부채 id 는 계보마다 다르다** — `origin/main` 이 정본:
>   홈 상수 하드코딩 = **debt-026** · can_relay 이름 충돌 = **debt-025** · 구동축 브링업 = **debt-027**.

> ❌ **정정 2026-08-03 — 위 2026-08-02 종결 판정 중 3 건이 실측으로 반증됐다. 위 원문은 이력으로 보존한다.**
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10
> (값 정본은 그대로 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - **조향 0° 채택 정본 = `[7871815, 7840086]`.** `[7871810, 7839894]` 는 **폐기**한다.
>   2026-08-02 종결은 역산식 `0° = CAN_0x6064 + Seer_deg × 57344` 를 §4-2 에 적어 놓고도
>   **채택값에 적용하지 않고 raw 판독값을 0° 로 박았다.** node3 은 오차 6c 라 드러나지 않았고,
>   node4 는 **193c** 로 드러났다.
> - 실측 확정 **2026-08-03 11:44** — `orin_steer_crosscheck.py`, 판다 **SILENT·passthrough**(제어권 미취득),
>   **송신 0건**(AST 검사), **사용자 확인 Seer 표시 0°**, 2회 독립 실행이 counts 단위까지 동일:
>   node3 → 0° 추정 **7,871,816** · node4 → 0° 추정 **7,840,087**.
>   ⚠ **용어 구분**: 「실측 0° 추정」 `[7871816, 7840087]` ≠ 「채택 정본」 `[7871815, 7840086]`(1c 차).
>   문서·config 에는 **채택 정본**을 쓴다.
> - ❌ 「구값 `7871815 / 7840086` 은 **출처 없는 값**」 판정을 **철회**한다 —
>   출처는 **Seer 가 실시간으로 내는 `0x607A` 조향 목표**이고, 값도 맞다.
> - ⚠ 폐기값과의 차이 193c = **0.0034°** 로 **거동상 무의미**하다. 과장하지 말 것.
> - `7882020 / 7859062` 가 0° 가 아니라는 판정은 **유지**하되 **라벨을 정정**한다 —
>   이는 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)다. **값 수정 금지, 라벨만 정정.**
>   새 0° 기준 편차는 **+0.178° / +0.331°**(위 원문의 `+0.332°` 는 폐기된 0° 기준으로 계산한 잔재).
>   「호밍은 조향을 0° 에 정확히 놓지 않는다」는 결론은 그대로 유효하다.

> ❌ **정정 2026-08-03 15:40 — 호밍 10회 연속 실측으로 확정. 위 두 블록의 원문은 이력으로 보존한다.**
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (`orin_home_experiment.py --repeat 10`, 15:33~15:40, 접지 상태, 펌웨어 `DEV-cc5e0491-DEBUG`)
>
> **① 호밍은 정상 동작한다 — 10 / 10 전부 `DONE`.** 소요 **35.0 s**(34.99~35.16, 편차 0.17 s).
>   단계 ENABLE → SET_SPEED → START → WAIT(**31.7 s**, −리밋 탐색) → RESTORE → GOZERO → GOZERO_W → DONE.
>   리밋 도달은 10회 모두 DI(Digital Input) `0x01`→`0x09`(bit3 = −Limit)로 관측됐다.
>   09:58 의 `ERR_TIMEOUT` **1회**가 유일한 실패이고 그 뒤 **12회 연속 성공** — 재현되지 않는다.
>   `ERR_TIMEOUT` 은 깔끔한 terminal 이라 **재시도로 충분**하다.
> **② 호밍 후 정착값(10회 재현)** — node3 **7,882,021**(σ 2.8 c) · node4 **7,859,065**(σ 3.2 c).
>   조향 0° 대비 **+0.178° / +0.331°**. σ ≈ 3 counts ≈ 0.00005° — 매번 같은 자리에 선다.
>   ⇒ **이 편차는 결함이 아니라 설계 동작이다.** 펌웨어 상수 `SEER_HOME_ZERO_N3/N4`(7882020 / 7859062)가
>   그 목표이고 실측이 **1~3 counts** 로 일치한다. **값 수정 금지.**
> **③ `counts/° = 57,344`** — 지령각→CAN 기울기 **1.000000** 실측. **④ `0x6098` = 1**, 리밋 스위치 **실재**.
>
> ⚠ **「조향 0° = `[7871815, 7840086]`」는 Seer 좌표계 기준이다.** 물리 직진과 같은지는 **미확인**이다.
> 값 채택은 유효하나(내비게이션이 Seer 좌표계를 쓴다), **「물리 0° 실측 확정」이라는 라벨은 무효**다.
> 물리 정렬 오차 상한은 여전히 육안 한계 ≈±1°(±57,344 c)로, 위에서 다툰 193 c 의 약 297배다.
>
> > ❌ **재정정 2026-08-03 17:30 — 바로 위 「물리 직진과 같은지 미확인」은 과했다. 두 질문을 뭉갰다.**
> > - **(a) 육안 확인이 「조향 0° = 몇 counts 인가」 역산에 기여하나 → 아니오. 이 판정은 유지된다.**
> >   역산식 `0° = CAN + Seer°×57344` 는 **항등식**이라 바퀴를 어느 자세에 두어도 같은 값이 나온다.
> > - **(b) 「Seer 0° 표시 = 바퀴가 물리적으로 직진」이 성립하나 → 예.**
> >   Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 바퀴 직진 상태에서
> >   Seer 앞바퀴 2축 0° 를 **육안 확인**했다(can_relay GUI 로도 동일 확인).
> > - ⇒ **(b) 를 (a) 에 딸려 무효화한 것이 오류다. 「물리 직진 앵커 부재」는 거짓이다 — 앵커는 실재한다.**
> > - 앵커 **정밀도는 ≈±1°(±57,344 counts)**: **직진 판정에는 충분**하고,
> >   **counts 소수 자리(193 c = 0.0034° = ±1° 의 1/297) 판정에는 불충분**하다.
> > - counts 정본이 **공학적 채택값**인 이유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
> > - 「非-Seer 계측 필요」는 **「counts 소수 자리까지 판정하려면」** 이라는 조건이 붙을 때만 참이다.
>
> ❌ **아래 근거·주장은 폐기됐다 — 인용 금지**(위 블록들이 사용한 것 포함):
> - 「CAN 과 Seer 가 **독립 경로**라 교차검증 성립」 — Seer 1040·1005 는 **둘 다 `0x6064` 유래**다(독립 앵커 아님).
>   따라서 위 블록의 「**2회 독립 실행이 counts 단위까지 동일**」·「**사용자 확인 Seer 표시 0°**」는
>   결과에 기여 0 인 **무효 근거**다(역산식이 항등식이라 자세와 무관하게 같은 값이 나온다). **값 채택만 유효.**
>   - > ❌ **재정정 2026-08-03 17:30 — 무효 범위는 (a) counts 역산에 한정된다.**
>     > 「사용자 확인 Seer 표시 0°」는 **counts 값을 정하는 데** 기여하지 않을 뿐,
>     > **「바퀴가 물리적으로 직진이다」는 별도 사실은 유효**하다 — Seer 1005/1040 은 EasyDRIVE
>     > `steerOffset` 으로 교정된 조향각이고 사용자가 직진 자세에서 앞바퀴 2축 0° 를 육안 확인했다
>     > (can_relay GUI 동일 확인). **정밀도 ≈±1°(±57,344 c)**. ⇒ 이 줄을 「물리 앵커가 없다」로 읽지 말 것.
> - 「**회귀 통과가 값을 검증한다**」 — 정본을 바꿔도 전부 통과했다(변이 시험). 아래 「검증」 절 참조.
> - 「`Switch on disabled` 라 호밍 불가」 · 「`0x6098` = 0 이라 호밍 비활성」 · 「RstStart 가 1 로 고착」
> - 「`0x6064` = 0 은 전원 사이클로만 풀리는 래치」 · 「축이 이미 홈이면 무동작 즉시 완료 → 타임아웃」
> - **`+0.334°` / `+0.332°` 표기** → 전부 **`+0.331°`**(node4). node3 은 `+0.178°`.

> ### ❌ 재정정 2026-08-03 17:00 — 바로 위 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
> 위 원문은 삭제하지 않는다. 재계산에 쓴 원자료:
> `Log/home_experiment_260803_153319_summary.json` · `Log/home_experiment_260803_153319.jsonl` ·
> `Log/home_experiment_260803_091956_summary.json` · `Log/home_experiment_260803_095815_summary.json` ·
> `Log/homing_edge_260803_144305.json` · `Log/homing_edge_260803_144602.json` ·
> `Log/homing_edge_260803_152520.json` · `Log/seer_homing_260803_100813.jsonl` ·
> `Log/steer_two_phase_260803_131305.jsonl` ·
> `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h`
>
> **E1. `:44` 「그 뒤 12회 연속 성공」 → 12회.** 2026-08-03 호밍 개시는 **총 13회 = 성공 12 / 실패 1**:
> 09:58 `ERR_TIMEOUT`(`final_state`=6, 120.26 s) 1회 · 14:46 1회(37.0 s) · 15:25 1회(34.91 s) ·
> 15:33~15:40 **10회**. 09:58 실패 뒤의 연속 성공은 **12회**다.
> (`fsm_trace` 가 빈 회차 — 14:43 · 14:45×4 · 15:29 — 는 호밍이 개시되지 않아 시도에서 제외.)
> ⚠ **「10회 연속 10/10」 자체는 유효**하다. 다만 15:33 실행의 `baseline` 은 호밍이 아니라
> **레지스터 스냅샷**이다(`Tools/docking_field_kit/orin_home_experiment.py:390`
> `base = snapshot(rig, "baseline")`) — 호밍 횟수로 세지 말 것.
>
> **E2. 「`0x6064` = 0 은 09:58 단 1회 · 재현 안 됨」 → [거짓] 재현된다.**
> 09:19 baseline `pos3`/`pos4` = 0 · 09:58 baseline·`run1_pre`·`run1_post` 전부 0 ·
> 10:08 `seer_homing_260803_100813.jsonl` 의 `0x6064` SDO 응답은 **node3 10,327 / 10,327 ·
> node4 10,327 / 10,327 전량 0** · 리부팅 뒤 **14:43** `homing_edge_260803_144305.json` node4 `pos` = 0.
> ⚠ **다만 인과는 미판정**이다 — 성공한 15:33 10회 실행 중에도 `0x6064` 는 0 을 다수 반환했다
> (node3 32,801 / 38,764 · node4 32,726 / 38,737 재집계). ⇒ 「0 이면 호밍이 실패한다」도
> 「0 은 09:58 실패와 무관하다」도 **양쪽 미판정**이다. 어느 쪽으로도 단정하지 말 것.
>
> **E3. `:42` 「WAIT 31.7 s」 → WAIT 체류는 31.30 s.** state 4 → state 8 체류 평균 **31.302** ·
> 중앙 **31.295**(31.21~31.38). **31.7 s 는 개시(t=0)~RESTORE(state 8) 절대시각**(평균 31.687)이다.
>
> **E4. `:41` 「35.0 s」 → 평균 35.07 · 중앙 35.05.** (범위 34.99~35.16, **편차 0.17 s 는 정확**.)
>
> **E5. `:50` 「counts/° = 57,344」 → node3 57,344.0 / node4 57,344.3.**
> `Log/steer_two_phase_260803_131305.jsonl` phase A 5점(−5 / −2.5 / 0 / +2.5 / +5°) 회귀 —
> node3 **57,344.00**(÷57,344 = 1.0000000) · node4 **57,344.28**(1.0000049).
> 「기울기 1.000000」은 **node3 에만** 해당한다.
>
> **E6. `:46-47` 「σ 2.8 c / 3.2 c」는 모표준편차 기준**이다 — node3 **2.80** · node4 **3.21**.
> 표본표준편차로는 **2.95 / 3.38**. 원자료 10회 정착값은 node3 7,882,021×8 · 7,882,014×2 /
> node4 7,859,065×8 · 7,859,058×2 로 **두 값만 나오는 이산 분포**다(연속 산포로 읽지 말 것).
>
> **E7. `:48` 「결함이 아니라 설계 동작이다」 → 과잉 확정.** 실측이 보인 것은 **재현되는 정착 동작**뿐이고,
> 그것이 의도된 사양임을 보이는 벤더·설계 문서 근거는 **확인하지 못했다(미확인)**. ⇒ 이 문장은
> **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」** 으로 읽는다.
>
> **E8. `:52-53` 조향 0° `[7871815, 7840086]` — 값은 유지, 근거는 강등.** 「실측 확정」이 아니라
> **공학적 채택값**이다(역산식이 항등식이라 실측이 값을 결정하지 못한다). **「실측 확정」 표현 금지.**
>
> **E9. `seer_home_cancel_frames()` = `safety_seer_gate.h:312-316`** — 이 문서 `:266` · `:271-272` 의
> 표기는 **이미 정확**하다(2026-08-03 17:00 본문 재확인. `:307-309` 는 폐기 좌표).

> ⚠ **이름 주의.** 이 패키지는 `Tools/Can_Relay/` 의 **판다 펌웨어 프로젝트가 아니다.**
> 저장소에서 `can_relay` 는 지금까지 그 펌웨어를 뜻해 왔다(`docs/can_relay/`,
> `docs/code_review/can_relay_firmware/`, `README.md:1` "CATL-Ford CAN-Relay").
> 이쪽은 그 릴레이를 **호스트에서 사용하는** rclpy 드라이버다.
> 문서·주석에서 인용할 때 어느 쪽인지 반드시 병기할 것 — debt-015.

## 무엇을 하는가

comma.ai panda 기반 CAN 릴레이를 경유해 Tongyi 4축 서보(구동 node1·2 / 조향 node3·4)를 구동한다.
Seer 마스터가 붙어 있는 상태에서 릴레이 intercept 로 주도권을 가져와 지령을 덮어쓴다.

## `src/Actuators/motor_control` 과 무엇이 다른가

| | `motor_control` | 이 패키지 |
| --- | --- | --- |
| 물리 경로 | socketcan `can1` 직결 | USB → panda → bus 2 |
| Seer | **분리 필수** | **연결 필수** |
| 정지 근간 | guard RTR 20 Hz 중단 → 500 ms HALT | heartbeat 상실 → 펌웨어가 구동 0 + 릴레이 개방 |
| RTR 송신 | 가능(필수) | **불가** — 판다 패킷 헤더에 RTR 비트가 없다 |

두 안전 모델은 **배타적**이다. 한쪽 전제가 다른 쪽 실패조건이므로 합칠 수 없다.
**두 패키지를 동시에 띄우지 말 것** — 이를 막는 기계 장치는 아직 없다(debt-018).

## 쓰는 법

```bash
colcon build --packages-select can_relay --symlink-install && source install/setup.bash

# 하드웨어 없이 (기본 검증용)
ros2 launch can_relay can_relay.launch.py link:=mock

# 실기 — 기동만으로는 아무것도 움직이지 않는다. 제어권은 명시 호출로만 잡는다.
ros2 launch can_relay can_relay.launch.py

# 1) 제어권 획득 — 이것이 먼저다. 호밍도 지령도 백엔드 기동 후에만 받는다.
ros2 service call /can_relay_node/engage std_srvs/srv/SetBool "{data: true}"

# 2) 조향 호밍 — **조향 지령보다 반드시 먼저. 전원 사이클마다 다시 해야 한다.**
#    ⚠ 물리 스윙 100°+ 가 발생한다. 이동구역을 먼저 비울 것.
#    소요 35.0 s (10회 실측, 편차 0.17 s). 31.7 s 는 −리밋 탐색, 나머지가 ≈137° 복귀 스윙 —
#    ❌ 재정정 2026-08-03 17:00: 소요는 평균 35.07 / 중앙 35.05 (34.99~35.16, 편차 0.17 은 정확).
#    −리밋 탐색(WAIT) 체류는 31.30 s 이고, 31.7 s 는 개시~RESTORE 절대시각이다 (E3·E4).
#    두 구간 모두 축이 움직인다. DONE 을 확인하기 전에 접근하지 말 것.
#    펌웨어 시퀀서(0xea)를 쓰므로 **진행 중 취소가 된다**
ros2 service call /can_relay_node/home std_srvs/srv/Trigger
ros2 service call /can_relay_node/home_cancel std_srvs/srv/Trigger   # 진행 중 취소

# 3) 지령 — 조향은 호밍 완료 뒤에만 수리된다(아래 게이트 참조). 구동은 게이트 없음.
ros2 topic pub --once /can_relay_node/steer_deg std_msgs/Float64 "{data: 45.0}"
ros2 topic pub --once /can_relay_node/drive_mmps std_msgs/Float64 "{data: 50.0}"

# 4) 정지 → 제어권 반환
ros2 service call /can_relay_node/stop std_srvs/srv/Trigger
ros2 service call /can_relay_node/engage std_srvs/srv/SetBool "{data: false}"
```

> ❌ **절차 정정 2026-08-03 — 종전 순서(`engage` → `steer_deg` → … → 맨 끝 `home`)는 실행 불가였다.**
> `require_homed_for_steer` **기본값이 True** 라서(`can_relay/driver_node.py` › 파라미터 선언
> `("require_homed_for_steer", True)`, `can_relay/backend.py` › `RelayConfig.require_homed_for_steer: bool = True`)
> **호밍 전 조향 지령은 반드시 거부된다**:
> - `backend.py` › `set_steer_deg()` → `UnsafeCommand("호밍이 완료되지 않았다 — 조향 0° 기준이 없어 지령을 보낼 수 없다. \`~/home\` 를 먼저 수행할 것(전원 사이클마다 필요)")`
> - `backend.py` › `set_motor_cmds()`(저수준 `MotorCmdArray` 경로) → `"node{n} 호밍 미완료 — 조향 거부"` 로 항목 건너뜀
>
> 근거는 매뉴얼 §Home 35 — **홈 설정은 전원이 켜져 있는 동안만 유효**하므로, 기동 직후 `0x6064` 가
> 어느 각도를 뜻하는지 알 수 없다. `~/home` 자체도 `backend.home()` 의 `if not self._running` 검사 때문에
> **`~/engage` 이후에만** 받는다. `~/drive_mmps`(구동)에는 이 게이트가 걸리지 않는다.

`~/drive_mmps` 는 워치독(`cmd_timeout_s`, 기본 0.3 s)이 걸려 있다 — 한 번 쏘고 두면
0.3 초 뒤 자동으로 0 이 된다. 계속 주행하려면 주기 발행이 필요하다. 이것은 결함이 아니라 설계다.

## 안전 규칙 (코드가 강제한다 — 회귀 ~~177건~~ **188건**이 고정)

> ❌ **정정 2026-08-03 15:40 — 「177건」은 낡았다.** 2026-08-03 재실행 결과 **188 passed**
> (`PYTHONPATH=. python3 -m pytest test -q`, 5.52 s). 원 표기는 이력으로 보존한다.
> ⚠ **건수는 안전 근거가 아니다** — 「회귀 통과가 값을 검증한다」는 폐기된 주장이다. 아래 「검증」 절 참조.

| 규칙 | 강제 지점 |
| --- | --- |
| 비유한(NaN/inf) 지령 **거부**, 워치독도 갱신 안 함 | `safety.finite()` |
| 조향 **±90° 클램프**를 counts 생성 지점에 | `safety.steer_deg_to_counts()` |
| 구동 지령 **주기 재송신**, 만료 시 0 | `backend._loop` |
| 정지는 **어떤 상태에서도 수용** | `backend.stop()` |
| heartbeat 유지 = 펌웨어 fail-safe 무장 | `link.acquire()` + 제어 루프 |
| 호밍 중(bit15=0) **위치 사용 금지** | `safety.position_trustworthy()` |
| 호밍 완료는 **0→1 전이**로만 판정 | `safety.HomingJudge` |
| 피드백 만료 시 각도 `None`(0 으로 채우지 않음) | `backend.steer_angles_deg()` |
| 브링업·호밍·제어권은 **명시 요청 전용** | `allow_bringup=false`, `~/home`, `~/engage` |

## 주요 상수의 근거

| 상수 | 값 | 근거 |
| --- | --- | --- |
| `COUNTS_PER_DEG` | 57344 | 16384×4×315/360. 실측 홈↔90° Δ = +5,160,960 = 정확히 90.00° |
| `STEER_LIMIT_DEG` | 90.0 | **실측 검증 범위**. 기구 한계(±140°)는 다른 기체 config 값이라 쓰지 않는다 |
| `VEL_MAX_UNITS` | 4889 | ≈0.2 m/s. ⚠ 실측이 아니라 config 환산 + 계승된 안전 상한 |
| `steer_home_counts` | 7871815 / 7840086 | ✅ **정본**(2026-08-03 실측 확정 — 「debt-007/016 미판정」 딱지 **철회**, 재확인 15:40). ~~Seer 표시 0° 자세의 `0x6064` 를 `0° = CAN + Seer°×57344` 로 역산한 값이다.~~ → **Seer 내부 조향 영점 상수**다(아래 정정). 값 정본 파일은 `config/machine/foil_a082.yaml`. ⚠ 7882020 / 7859062 는 홈이 아니라 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)로 여기서 +0.178° / +0.331° 떨어진 별개 값이다. ❌ **재정정 2026-08-03 17:00 (E8)**: 「실측 확정」이 아니라 **공학적 채택값**이다 — 역산식이 항등식이라 실측이 값을 결정하지 못한다. 값은 유지, **「실측 확정」 표현 금지** |
| `counts/°` (실측) | 57,344 | ✅ 2026-08-03 실측 — 지령각→CAN 기울기 **1.000000**. 위 `COUNTS_PER_DEG` 이론값과 일치. ❌ **재정정 2026-08-03 17:00 (E5)**: 노드별로 **node3 57,344.0**(기울기 1.0000000) · **node4 57,344.3**(1.0000049) 이다 — 「1.000000」은 node3 에만 해당 |
| `0x6098`(RstMode) | 1 | ✅ 2026-08-03 실기 판독 — **Home 1(−리밋)**, 리밋 스위치 **실재**(호밍 10회 모두 DI bit3 전이 관측) |
| 호밍 소요 | 35.0 s | ✅ 2026-08-03 10회 실측(편차 0.17 s). 그중 WAIT(−리밋 탐색) **31.7 s**. ❌ **재정정 2026-08-03 17:00 (E3·E4)**: 평균 **35.07** · 중앙 **35.05**(34.99~35.16, 편차 0.17 은 정확). **WAIT 체류는 31.30 s**(평균 31.302 / 중앙 31.295)이고 **31.7 s 는 개시~RESTORE 절대시각**(평균 31.687)이다 |

> ❌ **정정 2026-08-03 15:40 — `steer_home_counts` 근거 서술 정정(값은 그대로 정본).**
> 역산식 `0° = CAN + Seer°×57344` 는 **항등식**이다 — Seer 1040 은 판다가 엿듣는 **바로 그 `0x6064`** 의
> 아핀 변환이라(1005 도 `0x6064` 유래) 자세와 무관하게 같은 값을 돌려준다.
> ⇒ 이 값이 확정하는 것은 **Seer 내부 조향 영점 상수**이며, **물리적 직진에 대해서는 아무것도 말하지 않는다.**
> - > ❌ **재정정 2026-08-03 17:30 — 「아무것도 말하지 않는다」는 과했다.**
>   > 항등식이라 **counts 값을 결정하지 못한다**는 것까지가 참이다. 그와 별개로
>   > **「Seer 0° 표시 = 바퀴 물리적 직진」은 성립**한다 — Seer 1005/1040 은 EasyDRIVE `steerOffset`
>   > 으로 교정된 조향각이고, 사용자가 직진 자세에서 Seer 앞바퀴 2축 0° 를 육안 확인했다
>   > (can_relay GUI 동일 확인). ⇒ **물리 직진 앵커는 실재**하며 **정밀도가 ≈±1°(±57,344 c)** 일 뿐이다.
>   > 이 값이 「공학적 채택값」인 이유도 **앵커 부재가 아니라 앵커 정밀도 부족**(193 c = ±1° 의 1/297)이다.
> 내비게이션이 Seer 좌표계를 쓰므로 **값에 맞추는 공학적 결정은 옳다** — 무효인 것은 「물리 0° 실측 확정」 라벨뿐이다.
> **인용 금지**: 「CAN↔Seer 독립 교차검증」·「2회 독립 실행 일치가 강한 증거」·「사용자 Seer 0° 확인이 이상적 조건」.
| `GUARD_TIME` / `LIFE_FACTOR` | 500 ms / 1 | 캡처 t=17.891~17.896 의 Seer init 값 |
| 호밍 속도 | 2500 | 0.1 r/min → 250 r/min. 캡처 t=17.918 |
| safety_mode | 30 | `SAFETY_SEER_GATE`. **릴레이를 강제 전환하지 않는 유일한 모드** |
| CAN 속도 | 250 kbps | 판다 부팅 기본값. ⚠ 500 k 로 덮으면 버스가 죽는다 |

## 검증

```bash
cd src/Comm/CAN/can_relay && PYTHONPATH=. python3 -m pytest test -q   # 188 passed (2026-08-03 15:4x 재실행 확인)
```

> ❌ **정정 2026-08-03 15:40 — 종전 표기 `177 passed` 는 낡았다(현행 **188 passed**, 5.52 s).**
> 그리고 **「회귀 통과가 값을 검증한다」는 폐기된 주장이다.** 08-02~08-03 사이 `steer_home_counts` 정본과
> 테스트 픽스처가 **모순인 채 전건 green** 이었다 — 테스트가 YAML 을 로드하지 않고 픽스처가 하드코딩이라
> 정본을 쓰레기 값으로 바꿔도 전부 통과했다(변이 시험 실증).
> 지금은 `test/test_steer_home_sync.py`(11건)가 YAML↔픽스처 동기화를 실제로 잡는다 —
> **값의 근거는 회귀가 아니라 실측**이며, 회귀는 「사본이 정본과 어긋나지 않음」만 보장한다.

`protocol.py` 가 만드는 프레임 **28종 전부**가 실측 캡처 `Log/homing_capture_220350.jsonl`
(Seer 마스터 180 s, 253,510 프레임)에 **바이트 동일**로 존재한다(총 12,958건 일치).

**실기 미검증 항목** — 지면 사용 전 반드시 해소:
- 구동축 브링업 시퀀스(debt-017) — `allow_bringup` 기본 false. 잭업에서만 확인할 것
- 판다 guard RTR 불가 → Seer forward 의존(debt-012)
- `kin_steer_sign` 미확정(debt-004) — 확정 전 `cmd_vel` crab/스핀 금지
- 판다 fail-safe 정지 실효성(debt-013) — 이 패키지 정지의 근간이다
- ~~**물리 직진 앵커 부재** — 조향 0° 는 **Seer 좌표계 기준**까지만 확정이다. 非-Seer 계측이 필요하다(위 정정 참조)~~
  - ❌ **재정정 2026-08-03 17:30 — 「앵커 부재」는 거짓. 항목명을 「앵커 정밀도 부족」으로 바꾼다.**
    **물리 직진 앵커는 실재**한다(Seer 1005/1040 = EasyDRIVE `steerOffset` 교정 조향각 + 사용자가
    직진 자세에서 앞바퀴 2축 0° 육안 확인, can_relay GUI 동일 확인). 미해소로 남는 것은
    **앵커 정밀도가 ≈±1°(±57,344 c)** 라 **그 이하의 정렬 오차(예: 193 c = 0.0034°)는 미확인**이라는 점이다.
    ⇒ 非-Seer 계측은 **counts 소수 자리까지 판정하려는 경우에만** 필요하다.

✅ **해소된 항목**(2026-08-03 15:40): **호밍 실기 검증** — `~/home` 이 쓰는 펌웨어 시퀀서(`0xea`) 경로가
**10 / 10 성공**, 35.0 s(편차 0.17 s), 정착값 재현 σ ≈ 3 counts. 취소 경로(`~/home_cancel`)의
실효성은 **여전히 미검증**이다(호밍 10회는 전부 완주시켰다).
❌ **재정정 2026-08-03 17:00**: 소요는 **평균 35.07 · 중앙 35.05**(E4), σ 는 **모표준편차 node3 2.80 /
node4 3.21**(표본 2.95 / 3.38 — E6)이다. 「10 / 10 성공」은 유효하되, 같은 날 호밍 개시는
**총 13회(성공 12 / 실패 1)** 였다(E1).

## 판다 펌웨어(`Tools/Can_Relay/`)와의 연동

| 펌웨어 기능 | 요청 | 이 패키지 |
| --- | --- | --- |
| safety gate | `0xdc`(mode 30) | `link.acquire()` |
| 주도권·인터셉트 | `0xe9`·`0xe8` | `link.acquire()` / `release()` |
| heartbeat fail-safe | `0xf3` | 제어 루프가 0.2 s 마다 |
| **호밍 시퀀서** | `0xea`·`0xeb` | `~/home` · **`~/home_cancel`** |
| **per-bus CAN 에러** | `0xc3` | `diagnostics` 의 `bus0`·`bus2` |

**호밍은 SDO 로 `0x60FB:04=1` 을 직접 보내지 않는다.** 펌웨어 시퀀서를 쓰는 이유는 하나 —
`seer_home_cancel_frames()`(~~`safety_seer_gate.h:307-309`~~ → **`:312-316`**)가 `0x60FB:04=0` 취소 프레임을 내므로
**진행 중 중단이 성립**하기 때문이다. 직접 송신 경로에는 그 수단이 없다.
폴백도 두지 않는다 — 펌웨어가 시퀀서를 지원하지 않으면 실패로 보고하고, 덜 안전한 경로로
내려가지 않는다.

> ❌ **정정 2026-08-03 15:40 — 두 가지. 원문은 이력으로 보존한다.**
> 1. **dangling 인용**: `seer_home_cancel_frames()` 는 `safety_seer_gate.h:307-309` 가 아니라
>    **`:312-316`** 에 있다(2026-08-03 `grep -n` 확인). 같은 잘못된 좌표가 코드 주석 **6곳**에 남아 있다 —
>    `can_relay/driver_node.py:15`, `can_relay/link.py:69` · `:488`,
>    `can_relay/backend.py:20` · `:507` · `:533`. **코드는 이번에 수정하지 않았다**(지적만).
> 2. **「직접 송신 경로에는 그 수단이 없다」의 근거로 인용되는 코드 주석이 과장이다.**
>    `can_relay/link.py:68` — *"SDO 로 `0x60FB:04=1` 을 직접 보내면 드라이브 내부 루틴이 시작돼
>    **소프트웨어가 멈출 수 없다**"*, 같은 취지가 `can_relay/backend.py:531-533`
>    (*"직접 송신 경로에서는 취소 프레임을 낼 수단이 없어 시작 후 **하드웨어 E-STOP 밖에 남지 않는다**"*).
>    취소는 **실재한다** — 취소의 실체는 `0x60FB:04=**0**` SDO 쓰기 한 개이고
>    (`seer_home_cancel_frames()` 본문이 그것뿐이다), 이 패키지는 `~/home_cancel` 로 이를 노출한다
>    (`driver_node.py:194-195`, `:311`). ⇒ **정확한 서술**: 「멈출 수 없다」가 아니라
>    「호스트가 직접 취소하면 heartbeat 상실·제어권 반환 시 취소가 보장되지 않으므로,
>    **취소를 펌웨어 fail-safe 와 같은 계층에 두려고** 시퀀서를 쓴다」이다.
>    **코드는 이번에 수정하지 않았다** — README 의 지적으로만 남긴다.

`REC(Receive Error Counter)`·`TEC(Transmit Error Counter)`는 bxCAN 에서 **uint8** 이라
255 에서 포화한다. `bus_off`·`error_passive`·`error_warning` 이 서면 `diagnostics` 가 ERROR 가 된다.

panda 파이썬 라이브러리는 사본이 둘이다 — `Tools/docking_field_kit/panda`(상위집합) 를 먼저
쓰고, 없으면 `Tools/Can_Relay/panda-firmware/python` 으로 내려간다. `can_health()` 는 호스트
헬퍼에 의존하지 않고 직접 `controlRead` 하므로 어느 사본이든 동작한다.

## 미구현 (의도적)

- **역기구학을 자체 구현하지 않는다.** 저장소에 이미 3벌 있고 접기 임계가 ±90°/±115°/±140° 로
  갈려 있다. `cmd_vel` 은 `motor_control.kinematics` 를 빌려 쓰며, import 실패 시 조용히
  대체하지 않고 구독을 만들지 않는다.
- `cmd_vel` 은 **동일각(직진·크랩)만** 지원한다. 스핀·선회는 축별 각이 갈리므로 거부한다.
- `wheel_motor_state`(2WS 모션 스택이 기다리는 것) 발행은 Phase 2.

## 관련 문서

- 설계 결정: `docs/adr/2026-07-29-can-relay-ros2-package.md`
- 감사 기록: `docs/code_review/can_relay_ros2/2026-07-29.md` (10인 + 적대적 심문 3인)
- 부채: `docs/debt/registry.md` debt-004·007·012·013·015·016·017·018
