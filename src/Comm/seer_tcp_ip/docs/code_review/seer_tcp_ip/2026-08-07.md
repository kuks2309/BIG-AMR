# seer_tcp_ip 인벤토리 (2026-08-07, 갱신 2026-08-10 · 08-17 · 08-18)

> **이 문서는 인벤토리(코딩 SOP §2 선행 산출물)다 — severity 평가·리뷰 verdict 는 담지 않는다.**
> 결함 평가는 별도 code_review 패스 소관이며, 저자는 자기 코드에 verdict 를 찍지 않는다
> (coding.md §5 never-self-approve).
>
> 대상: `src/Comm/seer_tcp_ip/` (신규, ADR [2026-08-07-seer-api-tcp-hal](../../../../../../docs/adr/2026-08-07-seer-api-tcp-hal.md))
> 정본 위치는 이 파일(모듈 로컬), 루트 집계는 `docs/code_review/seer_tcp_ip/2026-08-07.md`.

> ⚠ **2026-08-10 갱신 — 포트 정책의 전제가 실측으로 뒤집혔다.** 초판은 "지령 포트는 동시연결 1
> (문서 v1.2.1)" 을 전제로 `EXCLUSIVE_PORTS`·`is_exclusive`·`allow_exclusive` 라 이름 붙였으나,
> 실기·원본 하드 조회 결과 **한도는 5 이고 초과 시 거동은 거부형**이다. 게이트는 유지하되 근거를
> **지령 중재**로 바꾸고 이름을 `GUARDED_PORTS`·`is_guarded`·`allow_guarded` 로 교체했다.
> 근거·경위는 ADR §Context·§Verification 과 `docs/claude-mistake/2026-08-07-002`.

> ⚠ **2026-08-17 개명** — 패키지가 `src/Comm/TCP_IP/seer_api` → **`src/Comm/seer_tcp_ip`** 로
> 옮겨졌고 패키지·파이썬 모듈명이 모두 `seer_tcp_ip` 가 됐다(ADR §Decision 1 개정).
> 아래 함수표의 `<파일>:<줄>` 앵커는 그대로 유효하다 — 파일 내용은 import 경로 외에 바뀌지 않았다.

## 0. 편호·요청 형태의 근거 (코드 주석에 두지 않는다)

주석은 현재 동작만 담는다 — 어디서 알았는지는 여기 적는다.

| 근거 | 무엇을 주는가 |
|---|---|
| `References/Seer-Driver/robokit_tcp_api.md` 편호 표 | 편호와 뜻. **4005·4006·1060·1040·1302·1500·3066·4010·6004 가 없다** |
| `T-Robot_seer_gui/seer_core/client.py` (로컬 `/home/nvidia/T-Robot_seer_gui`, 485줄) | 위 편호의 **요청 JSON 형태까지**. 문서에 없는 편호의 정본 |
| 실기(192.168.44.82) 조회 | 응답 키 실측 — 아래 §6 |

**근거가 없는 편호는 감싸지 않는다.** 3055/3056 은 편호만 있고 필드명이 없어 저수준 dict 만 받고,
2022 는 문서(스캔 시작)와 참조 클라이언트(지도 전환)가 어긋나 **감싸지 않았다**(→ debt).

⚠ **`open_loop_move` 의 `duration` 은 dead-man 타이머다.** 그 시간 안에 새 지령이 없으면 로봇이
스스로 선다(`0` = 무한). 이전 구현은 이 필드를 아예 보내지 않았고, 그래서 보내는 쪽이 죽었을 때
로봇이 서는지 말할 수 없었다. 지금은 필수 인자다.


## 1. 목적

Seer(SRC) 컨트롤러와의 Robokit TCP/IP API 통신을 단일 지점으로 모은다. 이전에는 16B 헤더가
`seer_read_lidar_install.py:24` 와 `seer_lidar_tf_node.py:27` 에 각각 재구현돼 있었고, 두 구현 모두
seq 고정·응답 seq 미대조·요청 간격 무제한이었다.

## 2. 함수표

### `ports.py` — 포트 정책 (ROS 무관)

| 위치 | 심볼 | 용도 | 인자 → 반환 | 비고 |
|---|---|---|---|---|
| `ports.py:15-22` | `API_PORT_*` | 포트 상수 | — | 공식 SDK `rbkNetProtoEnums.py` 이름 유지 |
| `ports.py:25-33` | `MAX_CONNECTION_PARAM` | 포트 → 한도 파라미터 이름 | dict | API 1400 조회용. `libNetProtocol.so` 문자열·실기 응답 키에서 확정 |
| `ports.py:38-46` | `OBSERVED_MAX_CONNECTIONS` | **관측** 동시연결 한도 | dict | **[실측 2026-08-07]** 19204·19301=10, 19205/06/07/10=**5**. 정본 아님 — 런타임 파라미터 |
| `ports.py:50` | `CONNECTION_LIMIT_RET_CODE` | 한도 초과 `ret_code` | `61001` | 응답 편호가 **포트 번호**로 온다(요청+10000 아님) |
| `ports.py:59-65` | `GUARDED_PORTS` | 직결 차단 포트 집합 | frozenset | 19205/06/07/10. **명시 집합** — 한도에서 파생하면 비어서 게이트가 사라진다 |
| `ports.py:67` | `MIN_REQUEST_INTERVAL_S` | 요청 간 최소 간격 0.1s | — | 가이드 §1 ≥100~200ms |
| `ports.py:70` | `RESPONSE_TYPE_OFFSET` | 응답 편호 = 요청 + 10000 | — | 가이드 §3. 오류 응답은 이 규칙을 안 따른다 |
| `ports.py:73-79` | `is_guarded` | 지령·설정 포트 판정 | `port:int` → `bool` | 게이트 강제의 근거. 근거는 연결 수가 아니라 **중재** |
| `ports.py:81-88` | `observed_max_connections` | 관측 한도 조회 | `port:int` → `int\|None` | 참고용. 판정은 `SeerApi.get_max_connections` |

### `transport.py` — 전송 계층 (ROS 무관)

| 위치 | 심볼 | 용도 | 인자 → 반환 | 예외 |
|---|---|---|---|---|
| `transport.py:27-31` | `HEAD_FMT`/`HEAD_LEN`/`SYNC`/`VERSION`/`RSV` | 16B 헤더 상수 | — | `HEAD_FMT` 는 공식 `PACK_HEAD_FMT_STR` 과 동일 문자열 |
| `transport.py:34-36` | `SeerProtocolError` | 프로토콜 오류 | — | 헤더·편호·seq·ret_code |
| `transport.py:38-45` | `SeerGuardedPortError` | 지령 포트 직결 시도 | — | ADR §Decision 3 강제. 옛 이름 `SeerExclusivePortError` |
| `transport.py:47-54` | `SeerConnectionLimitError` | 한도 초과 거부(61001) | — | `SeerProtocolError` 의 하위 — 기존 호출자를 안 깬다 |
| `transport.py:56-71` | `pack` | 요청 프레임 생성 | `seq, api_type, msg` → `bytes` | — |
| `transport.py:73-83` | `unpack_head` | 응답 헤더 해석 | `head:bytes` → `(seq, json_len, api_type)` | `SeerProtocolError`(길이·sync) |
| `transport.py:86-` | `SeerTransport` | 포트 1개 TCP 1문1답 연결 | — | 스레드 안전 아님 |
| `transport.py:93-114` | `.__init__` | 연결 파라미터·시험 주입점 | `ip, port, timeout, min_interval, clock, sleep` | — |
| `transport.py:116-122` | `.connect` | lazy 연결 | → `self` | `OSError` |
| `transport.py:124-129` | `.close` | 소켓 해제 | — | — |
| `transport.py:132-133` | `.is_connected` | 연결 여부 | → `bool` | — |
| `transport.py:142-154` | `.request` | 편호 요청 → dict | `api_type, msg, expect_type` → `dict` | `SeerProtocolError`, `SeerConnectionLimitError`, `ConnectionError` |
| `transport.py:156-182` | `.request_raw` | 편호 요청 → (bytes, 편호) | 동상 → `(bytes, int)` | 동상. 실패 시 소켓 close |
| `transport.py:184-203` | `._raise_connection_limit_if_that` | 한도 거부 서명 판별 | `resp_type, body` → None/예외 | `SeerConnectionLimitError` |
| `transport.py:205-211` | `._throttle` | 최소 간격 대기 | — | — |
| `transport.py:213-226` | `._recv_exact` | 정확히 n 바이트 수신 | `n:int` → `bytes` | `ConnectionError`(조기 종료) |

### `api.py` — 편호 바인딩 (ROS 무관, 517줄)

| 위치 | 심볼 | 용도 | 포트 / 편호 |
|---|---|---|---|
| `api.py:14-79` | `API_*` · `CONTROL_PREEMPTED_RET_CODE` | 편호 상수 50종 + 제어권 거부 코드 40020 | — |
| `api.py:82-` | `SeerApi` | 포트별 연결 보유 + 이름 호출 | — |
| `api.py:89-99` | `.__init__` | `allow_guarded` 기본 False | — |
| `api.py:101-119` | `.transport` | 포트 전송 획득(**게이트 강제 지점**) | `SeerGuardedPortError` |
| `api.py:121-124` | `.close` | 전 연결 해제 | — |
| `api.py:133-142` | `.call` | 공통 호출 + ret_code 검사 | — |
| `api.py:144-150` | `._raise_on_ret_code` | ret_code≠0 → 예외 | — |
| `api.py:152-153` | `.get_robot_info` | model·vehicle_id·version | 19204 / 1000 |
| `api.py:155-157` | `.get_pose` | x, y, angle | 19204 / 1004 |
| `api.py:159-161` | `.get_speed` | vx, vy, w, steer_angles | 19204 / 1005 |
| `api.py:163-164` | `.get_battery` | 배터리 | 19204 / 1007 |
| `api.py:166-167` | `.get_io` | DI/DO | 19204 / 1013 |
| `api.py:169-176` | `.get_lasers` | `lasers[]` | 19204 / 1009 |
| `api.py:178-180` | `.get_alarms` | fatals/errors/warnings | 19204 / 1050 |
| `api.py:182-188` | `.iter_alarms` | (level, code, ts) 평탄화 | 19204 / 1050 |
| `api.py:190-192` | `.get_all_status` | 배치 1 | 19204 / 1100 |
| `api.py:194-196` | `.get_map_status` | current_map·md5·maps[] | 19204 / 1300 |
| `api.py:198-201` | `.get_param` | 플러그인 파라미터 | 19204 / 1400 |
| `api.py:203-217` | `.get_max_connections` | **현재** 한도를 로봇에 질의 | 19204 / 1400 |
| `api.py:219-221` | `.get_run_info` | 운행시간·주행거리·컨트롤러 온도/전압 | 19204 / 1002 |
| `api.py:223-225` | `.get_mode` | 운행 모드 | 19204 / 1003 |
| `api.py:227-229` | `.get_blocked` | 피차단·감속·최근접 장애물 | 19204 / 1006 |
| `api.py:231-233` | `.get_brake` | brake 상태 | 19204 / 1008 |
| `api.py:235-237` | `.get_path` | 경로 | 19204 / 1010 |
| `api.py:239-241` | `.get_area` | 현재 area | 19204 / 1011 |
| `api.py:243-249` | `.get_estop` | emergency·soft_emc·driver_emc·electric | 19204 / 1012 |
| `api.py:251-253` | `.get_reloc_status` | 재측위 진행 | 19204 / 1021 |
| `api.py:255-257` | `.get_loadmap_status` | 지도 로드 상태 | 19204 / 1022 |
| `api.py:259-265` | `.get_slam_status` | 스캔 상태(+`resultmap`) | 19204 / 1025 |
| `api.py:267-274` | `.get_motor_info` | 축별 encoder·position | 19204 / 1040 |
| `api.py:276-278` | `.get_control_owner` | 제어권 소유자 | 19204 / 1060 |
| `api.py:280-282` | `.get_all_status2` | 배치 2 (steer·r_steer rad) | 19204 / 1101 |
| `api.py:284-286` | `.get_all_status3` | 배치 3 | 19204 / 1102 |
| `api.py:288-290` | `.get_init_status` | 초기화 상태 | 19204 / 1111 |
| `api.py:292-294` | `.get_stations` | 스테이션 목록 | 19204 / 1301 |
| `api.py:296-317` | `.get_map_md5` | 지도 md5. **`.smap` 자동 부착 + 호출자 형태 키**, all-or-nothing | 19204 / 1302 |
| `api.py:319-321` | `.get_robot_model` | 모델 JSON | 19204 / 1500 |
| `api.py:324-342` | `.download_map` | `.smap` 원문 바이트 + md5 대조 | **19207** / 4011 |
| `api.py:344-361` | `._raise_if_error_payload` | 짧은 응답만 에러로 판정 | — |
| `api.py:364-374` | `.seize_control` | **제어권 획득**(기존 소유자를 뺏는다) | **19207** / 4005 |
| `api.py:376-378` | `.release_control` | 제어권 반납 | **19207** / 4006 |
| `api.py:381-383` | `.stop` | 즉시 정지 | **19205** / 2000 |
| `api.py:385-400` | `.open_loop_move` | 개루프 운동. **`duration_ms` 필수(dead-man)** | **19205** / 2010 |
| `api.py:402-404` | `.calibrate_gyro` | 자이로 캘리브 | **19205** / 2001 |
| `api.py:406-409` | `.relocate` | 재측위(좌표) | **19205** / 2002 |
| `api.py:411-413` | `.relocate_with` | 재측위(isAuto·home 등 dict) | **19205** / 2002 |
| `api.py:415-417` | `.confirm_location` | 측위 확정 | **19205** / 2003 |
| `api.py:420-432` | `.go_target` | 고정경로. `source_id` 기본 SELF_POSITION + 옵션 통과 | **19206** / 3051 |
| `api.py:434-441` | `.go_target_list` | 지정경로(재계획 없음) | **19206** / 3066 |
| `api.py:443-445` | `.go_point` | 자유 내비(dict 통과) | **19206** / 3050 |
| `api.py:447-449` | `.patrol` | 순찰(dict 통과) | **19206** / 3052 |
| `api.py:451-457` | `.translate` | 평동 — **필드명 미확인, dict 통과** | **19206** / 3055 |
| `api.py:459-461` | `.turn` | 회전 — **필드명 미확인, dict 통과** | **19206** / 3056 |
| `api.py:463-465` | `.pause_task` | 작업 일시정지 | **19206** / 3001 |
| `api.py:467-469` | `.resume_task` | 작업 계속 | **19206** / 3002 |
| `api.py:471-473` | `.cancel_task` | 작업 취소 | **19206** / 3003 |
| `api.py:476-478` | `.set_mode` | 운행 모드 전환(dict 통과) | **19207** / 4000 |
| `api.py:480-486` | `.set_params` | 파라미터 설정. `save` 로 4001/4002 갈림 | **19207** / 4001·4002 |
| `api.py:488-490` | `.reload_params` | 파라미터 재로드 | **19207** / 4003 |
| `api.py:492-494` | `.clear_fatal` | Fatal 클리어 | **19207** / 4004 |
| `api.py:496-501` | `.upload_map` | 지도 업로드(활성 전환 안 함) | **19207** / 4010 |
| `api.py:503-508` | `.soft_estop` | 소프트 비상정지(해제해야 움직인다) | **19210** / 6004 |
| `api.py:510-512` | `.speaker` | 스피커(dict 통과) | **19210** / 6000 |
| `api.py:514-517` | `.set_do` | DO 출력 | **19210** / 6001 |

### `control.py` — 제어권 세션·dead-man jog (ROS 무관, 172줄)

편호 래퍼가 아니라 **동작**이라 별 모듈이다. 스레드를 만들지 않는다 — 호출자가 `tick()` 을 부른다.

| 위치 | 심볼 | 용도 | 인자 → 반환 | 예외 |
|---|---|---|---|---|
| `control.py:16-17` | `SeerControlError` | 제어권 상태가 요구와 불일치 | — | — |
| `control.py:20-` | `SeerControlSession` | 획득→사용→정지→반납 컨텍스트 매니저 | — | — |
| `control.py:33-43` | `.__init__` | `nick_name` 필수(빈 값 거부), `stop_on_exit` | — | `ValueError` |
| `control.py:45-59` | `.acquire` | 소유자 기록 후 4005 | → 이전 소유자 dict | `SeerControlError`(이중 획득) |
| `control.py:61-70` | `.release` | 정지 후 4006. 정지 실패에도 반납 시도 | — | 정지 예외 전파 |
| `control.py:72-78` | `.__enter__`/`.__exit__` | 예외 경로에서도 반납 | — | — |
| `control.py:81-` | `JogKeepalive` | `duration` 실은 2010 주기 재송신 | — | — |
| `control.py:96-117` | `.__init__` | **`interval_s*1000 < duration_ms` 불변식 검증** | — | `ValueError` |
| `control.py:119-121` | `.set_velocity` | 다음 tick 부터 적용(즉시 송신 안 함) | — | — |
| `control.py:123-127` | `.due` | 재송신 시점인가 | → `bool` | — |
| `control.py:129-139` | `.tick` | 시점이면 1회 송신 | → 보냈으면 `True` | — |
| `control.py:141-145` | `.stop` | 2000 + 속도 0 + 스케줄 리셋 | → dict | — |
| `control.py:155-163` | `preempted_by_control` | 예외가 40020(제어권 없음)인가 | `Exception` → `bool` | — |
| `control.py:166-172` | `describe_owner` | 1060 을 한 줄 요약(진단) | `SeerApi` → `str` | — |

### `__init__.py` — 공개 표면

| 위치 | 심볼 | 용도 |
|---|---|---|
| `__init__.py:7-25` | re-export | `SeerApi`, `SeerTransport`, 예외 3종, `pack`/`unpack_head`, 포트 상수, `is_guarded`, `observed_max_connections` |
| `__init__.py:27-43` | `__all__` | 공개 표면 고정 — 여기에 없는 심볼은 내부 취급 |

### `mutation_check.py` — 회귀 검출력 검사 도구 (패키지 루트, 설치 대상 아님)

| 위치 | 심볼 | 용도 |
|---|---|---|
| `mutation_check.py:39-155` | `MUTATIONS` | (id, 되돌리는 내용, 파일, 치환쌍) **33건**. 대부분 이관 전 두 구현이 실제로 갖고 있던 결함 + 08-10 정정분 |
| `mutation_check.py:158-` | `main` | 하나씩 변조 → 전체 시험 → 통과하면(미검출) `exit 1`. 앵커 불일치도 `exit 1` |

⚠ **하네스 자체의 함정 2가지 — 둘 다 실제로 밟았다.**
1. `__pycache__` 오염 — `.pyc` 유효성이 (mtime 초, 파일크기)로 판정되므로 같은 초에 같은 크기로 쓴
   변조는 **직전 항목의 바이트코드가 재사용된다.** 초판이 그 오염으로 27/27 을 잘못 보고했다.
   `python -B` + `PYTHONDONTWRITEBYTECODE` + `__pycache__` 삭제로 닫았다.
2. **무력한 돌연변이** — `frozenset(…) or frozenset({…})` 로 쓰면 파생 집합이 비었을 때 falsy 라
   `or` 가 원본으로 되돌아가 **변조가 아무 일도 하지 않는다.** P2 가 그래서 미검출로 나왔다.
   블록을 통째로 치환하도록 고쳤다. 「미검출」이 나오면 **시험을 의심하기 전에 돌연변이를 의심한다.**

## 3. 전역변수표

가변 전역 **0개**. 모듈 수준 이름은 전부 상수(포트·편호·헤더 포맷)이며 런타임에 바뀌지 않는다.
연결 상태(`_sock`, `_seq`, `_last_request_at`)는 `SeerTransport` 인스턴스 안에만 있다.

| 이름 | 위치 | 가변 | 누가 바꾸나 |
|---|---|---|---|
| `API_PORT_*`, `MAX_CONNECTION_PARAM`, `OBSERVED_MAX_CONNECTIONS`, `CONNECTION_LIMIT_RET_CODE`, `GUARDED_PORTS`, `MIN_REQUEST_INTERVAL_S`, `RESPONSE_TYPE_OFFSET` | `ports.py:15-70` | 불변 | 아무도(소스 수정만) |
| `HEAD_FMT`, `HEAD_LEN`, `SYNC`, `VERSION`, `RSV` | `transport.py:27-31` | 불변 | 아무도 |
| `API_*`(편호 50종) · `CONTROL_PREEMPTED_RET_CODE` | `api.py:14-79` | 불변 | 아무도 |

⚠ `OBSERVED_MAX_CONNECTIONS` 는 **코드에서는 불변이지만 로봇에서는 가변**이다(런타임 파라미터,
`minValue` 1 ~ `maxValue` 20). 상수를 진실로 쓰지 말고 판정이 필요하면
`SeerApi.get_max_connections()` 로 로봇에 묻는다.

## 4. 의존성

- 표준 라이브러리만: `socket`, `struct`, `json`, `itertools`, `time`, `hashlib`.
- `control.py` → `api.py`(`SeerApi`)·`ports.py`. 역방향 의존 없음.
- **ROS 의존 0** — 단독 스크립트·pytest 에서 그대로 import 가능해야 한다(이관 대상 중 하나가
  패키지 밖 단독 스크립트다).
- 패키지 자체는 `ament_python` 으로 설치돼 `seer_lidar_tf` 가 exec_depend 로 참조한다.

## 5. 소비자 (이관 후)

| 소비자 | 쓰는 API | 포트 |
|---|---|---|
| `src/Sensors/Lidar/2D/seer_lidar_tf/seer_lidar_tf/seer_lidar_tf_node.py` | `SeerApi.get_lasers()` | 19204 |
| `src/Sensors/Lidar/2D/seer_read_lidar_install.py` | `SeerApi.get_lasers()` | 19204 |

둘 다 19204(조회 포트)이므로 broker 없이 라이브러리 직결이 정책상 허용된다.

## 6. 실기 확인 범위 (192.168.44.82 Foil_A082)

**조회 경로만 확인했다.** 쓰기 API 는 로봇을 움직이거나 설정을 바꾸므로 승인 없이 호출하지 않았다.

| 확인 | 결과 |
|---|---|
| 1002·1003·1006·1012·1021·1022·1111 | 전부 응답. 키 실측 — 1002 는 `controller_humi/temp/voltage·odo·time`, 1006 은 `block_x/y·blocked·nearest_obstacles·slowed`, 1012 는 `driver_emc·electric·emergency·soft_emc` |
| 1060 제어권 | `locked=false`(비어 있음) |
| 1301 스테이션 | 4개 |
| 1040 모터 | `FrontWalk`·`RearWalk`·`FrontSteer`·`RearSteer` |
| **1302 맵 md5** | **`.smap` 확장자 필수** — 1300 이 주는 이름(`260709_test`)을 그대로 넣으면 `ret_code 40051 "no this map file"`. 붙이면 md5 가 1300 의 `current_map_md5` 와 **일치**(`a20cbe5c…`) |
| 1302 all-or-nothing | 없는 지도가 하나라도 섞이면 **요청 전체 거부**(부분 결과 없음) |

⚠ **미확인(단위 시험만)**: 4005·4006·2000·2001·2003·2010·3001·3002·3003·3050·3051·3052·3055·3056·
3066·4000·4001·4002·4003·4004·4010·6000·6001·6004 — 「우리가 올바른 편호·포트·JSON 을 만든다」까지만
보증된다. 로봇이 받아들이는지는 별개다.
