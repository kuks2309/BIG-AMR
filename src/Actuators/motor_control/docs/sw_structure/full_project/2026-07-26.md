## 2026-07-26 21:30 (KST) — motor_control — 전체 프로젝트 SW 구조 (패키지 병기)

> 루트 정본: `docs/sw_structure/full_project/2026-07-26.md` (전체 그래프·drawio·⑤ 구조 관찰 참조)
> 본 파일은 full_project 정본의 **motor_control 해당 부분** 발췌(이중 기록). 실측 근거만.

### 역할
Tongyi 4축 AMR CANopen SDO 폴링 마스터. `cmd_vel`(Twist)·`estop`(Bool, 리터럴 상대명) 구독 → 운동학 변환 → CAN(socketcan). `odom`/`joint_states`/`diagnostics`+TF 발행.

### 파일 의존 (import)
- `driver_node.py` → `backend.py`(:22), `kinematics.py`(:23)
- `backend.py` → `protocol.py`(:18), `kinematics.py`(ModuleCommand, :19)

### 클래스
- `MotorControlNode`(rclpy.Node inherit, :32) *-- `TongyiSdoBackend`(:73) · `DualSteerKinematics`/`DiffDriveKinematics`(:61,64)
- `TongyiSdoBackend`(:63) --> `ModuleConfig`(:31)·`NodeState`(:40), ..> `protocol.Frame`(:49)
- 운동학: `DualSteerKinematics`(:27)·`DiffDriveKinematics`(:91) (스티어/차동, `ModuleCommand` 인터페이스)

### 런타임 (call)
- sub `cmd_vel`(:85), `estop`(:86, 리터럴 상대명); pub `odom`(:87)·`joint_states`(:88)·`diagnostics`(:89); TF odom→base_link(:90,157)
- 외부 경계: CAN `socketcan can1`(:72)

> ⚠ **정정 2026-07-27 — 위 런타임 목록은 2026-07-26 후속 수정 전 스냅샷이다. 문서 :4 의 "실측 근거만" 주장이 이 절에서는 성립하지 않는다.**
> 현행 코드(`grep -n` 2026-07-27 실측, `driver_node.py` 236줄):
> - sub `cmd_vel`(**:89**), `estop`(**:90**), **`freewheel`(:91) ← 기존 목록에서 통째로 누락돼 있었다**
> - pub `odom`(**:92**)·`joint_states`(**:93**)·`diagnostics`(**:94**); TF `TransformBroadcaster`(**:95**)·`sendTransform`(**:166**)
> - 외부 경계: CAN `socketcan can1`(**:72** — 이 인용은 현행과 일치)
> `freewheel` 은 같은 패키지 인벤토리에는 이미 있었다([`docs/2026-07-09-inventory.md`](../../2026-07-09-inventory.md) 함수표 행 `| 23a | MotorControlNode._on_freewheel | Bool | — | /freewheel → backend.freewheel 전달 |`) — 본 문서만 누락.
> 안전상 중요: `/freewheel` 은 구동축 servo-off 로 **홀딩토크를 상실**시킨다(`motor_control/backend.py` › `TongyiSdoBackend.freewheel()` docstring "⚠ 홀딩토크 상실 — 경사면에서 굴러갈 수 있음").
> (클래스·import 절의 인용은 2026-07-27 재확인 시 현행과 일치했다: `MotorControlNode`:32, `TongyiSdoBackend`:63, `ModuleConfig`:31, `NodeState`:40, `DualSteerKinematics`:27, `DiffDriveKinematics`:91, import :22·:23, `_to_can`:49. 어긋난 것은 본 런타임 절이다.)
>
> ### ⚠ 재감사 2026-07-27b — **위 정정 블록의 좌표가 다시 틀렸다**(원문은 이력 보존을 위해 남긴다)
> 위 블록의 인용을 하나씩 열어 대조한 결과:
> - `backend.py:163` 에는 "⚠ 홀딩토크 상실 …" 문구가 **없다**. 그 문구는 `TongyiSdoBackend.freewheel()` docstring 안에 있고,
>   같은 세션 안에서 :223 → :273 으로 이동했다(파일이 동시 편집 중 — `backend.py` 494 → 549 → **635줄**, 2026-07-27 `wc -l`).
> - `docs/2026-07-09-inventory.md:33` 은 `_on_freewheel` 행이 아니라 `| 10 | TongyiSdoBackend.start |` 행이다.
>   `_on_freewheel` 행은 그 문서의 **함수표 `| 23a |` 행**이다(그 문서도 감사 주석 추가로 줄번호가 밀리므로 행 번호로 인용한다).
> - 런타임 좌표 `cmd_vel(:89)·estop(:90)·freewheel(:91)`, `odom(:92)·joint_states(:93)·diagnostics(:94)`,
>   `TransformBroadcaster(:95)`·`sendTransform(:166)`, `socketcan can1(:72)` 도 **모두 현행과 어긋난다**
>   (2026-07-27 재측정 시점엔 각각 :207/:208/:209, :210/:211/:212, :213, :294, :190 이었고 그 값도 이후 편집으로 다시 밀린다).
> - 클래스·import 좌표(`MotorControlNode`:32, `TongyiSdoBackend`:63, `ModuleConfig`:31, `NodeState`:40,
>   `DualSteerKinematics`:27, `DiffDriveKinematics`:91, `_to_can`:49)도 **"현행과 일치" 가 더 이상 아니다**
>   (재측정: 각각 :44, :121, :61, :98, :63, :127, :107 — 역시 계속 이동 중). 일치했던 것은 `driver_node.py` import :22·:23 뿐이다.
> ⇒ **이 문서에서 코드를 `file:line` 으로 인용하지 말 것.** 심볼명(클래스·함수·토픽 문자열) 앵커를 쓰고,
>   부득이 줄번호를 쓰면 **측정 시각을 병기**하라. 「`/freewheel` 누락」·「홀딩토크 상실」이라는 **내용 판정 자체는 유효**하다.

---
