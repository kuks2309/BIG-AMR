# mcl2d_ros2 함수표 · 전역변수표 (모듈 로컬 권위본)

> 대상: 패키지 전체(노드 2 + 변환 헤더 + 비교 하니스). 코어 알고리즘 함수는
> `mcl2d_core` 의 표([src/Navigation/mcl2d_core/docs/function_table.md](../../mcl2d_core/docs/function_table.md)) 소관.

## mcl2d_localization_node.cpp — `Mcl2dLocalizationNode` (rclcpp_lifecycle::LifecycleNode)

| 함수 | 위치 | 입력 | 출력 | 부작용 |
| --- | --- | --- | --- | --- |
| `Mcl2dLocalizationNode()` 생성자 | mcl2d_localization_node.cpp:37-74 | — | — | 파라미터 **선언만**(map_path, init_x/y/theta, 프레임 3종, publish_tf, autostart, 코어 손잡이 8종, laser_mounts). 값 읽기·자원 생성 없음 — 재선언 예외 방지로 선언은 생성자 1회 고정 |
| `on_configure(state)` | mcl2d_localization_node.cpp:77-147 | lifecycle State | CallbackReturn | 파라미터 읽기·검증, 맵 로드, 로컬라이저 생성, lifecycle publisher·TF 브로드캐스터/버퍼/리스너 생성. 실패(맵 부재·파손, mounts 형식 오류) 시 FAILURE 반환(unconfigured 잔류) |
| `on_activate(state)` | mcl2d_localization_node.cpp:149-187 | lifecycle State | CallbackReturn | publisher 활성화(base 호출), 증분 기준점·스캔 캐시 리셋, 구독 3종 생성(/odom BEST_EFFORT·/initialpose·/scan SensorDataQoS) |
| `on_deactivate(state)` | mcl2d_localization_node.cpp:189-194 | lifecycle State | CallbackReturn | publisher 비활성화(base 호출) + 구독 해제 — 콜백·TF 발행 완전 정지 |
| `on_cleanup(state)` | mcl2d_localization_node.cpp:196-200 | lifecycle State | CallbackReturn | release() — 전 자원 해제, 재 configure 로 맵 교체 가능 |
| `on_shutdown(state)` | mcl2d_localization_node.cpp:202-206 | lifecycle State | CallbackReturn | release() — active 직행 종료까지 커버 |
| `readTuned(name, field)` | mcl2d_localization_node.cpp:212-221 | 파라미터명, 코어 파라미터 필드 참조 | — (field 갱신) | 이식 기준값(호출 전 field 값)과 다르면 WARN 로그 |
| `resetSubscriptions()` | mcl2d_localization_node.cpp:223-228 | — | — | 구독 3종 reset |
| `release()` | mcl2d_localization_node.cpp:230-241 | — | — | 구독·TF·publisher·로컬라이저·캐시 전부 reset |
| `isStopped(o, cur, dt)` | mcl2d_localization_node.cpp:247-261 | 오도 메시지, 현 자세, dt | bool(정지 여부) | 없음(순수 판정). pose 증분 1차, twist 는 dt 불능 시 폴백 |
| `onInitialPose(m)` | mcl2d_localization_node.cpp:264-289 | /initialpose (map 프레임만 수용) | — | 파티클 재초기화, prev_odom_/prev_stamp_ 리셋(⚠ 미결 주석 참조) |
| `onOdom(o)` | mcl2d_localization_node.cpp:291-346 | /odom | — | loc_->update() 구동, /mcl_pose 발행(+공분산), publish_tf_ 면 TF 발행 |
| `publishMapToOdom(est, stamp)` | mcl2d_localization_node.cpp:348-382 | 추정 자세, 스탬프 | — | map→odom TF 발행. odom→base TF 부재 시 WARN 후 미발행(map→base 폴백 금지) |
| `main` | mcl2d_localization_node.cpp:400-427 | argc/argv | int | autostart(기본 true)면 spin 전 configure→activate 동기 구동, 실패 시 FATAL + exit 1. 이후 spin |

### 멤버 변수 (전역변수 없음 — 모듈 전역 0)

| 변수 | 위치 | 용도 |
| --- | --- | --- |
| `params_` | mcl2d_localization_node.cpp:384 | 로컬라이저·정지판정 공유 단일 소유 파라미터. 매 configure 마다 기준값에서 재독 |
| `loc_` | mcl2d_localization_node.cpp:385 | 코어 로컬라이저(시드 17 고정). configure 생성·cleanup 해제 |
| `pub_` (LifecyclePublisher) / `sub_odom_` / `sub_scan_` / `sub_init_` | mcl2d_localization_node.cpp:386-389 | /mcl_pose(비활성 시 publish 무시) · /odom(BEST_EFFORT) · /scan(SensorDataQoS) · /initialpose — 구독은 active 동안만 존재 |
| `tf_` / `tf_buffer_` / `tf_listener_` | mcl2d_localization_node.cpp:390-392 | map→odom 발행 / odom→base 조회. configure 생성·cleanup 해제 |
| `prev_odom_` / `prev_stamp_` / `scan_` | mcl2d_localization_node.cpp:393-395 | 증분 기준점·최신 스캔 캐시(optional). activate 시 리셋 |
| `map_frame_` / `odom_frame_` / `base_frame_` / `publish_tf_` | mcl2d_localization_node.cpp:396-397 | 프레임명·TF 발행 스위치 |
| `kUnsupportedAxisVar` | mcl2d_localization_node.cpp:30 | z·roll·pitch 미지원 표시용 큰 분산(1e6) |

## smap_map_server.cpp — `SmapMapServer` (rclcpp::Node)

| 함수 | 위치 | 입력 | 출력 | 부작용 |
| --- | --- | --- | --- | --- |
| `SmapMapServer()` 생성자 | smap_map_server.cpp:37-142 | ROS 파라미터(map_path 필수, frame_id, margin, resolution, publish_reflectors) | — | .smap → OccupancyGrid 변환 후 /map latch 발행 1회. 실패 시 throw |
| `publishReflectors(m, latched)` | smap_map_server.cpp:147-175 | 맵, QoS | — | /map_reflectors MarkerArray latch 발행 |
| `main` | smap_map_server.cpp:182-188 | argc/argv | int | rclcpp::spin |

## conversions.hpp (헤더 온리, 노드·하니스 공유)

| 함수 | 위치 | 입력 | 출력 | 부작용 |
| --- | --- | --- | --- | --- |
| `yawFromQuat(z, w)` | conversions.hpp:18-21 | 쿼터니언 z·w | yaw(rad) | 없음(순수) |
| `fromRosScan(s)` | conversions.hpp:24-33 | sensor_msgs/LaserScan | mcl2d::LaserScan | 없음(무손실 복사) |
| `fromRosOdom(o)` | conversions.hpp:36-43 | nav_msgs/Odometry | mcl2d::Pose2D | 없음 |
| `toRosPose(p)` | conversions.hpp:46-54 | mcl2d::Pose2D | PoseWithCovarianceStamped | 없음(공분산은 호출자가 채움) |

## launch/

| 함수 | 위치 | 입력 | 출력 | 부작용 |
| --- | --- | --- | --- | --- |
| `generate_launch_description` (localization) | localization.launch.py:20-58 | launch 인자(map_path 필수, params_file, odom_topic, scan_topic, autostart) | LaunchDescription | mcl2d_localization lifecycle 노드 1개 기동(YAML 먼저, map_path 나중 — 명령줄이 파일 값을 이김). autostart 는 bool 로 명시 변환해 전달 |
| `generate_launch_description` (bringup) | bringup.launch.py:26-73 | launch 인자(map_path, params_file, lidar/icp/localization 토글) | LaunchDescription | 라이다·icp·측위 계층 포함(IncludeLaunchDescription, FindPackageShare 지연 해석) |

## test/compare_impls.cpp — 비교 하니스 (노드 미포함)

| 함수 | 위치 | 용도 |
| --- | --- | --- |
| `Occ` 구조체 | compare_impls.cpp:18-… | .smap 점군 → 점유 판정 보조 |
| `main` | compare_impls.cpp:67-… | 변환 무손실 round-trip + non-ROS vs ROS 경유 pose 수열 대조 |
