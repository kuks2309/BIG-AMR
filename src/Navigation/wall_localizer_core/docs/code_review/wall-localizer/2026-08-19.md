# wall_localizer 인벤토리 — 벽 3면 라이다 정밀 측위 (2026-08-19)

> **인벤토리(코딩 SOP §2 선행 산출물)** — severity 평가·리뷰 verdict 는 담지 않는다.
> 계획 단계에서 먼저 작성했고, 구현 후 §6 갱신에서 줄 앵커를 실제 값으로 확정했다.
> **이 파일이 루트 정본이다.** 패키지 병기(사본)는
> `src/Navigation/wall_localizer_core/docs/code_review/wall-localizer/` ·
> `src/Navigation/wall_localizer_ros2/docs/code_review/wall-localizer/` 아래에 둔다.
> 사전승인: [ADR 2026-08-18 벽 3면 라이다 정밀 측위](../../adr/2026-08-18-wall3-precision-localizer.md)
> 테스트 파일 행은 파일 단위 앵커(`:1`)를 쓴다.

## 1. 무엇을 만드나

스테이션 국소 정밀 측위: 라이다 스캔에서 직선(벽)을 추출해, YAML 로 정의한 기준 벽 3면과
대응시켜 SE(2) 자세(x·y·yaw)를 최소자승으로 푼다(BlueBotics ANT 의 자연 특징 방식).
코어는 ROS 비의존(mcl2d_core 관례) — 합성 스캔으로 ctest 단위검증한다.

- 단위: 내부 m·rad 단일. 직선 법선형 `n·p = d`, `d ≥ 0`, `n` = 원점→직선 수직(원점 정향).
- 변환 표기: `T_a_b` — `p_a = T_a_b · p_b`. 출력 자세는 `T_station_base`.

## 2. 함수표 — `src/Navigation/wall_localizer_core/`

### types.hpp (헤더 온리 — 타입·기하 유틸)

| 함수/타입 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `Point2D`·`Pose2D`·`LineNormalForm`·`ExtractedSegment`·`WallRef` | types.hpp:18-57 | — | — | 공용 자료형 (m·rad) | — |
| `ExtractParams`·`MatchParams`·`SolveParams`·`QualityParams`·`WallLocalizerParams` | types.hpp:96-152 | — | — | 파라미터 묶음(기본값 포함) | — |
| `normalizeAngle(a_rad)` | types.hpp:60-70 | rad | rad ∈ [−π, π) | 각도 정규화 | 없음(순수) |
| `transformPoint(T_a_b, p_b)` | types.hpp:72-78 | 변환·점 | 점 | p_a = T·p_b | 없음(순수) |
| `compose(T_a_b, T_b_c)` | types.hpp:80-85 | 변환 2개 | 변환 | 합성 | 없음(순수) |
| `inverse(T_a_b)` | types.hpp:87-94 | 변환 | 변환 | 역변환 | 없음(순수) |

### line_extractor.hpp:14-27 · line_extractor.cpp

| 함수 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `scanToPoints(ranges_m, angle_min_rad, angle_inc_rad, p)` | line_extractor.cpp:76-96 | 거리 배열·시작각·증분각·`ExtractParams` | `vector<Point2D>` (라이다 프레임) | 거리/섹터 게이트 통과 점군 | 없음(순수) |
| `fitLineTLS(points, i0, i1, rms_m)` | line_extractor.cpp:98-149 | 점군 구간 | `LineNormalForm`(+rms) | 총최소자승(주성분) 직선 적합, 원점 정향 | 없음(순수) |
| `extractSegments(points_lidar, p)` | line_extractor.cpp:151-224 | 점군·`ExtractParams` | `vector<ExtractedSegment>` | 간격 클러스터링→split-and-merge→공선 병합→게이트. 결정론 | 없음(순수) |
| (내부) `pointToChordDist`·`splitRecursive`·`projectOntoLine`·`makeSegment` | line_extractor.cpp:14-73 | — | — | 분할·사영 보조 (무명 namespace) | 없음(순수) |

### wall_matcher.hpp · wall_matcher.cpp

| 함수/타입 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `OrientedWall` | wall_matcher.hpp:15-19 | — | — | 기준 벽 + 정향 고정 법선형(스테이션) | — |
| `PredictedWall` | wall_matcher.hpp:22-30 | — | — | 라이다 프레임 투영 벽(대응 기준) | — |
| `WallMatch` | wall_matcher.hpp:32-37 | — | — | 기준 벽↔추출 선분 대응 쌍 | — |
| `orientWall(w, robot_pos_station)` | wall_matcher.cpp:10-31 | 벽 끝점·로봇 기준 위치 | `OrientedWall` | 법선을 로봇 쪽 d>0 으로 정향 고정 | 없음(순수) |
| `predictWallsInLidar(walls, T_station_lidar)` | wall_matcher.cpp:33-57 | 정향 벽·라이다 자세 | `vector<PredictedWall>` | 스테이션→라이다 투영 | 없음(순수) |
| `matchWalls(predicted, segments, walls, p)` | wall_matcher.cpp:59-144 | 투영 벽·선분·정향 벽·`MatchParams` | `vector<WallMatch>` | 각도·거리·겹침(예측 벽 구간 대비) 게이트, 점수순 탐욕 1:1 | 없음(순수) |

### pose_solver.hpp · pose_solver.cpp

| 함수/타입 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `SolveResult` | pose_solver.hpp:12-19 | — | — | 해석 결과(자세·ok·사유·normal_spread) | — |
| `solvePose(matches, p)` | pose_solver.cpp:8-75 | 대응 목록·`SolveParams` | `SolveResult{T_station_lidar}` | yaw=가중 원형평균, 병진=2×2 정규방정식, Σnnᵀ 가관측성 검사 | 없음(순수) |

### wall_localizer.hpp · wall_localizer.cpp (파사드)

| 함수/타입 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `Status`(OK/DEGRADED/LOST) | wall_localizer.hpp:15-20 | — | — | 판정 3상태 | — |
| `WallFit`·`LocalizeResult` | wall_localizer.hpp:23-44 | — | — | 벽별 잔차·결과 자료형 | — |
| `WallLocalizer(walls, params, T_base_lidar, initial_T_station_base)` | wall_localizer.cpp:8-21 | 기준 벽·파라미터·외부 파라미터·초기 추정 | — | 구성 + 벽 법선 정향 고정 | 멤버 초기화 |
| `WallLocalizer::reset(guess)` | wall_localizer.cpp:23-28 | 초기 추정 | — | 추적 초기화 | 상태 재설정 |
| `WallLocalizer::registerReject()` (private) | wall_localizer.cpp:30-41 | — | — | 연속 기각 누적, 한도 초과 시 초기 추정 복귀 | `consecutive_rejects_`·`has_fix_`·`last_T_station_base_` |
| `WallLocalizer::update(ranges_m, angle_min_rad, angle_inc_rad)` | wall_localizer.cpp:43-157 | 스캔 1회분 | `LocalizeResult` | 추출→(재)대응→해석 반복(≤max_iterations)→잔차·점프 게이트→판정 | 추적 상태 갱신 |
| `WallLocalizer::lastPose()`·`hasFix()` | wall_localizer.hpp:64-71 | — | 자세/bool | 조회 | 없음 |

### 테스트 — test 디렉터리 (파일 단위 앵커)

| 파일 | 위치 | 검증 내용 |
| --- | --- | --- |
| `sim_scan.hpp:1` | test/ | 합성 스캔(벽 선분 레이캐스트, 시드 고정 잡음) + CHECK/CHECK_NEAR 매크로 (NDEBUG 무관) |
| `test_line_extractor.cpp:1` | test/ | 직교 2벽→선분 2개·법선/거리 정확, 최소 길이 게이트, 간격 분리, 잡음 하 단일 선분 유지 |
| `test_pose_solver.cpp:1` | test/ | 정확 대응 왕복 복원(<1e-9), 평행 퇴화·대응 부족 실패, 변환 왕복 항등, normalizeAngle 범위 |
| `test_wall_localizer.cpp:1` | test/ | E2E: 무잡음 <1mm/0.05°, σ=10mm <5mm/0.3°, 가림 DEGRADED, 벽 1면 LOST, 평행 구성 LOST, 점프 게이트+복귀, 연속 추적 |
| `CMakeLists.txt:1` | 루트 | 정적 라이브러리 + ctest 3건 등록 (Release 기본, -Wall -Wextra) |

## 3. 함수표 — `src/Navigation/wall_localizer_ros2/`

| 함수 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `WallLocalizerNode()` | wall_localizer_node.cpp:45-79 | ROS 파라미터(YAML) | — | 파라미터 선언·검증→발행/구독 배선 (코어는 외부 파라미터 확보 후 지연 생성) | 노드 초기화 |
| `loadWallsFromParams()` (private) | wall_localizer_node.cpp:82-114 | `wall_names`·`walls.<name>` | `vector<WallRef>` | YAML 끝점→기준 벽. 형식 오류 시 FATAL+예외로 기동 실패 | 없음(읽기) |
| `declareTuningParams()` (private) | wall_localizer_node.cpp:116-157 | 튜닝 파라미터 | — | 코어 기본값 노출, 각도는 deg 경계 변환 | `params_` 채움 |
| `lookupLaserExtrinsic(laser_frame)` (private) | wall_localizer_node.cpp:159-189 | 스캔 frame_id | bool | `base_frame←laser` TF 1회 lookup·캐시 (평면 yaw 추출), use_tf_extrinsic=false 면 파라미터 사용 | `T_base_lidar_` 캐시 |
| `scanCallback(msg)` (private) | wall_localizer_node.cpp:191-220 | `sensor_msgs/LaserScan` | — | 코어 지연 생성→update→OK/DEGRADED 시 `wall_pose` 발행, 매 스캔 진단 발행 | 발행·코어 상태 갱신 |
| `publishDiagnostics(stamp, res)` (private) | wall_localizer_node.cpp:222-260 | 결과 | — | DiagnosticArray(상태 레벨·벽별 잔차·매칭) 발행 | 발행 |
| `main(argc, argv)` | wall_localizer_node.cpp:281-296 | — | int | rclcpp 초기화·spin, 파라미터 예외 시 exit 1 | 프로세스 수명 |

부속: `package.xml:1` · `CMakeLists.txt:1`(코어 소스 직접 컴파일) ·
`config/wall_localizer.yaml:1`(예시 U자 스테이션 — 실측값으로 교체 필요) ·
`launch/wall_localizer.launch.py:1`(params_file·scan_topic 인자).

## 4. 전역변수표 (두 패키지 공통)

| 이름 | 위치 | 형 | 누가 바꾸나 | 용도 |
| --- | --- | --- | --- | --- |
| (없음) | — | — | — | 가변 전역 0 — 상태는 전부 `WallLocalizer`/`WallLocalizerNode` 멤버 |

멤버 상태(전역 아님): `last_T_station_base_`·`has_fix_`·`consecutive_rejects_`·`oriented_walls_`
(코어, update/reset/registerReject 만 변경) · `localizer_`·`T_base_lidar_`·통신 핸들(노드, 생성자/콜백).

## 5. 의존성

- 코어: C++17 표준 라이브러리만 (외부 의존 0)
- ROS2: rclcpp · sensor_msgs · geometry_msgs · diagnostic_msgs · tf2 · tf2_ros
- 커스텀 인터페이스(.msg/.srv/.action) 신설 없음

## 6. 시뮬레이션 시험 킷 — `Tools/wall_localizer_sim/` (비-ROS2 배치 규약: python3 직접 실행)

| 함수/파일 | 위치 | 입력 | 출력 | 용도 | 부작용 |
| --- | --- | --- | --- | --- | --- |
| `truth_at(t, trajectory)` | sim_eval.py:1 | 시각·궤적 종류 | (x, y, yaw) | 접근 주행/정지 참값 궤적 (해석식) | 없음(순수) |
| `raycast(walls, ox, oy, dx, dy)` | sim_eval.py:1 | 벽 목록·원점·방향 | (거리, 벽 idx) | 빔-벽 교차 (클러터 포함) | 없음(순수) |
| `SimEval` (rclpy Node) | sim_eval.py:1 | CLI 인자(시나리오·σ·가림·클러터·시간) | jsonl + summary dict | 합성 스캔 발행 + `/wall_pose`·진단 구독 대조, 스탬프 일치 페어링, 오차 통계 | 토픽 발행/구독·파일 기록 |
| `run_scenarios.sh:1` | Tools/wall_localizer_sim/ | — | Log/wall_localizer_sim/ | 시나리오 일괄: 기동 게이트(중복 발행자 0 확인)→노드 기동→평가→정리→종합표 | 프로세스 기동/종료 |

## 7. 검증 기록 (2026-08-19, 이 장비 aarch64/gcc 11.4/Humble)

- 코어 ctest **3/3 PASS** (`test_line_extractor`·`test_pose_solver`·`test_wall_localizer`)
- 변이 검증: `solvePose` 병진 우변 부호를 고의 파괴 → 2/3 실패 확인 후 원복 → 3/3 PASS
  (테스트가 실제로 실패할 수 있음을 증명)
- `colcon build --packages-select wall_localizer_ros2` 성공
- SIL 스모크: 합성 스캔(U자 스테이션, 참값 x=0.5 y=0.1 yaw=2°) → `/wall_pose`
  x=0.500035 y=0.099981 yaw=2.006° (오차 35µm·19µm·0.007°), 진단 OK·벽 3면 전부 대응
- 시나리오 시뮬레이션 8종(§6 킷, 20Hz·720빔): σ=10mm 에서 정지·주행·가림·클러터·초기오차
  모두 xy RMSE ≈3mm·yaw ≈0.09°·해율 ≥97.7%, 가림 시 DEGRADED 강등 정상.
  σ=20mm 는 기본 임계 밖(해율 0.3%) — split_dist_m ≈ 잡음 3σ 로 조정 시 94.7% 회복.
  1차 실행은 노드 잔류 누적(다중 발행자)으로 전량 무효 처리 후 프로세스 게이트 교정·재실행.
  상세: `code_updates/2026-08-19-wall-localizer-sim.md` · 로그 `Log/wall_localizer_sim/`
- 실기(실 스테이션 벽·SICK 라이다) 정밀도 측정은 **미실시** — ros2 도메인 §5 기동 규율 적용 대상
