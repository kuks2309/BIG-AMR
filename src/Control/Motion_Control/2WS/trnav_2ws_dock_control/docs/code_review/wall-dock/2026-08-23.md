# trnav_2ws_dock_control 인벤토리 — LGIT 도킹 코어 이식 W1 (2026-08-23)

> **인벤토리(코딩 SOP §2 선행 산출물).** 신규 패키지 — 계획 단계 작성 후 구현·검증에서 확정.
> **이 파일이 루트 정본**, 패키지 병기는
> `src/Control/Motion_Control/2WS/trnav_2ws_dock_control/docs/code_review/wall-dock/`.
> 사전승인: [ADR 2026-08-23 2WS 도킹 이식](../../adr/2026-08-23-2ws-dock-port.md)
> 원본: LGIT-C6-Cobot `7a2df984` `src/Skills/Docking_Control/dock_control` — **파일 무수정 복사**.
> 함수 상세는 원본 함수표를 승계한다(복사돼 오는
> `trnav_2ws_dock_control/docs/function_table.md` — 원본 표 자체가 정본 대응 줄번호·부작용을
> 전수 등재하고 있어 여기 중복 전사하지 않는다).

## 1. 파일 구성 (전부 무수정 복사, 앵커는 파일 단위)

| 파일 | 역할 |
| --- | --- |
| `include/dock_control/dock_core.hpp:1` · `src/dock_core.cpp:1` | 도킹 순수함수 코어(PID 엔진·기하 진입·crab 합성·조향 유지 목표·IMU 방어·원위치 판정 등 20종) — ROS 의존 0 |
| `test/golden/golden.tsv:1` · `test/golden/golden_check.cpp:1` | 정본(phase1_gui.py) 골든 벡터와 C++ 대조기(허용 1e-12, 미구현=실패) |
| `test/unit/phase4_steer_check.cpp:1` · `test/unit/phase4_axis_gate_check.cpp:1` · `test/unit/geom_entry_check.cpp:1` · `test/unit/return_home_check.cpp:1` · `test/unit/reachable_steer_check.cpp:1` · `test/unit/rear_frame_check.cpp:1` | 단위 검사기 6종 (코어만 링크) |
| `test/sil/dock_core_sil.cpp:1` | 코어 폐루프 SIL(Phase 4 + 재접근 + 3사이클 완료 게이트, 진입 조건 지도) |
| `docs/function_table.md:1` · `docs/dock_control_code_updates.md:1` | 원본 함수표·이력(승계 사본 — 갱신 권위는 이식 후 우리 쪽) |
| `CMakeLists.txt:1` · `package.xml:1` | **신작** — 코어 정적 라이브러리 + ctest 등록(golden·unit·sil), 의존 0(빌드 도구 제외) |
| `README.md:1` | **신작** — 출처 스냅샷·무수정 계약·W2/W3 예정 명시 |

## 1-1. W2 추가 파일 (개작·신작 — 무수정 이식과 구분)

| 파일 | 성격 | 내용 |
| --- | --- | --- |
| `include/dock_control/dock_ik.hpp:1` · `src/dock_ik.cpp:1` | **개작 2곳** | 원본 dock_ik 의 steer-hold 어댑터 — IK 타입만 `TwoWsDualSteerIK`(trnav_2ws_kinematics)로 교체, 바퀴별 임계 유지·resetHold·lastSteer 계약 동일 |
| `test/golden/ik_parity_check.cpp:1` | **개작 2곳** | 골든 위임 188건(orbit/fwd)을 우리 IK 로 재계산 — QD 기하 주입으로 수학 등가 증명(비트 동일 요구), `\|v\|<1e-6` 조기반환 차이는 분리 집계 |
| `include/dock_control/dock_obs.hpp:1` · `src/dock_obs.cpp:1` | **신작** | `/wall_pose`(T_station_base) + 도킹 목표(스테이션 프레임) → 코어 관측 {e_d, e_lat, e_yaw}. 접근축은 base_link 기준 각도 설정(0=전방·±90=측면 — 실기 검증으로 확정) |
| `test/unit/dock_obs_check.cpp:1` | **신작** | 항등·축별 오프셋·로봇 회전·접근축 90°·yaw wrap·부호 대칭 |
| `test/unit/steer_hold_check.cpp:1` | **신작** | 임계 미만 바퀴별 조향 유지·resetHold·orbit/forward 경로 |

## 2. 이식 제외 (W2/W3 소관)

| 항목 | 사유 |
| --- | --- |
| `dock_ik.hpp/cpp` | QD IK(trnav_qd_kinematics) 의존 — W2 에서 `trnav_2ws_kinematics` 기반 steer-hold 어댑터 신작 |
| `gen_golden.py` | 정본 `phase1_gui.py` 가 있어야 재생성 가능 — TSV 만 자산화, 재생성은 LGIT 저장소에서 |
| ROS 조립(액션 서버) | W3 — 관측(`/wall_pose`)·지령 체인 결합과 함께 |

## 3. 전역변수표

전역 0 — 원본 규약 승계(상수 전부 인자/구조체 주입, `⟦CI:dock-no-drive-constants⟧` 정신).

## 4. 검증 기록 (2026-08-23, 이 장비에서 재실행)

- 무수정 확인: 복사 전후 md5 동일(dock_core.cpp 대표 확인)
- 빌드: plain cmake(-Wall -Wextra -Wpedantic), package.xml 없음 = colcon 무시(mcl2d_core 관례)
- **ctest 8/8 PASS** — 골든 대조 **1,805건 중 0 불일치**(허용 1e-12; 위임 188건
  orbitWheelCmd·fwdWheelCmd 는 W2 의 2WS IK 어댑터 등가 검증에서 처리) +
  단위 검사기 6종 + SIL 스모크
- **W2 검증(같은 날)**: ctest **11/11 PASS** — ik_parity 188건 **임계 밖 불일치 0**
  (허용 1e-12 — 2WS 이식본 IK 는 QD 원본과 연산 순서 차이로 1~2 ulp, 실측 최대 4.4e-16;
  `|v|<1e-6` 56건은 IK 조기반환 구간으로 steer_hold_check 가 유지 동작을 별도 검증) +
  dock_obs_check(항등·축분해·프레임 회전·wrap·부호 8케이스, e_lat 부호 변이 검증 PASS) +
  steer_hold_check(바퀴별 유지·resetHold·자유 IK 회전 표현 5케이스)
- 잔여: W3(액션 서버·지령 체인 결합·SIL/HIL) — ADR 후속 단계 참조
