
## 2026-08-25 — VERIFY 진입 d 판정을 이동평균으로 (trigger_avg_cycles)

- 무엇: kApproach 의 VERIFY 진입 판정 d 를 최근 N 주기(기본 5, 30 Hz 에서 ≈0.17 s)
  이동평균으로 교체. 제어(PID)는 순간값 유지, goal 시작·재접근 재진입 시 이력 초기화.
  `trigger_avg_cycles`=1 이면 종전 순간 판정.
- 왜: 실기(0.08/0.4 m/s 캠페인, PGV 대조)에서 순간 판독 트리거가 feature_pose x 잡음
  (±15 mm)의 하향 출렁임에 물려 조기 종료 → 종방향 잔차가 공차+잡음폭(−11.5~−36 mm)
  으로 남는 것을 확인. 사용자 가설(피드백 거리 제어 부재) 검토에서 확정, ① 승인.

## 2026-08-25 — 종방향 잔차 대책 2종 (이동평균 트리거 + 정밀 크립)

- **①(trigger_avg_cycles, 기본 5)**: VERIFY 진입 판정의 d 를 최근 N 주기 이동평균으로.
  근거: 실기 bag 분해 — 순간 판독 트리거가 관측 잡음 하강 순간 조기 종료를 만들어
  공차+잡음폭(0.4 m/s 에서 27~36 mm)이 잔차로 남음. 실기 1회 검증: 부족 33→18 mm.
- **②(creep_*, 기본 enable)**: 검증 평균에서 횡·각 공차 통과 & |d|≤creep_max_mm(80)이면
  후퇴 재접근 대신 **정밀 크립** — 내부 목표 creep_tol_mm(2)·속도 상한 creep_v_mps(0.015)
  로 kApproach 재진입, 최대 creep_attempts(2)회 후 재검증. 성공 판정 공차는 goal 그대로.
  전체 재접근(kReapproach) 진입 시 creep_mode 해제.
- 근거·승인: 사용자 목표 정밀도 ≤3 mm, "1번은 승인, 2번은 1번 후에 진행 결정" → "진행".
  측정단은 전방 보드 티치로 σ 0.69 mm 확보(pgv_ros2 계측 로그 2026-08-25 21:42 절).
- 검증: 빌드 성공 · ① 실기 1회(위) · ② SIL 폐루프(Tools/dock_sil) — 결과 아래 추기.
- ② SIL 결과(σ2 mm 잡음 플랜트): D1·D2 모두 성공, **플랜트 참오차 종 −2.8 mm**·횡
  +0.3/+0.9 mm·yaw ≤0.19° — 목표 ≤3 mm 충족. 실기 3 mm 캠페인은 보드 기준면
  스테이션에서 수행 예정(측정단 σ 0.69 mm 확보 상태).

## 2026-08-27 — 무정지 전환용 skip_settle_if_aligned 파라미터 신설

주행(translate exit_speed 체이닝) 직후 도킹으로 이어질 때 kPreYaw 의 무조건
2 s settle 이 정지를 만들던 것을, yaw 공차 내 + 스핀 미경유 진입에 한해 생략
(기본 false, 본 기체 yaml true). 스핀 경유 진입은 조향 ±90° 라 기존 settle
경로 유지. 속도 연속 조건(게이트 ≥ 진입속도/dist_kp)은 호출측(UI) 소관.
검증: colcon build · 실기 저속 무정지 전환은 후속.

## 2026-08-28 — 접근 속도 슬루 approach_decel_mps2 신설 (속도 절벽 제거)

rosbag 실측(seamless_070025)에서 무정지 전환 직후 지령이 242→30 mm/s 로 40 ms
만에 추락(near-zone 캡 즉시 적용)해 "잠시 멈춤"으로 보이는 문제를 확인.
사용자 사양 — 주행 감속(0.3 m/s²)으로 도킹 설정속도까지 내려와 전환하고, 도킹도
같은 감속률로만 지령을 낮춘다 — 에 따라 kApproach 지령에 하향 슬루
`approach_decel_mps2`(기본 0=미적용, 본 기체 yaml 0.3)를 추가. 슬루 초기값은
무정지 진입 계약(출구속도=goal max_speed)에 맞춰 goal 속도, settle 경로는 0.
near-zone·크립 전환도 이 슬루를 타고 내려간다(하향만 제한, 상향·역전 즉시).
검증: colcon build. 실기 무정지 1왕복은 후속(mcl 랙 오버런은 별건 대책 예정).

## 2026-08-28 — 사전 대기(armed) 인수 arm_engage_dist_m 신설

주행 액션과의 전환 지령 공백을 원천 제거하기 위해(사용자 설계: "도킹 노드는
이미 실행되고 그 다음에 전환") goal 수락 시 mux 를 즉시 뺏지 않는 armed 모드
추가 — `arm_engage_dist_m`(기본 0=즉시 인수, 본 기체 yaml 1.0, goal 수락 시
재독)이 >0 이면 wall 관측 잔거리 |e_d| 가 이내로 들어온 순간 mux 인수.
대기 중에는 지령·정지 미발행(주행 액션이 활성). mux 요청을 requestMux() 로
분리하고 응답 대기 기준을 요청 시각(mux_req_time_)으로 교정(armed 대기 시간이
2 s 창을 잠식하던 문제 예방). 전방 전이 틱 재실행(rerun)과 합쳐 전환 구간
0 지령이 사라진다. 검증: colcon build, 실기 1왕복은 후속.

## 2026-08-28 — armed 인수 슬루 시작점을 버스 실측 지령속도로

게이트 1.0 실주행(run_075115)에서 순항 0.95 가 인수 순간 goal 상한 0.6 으로
낙하하는 계단이 남아(슬루 초기값이 goal 속도 고정), /motor/wheel_cmd 구독
(bus_cmd_sub_·last_bus_speed_)을 추가해 armed 인수 순간 `v_slew_ =
max(goal 속도, 버스 실측)`으로 초기화 — 순항 속도부터 0.3 m/s² 로 연속
감속한다. 물리 조건상 게이트는 감속거리 이상(1.0 m/s 기준 ≥1.67, 권장 2.0 m).
검증: colcon build, 게이트 2.0 실주행은 후속.

## 2026-08-28 — armed 인수 거리를 제동거리로 자동 확장

실주행(09:02 런)에서 게이트 1.0 m·진입 0.92 m/s 로 인수 → 제동거리 1.41 m 라
목표를 지나쳐 복귀 사냥("도착 안 됨", 25 s 후 사용자 취소). armed 판정을
`max(arm_engage_dist_m, v_bus²/(2·approach_decel)+0.2)` 로 바꿔 어떤 진입
속도에서도 목표 앞에서 멈출 수 있는 지점에 이어받는다. 검증: colcon build,
실주행 후속.
