# trnav_2ws_dock_control 인벤토리 — LGIT 도킹 코어 이식 W1 (2026-08-23)

> **인벤토리(코딩 SOP §2 선행 산출물).** 신규 패키지 — 계획 단계 작성 후 구현·검증에서 확정.
> **이 파일이 루트 정본**, 패키지 병기는
> `src/Control/Motion_Control/2WS/trnav_2ws_dock_control/docs/code_review/wall-dock/`.
> 사전승인: [ADR 2026-08-23 2WS 도킹 이식](../../adr/2026-08-23-2ws-dock-port.md)
> 원본: LGIT-C6-Cobot `7a2df984` `src/Skills/Docking_Control/dock_control` — **파일 무수정 복사**.
> 함수 상세는 원본 함수표를 승계한다(복사돼 오는
> `trnav_2ws_dock_control/docs/function_table.md` — 원본 표 자체가 정본 대응 줄번호·부작용을
> 전수 등재하고 있어 여기 중복 전사하지 않는다).

## 1. 파일 구성 (전부 무수정 복사, 앵커는 파일 단위)

| 파일 | 역할 |
| --- | --- |
| `include/dock_control/dock_core.hpp:1` · `src/dock_core.cpp:1` | 도킹 순수함수 코어(PID 엔진·기하 진입·crab 합성·조향 유지 목표·IMU 방어·원위치 판정 등 20종) — ROS 의존 0 |
| `test/golden/golden.tsv:1` · `test/golden/golden_check.cpp:1` | 정본(phase1_gui.py) 골든 벡터와 C++ 대조기(허용 1e-12, 미구현=실패) |
| `test/unit/phase4_steer_check.cpp:1` · `test/unit/phase4_axis_gate_check.cpp:1` · `test/unit/geom_entry_check.cpp:1` · `test/unit/return_home_check.cpp:1` · `test/unit/reachable_steer_check.cpp:1` · `test/unit/rear_frame_check.cpp:1` | 단위 검사기 6종 (코어만 링크) |
| `test/sil/dock_core_sil.cpp:1` | 코어 폐루프 SIL(Phase 4 + 재접근 + 3사이클 완료 게이트, 진입 조건 지도) |
| `docs/function_table.md:1` · `docs/dock_control_code_updates.md:1` | 원본 함수표·이력(승계 사본 — 갱신 권위는 이식 후 우리 쪽) |
| `CMakeLists.txt:1` · `package.xml:1` | **신작** — 코어 정적 라이브러리 + ctest 등록(golden·unit·sil), 의존 0(빌드 도구 제외) |
| `README.md:1` | **신작** — 출처 스냅샷·무수정 계약·W2/W3 예정 명시 |

## 1-1. W2 추가 파일 (개작·신작 — 무수정 이식과 구분)

| 파일 | 성격 | 내용 |
| --- | --- | --- |
| `include/dock_control/dock_ik.hpp:1` · `src/dock_ik.cpp:1` | **개작 2곳** | 원본 dock_ik 의 steer-hold 어댑터 — IK 타입만 `TwoWsDualSteerIK`(trnav_2ws_kinematics)로 교체, 바퀴별 임계 유지·resetHold·lastSteer 계약 동일 |
| `test/golden/ik_parity_check.cpp:1` | **개작 2곳** | 골든 위임 188건(orbit/fwd)을 우리 IK 로 재계산 — QD 기하 주입으로 수학 등가 증명(비트 동일 요구), `\|v\|<1e-6` 조기반환 차이는 분리 집계 |
| `include/dock_control/dock_obs.hpp:1` · `src/dock_obs.cpp:1` | **신작** | `/wall_pose`(T_station_base) + 도킹 목표(스테이션 프레임) → 코어 관측 {e_d, e_lat, e_yaw}. 접근축은 base_link 기준 각도 설정(0=전방·±90=측면 — 실기 검증으로 확정) |
| `test/unit/dock_obs_check.cpp:1` | **신작** | 항등·축별 오프셋·로봇 회전·접근축 90°·yaw wrap·부호 대칭 |
| `test/unit/steer_hold_check.cpp:1` | **신작** | 임계 미만 바퀴별 조향 유지·resetHold·orbit/forward 경로 |

## 1-2. W3 파일 (신작 — 액션 조립)

| 파일 | 내용 |
| --- | --- |
| `trnav_2ws_interfaces/action/AMRMotionDockApproach.action:1` | 도킹 접근 액션 IDL(공개표면) — goal 목표/접근축/허용오차, feedback 페이즈+3축, result 사유 |
| `trnav_2ws_dock_ros/src/dock_approach_action_server.cpp:1` | 액션 서버 노드 — /wall_pose 구독, 30 Hz 제어 루프(페이즈 5종: PRE_YAW(제자리 회전·settle-then-drive)→PRE_ALIGN→APPROACH(전/후 조향 분할 yaw)→VERIFY→REAPPROACH + yaw 행 방지 가드), WheelSetArray→/motion/wheel_cmd/dock, /select_motion_source(40) 전환·원복, 신선도/유예/runaway/timeout 게이트 + VERIFY 진입 d 판정은 최근 trigger_avg_cycles(기본 5) 이동평균(제어는 순간값) |
| `trnav_2ws_dock_ros/package.xml:1` · `CMakeLists.txt:1` | ament 패키지 — 코어·어댑터 소스 직접 컴파일 |
| `trnav_2ws_dock_ros/config/dock_approach_params.yaml:1` | 게인·한계·기하(arm_m ±0.6039)·게이트 — 리터럴 금지 규약 승계 |
| `trnav_2ws_dock_ros/launch/dock_approach.launch.py:1` | params_file 인자 런치 |

## 1-3. 폐루프 SIL 킷 (`Tools/dock_sil/`, 신작)

| 파일 | 내용 |
| --- | --- |
| `dock_sil_plant.py:1` | 플랜트 노드 — `/dock_sil/wheel_cmd`(WheelSetArray) 소비 → 인라인 듀얼스티어 순기구학 적분(조향 슬루 57.1°/s 실측 반영) → 가상 벽 레이캐스트 스캔 `/dock_sil/scan`(σ=2mm 실측 잡음) 발행 + 가짜 `/dock_sil/select_motion_source` + 참값 jsonl 기록 |
| `walls_sil.yaml:1` | SIL 스테이션 벽 3면(플랜트와 동일 기하) — wall_localizer 파라미터 |
| `run_dock_sil.sh:1` | 전 체인 기동(플랜트→wall_localizer→dock 서버, 전 토픽 /dock_sil/* 리맵 격리)→액션 goal 전송→결과·참값 대조→정리 |

## 2. 이식 제외 (W2/W3 소관)

| 항목 | 사유 |
| --- | --- |
| `dock_ik.hpp/cpp` | QD IK(trnav_qd_kinematics) 의존 — W2 에서 `trnav_2ws_kinematics` 기반 steer-hold 어댑터 신작 |
| `gen_golden.py` | 정본 `phase1_gui.py` 가 있어야 재생성 가능 — TSV 만 자산화, 재생성은 LGIT 저장소에서 |
| ROS 조립(액션 서버) | W3 — 관측(`/wall_pose`)·지령 체인 결합과 함께 |

## 3. 전역변수표

전역 0 — 원본 규약 승계(상수 전부 인자/구조체 주입, `⟦CI:dock-no-drive-constants⟧` 정신).

## 4. 검증 기록 (2026-08-23, 이 장비에서 재실행)

- 무수정 확인: 복사 전후 md5 동일(dock_core.cpp 대표 확인)
- 빌드: plain cmake(-Wall -Wextra -Wpedantic), package.xml 없음 = colcon 무시(mcl2d_core 관례)
- **ctest 8/8 PASS** — 골든 대조 **1,805건 중 0 불일치**(허용 1e-12; 위임 188건
  orbitWheelCmd·fwdWheelCmd 는 W2 의 2WS IK 어댑터 등가 검증에서 처리) +
  단위 검사기 6종 + SIL 스모크
- **W2 검증(같은 날)**: ctest **11/11 PASS** — ik_parity 188건 **임계 밖 불일치 0**
  (허용 1e-12 — 2WS 이식본 IK 는 QD 원본과 연산 순서 차이로 1~2 ulp, 실측 최대 4.4e-16;
  `|v|<1e-6` 56건은 IK 조기반환 구간으로 steer_hold_check 가 유지 동작을 별도 검증) +
  dock_obs_check(항등·축분해·프레임 회전·wrap·부호 8케이스, e_lat 부호 변이 검증 PASS) +
  steer_hold_check(바퀴별 유지·resetHold·자유 IK 회전 표현 5케이스)
- **W3 조립(같은 날)**: `AMRMotionDockApproach.action` 추가(인터페이스 빌드 OK) +
  `trnav_2ws_dock_ros` 액션 서버(콜백·페이즈 4종·신선도/유예/runaway/timeout 게이트·
  mux 40 전환·모든 종료 경로 정지→원복) colcon 빌드 OK, 스모크(액션 노출·파라미터 로드,
  goal 미전송 — 실기 mux 보호) PASS. 게인·arm 초기값은 params yaml 에 재튜닝 대상 명기
- **폐루프 SIL(같은 날, Tools/dock_sil — 전 토픽 /dock_sil/* 격리·실모터 무접촉)**:
  D1(시작 0.4,0.1,2°) success — 참오차 d −3.0·lat +0.2 mm·yaw 0.19° /
  D2(수평 0.35 m, PRE_ALIGN 경로) success — d −2.9·lat +0.9 mm·yaw 0.00°.
  SIL 이 잡은 설계 결함 2건 수정: ① 속도차 yaw 는 전방 접근(조향≈0)에서 무권한·부호
  불안정(+17° 폭주 — runaway 방어선 실증 작동) → **전/후 조향 분할** 채널로 교체
  ② yaw 미달 시 완료 게이트 행 → **PRE_YAW 페이즈**(제자리 회전, settle-then-drive
  2 s) + 행 방지 가드(VERIFY 이관). d≈−3 mm 경계 수렴은 실기 튜닝 대상(v_near·dist_kp)
- 잔여: HIL(실기 — 접근축·부호 검증 사용자 입회, 게인 튜닝) — **실모터 구동은 사용자
  승인 전 금지**(2026-08-23 지시)

### 2026-08-28 파라미터·함수 추가 (dock_approach_action_server.cpp)

| 항목 | 기본 | 용도 |
| --- | --- | --- |
| `arm_engage_dist_m` | 0.0=즉시 인수 (본 기체 yaml 1.0) | 사전 대기(armed) — 실제 인수 거리는 max(설정값, 실측 버스속도 제동거리 v²/2a + 0.2 m). goal 수락 시 재독. 멤버 `arm_engage_dist_m_`·`armed_` |
| `requestMux()` | — | mux 인수 요청 분리(수락 시/게이트 진입 시 공용), 응답 대기 기준 `mux_req_time_` |
| `bus_cmd_sub_`·`last_bus_speed_` | /motor/wheel_cmd 구독 | armed 인수 순간 슬루 시작점 = 버스 실측 지령속도(순항이 goal 상한보다 높아도 계단 없음) |

### 2026-08-27 파라미터 추가 (dock_approach_action_server.cpp)

| 파라미터 | 기본 | 용도 |
| --- | --- | --- |
| `skip_settle_if_aligned` | false (본 기체 yaml true) | 무정지 전환 — yaw 공차 내·스핀 미경유 진입 시 kPreYaw settle 생략, 즉시 접근. 멤버 `skip_settle_if_aligned_` (dock_approach_action_server.cpp:122,632) |
| `approach_decel_mps2` | 0.0=미적용 (본 기체 yaml 0.3) | kApproach 지령 하향 슬루(m/s²) — 무정지 진입 시 goal 속도에서 시작해 이 감속률로만 낮춤(속도 절벽 제거). 멤버 `approach_decel_mps2_`·`v_slew_` (dock_approach_action_server.cpp:125,640) |

### 2026-08-25 파라미터 추가 (dock_approach_action_server.cpp)

| 파라미터 | 기본 | 용도 |
| --- | --- | --- |
| `trigger_avg_cycles` | 5 | VERIFY 진입 d 판정을 최근 N 주기 이동평균으로 (1=순간 판정) |
| `creep_enable` | true | 검증 잔차(횡·각 통과, \|d\|≤creep_max)면 후퇴 없이 정밀 크립 재접근 |
| `creep_tol_mm` / `creep_v_mps` | 2.0 / 0.015 | 크립 내부 목표·속도 상한 |
| `creep_max_mm` / `creep_attempts` | 80 / 2 | 크립 적용 잔차 상한·최대 횟수 |
