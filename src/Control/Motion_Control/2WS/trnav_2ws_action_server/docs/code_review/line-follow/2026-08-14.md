# line_follow 인벤토리 (계획 단계 — 신규 파일)

> 패키지 병기본. 루트 정본: `docs/code_review/line-follow/2026-08-14.md`
> ADR: `docs/adr/2026-08-14-line-follow-2ws-action.md`
> 선행(인식 계층): `docs/adr/2026-08-13-line-following-ai-port.md`

coding.md §2 「신규 파일 → 설계할 함수·전역변수를 계획 단계에서 표로 작성」에 따른
**사전 인벤토리**다. 결함 평가(severity)는 포함하지 않는다 — 구현 후 별도 리뷰 패스 소관.

## 목적

`/line/error`(`ai_msgs/LineError`)를 소비해 바닥 라인을 추종하는 2WS 액션 서버.
조향각 공간 PD + 커브 감속, 라인 소실 시 조향 유지 coast, 진행 방향 ↔ 카메라 정합 검사.

## 구성

| 파일 | 역할 |
| --- | --- |
| `include/…/line_follow/line_follow_core.hpp` | 제어 수학·소실 FSM (ROS 무의존, gtest 대상) |
| `include/…/line_follow/line_follow_action_server.hpp` | 서버 클래스 선언 |
| `src/line_follow/line_follow_action_server.cpp` | 서버 구현 (`yaw_control` 패턴) |
| `src/line_follow/line_follow_main.cpp` | 노드 진입점 |
| `config/line_follow_params.yaml` | 게인·임계 (런타임 튜닝) |
| `test/line_follow_core_test.cpp` | 코어 단위테스트 |

## 함수표

### `line_follow_core.hpp` (공개 — 순수)

| # | 함수/타입 | 입력 | 출력 | 기능 | 위치(file:line) |
| --- | --- | --- | --- | --- | --- |
| 1 | `Gains` (struct) | — | — | `kp_offset`·`kd_offset`·`kp_angle`·`max_steer_rad`·`slow_gain` | line_follow_core.hpp:18-25 |
| 2 | `Command` (struct) | — | — | `steer_rad`·`v_target` | line_follow_core.hpp:27-31 |
| 3 | `clampAbs` | `v`·`limit` | `double` | 대칭 포화 | line_follow_core.hpp:33-36 |
| 4 | `computeCommand` | `offset_f`·`offset_rate`·`angle_rad`·`max_speed`·`reverse`·`Gains` | `Command` | 조향 PD(진행방향 부호 반영·클램프) + 커브 감속 목표속도 | line_follow_core.hpp:47-58 |
| 5 | `rampSpeed` | `current`·`target`·`accel`·`dt` | `double` | 변화율 상한 속도 ramp | line_follow_core.hpp:60-64 |
| 6 | `Phase` (enum) | — | — | `WAIT_LINE`·`FOLLOWING`·`LOST_COAST`·`STOPPING` | line_follow_core.hpp:70-76 |
| 7 | `LostCoastFsm::update` | `good`·`offset`·`now_s` | `Phase` | 소실 coast 상태기계. 재개 시 `resumed()` 1회 true | line_follow_core.hpp:88-121 |
| 8 | `LostCoastFsm::phase` / `resumed` | — | `Phase` / `bool` | 상태 조회 | line_follow_core.hpp:123-130 |

### `line_follow_action_server.cpp`

| # | 함수 | 입력 | 출력 | 기능 | 위치(file:line) |
| --- | --- | --- | --- | --- | --- |
| 1 | `LineFollowActionServer::LineFollowActionServer` | `node`·`action_mutex` | — | 베이스 초기화, BicycleModel·TransientGuard·LocalizationMonitor 생성, `/line/error` 구독, 파라미터 로드 | line_follow_action_server.cpp:23-68 |
| 2 | `LineFollowActionServer::reloadTuning` | — | `void` | 게인·임계 재적재. goal 실행 직전 호출 → 재기동 없이 튜닝 | line_follow_action_server.cpp:70-90 |
| 3 | `LineFollowActionServer::lineErrorCallback` | `LineError` | `void` | 최신 스냅샷 mutex 캐시 (callback ↔ execute 스레드) | line_follow_action_server.cpp:92-98 |
| 4 | `LineFollowActionServer::getLineSnapshot` | — | `LineSnapshot` | 스냅샷 사본 반환 | line_follow_action_server.cpp:100-104 |
| 4-a | `LineFollowActionServer::resetLineSnapshot` | — | `void` | goal 진입 시 캐시 무효화 — 직전 goal 의 오차·카메라로 판단하지 않는다 | line_follow_action_server.cpp:106-114 |
| 5 | `LineFollowActionServer::expectedCamera` | `reverse` | `std::string` | 진행 방향에 대응하는 카메라 논리명 | line_follow_action_server.cpp:116-119 |
| 6 | `LineFollowActionServer::validateGoal` | `Goal` | `bool` | 속도·coast·한도 검사 | line_follow_action_server.cpp:121-150 |
| 7 | `LineFollowActionServer::execute` | `GoalHandle` | `void` | mux 선택 → Phase 0 조향정렬 → 주 루프(FSM·PD·가드·거리) → Phase 4 조향복귀 | line_follow_action_server.cpp:152-627 |

### `line_follow_main.cpp`

| # | 함수 | 기능 | 위치(file:line) |
| --- | --- | --- | --- |
| 1 | `main` | 노드 `amr_line_follow_node` 생성·spin | line_follow_main.cpp:5-16 |

### 패키징·설정 파일 (함수 없음 — 등재만)

| 파일 | 내용 | 위치 |
| --- | --- | --- |
| `line_follow_core_test.cpp` | 코어 단위테스트 (부호·클램프·감속·ramp·FSM) | line_follow_core_test.cpp:1-179 |
| `line_follow_params.yaml` | 기하·게인·임계·가드 파라미터 | line_follow_params.yaml:1-58 |
| `line_follow.launch.py` | 노드 `trnav_line_follow_node` 기동 + params 적재 | line_follow.launch.py:1-29 |
| `sil_line_follow.launch.py` | SIL 폐루프 기동 — 플랜트·mux·supervisor(13)·pose 어댑터·**가상 라인 센서**·안전 더미·액션 노드 | sil_line_follow.launch.py:1-170 |

## 클래스 멤버 상태 표 (`line_follow_action_server.hpp`)

| # | 멤버 | 기능 | 위치(file:line) |
| --- | --- | --- | --- |
| 1 | `bicycle_model_` | 자전거 모형 → IK 변환 | line_follow_action_server.hpp:37 |
| 2 | `guard_` | `TransientGuard` — 조향 미도달 시 구동 차단 | line_follow_action_server.hpp:39 |
| 3 | `loc_monitor_` | `LocalizationMonitor` — pose 두절·점프 감시, 거리 적산 | line_follow_action_server.hpp:39 |
| 4 | `line_sub_` · `line_snapshot_` · `line_mutex_` | `/line/error` 최신값 캐시 | line_follow_action_server.hpp:43-51 |
| 5 | `select_source_client_` · `motion_source_id_{13}` | mux 소스 전환 (line_follow = 13) | line_follow_action_server.hpp:61-62 |
| 6 | `gains_` | PD 게인·클램프·감속 강도 (yaml, goal 마다 재적재) | line_follow_action_server.hpp:65 |
| 7 | `accel_` · `coast_decel_` · `stop_decel_` | 가속 / coast 감속 / 정지 감속 (m/s²) | line_follow_action_server.hpp:66-68 |
| 8 | `conf_threshold_` · `resume_max_offset_` | 추종·재개 최소 신뢰도 / 재개 허용 \|offset\| | line_follow_action_server.hpp:69-70 |
| 9 | `wait_line_timeout_sec_` · `input_stale_timeout_sec_` | 시작 대기 한도 / 스트림 두절 임계 | line_follow_action_server.hpp:71-72 |
| 10 | `offset_filter_window_` | offset 이동평균 창 | line_follow_action_server.hpp:73 |
| 11 | `forward_camera_` · `reverse_camera_` | 방향별 기대 카메라 논리명 (정합 검사용) | line_follow_action_server.hpp:76-77 |
| 12 | `steer_rate_limit_` · `walk_accel_limit_` · `walk_decel_limit_` | 지령 변화율 제한 (`yaw_control` 과 동일 역할) | line_follow_action_server.hpp:80-82 |
| 13 | `gate_blocked_timeout_sec_` · `max_timeout_sec_` | 조향 미도달 지속 한도 / 전역 시한 | line_follow_action_server.hpp:83-85 |

**전역 필요성 평가**: 파일 스코프 전역·`static` 가변 없음. 전부 파라미터 캐시로 클래스 멤버가 적절.

## 결과 status 코드

| 값 | 의미 |
| --- | --- |
| 0 | 성공 (`max_duration_sec` 또는 `max_distance` 도달) |
| −1 | 취소 |
| −2 | 파라미터 부적합 |
| −3 | 전역 타임아웃 |
| −4 / −5 / −6 | 측위 두절 / 점프 / TF 실패 |
| −8 | 조향 미도달 지속 |
| −9 | 라인 소실 (시작 대기 초과 또는 coast 초과) |
| −10 | `/line/error` 스트림 두절 |
| −11 | 카메라 불일치 (진행 방향과 다른 카메라의 오차 수신) |

## 중복 확인 (coding.md §2)

| 후보 | 기존 유사물 | 판정 |
| --- | --- | --- |
| 서버 골격·mux 선택·Phase 0/4 | `yaw_control_action_server.cpp` | **패턴 복제**(코드 공유 아님) — 저장소의 10개 액션이 모두 이 형태다. 공통 코어 추출은 별건(`turn`/`turn_reverse` 중복이 이미 등재돼 있다) |
| 조향 PD | `yaw_control` 의 heading PID | 입력이 다르다(heading 오차 vs 영상 횡오차). I 항 없음 |
| 소실 coast FSM | 없음 | 신규 — 원본(2WD) 설계 계승 |
| 속도 ramp | `yaw_control` 의 `velProfile` 람다 | 코어 `rampSpeed` 로 분리해 단위검증 (같은 수식) |
| 거리 적산 | `yaw_control` 의 시작방향 투영 | **채택하지 않음** — 곡선에서 과소평가. 경로장 적산 사용 |

## 검증 (구현 직후)

| 항목 | 결과 |
| --- | --- |
| colcon 빌드 | `trnav_2ws_interfaces` · `trnav_2ws_action_server` 오류 0 (신규 파일 경고 0) |
| 단위 테스트 | **19 tests, 0 failures** (`colcon test` — 부호 규약·포화·커브감속·ramp·FSM) |
| 액션 등록 | `amr_motion_line_follow_abstract` 서버 기동 확인 (ROS_DOMAIN_ID=78) |
| [A] 정상 추종 | cam_f · offset +0.05 · 전진 2 s → **status 0**, wheel_cmd 112건, 전륜조향 −3.46~0.00°, 속도 0~+0.087 m/s |
| 조향 부호 | offset +0.05 · kp 1.2 → 3.44° 기대, **실측 −3.46°**(전진에서 음수 = 우회전) |
| [B] 카메라 불일치 | 전진 goal + `cam_r` 오차 → **status −11** |
| [C] 라인 소실 | 추종 중 `detected=False` → coast 1 s 후 **status −9**, t=2.5 s(소실 1.5 s + coast 1.0 s) |
| [D] 후진 추종 | cam_r · reverse=true → **status 0**, 전륜조향 **+0.00~+3.46°**·속도 **−0.087 m/s** — 전진과 부호만 반전 |

**스모크가 찾아낸 결함 1건(수정 완료)**: goal 사이에 남은 `line_snapshot_` 때문에 시나리오 C 가
시작 즉시 −11 로 죽었다(t=0.0 s). 직전 goal 의 카메라 이름이 캐시에 남아 있었기 때문이다.
`resetLineSnapshot()` 을 execute 진입부에 추가해 이번 goal 동안 도착한 데이터만 쓰게 했다.

**미검증**: 실기 주행. 위는 합성 `/line/error` 입력이며 **실제 조향축·구동축이 붙지 않은**
상태다. 조향 피드백(`wheel_motor_state`)이 없어 `TransientGuard` 의 gate_blocked 경로와
status −8 은 발화하지 않았다. 측위(`/robot_pose`)도 없어 거리 종료(`max_distance`)·
−4/−5/−6 경로 역시 미검증이다. 게인은 원본(2WD 차동) 값이라 이 기체에서 재조정이 필요하다.

**최종 verdict 미확정** — coding.md §5 헌법(never-self-approve)에 따라 저자가 `✅` 를 찍을 수
없다. `code_review` 패스 또는 사람 리뷰가 필요하다.
