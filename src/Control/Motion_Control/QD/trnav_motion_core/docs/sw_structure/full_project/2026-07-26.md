## 2026-07-26 21:30 (KST) — trnav_motion_core — 전체 프로젝트 SW 구조 (패키지 병기)

> 루트 정본: `docs/sw_structure/full_project/2026-07-26.md` (전체 그래프·drawio·⑤ 구조 관찰 참조)
> 본 파일은 full_project 정본의 **trnav_motion_core 해당 부분** 발췌(이중 기록). 실측 근거만.

### 역할
플랫폼 무관 라이브러리: `TrapezoidalProfile`·`TransientGuard`·`LocalizationMonitor`·`RobotGeometry`·`RecursiveMovingAverage`·`math_utils`·`ActionMutex`.

### 파일 의존 (include)
- 내부 헤더는 상호 독립(leaf). `.cpp` 는 자기 헤더만. 외부 소비:
  - `qd_wheel_set_packer.hpp`(qd) → `robot_geometry.hpp`(:4)
  - `qd_action_server_base.hpp`(qd) → `action_mutex.hpp`·`math_utils.hpp`·`robot_geometry.hpp`
  - action server 9개 → `motion_profile`·`transient_guard`·`localization_monitor`·`action_mutex`·`math_utils`

### 클래스
- `LocalizationMonitor`(:22) o-- rclcpp::Node(:23, pose_topic PoseStamped 구독) · `TrapezoidalProfile`(:45) · `TransientGuard`(:10) · `RobotGeometry`(struct :21) · `RecursiveMovingAverage`(:11) · `ActionMutexGuard`(struct :17)

### 런타임 (call)
- `LocalizationMonitor` → `<pose_topic>`(PoseStamped, sub) localization_monitor.cpp:23

---
