## 2026-07-26 21:30 (KST) — trnav_motion_action_server — 전체 프로젝트 SW 구조 (패키지 병기)

> 루트 정본: `docs/sw_structure/full_project/2026-07-26.md` (전체 그래프·drawio·⑤ 구조 관찰 참조)
> 본 파일은 full_project 정본의 **trnav_motion_action_server 해당 부분** 발췌(이중 기록). 실측 근거만.

### 역할
액션별 pilot 노드 9종(spin/turn/translate_forward/translate_reverse/crab_linear/yaw_control/yaw_control_reverse/mpc/mpc_reverse). 각 `*_main.cpp` = 실행 진입점(add_executable amr_*_node).

### 파일 의존 (include) — 정의적 엣지
- 각 `*_action_server.hpp` → `qd_action_server_base.hpp`(inherit 기반) : spin/turn:7, tfwd/trev/crab/mpc/mpcr:18, yaw/yawr:13
- 고유 controller/kinematics: spin·turn→`motion_profile`; tfwd·trev→`qd_path_controller`+`qd_bicycle_model`; crab→`qd_path_controller`+`qd_crab_inverse_kinematics`; yaw·yawr→`qd_bicycle_model`; mpc·mpcr→`qd_mpc_controller`+`qd_bicycle_model`
- 공통 코어 include(action_mutex/motion_profile/transient_guard/localization_monitor)는 정본 §부록 A 위치 전수

### 클래스 (inherit + compose)
- `{Spin,Turn,TranslateForward,TranslateReverse,CrabLinear,YawControl,YawControlReverse,Mpc,MpcReverse}ActionServer` --|> `QdActionServerBase<AMRMotion*>` (9)
- compose: 각 서버 *-- {QdPathController|QdCrabIK|QdBicycleModel|QdMpcController} + `TransientGuard` + `LocalizationMonitor` (헤더 멤버 :49-56 등)

### 런타임 (call)
- client `/select_motion_source`(SelectMotionSource): spin:36,turn:26,tfwd:42,trev:42,crab:41,yaw:68,yawr:66,mpc:183,mpcr:173 — **in-repo 서버 부재(mux 외부/미구현)**
- pub `<name>_path`(Path)·`<name>_debug`(Float64MultiArray): mpc:176-177, crab:115-116, tfwd:126-127, trev:112-113, mpcr:164-166
- pub `/motion/wheel_cmd/<action>`(WheelSetArray) — **in-repo 구독자 부재**

---
