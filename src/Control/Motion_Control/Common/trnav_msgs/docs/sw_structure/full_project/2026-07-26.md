## 2026-07-26 21:30 (KST) — trnav_msgs — 전체 프로젝트 SW 구조 (패키지 병기)

> 루트 정본: `docs/sw_structure/full_project/2026-07-26.md` (전체 그래프·drawio·⑤ 구조 관찰 참조)
> 본 파일은 full_project 정본의 **trnav_msgs 해당 부분** 발췌(이중 기록). 실측 근거만.

### 역할
모션 메시지/서비스/액션 정의(rosidl). msg 21종(WheelSet/WheelSetArray/MotorCmd/MotorState/Safety* 등), srv 8종(SelectMotionSource·LidarSafety·SafetyZone 등), action 2종(RotateAngle·TranslateDistance).

### 의존 (package.xml / CMakeLists)
- buildtool: ament_cmake, rosidl_default_generators; DEPENDENCIES builtin_interfaces std_msgs geometry_msgs (CMakeLists:60)
- 소비처: `trnav_motion_qd`·`trnav_motion_action_server`(WheelSetArray/MotorStatus/WheelMotor), `SelectMotionSource`(9 서버 client)

### 클래스
클래스 없음 — 인터페이스 정의 패키지(파일 의존/생성 코드로 대체).

---
