# cctv_webview 인벤토리 — 함수표·전역변수표 (2026-09-03)

> 목적: MJPEG(Motion JPEG) 패스스루 웹 뷰어. 카메라 JPEG 바이트를 서버가 디코드 없이
> multipart 로 흘리고, 브라우저가 디코드·표시한다. AI 검출은 좌표만 받아 브라우저가
> 오버레이로 그린다(서버는 픽셀을 만지지 않는다 — 설계 불변식).

## 함수표 — app.py (ROS 노드·서버 기동)

| 함수 | 인자 | 반환 | 용도 | 위치 |
| --- | --- | --- | --- | --- |
| `sensor_qos` | 없음 | `QoSProfile` | 퍼블리셔 호환(best-effort keep-last 1) | app.py:40 |
| `CctvWebview.__init__` | 없음 | — | 파라미터 선언(camera_topics·port·bind·stream_hz)·구독·`make_server` 기동(데몬 스레드) | app.py:50 |
| `CctvWebview._on_detections` | `name,msg` | — | DetectionArray → 좌표 dict 로 `DetectionStore.put` | app.py:97 |
| `CctvWebview._report` | 없음 | — | 30초 주기 수신 통계 로그 | app.py:105 |
| `main` | — | — | rclpy 스핀 | app.py 말미 |

## 함수표 — server.py (HTTP 계층, ROS 무관)

| 함수 | 인자 | 반환 | 용도 | 위치 |
| --- | --- | --- | --- | --- |
| `_tile` | `name,area,flip` | `str` | 카메라 1대 타일 HTML — flip 이면 img 에 `class="flip"`(CSS 180°) | server.py:31 |
| `build_index_html` | `names,stream_hz,flipped` | `str` | 대문 페이지(차량 배치 grid + 오버레이 JS + flip 표시) | server.py:101 |
| `_Handler.do_GET` | — | — | 라우팅: `/`·`/stream/<n>`·`/snapshot/<n>`·`/status`·`/detections` | server.py:194 |
| `_Handler._send_html/_send_json/_send_status` | — | — | 응답 헬퍼 | server.py:209-228 |
| `_Handler._send_snapshot` | `name` | — | 최신 JPEG 1장(원본 바이트) | server.py:230 |
| `_Handler._send_stream` | `name` | — | multipart MJPEG 루프(`get_newer_than` 로 중복 전송 방지) | server.py:242 |
| `make_server` | `store,camera_names,port,bind,stream_hz,log,detections,flipped` | `ThreadingHTTPServer` | 설정 주입 핸들러 타입 생성 | server.py:274 |

## 함수표 — frame_store.py (순수 자료구조)

| 함수 | 인자 | 반환 | 용도 | 위치 |
| --- | --- | --- | --- | --- |
| `FrameStore.put/get/get_newer_than/names/stats` | — | — | 카메라별 최신 JPEG 1장 덮어쓰기 보관(OOM 선례 방지) | frame_store.py:26-57 |
| `DetectionStore.put/snapshot` | — | — | 카메라별 최신 검출 좌표 보관·나이 계산 | frame_store.py:72-93 |
| `camera_label` | `topic` | `str` | 토픽 → 논리 이름 | frame_store.py:96 |
| `display_name` | `name` | `str` | 논리 이름 → 위치 표기 | frame_store.py:112 |

## 함수표 — launch/cctv_webview.launch.py (로스터 파생 기동)

| 함수 | 인자 | 반환 | 용도 | 위치 |
| --- | --- | --- | --- | --- |
| `_find_shared_config` | 없음 | `str\|None` | CAMERA_CONFIG env 우선, 상향 탐색으로 로스터 경로 | cctv_webview.launch.py:24 |
| `_camera_topics` | 없음 | `list[str]` | 로스터 이름 → `/<name>/image_raw/compressed` 토픽(부재 시 _FALLBACK) | cctv_webview.launch.py:40 |
| `_flipped_cameras` | 없음 | `list[str]` | 로스터 `flip: true` 카메라(빈 목록은 [""] sentinel) | cctv_webview.launch.py:52-68 |
| `generate_launch_description` | 없음 | `LaunchDescription` | port·bind·stream_hz 인자 + 노드 1개 | cctv_webview.launch.py:51 |

## 전역변수표

| 이름 | 소유 | 용도 | 위치 |
| --- | --- | --- | --- |
| `BOUNDARY` | server.py | multipart 경계 문자열 | server.py:15 |
| `LAYOUT_AREAS` | server.py | 이름→CSS grid-area(차량 배치) | server.py:25 |
| `_OVERLAY_JS` | server.py | 검출 오버레이 스크립트(신선/낡음/만료 판정) | server.py:41 |
| `POSITION_NAMES` | frame_store.py | 이름→위치 한글 표기 | frame_store.py:102 |
| `DEFAULT_TOPICS` | app.py | 로스터 부재 시 기본 토픽 | app.py:? (launch 가 로스터에서 유도) |
