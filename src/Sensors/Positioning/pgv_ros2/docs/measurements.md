# PGV 계측 기록 (도킹 정밀도 검증)

> 도킹 기준점·반복 시험의 실측 로그. Seer 측위(loc)와 PGV 태그 판독을 짝으로 기록한다 —
> loc 은 지도 좌표의 대략 위치, PGV 가 태그 기준 정밀 편차(0.1 mm/카운트, 2026-08-24 실측 확정).

## 2026-08-25 03:42 (KST) — 기준점 기록 (사용자 지시 "현재 loc 위치 기록")

| 항목 | 값 | 비고 |
| --- | --- | --- |
| Seer map | `260709_test` | |
| Seer loc x / y | **-3.7431 m / 13.9314 m** | API 1004 |
| Seer loc angle | -0.0051 rad (≈ -0.29°) | |
| Seer confidence | **0.4055** | ⚠ 낮음 — 지도 좌표는 참고치로만 쓸 것 |
| PGV tag | #0, `error=false` | Data Matrix 태그 위 |
| PGV x_raw / y_raw | **185 / 4** (18.5 mm / 0.4 mm) | 태그 원점 기준 |
| PGV angle_raw | **446** (44.6°) | 태그 방위 기준 절대각 |

- 맥락: 2026-08-24 10 mm 후진 시험 직후 위치 그대로(당시 186/4/444). 약 11시간 정치 후
  드리프트 ≤ 2카운트(≤ 0.2 mm/0.2°) — 계측 기준으로서 장기 안정성 양호.
- 도킹 검증 시 이 행을 기준으로 반복 도킹 완료 시점의 PGV 값과 대조한다.

## 2026-08-25 04:01 (KST) — 위 기준점을 smap 스테이션 **LM5** 로 등록

- 절차: 4005 제어 선점(`big-amr-claude`) → **2025 `robot_control_upload_and_loadmap_req`**
  (맵 JSON 전문 업로드+재적재, ret_code 0) → 4006 해제. 등록값 `pos=(-3.7431, 13.9314)`,
  `dir=-0.0051 rad`(= 기록 시점 loc·방향 그대로).
- 검증: 재다운로드(md5 `785f9e86169017a7e5b1962cf2647b44` 일치)에서 LM5 실재 확인,
  측위 생존(loc 불변, confidence 0.4055 → 0.4604).
- 백업: 업로드 전 원본 `Log/smap_backup/260709_test_2026-08-25_orig.smap`
  (md5 `a00294f07082d67917702abe024e6229`) — 원복은 이 파일을 2025 로 재업로드.
- ⚠ 미확인 2건: ① LM5 는 `advancedCurveList`(경로망)에 연결이 없다 — `go_target("LM5")`
  가 자유 주행으로 진입하는지, 경로망 필수라 거부되는지 미시험. ② 업로드 직전 다른 주체가
  제어를 쥐고 있었다(40020) — 선점·해제로 처리했으므로 그쪽 시스템은 재선점이 필요할 수 있다.
