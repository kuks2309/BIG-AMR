## 2026-07-26 21:30 (KST) — iahrs_driver — 전체 프로젝트 SW 구조 (패키지 병기)

> 루트 정본: `docs/sw_structure/full_project/2026-07-26.md` (전체 그래프·drawio·⑤ 구조 관찰 참조)
> 본 파일은 full_project 정본의 **iahrs_driver 해당 부분** 발췌(이중 기록). 실측 근거만.

### 역할
iAHRS IMU 시리얼 드라이버 → `imu/data`(sensor_msgs/Imu) 발행, euler angle reset 서비스/토픽.

### 클래스
- `IAHRS`(iahrs_driver.cpp:34) --|> rclcpp::Node

### 런타임 (call)
- pub `imu/data`(Imu, :52); service `all_data_reset`(interfaces/ImuReset, 광고명 :60, create @:59; `euler_angle_reset` 은 멤버·콜백명); sub `imu/reset`(Bool, 토픽명 :65, create @:64)
- 외부 경계: IMU 시리얼 포트

### 의존
- `interfaces` 패키지(ImuReset.srv) build/exec_depend

---
