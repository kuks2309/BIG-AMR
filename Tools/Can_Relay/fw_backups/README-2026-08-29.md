# 2026-08-29 판다 프로비전 (신규 보드에 검증본 플래시 — 이미지 변경 없음)

## 무엇을 했나

미프로비전 신규 판다 1대에 **기존 검증본 그대로** 플래시했다. 새 빌드 없음.

| 항목 | 값 |
|---|---|
| 대상 판다 시리얼 | `4f0040001351333033383534` (provision 상태 `unprovisioned`) |
| 플래시 전 상태 | version `DEV-23456789-DEBUG` · 서명 `a2f5a32ae348be2b49cf7686d8ab1024` · safety_mode 19 (= 우리 펌웨어 아님) |
| 설치 이미지 | `panda-firmware/board/obj/panda.bin.signed` (= `fw_backups/panda.bin.signed.athome_fix_2026-08-03_91fe4282`, md5 동일 확인) |
| 크기 · 이미지 md5 | 30,488 B (≤ 49,152 B 앱영역 한도) · `91fe428287da0e96adb0944de03073c9` |
| version | `DEV-cc5e0491-DEBUG` (obj/version 사이드카와 일치) |
| 서명 md5(앞16) | `0fb71f31270408c1` — **장치 조회(0xd3+0xd4) = 파일, 원시 128B 바이트 동일** |
| 도구 | `Tools/docking_field_kit/flash_panda.py --fw <위 이미지>` (번들 기본 이미지는 2026-07-23 구버전이라 `--fw`로 검증본 명시) |

## 어떻게

1. 사전조사: 연결 판다 1대뿐(`4f00…3854`)이며 운용 보드(`400025…3439`)·자작 R01 보드(`4c00…3495`)는
   미연결 — 대상 오인 위험 없음. `amr-can-relay.service` 는 active 이나 engage 전 USB 미점유라 충돌 없음.
2. 이미지 확정: `board/obj/panda.bin.signed` 가 검증본 athome_fix 와 md5 동일(`91fe4282…`), 사이드카
   version `DEV-cc5e0491-DEBUG`, 크기 30,488 B ≤ 49,152 B.
3. 플래시: `flash_panda.py --fw board/obj/panda.bin.signed` (사용자 승인·실행). flasher 는 섹터 1~3 소거 후
   `APP_ADDRESS_FX`(0x8004000)에 signed blob 기록.

## 검증 상태

- ✅ **장치 서명 = 저장소 이미지** (rewrite-guide §4 step 5: 파일 아닌 **장치** 조회) — `get_signature()`
  (0xd3+0xd4, 128 B) 가 `.signed` 파일 말미 128 B 와 **원시 바이트 동일**, md5 앞16 `0fb71f31270408c1`.
- ✅ version 사이드카 일치(`DEV-cc5e0491-DEBUG`), bootstub 아님(앱 구동), safety_mode 19→**0**(SAFETY_SILENT).
- ✅ **부팅 250 kbps 온-버스 정합 실증 (2026-08-29, 실기 버스 장착 후)**: SILENT 수신 8 s →
  **29,739 프레임(3,717 fps)**, `can_rx_errs` 0 전·후 유지, `can_fwd_errs`·`fault_status`·
  `heartbeat_lost` 전부 0. bus0 14,870 · bus2 14,869 프레임, 양쪽 동일 ID 12종
  `0x601~0x604`(드라이브 노드 1~4 SDO 요청)·`0x581~0x584`(응답)·`0x701~0x704`(하트비트) =
  Seer↔4축 드라이브 통신 정상, 이 보드는 SILENT 청취. §A-1 재현.
  - **배선 위상 정상**: bus0+bus2 **동시 수신**(passthrough 릴레이가 Seer↔모터 브리지 → 양쪽 미러)
    = 참조 정상 보드/운용 판다와 동일 위상. **자작 R01 보드(bus1 단일 수신, 동시 MITM 불가)와 다름**
    — 이 보드는 R01 결함 없음.
  - ⚠ `can_send_errs` 증가는 **TX 실패가 아니라 USB rx 큐 오버플로**다(고수신율+호스트 큐). SILENT 라
    애초에 송신 안 함 — 오진 금지(memory: 판다 health 카운터).
- ✅ **10분 소크 (2026-08-29 17:12~17:22, SILENT 수신)**: 30 s 간격 19 샘플 전부 `can_rx_errs` 0 유지
  (0→0), `fwd_errs`·`fault`·`heartbeat_lost` 전부 0, 프레임율 3,204 fps 안정(±3), bus0≈bus2≈
  48,060 프레임/30s 균형 지속. `can_send_errs` 954,765→954,769(+4, USB rx 큐, 무해). VERDICT PASS —
  250 kbps 정합 장시간 안정. (모니터 스크립트 1회용: 세션 스크래치패드 `monitor_bus.py`.)
- ⏳ **미검증**: 릴레이 engage/투명중계/정지(S4) 등 능동 기능 — 하드웨어(engage·차체 부양) 필요.

## 되돌리는 법

- 다른 이미지로 교체: `flash_panda.py --fw <이미지 경로>`. 판다 2대 이상 연결 시 `Panda()` 가 시리얼
  무지정으로 붙으므로 대상 외 보드 분리 확인.
- 플래시 실패로 부트스텁 갇힘: `flash_panda.py --recover` (DFU).
