# amap-2 현장 도킹 — 인수인계 (이어서 진행)

> ## ❌ 정정 2026-08-03 15:40 — 호밍 **10회 연속 실측**으로 확정. 아래 모든 블록에 우선한다
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (`orin_home_experiment.py --repeat 10`, 15:33~15:40, 접지 상태, 펌웨어 `DEV-cc5e0491-DEBUG`)
>
> **확정 (실측만)**
> - **호밍 10/10 성공**, **35.0 s**(34.99~35.16, 편차 0.17 s).
>   ENABLE → SET_SPEED → START → **WAIT(31.7 s, −리밋 탐색)** → RESTORE → GOZERO → GOZERO_W → DONE.
>   09:58 `ERR_TIMEOUT` 1회 후 **12회 연속 성공**, 재현 안 됨 — **실패해도 재시도로 충분**하다.
> - **정착값(10회 중앙, σ≈3 c)** node3 **7,882,021** · node4 **7,859,065**.
>   펌웨어 상수 `SEER_HOME_ZERO_N3/N4` = `7882020 / 7859062` 와 **1~3 counts 일치**.
>   ⇒ **값은 유지, 라벨만** 「**호밍 후 정착값(0° 아님)**」. 조향 0° 대비 **+0.178° / +0.331°**.
>   ⇒ **설계 동작이다** — 호밍은 조향을 0° 가 아니라 이 지점에 놓는다(결함 아님).
> - **`0x6098` = 1**(Home 1, −리밋). **리밋 스위치 실재** — 10회 모두 DI `0x01`→`0x09`(bit3 = −Limit).
>   ⇒ 본 문서 §2 정정 블록의 「실제 호밍 개시 트리거는 `0x60FB.04 = 1`(RstStart)」·「Home 1(음의 리밋)」
>   서술은 **10회 실측으로 재확인**됐다.
> - **counts/° = 57,344**(지령각→CAN 기울기 실측 1.000000).
>
> **⚠ 조향 0° `[7871815, 7840086]` 은 Seer 좌표계 기준이다 — 물리 직진은 미확인.**
> 아래 블록들이 근거로 드는 **11:44 교차검증은 항등식이라 반증**됐다(정본 §11-1 · §12-2):
> Seer 1005·1040 은 **둘 다 `0x6064` 유래**(독립 앵커 아님)이고, 역산식
> `0° = CAN + Seer°×57344` 는 기울기 ×57,344 = 1.0000000 이라 **CAN 항이 소거**된다.
> ⇒ 「CAN↔Seer 독립 경로」·「2회 독립 실행 일치」·「사용자 Seer 0° 확인이 이상적 조건」은 **정당화로서 무효**.
> **값 채택은 유효**(내비게이션이 Seer 좌표계를 쓴다) — 무효인 것은 「물리 0° 실측 확정」이라는 라벨이다.
>
> > ❌ **재정정 2026-08-03 17:30 — 위 「물리 직진은 미확인」은 두 질문을 뭉갠 오주장이다.**
> > - **(a) 육안 확인이 「조향 0° = 몇 counts」 역산에 기여하나 → 아니오. 판정 유지**(역산식이 항등식).
> > - **(b) 「Seer 0° 표시 = 바퀴 물리적 직진」이 성립하나 → 예.** Seer 1005/1040 은 EasyDRIVE
> >   `steerOffset` 으로 **교정된** 조향각이고, 사용자가 직진 자세에서 Seer 앞바퀴 2축 0° 를
> >   **육안 확인**했다(can_relay GUI 동일 확인).
> > - ⇒ **「물리 직진 앵커 부재」는 거짓.** 앵커는 실재하고 **정밀도가 ≈±1°(±57,344 counts)** 다 —
> >   **직진 판정에 충분**, **counts 소수 자리(193 c = 0.0034° = ±1° 의 1/297)에는 불충분**.
> > - counts 정본이 **공학적 채택값**인 이유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
> > - 「非-Seer 계측 필요」는 **「counts 소수 자리까지 판정하려면」** 조건이 붙을 때만 참이다.
>
> **폐기 (인용 금지)**: 「`Switch on disabled` 라 호밍 불가」·「`0x6098`=0 이라 비활성」·「RstStart 고착」·
> 「`0x6064`=0 은 전원 사이클로만 풀리는 래치」·「축이 이미 홈이면 무동작 즉시 완료」·
> 「CAN↔Seer 독립 교차검증」· 편차 **`+0.334°` / `+0.332°`** → **`+0.331°`**.
>
> ⚠ 본 문서 §4 의 **B(`gate`)·C(`gatecheck`)·도킹 보류는 그대로 유지**된다 —
> 이번 실측은 **호밍 동작**을 확정했을 뿐 2026-07-25 인시던트·누출 문제를 해소하지 않았다.

> ## ❌ 재정정 2026-08-03 17:00 — 바로 위 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> 위 원문은 삭제하지 않는다. 재계산 원자료: `Log/home_experiment_260803_153319_summary.json` ·
> `Log/home_experiment_260803_153319.jsonl` · `Log/home_experiment_260803_091956_summary.json` ·
> `Log/home_experiment_260803_095815_summary.json` · `Log/homing_edge_260803_144305.json` ·
> `Log/homing_edge_260803_144602.json` · `Log/homing_edge_260803_152520.json` ·
> `Log/seer_homing_260803_100813.jsonl` · `Log/steer_two_phase_260803_131305.jsonl` ·
> `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h`
>
> - **E1. `:11` 「12회 연속 성공」 → 12회.** 2026-08-03 호밍 개시는 **총 13회 = 성공 12 / 실패 1**
>   (09:58 `ERR_TIMEOUT`(`final_state`=6, 120.26 s) 1 · 14:46 1(37.0 s) · 15:25 1(34.91 s) ·
>   15:33~15:40 10). `fsm_trace` 가 빈 14:43 · 14:45×4 · 15:29 은 호밍 미개시라 제외.
>   ⚠ **「10회 연속 10/10」 은 유효.** 15:33 실행의 `baseline` 은 호밍이 아니라 **레지스터 스냅샷**이다
>   (`Tools/docking_field_kit/orin_home_experiment.py:390` `base = snapshot(rig, "baseline")`).
> - **E2. 「`0x6064`=0 은 09:58 1회 · 재현 안 됨」 → [거짓] 재현된다** — 09:19 baseline 0 ·
>   09:58 baseline·`run1_pre`·`run1_post` 0 · 10:08 `seer_homing_260803_100813.jsonl` 의 `0x6064`
>   SDO 응답 **node3 10,327/10,327 · node4 10,327/10,327 전량 0** · 리부팅 뒤 **14:43** node4 `pos`=0.
>   ⚠ **인과는 미판정** — 성공한 15:33 10회 중에도 0 이 다수 반환됐다(node3 32,801/38,764 ·
>   node4 32,726/38,737). 「0 → 실패」도 「0 은 무관」도 **양쪽 미판정**.
> - **E3. `:10` 「WAIT 31.7 s」 → WAIT 체류 31.30 s**(평균 31.302 / 중앙 31.295, 31.21~31.38).
>   31.7 s 는 **개시(t=0)~RESTORE 절대시각**(평균 31.687)이다.
> - **E4. `:9` 「35.0 s」 → 평균 35.07 · 중앙 35.05**(34.99~35.16, 편차 0.17 정확).
> - **E5. `:19` 「counts/° = 57,344, 기울기 1.000000」 → node3 57,344.0(1.0000000) /
>   node4 57,344.3(1.0000049)** — `steer_two_phase_260803_131305.jsonl` phase A 5점 회귀.
> - **E6. `:12` 「σ≈3 c」는 모표준편차 기준** — node3 **2.80** · node4 **3.21**(표본 2.95 / 3.38).
>   원자료는 node3 7,882,021×8 / 7,882,014×2, node4 7,859,065×8 / 7,859,058×2 의 **이산 2값 분포**.
> - **E7. `:15` 「설계 동작이다 (결함 아님)」 → 과잉 확정.** 확인된 것은 **재현되는 정착 동작**뿐이고
>   사양 근거는 **미확인** ⇒ **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**.
> - **E8. `:21`·`:26` 조향 0° — 값 유지, 근거 강등.** 「실측 확정」이 아니라 **공학적 채택값**이다.
>   `:91` 「실측 확정 2026-08-03 11:44」·`:102` 도 같은 강등 대상. **「실측 확정」 표현 금지.**

> ⚠ **조향 홈 관련 서술은 2026-08-02 종결 판정으로 갱신됐다. 아래 원문은 이력으로 보존한다.**
> 정본: `docs/verified_facts/2026-08-02-steer-home-closed.md`
> (값 정본은 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - 홈(0°) = **`[7871810, 7839894]`** — 직진 자세 실측(A무리). 세 경로 교차확인.
> - 구값 `7871815 / 7840086` 은 **틀린 값이 아니라 출처 없는 값**이었다 —
>   실측과 5c(**0.00009°**) / 192c(**0.0033°**) 차이로 거동상 동일하다.
> - **`7882020 / 7859062` 는 홈이 아니다** — 「**호밍 후 정착값**」(B무리)이며 0° 에서
>   **+0.178° / +0.332°** 벗어나 있다. **호밍은 조향을 0° 에 정확히 놓지 않는다.**
>   이 둘을 모두 "조향 홈"이라 부른 것이 4주간 재실험 반복의 원인이었다.
>   - ❌ **정정 2026-08-03 15:40**: 편차 표기 **`+0.332°` → `+0.331°`**(폐기된 0° 기준 잔재).
>     「홈이 아니라 호밍 후 정착값」·「0° 에 정확히 놓지 않는다」는 **판정은 10회 실측으로 확정**됐다
>     (정착값 node3 7,882,021 · node4 7,859,065, σ≈3 c — 정본 §0-2). **설계 동작이다.**
>     - ❌ **재정정 2026-08-03 17:00**: σ≈3 c 는 **모표준편차 2.80 / 3.21**(표본 2.95 / 3.38 — E6),
>       「**설계 동작이다**」는 **과잉 확정**이라 **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**
>       으로 읽는다(E7). 「홈이 아니라 정착값」·「0° 에 정확히 놓지 않는다」 판정 자체는 유효하다.
> - `debt-007` 은 **종결**(2026-08-02). 「미판정 · 값 변경 금지」 서술은 더 이상 유효하지 않다.
> - Seer 각도와 CAN counts 는 **음의 상관** — 0° 역산은 `CAN + Seer° × 57344`.

> # ❌ 정정 2026-08-03 — 바로 위 08-02 블록의 **채택값·구값 판정이 틀렸다**
> (원문은 이력으로 보존한다. 아래가 현재 정본이다.)
>
> - **조향 0° 정본 = `[7871815, 7840086]`** (구 `[7871810, 7839894]` 폐기).
>   08-02 종결은 `0° = CAN_0x6064 + Seer_deg × 57344` 역산식을 §4-2 에 적어 놓고도
>   **채택값에는 적용하지 않고 raw 판독값을 0° 로 박았다** — node3 오차 6c, node4 **193c**.
> - **실측 확정 2026-08-03 11:44** — `Tools/docking_field_kit/orin_steer_crosscheck.py`,
>   판다 SAFETY_SILENT·passthrough(제어권 미취득, 송신 0건을 AST 검사로 재확인),
>   **사용자 확인 Seer 표시 앞바퀴 2축 모두 0°**, 2회 독립 실행이 counts 단위까지 동일:
>   node3 CAN 7,871,823 + (−0.000°) → **0° 추정 7,871,816** · node4 CAN 7,840,052 + (+0.001°) → **0° 추정 7,840,087**.
>   ⚠ 용어 구분: 「실측 0° 추정」 `[7871816, 7840087]` ≠ 「**채택 정본**」 `[7871815, 7840086]`(1c 차).
>   **문서·코드에는 채택 정본을 쓴다.**
>   > ❌ **정정 2026-08-03 15:40 — 값 채택은 유지, 위 근거 3개는 무효다.**
>   > 「제어권 미취득·송신 0건(AST)」·「사용자 확인 Seer 표시 앞바퀴 2축 모두 0°」·
>   > 「2회 독립 실행이 counts 단위까지 동일」은 **결과에 기여하지 않는다** — 역산식이 **항등식**이라
>   > 바퀴를 어느 자세에 두고 실행해도 같은 값이 나온다(정본 §11-1 · §12-2).
>   > 확정된 것은 **Seer 내부 조향 영점 상수**이고 **물리적 직진은 미확인**이다.
>   > - > ❌ **재정정 2026-08-03 17:30**: 「사용자 확인 Seer 표시 앞바퀴 2축 모두 0°」가 무효인 것은
>   >   > **(a) counts 역산**에 한정된다. **「바퀴가 물리적으로 직진」이라는 사실 자체는 유효**하다 —
>   >   > Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 교정된 조향각이다(can_relay GUI 동일 확인).
>   >   > ⇒ **물리 직진 앵커는 실재**하고 **정밀도가 ≈±1°(±57,344 c)** 일 뿐이다.
>   > 「실측 확정 11:44」는 「**Seer 좌표계 기준 채택값**」으로 읽을 것.
> - ⇒ 위 `:8` 「구값 `7871815 / 7840086` 은 **출처 없는 값**이었다」는 **철회**한다.
>   **출처가 있다** — Seer 가 실시간으로 내는 `0x607A` 조향 목표다(본 문서 §2 관측이 바로 그것).
>   **값도 맞다** — 실측 0° 와 양 노드 1 count 이내.
> - ⚠ **과장 금지**: 차이 193c = **0.0034°** 로 **거동상 무의미**하다.
>   안전 문제가 아니라 **정본 정확성** 문제다.
> - ⚠ `7882020 / 7859062` 는 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)이며 0° 가 **아니다**.
>   **값은 수정하지 않고 라벨만 정정**한다. 0° 대비 편차는 재계산 결과 **+0.178° / +0.331°**
>   (위 `:43` 의 `+0.332°`, 그리고 다른 문서의 `+0.334°` 는 폐기된 0° 기준으로 계산된 잔재다).
>   *(줄번호 정정 2026-08-03 15:40: 구 `:11` → 현 `:43`. 정정 블록 추가로 이동했다.)*
> - 코드 반영 완료: `Tools/docking_field_kit/docking_drive.py:77` · `amap2_monitor.py:114`
>   모두 `STEER_HOME = {3: 7871815, 4: 7840086}`. 회귀 319건 통과.
> - 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.
>   부채 반영: `docs/debt/registry.md` debt-007(종결 유지·결론값 재정정) · debt-016(해결 유지·갱신값 재정정).

> 작성 2026-07-23 저녁(KST). 대상: amap-2(현장 PC) + 판다 #2(`1e003e...`, 우리 펌웨어) + 실 Seer + 실 모터.
> 목적: 다음 세션/내일 아침에 이 문서만 보고 바로 이어서 도킹 검증 진행.

## 0. 지금까지 확정된 상태 (검증됨)

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| 판다 #2 펌웨어 | ~~✅ `DEV-26524538-DEBUG`~~ → **구버전(2026-07-27 기준)**. 현행 빌드 = `Tools/Can_Relay/panda-firmware/board/obj/version`(현재 `DEV-d98bc1a5-DEBUG`, 빌드 시 git HEAD 로 생성 — `board/SConscript:89-92`)이며 250 kbps 부팅 기본값(`board/drivers/can_common.h:174-176`)+heartbeat fail-open(`board/main.c:257-258`) 포함 | flash 확인(2026-07-23) / 정정 근거: `docs/verified_facts/2026-07-27.md` §A-1, ADR `2026-07-27-panda-boot-bitrate-and-failsafe.md` D1·D2 |
| 전원 | ✅ 12V (voltage 12344mV) | health |
| 배선(클론 핀맵) | ✅ CAN0 H4/L5, CAN2 **H23**/L24, 12V 12·14/GND 1·26 | 실트래픽 흐름으로 입증 |
| 실 Seer↔모터 통신 | ✅ **활성** — Seer 폴링 ~22.8k/15s, 모터 응답 ~22.8k/15s | `amap2_monitor.py monitor` |
| **Seer home신호 점검** | ~~✅ **정상**~~ → **⚠ 미판정(2026-07-27)** — 관측된 것은 `0x607A` **목표값** 고정 전송뿐. 그 값이 현재 위치인지는 미판정. HOMING 모드 진입 0 은 관측 사실. | 아래 §2 + 정정 |
| amap-1 24h 신뢰성 | ✅ Run1·Run2 각 100%(총 40만+ 검증 0실패), Run3+ 연속 진행중 | amap-1 |

## 1. 킷 위치 & 구성 (amap-2)
`~/Project/T-Robotics/T-Driver-Analysis/tools/docking_field_kit/`
> **[2026-07-27 정정 — 경로]** 위는 **amap-2 기준(구)** 이며 현 로봇 PC(Ford-CATL-orin-nx)에 없다
> (`ls -d ~/Project/T-Robotics` → No such file or directory). 현 장비 사본:
> `~/Project/Ford-CATL-AMR/T-Driver-Analysis/tools/docking_field_kit/` ·
> `~/Project/CAN-Relay/docking_field_kit/`(`docs/can_relay/field-record-orin-nx-2026-07-25.md:11`).
> **정본 = 저장소 `Big-AMR/Tools/docking_field_kit/`.** 아래 `cd` 지시(§3)도 이 경로로 읽을 것.
- `panda/`(lib) · `panda.bin.signed`(펌웨어) · `flash_panda.py`
- `docking_drive.py` — 실 도킹 드라이버(PC→모터 구동, Seer 속임)
- **`amap2_monitor.py`** — 신규. 신뢰성/안전 모니터 + **Seer home신호 이상 검출**(아래)
- `seer_gate_bench.py`·`docking_scenario_bench.py` — PCAN 벤치(현장엔 PCAN 없으면 생략)
- `PINMAP.md`(⚠CAN2_H=pin23) · `RUNBOOK.md`(현장 절차)

## 2. 핵심 발견 — Seer의 home지령 (반드시 이해)

`amap2_monitor.py monitor`로 실 트래픽을 디코드한 결과:
- **Seer가 steer 노드 3·4에 `0x607A`(Target_position) = 정확히 home 값(node3=7871815, node4=7840086)을 매 사이클 지속 전송.**
  (⚠ 2026-07-27: "`= home 값`" 중 **목표값이 곧 현재 위치라는 함의는 미판정 전제**다 — 아래 정정 블록 참조. 전송 사실·값 자체는 관측 그대로.)
  > **✅ 정정 2026-08-03 — 이 관측이 곧 「구값의 출처」였다.**
  > 여기서 본 `0x607A` 목표 `7871815 / 7840086` 은 2026-08-03 실측 0°(`7871816 / 7840087`)와
  > **양 노드 1 count 이내로 일치**한다. ⇒ 2026-08-02 의 「출처 없는 값」 판정은 **철회**되고,
  > 이 값이 **조향 0° 채택 정본**이다. 다만 「목표값 = 현재 위치」라는 **함의 자체는 여전히 별개 명제**이며,
  > 확정된 것은 「Seer 가 0° 라 부르는 자세 ↔ CAN counts」의 **대응**뿐이다.
  > 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.
  > > ❌ **정정 2026-08-03 15:40** — 마지막 문장(「Seer 가 0° 라 부르는 자세 ↔ CAN counts 의 대응뿐」)이
  > > **정확한 서술이고 나머지가 과했다.** 「실측 0°(`7871816 / 7840087`)와 1 count 이내 일치」는
  > > **독립 확인이 아니다** — 그 「실측 0°」 자체가 같은 `0x6064` 에서 나온 항등식 산물이다(정본 §11-1).
  > > ⇒ 이 관측이 「구값의 **출처**」라는 판정은 **유효**하나, 「그러므로 **물리 0°**」로 격상하지 말 것.
- 값 종류 = 각 1개(변동 없음), 비home 이동·구동·~~HOMING모드(0x6060=6) 진입~~ **모두 0**.
- → ~~**정차 position-hold(정상). 작동이상 home신호 아님.**~~ (로봇이 서 있으니 Seer가 조향을 home에 붙잡아 두는 정상 서보 동작.)

> ### ⚠ 정정 (2026-07-27) — "`0x6060=6` 진입 0" 은 **호밍 부재의 증거가 아니다**
> 이 드라이브의 `0x6060` Modes_of_operation 유효값은 **1(PP)/3(PV)/4(PT)/7/8/9/10 뿐이고
> 6(CiA402 표준 Homing mode)은 지원하지 않는다** [Handbook V7.0 §6.6.3, page 152].
> 오히려 Home 1/2 는 **PP 모드(`0x6060=1`)** 에서만 유효하다 [같은 문서 §4.6, page 116].
> ⇒ `0x6060=6` 은 애초에 나올 수 없는 값이므로 그 부재는 아무것도 증명하지 않는다.
> **실차 Seer 의 실제 호밍 개시 트리거는 `0x60FB.04 = 1`(RstStart, "0-Reset off, 1-Reset on")**
> 이다 [Handbook V7.0 §6.9, page 171; Appendix I, page 196].
> 실측 시퀀스(`Log/homing_capture_220350.jsonl`):
> `0x6040=0x86` → `0x6099=2500`(t=17.918/17.919) → **`0x60FB.4=1`(t=17.925/17.926)**
> → `0x6041` bit15=0 최초 관측(t=17.956, ⚠ 전이 확정 구간 (5.138, 17.956]) → 탐색 ~29 s → `0x6000` sub 1 bit3(−Limit) 세트(t=47.025)
> → `0x6041` bit15 0→1(t=48.999/49.080) → `0x6060=1`(PP) → `0x6081=30000`/`0x6083=250`/`0x6084=250`
> → `0x607A`=조향 0° → `0x6040=0x3F`. 같은 캡처에서 `0x6098` 은 **write·read 모두 0건**이고
> 조향 controlword 는 0x3F/0x86 뿐(bit15 Reset Home 은 0건)이다.
> ⇒ 위 §0 표 :14 의 "HOMING 모드 진입 0 은 관측 사실" 및 아래 :40 의 동일 문구도 같은 이유로
> **호밍 부재 근거로 인용하지 말 것**. `amap2_monitor.py` 의 판정 집합에도 `0x60FB.04` 분기가 없어
> 실차 호밍을 미탐한다(같은 파일 docstring 정정 블록 참조 — 안전 직결).

> ## ⚠ [2026-07-27 정정 — 위 결론은 "정상 확정" 이 아니라 **미판정 모순**]
> **남는 관측 사실**(그대로 유효): Seer 가 node3·4 에 `0x607A` 목표값 `7871815`/`7840086` 을 매
> 사이클 **고정 전송**하며 값 종류는 각 1개, HOMING 모드(`0x6060=6`) 진입은 0.
>
> **취소되는 부분**: "= 정차 position-hold(정상)" 이라는 결론. 이 결론은 *그 목표값이 곧 현재 위치
> (home)* 라는 전제 위에 서 있는데, §2 가 실제로 본 것은 **목표값(`0x607A`)뿐이고 위치 피드백
> (`0x6064`)은 대조하지 않았다.** 그 전제 자체가 아직 갈리지 않은 모순으로 등록돼 있다:
> - `docs/verified_facts/2026-07-27.md` §B-1 표 — Seer 1040 encoder `−7,871,810` / `−7,840,091`
>   (position ≈ 0 rad, 운전자 육안 바퀴 0°) **vs** 판다 read node3 ≈ `−1,517` · node4 ≈ `+1,161`
>   counts. **조향 노드에서만 두 소스가 7.87M counts(=137°) 어긋난다 … 어느 쪽인지 미판정.**
>   (같은 시점 구동 노드는 부호만 반전되고 절댓값 일치.)
> - `docs/ros2_driver/2026-07-09-design-inputs.md:56,81` — ~~"부팅 시 `0x6064≈0` 이 **정상**, 홈 상수는
>   절대 목표(steerOffset **137.3°**)이며 **매 기동 시 스윙 필요**"~~.
>   > **⚠ 인용 정정 2026-08-03 — 줄번호 `:56,81` 은 죽었다. 인용 3건은 모두 철회된 상태다.**
>   > 그 파일은 2026-07-27 감사 이후로도 append 되어 자체 대응표(`:73-80`)마저 어긋난다.
>   > **줄번호가 아니라 절 제목·원문 문구로 찾을 것**(현재 위치, 2026-08-03 확인):
>   > - 구 `:56` 「부팅 직후 전 노드 `0x6064` ≈ 0 … 정상」 → 현 **`:162`** (§5-1 「콜드 스타트 브링업」).
>   >   바로 아래 `:163-167` 이 **「성립하지 않는다」로 철회**.
>   > - 구 `:56` 「홈 상수는 전원 사이클 불변」 → 현 **`:186`**. `:187` 재개방 → `:215` **철회**.
>   > - 구 `:81` §7 표 「조향 절대원점 불변성 ✓ 해소」 → 현 **`:272`**. 같은 줄에서
>   >   **⟦2026-07-27 재개방⟧ + ①②③ 철회**(0x6064≈0 / 전원 사이클 불변 / steerOffset 137.3° 단일값).
>   > ⇒ 이 3건을 **현재 근거로 인용하지 말 것.** 아래 정정 블록이 대체한다.
>   > ### ⚠ 정정 (2026-07-27) — 위 인용 3조각이 실기 캡처로 갱신됨
>   > 근거: `Log/homing_capture_220350.jsonl`(Seer 주도 호밍, 253,510 프레임 수동청취) +
>   > `References/motor_configuration/motor-config-analysis.md:118-143` + EasyDRIVE 파라미터 스크린샷.
>   > - **"steerOffset 137.3°" 라는 값은 config 에 없다.** 축별 실값은 node3 **138.000000°** /
>   >   node4 **137.250000°** 다 (`motor-config-analysis.md:24-30`, `docs/verified_facts/2026-07-27.md:215`).
>   >   홈 counts 는 여기서 파생된다 — **(steerOffset + 괄호보정) × 57,344**:
>   >   node3 (138.000000 − 0.726422) × 57344 = **7,871,816**(상수 7,871,815, Δ≈1),
>   >   node4 (137.250000 − 0.529735) × 57344 = **7,840,087**(상수 7,840,086, Δ≈1)
>   >   ⇒ 각도로는 node3 **137.27°** / node4 **136.72°** (`motor-config-analysis.md:119-121`).
>   >   > **✅ 정정 2026-08-03 — 이 파생값이 실측 0° 와 일치한다.**
>   >   > config 파생 `7,871,816 / 7,840,087` 은 2026-08-03 교차검증 실측 0° 추정과
>   >   > **완전히 같은 수**이며, 채택 정본 `[7871815, 7840086]` 과는 1c 차다.
>   >   > ⇒ 「상수 7,871,815 / 7,840,086 은 출처가 없다」는 2026-08-02 판정은 **이 계산으로도, 실측으로도 반증**된다.
>   > - **"부팅 시 `0x6064≈0` 이 정상" 은 성립하지 않는다.** 이 캡처의 baseline(t≈0.034) `0x6064` 는
>   >   node3 7,871,818 / node4 7,840,084 로 이미 홈 부근이다. `0x6064` 가 정확히 0 이 되는 구간은
>   >   **호밍 진행 중(t≈18.0~49.2)뿐**이며 그 0 은 실위치가 아니다.
>   > - **"매 기동 시 스윙" 은 이상거동이 아니라 설계된 이동이다.** 조향축에는 리밋 스위치가 실재하고
>   >   호밍 방식은 **Home 1(음의 리밋 트리거)** — 전 노드 `0x6098 = 1` 실기 판독, Handbook 기본
>   >   RstMode 도 1 [Handbook V7.0 §4.6, page 115-116]. 호밍은 원점(리밋)을 경유한 뒤
>   >   **조향 0° 로 복귀하는 데까지가 한 절차**이며 원점에 머무르지 않는다
>   >   (실측: `0x6000` sub 1 bit3(−Limit) 세트 t=47.025 → `0x6041` bit15 0→1 t=49.00/49.08 →
>   >    복귀 이동 중 bit3 해제 t=49.422). ⇒ 「원인 미상」·「이상 스윙」·「Home 36/37 기계 하드스톱」
>   >   류 서술은 오류다.
>   > - ~~⚠⚠ **홈 counts 는 전원 사이클 불변 상수가 아니다.**~~ 같은 세션 안에서 호밍 전/후 목표가 바뀐다 —
>   >   호밍 후(t≈49.14 전환) `0x607A` = node3 **7,882,020** / node4 **7,859,062** 로 t=180 까지 고정
>   >   (각 6,319 프레임). 호밍 전 값 대비 +10,205 / +18,976 counts = **+0.178° / +0.331°**.
>   >   ~~⇒ 절대 counts 를 하드코딩하지 말고 **매 기동 호밍 후 `0x6064` 리드백을 정본**으로 삼을 것.~~
>   >   > ### ❌ 정정 2026-08-03 — 위 두 문장(굵은 주장 + 권고)은 **모두 반증됐다. 따르지 말 것.**
>   >   >
>   >   > **① 「전원 사이클 불변이 아니다」 → 반증.** 조향축은 **절대 엔코더**다.
>   >   > 2026-08-03 실측에서 조향 `0x6064` 는 전원 사이클을 넘어 7.87 M 대를 유지했다
>   >   > (같은 전원 사이클에서 **구동축**은 증분 카운터라 −655,844 / −650,517 → 380 / −380 으로 리셋 —
>   >   > 이 리셋이 전원 사이클이 실제로 일어났다는 증거다). `docs/debt/registry.md` **debt-036** 참조.
>   >   > 이 오해는 **A무리(0°)와 B무리(GOZERO 정착값)를 같은 「홈」이라는 이름으로 부른 데서** 나왔다 —
>   >   > 바뀐 것은 **상수가 아니라 비교 대상**이었다.
>   >   >
>   >   > **② 「매 기동 호밍 후 `0x6064` 리드백을 정본으로」 → 따르면 안 되는 권고다.**
>   >   > 호밍 직후 리드백값은 **0° 가 아니라 GOZERO 정착값**(펌웨어 상수 `7882020 / 7859062`)이며
>   >   > 0° 에서 **+0.178° / +0.331°** 벗어나 있다. 이를 정본으로 삼으면 **정착값을 0° 로 굳힌다.**
>   >   > **정본은 config `steer_home_counts` = `[7871815, 7840086]`**
>   >   > (`src/Comm/CAN/can_relay/config/machine/foil_a082.yaml:133`, 사본
>   >   >  `src/Actuators/motor_control/config/tongyi_amr.yaml:39`).
>   >   >
>   >   > ⚠ `7882020 / 7859062` 자체는 **값을 수정하지 않는다** — GOZERO 목표로서는 옳다.
>   >   > **라벨만** 「홈/0°」 → 「**호밍 후 정착 목표(GOZERO)**」로 정정한다.
>   >   > 편차 표기도 **+0.178° / +0.331°** 로 통일한다(다른 문서의 `+0.334°` 는 폐기된 0° 기준 잔재).
>   >   > 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10 · §2.
>   >   >
>   >   > ❌ **정정 2026-08-03 15:40 — 위 ①②는 10회 실측으로 확정됐다(내용 변경 없음, 근거 승격).**
>   >   > - **② 확정**: 호밍 10회 정착값 node3 **7,882,021** · node4 **7,859,065**(σ≈3 c)이 펌웨어 상수
>   >   >   `7882020 / 7859062` 와 1~3 counts 일치. ⇒ **「호밍 후 리드백을 정본으로」는 정말 따르면 안 된다** —
>   >   >   매번 재현성 있게 정착값에 선다. 이것은 결함이 아니라 **설계 동작**이다.
>   >   > - **① 부분 정정**: 「조향축은 절대 엔코더」는 유효하나, 그 근거로 인용된
>   >   >   「`0x6064`=0 은 전원 사이클로만 풀리는 래치」는 **폐기**됐다(정본 §0-4 · §11-2).
>   >   > - **범위**: 편차 **+0.178° / +0.331°** 는 **Seer 좌표계 기준**이다. 물리 직진 앵커는 아직 없다.
>   >   >   - > ❌ **재정정 2026-08-03 17:30**: 「앵커는 아직 없다」는 **거짓**. 앵커는 **실재**하고
>   >   >     > **정밀도가 ≈±1°(±57,344 c)** 다(Seer 1005/1040 = EasyDRIVE `steerOffset` 교정 조향각 +
>   >   >     > 사용자 직진 자세 육안 확인, can_relay GUI 동일 확인). 이 편차들의 물리 절대 기준은
>   >   >     > **±1° 안에서만 보증**된다.
>   >   > - ❌ **재정정 2026-08-03 17:00** — ⓐ σ≈3 c 는 **모표준편차 node3 2.80 / node4 3.21**
>   >   >   (표본 2.95 / 3.38)이다(E6). ⓑ 「결함이 아니라 **설계 동작**」은 **과잉 확정**이다 —
>   >   >   확인된 것은 **재현되는 정착 동작**뿐이고 사양 근거는 **미확인** ⇒
>   >   >   **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**(E7).
>   >   >   「호밍 후 리드백을 정본으로 삼지 말 것」이라는 결론은 그대로 유효하다.
>   > - 위 §2·§3 의 「`0x607A` = home 값을 **매 사이클 지속 전송**」(:33) 은 **호밍 전 구간에 한정**된
>   >   관측이다. 2026-07-27 캡처에서는 그 값이 t≈17.88 에 끝나고 이후 7,882,020 / 7,859,062 로 전환된다.
>   > - 잔여 미확정: 호밍 후 정착값이 EasyDRIVE steerOffset(138.000 / 137.250) 대비 각각
>   >   **−0.5485° / −0.1988°** 낮다. 「직진 자세 = 육안 0°」 여부는 미확인
>   >   (`motor-config-analysis.md:143-144`).
> - `docs/verified_facts/2026-07-27.md` 사용 규칙 2 — **§B 항목은 "확정"으로 인용하지 않는다.**
> - config 원본도 경고를 달고 있다: `src/Actuators/motor_control/config/tongyi_amr.yaml:91`
>   `steer_home_counts: [7871815, 7840086]  # ⚠ debt-007 판정 전까지 무비판 신뢰 금지`.
>   > **⚠ 인용 정정 2026-08-03 — `tongyi_amr.yaml:91` 은 존재하지 않는 줄이다.**
>   > 해당 파일은 현재 **47줄**뿐이며, 인용한 경고 문구도 그대로 남아 있지 않다. 실제 위치:
>   > - `:39` `steer_home_counts: [7871815, 7840086]` ← **값 자체**
>   > - `:40` `# ⚠ 조향 홈 기준계 미판정 — registry.md debt-007 · docs/verified_facts/2026-07-27.md §B-1 참조`
>   > - `:25-38` 2026-08-03 정정 주석 블록(역산식·실측·GOZERO 구분)
>   >
>   > 또한 이 파일은 **정본이 아니라 사본**이다 — 정본은
>   > `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml:133`(같은 값, 같은 날 갱신 완료).
>   > 「debt-007 판정 전까지 무비판 신뢰 금지」도 더 이상 현재 상태가 아니다 —
>   > **debt-007 은 2026-08-02 종결**(결론값만 2026-08-03 재정정, 종결 자체는 유지).
>   > 잔여 미판정은 debt-007 이 아니라 **debt-036**(조향 `0x6064` 0 래치 조건 미판정)으로 승계됐다.
> - 같은 137° 가 실장비 손상 사고와 얽혀 있다(`docs/claude-mistake/2026-07-27-002`, node4 137°
>   범위이탈 갇힘 — 인과는 미확인).
>
> **판정에 필요한 측정**(verified_facts §B-1 "판정에 필요한 측정"): **✅ 2026-08-03 11:44 완료**
> ① **제어권 미획득(intercept off)** 상태에서 판다로 조향 `0x6064` 를 여러 번 직접 read
> ② 같은 순간 Seer 1040 `encoder` 동시 조회
> ③ 두 값이 부호만 다르고 절댓값이 같으면 → 판다 read 오염(제어권 중에만) 확정 /
>    계속 어긋나면 → 호밍 후 기준 재설정 확정.
>
> > ### ✅ 완료 2026-08-03 11:44 — 이 측정은 **더 이상 남은 과제가 아니다**
> > 도구 `Tools/docking_field_kit/orin_steer_crosscheck.py`, 판다 **SAFETY_SILENT·passthrough**
> > (제어권 미취득 = ① 충족, 송신 0건을 AST 구문트리 검사로 재확인), **사용자 확인 Seer 표시 0°**,
> > **2회 독립 실행이 counts 단위까지 동일**. 산출: `Log/steer_xcheck_reboot_0deg.jsonl` · `…_confirm.jsonl`.
> >
> > | 노드 | CAN `0x6064` 중앙 | Seer 1040 각도 | 0° 역산 |
> > | --- | --- | --- | --- |
> > | node3 | 7,871,823 (σ=3, n≈3,110) | −0.000° | **7,871,816** |
> > | node4 | 7,840,052 (σ=2, n≈3,110) | +0.001° | **7,840,087** |
> >
> > 판정: 부호 반전·절댓값 일치가 성립 → **판다 read 는 오염되지 않았고**, 두 소스는 같은 자세를 가리킨다.
> > 채택 정본 `[7871815, 7840086]` 과 양 노드 **1c 이내**.
> > ⇒ ①②③ 는 **완료**. 남은 미판정은 별개 항목 **debt-036**(조향 `0x6064` 가 0 으로 래치되는 조건)이다.
> > 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.
> >
> > > ❌ **정정 2026-08-03 15:40 — 「두 소스」가 아니다. ③ 판정 논리가 무효다.**
> > > Seer 1040 `motor_info.position` 은 판다가 엿듣는 **바로 그 `0x6064` 의 아핀 변환**이다
> > > (디더가 counts 단위로 일치, 기울기 ×57,344 = 1.0000000 — 정본 §12-1 · §12-2).
> > > Seer **1005 `steer_angles` 도 같은 `0x6064` 유래**다(정본 §0-3). ⇒ **독립 앵커가 아니다.**
> > > - **살아남는 것**: 「**판다 read 오염이 아니다**」는 판정. `0x6064` 실값이 판다·Seer 양쪽에서
> > >   같은 수로 읽히는 것은 관측 사실이다.
> > > - **무효**: 「두 소스가 같은 자세를 가리킨다 = 교차검증 성립」. 같은 프레임을 두 번 읽은 것이다.
> > > - ⇒ ~~**물리 직진 앵커는 여전히 부재**하다. 확정 범위는 **Seer 좌표계 기준**까지다.~~
> > >   ~~非-Seer 계측(휠 평면 대 차체 중심선 직접 계측 등)이 필요하다(정본 §0-5 · §11-5).~~
> > >   - > ❌ **재정정 2026-08-03 17:30 — 「앵커 부재」는 거짓이다.**
> > >     > 「독립 앵커가 아니다」는 **(a) counts 역산**에 대해서만 참이고, **(b) 「Seer 0° 표시 =
> > >     > 바퀴 물리적 직진」은 성립**한다 — Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로
> > >     > **교정된** 조향각이고 사용자가 직진 자세에서 앞바퀴 2축 0° 를 육안 확인했다
> > >     > (can_relay GUI 동일 확인). ⇒ **앵커 실재, 정밀도 ≈±1°(±57,344 c)**.
> > >     > 非-Seer 계측은 **counts 소수 자리(193 c = 0.0034°)까지 판정하려면** 필요하다.
>
> 판정 전 안전 규칙: `allow_homing_motion` 게이트(홈 5° 이탈 시 브링업 거부)를 **끄지 말 것**
> (verified_facts §B-1). 숫자는 어느 것도 변경하지 않았다.

**함의(도킹 안전):** 도킹 중(PC 주도)에도 Seer는 이 home지령을 계속 쏜다. 게이트가 이를 **차단**하지 않으면
PC의 구동/조향 명령과 모터에서 충돌한다. 그래서 `SEER_GATE`(auth=PC 시 0x601–604 write 차단+가짜ack)가 필수.
→ 내일 도킹 전 **`gatecheck`로 이 차단을 실측 검증**할 것(§4-C).

## 3. amap2_monitor.py 사용법
```bash
cd ~/Project/T-Robotics/T-Driver-Analysis/tools/docking_field_kit
python3 amap2_monitor.py monitor 30      # A: passthrough 리슨(무개입/안전). Seer폴링·모터응답·home신호 판정
python3 amap2_monitor.py monitor 28800   # A 8시간(야간 무인 안전). 로그 append
python3 amap2_monitor.py gate 30         # B: intercept 투명중계(auth=Seer). 판다 삽입이 통신 교란 안 하는지 (attended)
python3 amap2_monitor.py gatecheck 30    # C: auth=PC 게이트가 Seer home지령을 모터측에서 차단하는지 (attended, 모터 안 움직임)
python3 amap2_monitor.py seq 30 30       # A→B 순차 후 자동 passthrough 복귀
```
- 판정: `정상` = Seer폴링+모터응답 활성 & HOMING재진입 0 & (gatecheck 시) 누출 0.
- 로그: `~/docking_reliability/amap2_monitor.log`
- **안전**: monitor(A)는 무개입(야간 OK). gate(B)/gatecheck(C)는 판다를 실버스에 삽입 → **반드시 지켜보는 상태**에서. 종료 시 자동 passthrough 복귀. ~~USB 끊기면 판다 heartbeat 타임아웃(~5s)으로 fail-safe passthrough.~~
  > **[2026-07-27 정정]** heartbeat 상실 시 펌웨어는 **`SAFETY_SILENT` 로 복귀**한다 —
  > `Tools/Can_Relay/panda-firmware/board/main.c:248-249`. "fail-safe passthrough 복귀"는 부정확
  > (`docs/verified_facts/2026-07-27.md` §A-5 가 이 문구를 명시적으로 부정확 판정).
  > 릴레이 해제(`set_intercept_relay(false)`+`pc_authority=false`)는 **2026-07-27 추가분**
  > (`board/main.c:257-258`, ADR `2026-07-27-panda-boot-bitrate-and-failsafe.md` Decision 2)이며,
  > 이 문서가 전제하는 2026-07-23 시점 펌웨어(`panda.bin.signed`, md5 `d4188e02…`)에는 **없다**.
  > 타임아웃도 조건부: ignition on **5s** / off **2s**(`board/main.c:164-165`, `:233`
  > `check_started()` 분기). 단 릴레이 부팅 기본은 물리 통과다(`board/drivers/harness.h:91`,
  > verified_facts §A-3).

## 4. 다음 세션/내일 아침 권장 순서

> ## ⛔ [2026-07-27 정정 — 아래 순서는 **2026-07-25 실행 결과로 반증됨. 보류**] ⛔
> 이 문서는 2026-07-23 작성(:3)이고, 그 뒤 실차에서 아래 단계들이 실제로 실행돼 실패했다
> (`docs/can_relay/field-record-orin-nx-2026-07-25.md`):
> - **A(`monitor`)** — `:30` H0 통과(✅ 정상). 무개입 리슨이므로 **이 항목만 유효**하다.
> - **B(`gate`)** — `:31` intercept 전환 순간 Seer 가 node3·4 에 HOMING 설정(`0x6099=2500`) 발행 →
>   `:68-71` **Seer 리셋 인시던트**로 종료, `:32` H2 미실행·**HIL 전면 중단**.
> - **C(`gatecheck`)** — `:33` Seer write(`0x2B` controlword=0x3F, `0x23` target_position)가 **모터측으로
>   누출**. 즉 §4-C 가 요구한 "누출 0" 이 **실패**했다.
> - **도킹** — `:56` 실모터 구동(`docking_drive.py`)·추가 intercept 는 **원인 규명 전까지 보류,
>   실 구동 전 사용자 확인 필수**. `:54` 현 상태 판다 intercept/게이트 방식은 **실차 비안전**.
> - 왜 중대한가: `:49` **Seer 는 리셋되면 복구 시 항상 재호밍 = 조향 물리 이동 동반(안전 직결)**.
>
> ⇒ **B·C·도킹은 보류.** 원문은 이력으로 남기되 실행 가능한 절차로 읽지 말 것.
> (`docs/verified_facts/2026-07-27.md` 사용 규칙 3 — 반증 시 원 기록에도 정정을 남긴다.)

- **A (안전, 지금/야간 가능)**: `monitor`로 실 통신 건전성 + home신호 정상 재확인.
- **B (attended)** ⛔**보류(2026-07-25 인시던트)**: `gate 30` — 판다 삽입(투명중계)이 Seer↔모터를 교란하지 않는지. Seer가 calibrating 루프/재호밍 안 뜨는지 확인.
- **C (attended, 도킹 직전 필수)** ⛔**보류(2026-07-25 누출 발생 = 게이트 차단 실패)**: `gatecheck 30` — auth=PC에서 Seer의 지속 home지령이 모터측(bus2)에 **누출 0**인지. (모터는 안 움직임.)
- **도킹 (attended, 안전구역+E-STOP)** ⛔**보류(field-record :56, 사용자 승인 필수)**: `python3 docking_drive.py` → `take`→`enable`→`f 30`(저속 방향확인)→도킹→`stop`→`release`. 상세 RUNBOOK.md §5–6(RUNBOOK §5 도 동일 사유로 보류 표기됨).

## 5. amap-1 병행 신뢰성 (참고)
- 연속 러너 `~/run_reliability_loop.sh` → `reliability_24h.py`를 24h마다 자동 반복(Run3,4,…). 결과 `~/docking_reliability/run{N}_result.txt`, 진행 `~/docking_reliability/loop.log`·`live_status.txt`.
- Run1·Run2 = 각 24h/100%(0실패). 저장소 기록: `docs/can_relay/reliability-24h-results.md`.

## 6. 주의(교훈)
- 클론 핀맵: **CAN2_H=pin23**(pin22 死핀). comma 표준 믿지 말 것.
- gate/gatecheck/도킹은 판다 삽입 → 사람이 볼 때만. monitor는 무개입이라 야간 OK.
- 도킹 종료 시 `release` 필수(Seer 반환). 아니면 Seer가 다음 노드로 못 감.
