# 새 세션 시작 프롬프트 (복붙용)

> ## ❌ 정정 2026-08-03 15:40 — 호밍 **10회 연속 실측**으로 확정. 아래 모든 블록·복붙 블록에 우선한다
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (`orin_home_experiment.py --repeat 10`, 15:33~15:40, 접지 상태, 펌웨어 `DEV-cc5e0491-DEBUG`)
>
> - **호밍 10/10 성공**, **35.0 s**(편차 0.17 s). WAIT(−리밋 탐색) **31.7 s** → RESTORE → GOZERO → GOZERO_W → DONE.
>   09:58 `ERR_TIMEOUT` 1회 후 **12회 연속 성공**, 재현 안 됨 — 실패해도 **재시도로 충분**.
> - **정착값(10회 중앙, σ≈3 c)** node3 **7,882,021** · node4 **7,859,065** → 조향 0° 대비 **+0.178° / +0.331°**.
>   펌웨어 상수 `7882020 / 7859062` 와 1~3 c 일치. **값 유지, 라벨은 「호밍 후 정착값(0° 아님)」. 설계 동작.**
> - **`0x6098` = 1**(Home 1, −리밋) · **리밋 실재**(10회 DI `0x01`→`0x09`) · **counts/° = 57,344**.
> - **⚠ 조향 0° `[7871815, 7840086]` 은 Seer 좌표계 기준** — 물리 직진 **미확인**.
>   Seer **1005·1040 은 둘 다 `0x6064` 유래**라 독립 앵커가 아니다.
>   - > ❌ **재정정 2026-08-03 17:30 — 「물리 직진 미확인 / 앵커 부재」는 거짓이다. 두 질문을 뭉갰다.**
>     > - **(a) 육안 확인이 「조향 0° = 몇 counts」 역산에 기여하나 → 아니오. 판정 유지**(역산식이 항등식).
>     > - **(b) 「Seer 0° 표시 = 바퀴 물리적 직진」이 성립하나 → 예.** Seer 1005/1040 은 EasyDRIVE
>     >   `steerOffset` 으로 **교정된** 조향각이고, 사용자가 직진 자세에서 Seer 앞바퀴 2축 0° 를
>     >   **육안 확인**했다(can_relay GUI 동일 확인).
>     > - ⇒ 앵커는 **실재**, **정밀도 ≈±1°(±57,344 counts)** — 직진 판정엔 충분,
>     >   **counts 소수 자리(193 c = 0.0034° = ±1° 의 1/297)** 판정엔 불충분.
>     > - counts 정본이 **공학적 채택값**인 이유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
>
> **폐기 (복붙 블록 안에서도 인용 금지)**: 「`Switch on disabled` 라 호밍 불가」·「`0x6098`=0 이라 비활성」·
> 「RstStart 고착」·「`0x6064`=0 은 전원 사이클 래치」·「이미 홈이면 즉시 완료」·
> 「CAN↔Seer 독립 교차검증」· **`+0.334°` / `+0.332°`** → **`+0.331°`**.
>
> ⚠ **아래 복붙 블록(``` 안)에도 같은 정정을 직접 반영했다.** 블록만 붙여넣어도 폐기 주장은 들어가지 않는다.

> ## ❌ 재정정 2026-08-03 17:00 — 바로 위 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> 위 원문은 삭제하지 않는다. **아래 복붙 블록(``` 안)에도 이 재정정을 직접 반영했다.**
> 재계산 원자료: `Log/home_experiment_260803_153319_summary.json` ·
> `Log/home_experiment_260803_153319.jsonl` · `Log/home_experiment_260803_091956_summary.json` ·
> `Log/home_experiment_260803_095815_summary.json` · `Log/homing_edge_260803_144305.json` ·
> `Log/homing_edge_260803_144602.json` · `Log/homing_edge_260803_152520.json` ·
> `Log/seer_homing_260803_100813.jsonl` · `Log/steer_two_phase_260803_131305.jsonl` ·
> `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h`
>
> - **E1. `:9` 「12회 연속 성공」 → 12회.** 2026-08-03 호밍 개시는 **총 13회 = 성공 12 / 실패 1**
>   (09:58 `ERR_TIMEOUT`(`final_state`=6, 120.26 s) 1 · 14:46 1(37.0 s) · 15:25 1(34.91 s) ·
>   15:33~15:40 10). `fsm_trace` 가 빈 14:43 · 14:45×4 · 15:29 은 호밍 미개시라 시도에서 제외.
>   ⚠ **「10회 연속 10/10」 은 유효.** 15:33 실행의 `baseline` 은 호밍이 아니라 **레지스터 스냅샷**이다
>   (`Tools/docking_field_kit/orin_home_experiment.py:390` `base = snapshot(rig, "baseline")`).
> - **E2. `:219` 「`0x6064`=0 은 09:58 1회 관측 후 재현 안 됨」 → [거짓] 재현된다.**
>   09:19 baseline `pos3`/`pos4` = 0 · 09:58 baseline·`run1_pre`·`run1_post` 전부 0 ·
>   10:08 `seer_homing_260803_100813.jsonl` 의 `0x6064` SDO 응답 **node3 10,327/10,327 ·
>   node4 10,327/10,327 전량 0** · 리부팅 뒤 **14:43** `homing_edge_260803_144305.json` node4 `pos`=0.
>   ⚠ **다만 인과는 미판정** — 성공한 15:33 10회 중에도 0 이 다수 반환됐다
>   (node3 32,801/38,764 · node4 32,726/38,737). 「0 → 실패」도 「0 은 무관」도 **양쪽 미판정**.
> - **E3. `:8` 「WAIT 31.7 s」 → WAIT 체류 31.30 s**(평균 31.302 / 중앙 31.295, 31.21~31.38).
>   31.7 s 는 **개시(t=0)~RESTORE 절대시각**(평균 31.687)이다.
> - **E4. `:8` 「35.0 s」 → 평균 35.07 · 중앙 35.05**(34.99~35.16, 편차 0.17 정확).
> - **E5. `:12` 「counts/° = 57,344」 → node3 57,344.0 / node4 57,344.3**
>   (`steer_two_phase_260803_131305.jsonl` phase A 5점 회귀 = 1.0000000 / 1.0000049).
> - **E6. `:10` 「σ≈3 c」는 모표준편차 기준** — node3 **2.80** · node4 **3.21**(표본 2.95 / 3.38).
> - **E7. `:11` 「설계 동작」 → 과잉 확정.** 확인된 것은 **재현되는 정착 동작**뿐이며 사양 근거는
>   **미확인**이다 ⇒ **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**.
> - **E8. `:13` 조향 0° — 값 유지, 근거 강등.** 「실측 확정」이 아니라 **공학적 채택값**이다.
>   **「실측 확정」 표현 금지.**

> ⚠ **조향 홈 정본**: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0 — 조향 0° = **`[7871815, 7840086]`**(Seer 좌표계 기준).

> # ❌ 정정 2026-08-03 — 바로 위 08-02 블록의 **채택값·구값 판정이 틀렸다**
> (원문은 이력으로 보존한다. 아래가 현재 정본이다. 아래 복붙 블록 안에도 같은 정정을 직접 반영했다.)
>
> - **조향 0° 채택 정본 = `[7871815, 7840086]`** (구 `[7871810, 7839894]` 폐기).
>   08-02 종결은 역산식 `0° = CAN_0x6064 + Seer_deg × 57344` 를 §4-2 에 적어 놓고도
>   **채택값에 적용하지 않고 raw 판독값을 0° 로 박았다** — node3 오차 6c, node4 **193c**.
> - **실측 확정 2026-08-03 11:44** — `orin_steer_crosscheck.py`, 판다 SILENT·passthrough,
>   송신 0건(AST 검사), **사용자 확인 Seer 표시 0°**, 2회 독립 실행 동일:
>   node3 **0° 추정 7,871,816** · node4 **0° 추정 7,840,087**.
>   ⚠ 「실측 0° 추정」 `[7871816, 7840087]` ≠ 「**채택 정본**」 `[7871815, 7840086]`(1c 차) — 문서엔 **채택 정본**을 쓴다.
>   > ❌ **정정 2026-08-03 15:40** — **값 채택은 유지, 위 근거는 무효.**
>   > 「송신 0건(AST 검사)」·「사용자 확인 Seer 표시 0°」·「2회 독립 실행 동일」은 **결과에 기여하지 않는다** —
>   > 역산식이 **항등식**이라 자세와 무관하게 같은 값이 나온다(정본 §11-1 · §12-2).
>   > 확정 범위는 **Seer 좌표계 기준**이고 **물리 직진은 미확인**이다.
>   > - > ❌ **재정정 2026-08-03 17:30**: 「사용자 확인 Seer 표시 0°」가 무효인 것은 **(a) counts 역산**
>   >   > 에 한정된다. **「바퀴가 물리적으로 직진」이라는 사실 자체는 유효**하다(Seer 1005/1040 =
>   >   > EasyDRIVE `steerOffset` 교정 조향각, can_relay GUI 동일 확인). **물리 직진 앵커는 실재**하고
>   >   > **정밀도가 ≈±1°(±57,344 c)** 일 뿐이다.
> - ⇒ 위 `:8` 「구값은 **출처 없는 값**이었다」는 **철회**. 출처는 **Seer 의 실시간 `0x607A` 조향 목표**이고, 값도 맞다.
> - ⚠ **과장 금지**: 차이 193c = **0.0034°**, **거동상 무의미**. 안전이 아니라 정본 정확성 문제다.
> - ⚠ `7882020 / 7859062` 는 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)이며 0° 아님.
>   **값 수정 금지, 라벨만 정정.** 0° 대비 편차 재계산 **+0.178° / +0.331°**
>   (위 `:30` 의 `+0.332°` 및 타 문서의 `+0.334°` 는 폐기된 0° 기준 잔재).
>   *(줄번호 정정 2026-08-03 15:40: 구 `:11` → 현 `:30`. 정정 블록 추가로 이동했다.)*
> - 코드 반영 완료: `docking_drive.py:77` · `amap2_monitor.py:114` 모두 `STEER_HOME = {3: 7871815, 4: 7840086}`. 회귀 319건 통과.
> - 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10.

> 새 Claude Code 세션을 열고 아래 블록을 그대로 붙여넣으면 이어서 진행됩니다.
> (프로젝트 루트 `d:\kkw2\2026\Project\CATL-Ford\CAN-Relay` 에서 열 것.)

---

## ⛔ [2026-07-27 정정] 이 프롬프트를 **그대로 붙여넣지 말 것** ⛔

이 프롬프트는 **2026-07-23 기준**(본문 :11 "현재 상태(2026-07-23 저녁 확정)")이며, 그 이후 실차에서
아래 항목들이 **반증·보류**됐다. 배경을 모르는 새 세션이 이 블록을 그대로 받으면 곧장 실차
intercept·실모터 구동으로 유도된다.

| 프롬프트 항목 | 2026-07-27 판정 | 근거 |
| --- | --- | --- |
| 1) `monitor 30` | 유효(무개입 리슨) | field-record `:30` H0 ✅ |
| **2) `gate 30`** | ⛔ **보류** — intercept 전환이 Seer 리셋 유발(2026-07-25 인시던트) | `docs/can_relay/field-record-orin-nx-2026-07-25.md:31`, `:68-71` |
| **3) `gatecheck 30`** | ⛔ **보류** — 실차에서 Seer write 가 모터측으로 **누출**(차단 실패) | 같은 문서 `:33` |
| **4) `docking_drive.py` 실도킹** | ⛔ **보류** — 원인 규명 전까지 보류, 실 구동 전 사용자 확인 필수 | 같은 문서 `:56`, `:54`(실차 비안전) |
| 본문 "정상 position-hold" 단정 | ⚠ **미판정 모순** (아래 참조) | `docs/verified_facts/2026-07-27.md` §B-1 |
| 본문 `0x607A=7871815/7840086` | ✅ **2026-08-03 정정 — 이 값이 조향 0° 채택 정본이다** (실측 0° 추정 `7871816/7840087` 과 1c 이내). 「출처 없는 값」 판정 철회 | `docs/homing/2026-08-03-can-relay-homing-assets.md` §10 |
| 본문 인용 `design-inputs.md:56,81` | ❌ **인용 3건 철회됨 + 줄번호 사망**(현 `:162` / `:186` / `:272`) | 같은 파일 in-place 철회 주석 |
| 본문 인용 `tongyi_amr.yaml:91` | ❌ **존재하지 않는 줄**(파일 총 47줄, 실제 `:39`) | `wc -l` / `grep -n steer_home_counts` |
| 본문 "판정에 필요한 측정" | ✅ **2026-08-03 11:44 완료** (intercept off 판다 read + Seer 1040 대조) | `Log/steer_xcheck_reboot_0deg.jsonl` |
| 킷 경로 `~/Project/T-Robotics/...` | ❌ 현 장비에 없음 | `ls -d ~/Project/T-Robotics` → 없음 / 실제 경로 field-record `:11`, 정본 `Big-AMR/Tools/docking_field_kit/` |
| 펌웨어 `DEV-26524538` | 구버전 — 현행 빌드는 250 kbps 부팅 기본값+heartbeat fail-open 포함 | `Tools/Can_Relay/panda-firmware/board/obj/version`, ADR `2026-07-27-panda-boot-bitrate-and-failsafe.md` D1·D2 |

> ❌ **정정 2026-08-03 15:40 — 위 표 2행의 라벨 정정**
> - `본문 0x607A=7871815/7840086` 행: 「이 값이 조향 0° 채택 정본」은 **유지**하되,
>   괄호의 「실측 0° 추정 `7871816/7840087` 과 1c 이내」를 **독립 확인으로 읽지 말 것** —
>   그 추정 자체가 같은 `0x6064` 에서 나온 **항등식** 산물이다(정본 §11-1). **Seer 좌표계 기준**이다.
> - `본문 "판정에 필요한 측정"` 행: **완료 판정은 유지**(판다 read 오염 아님)하되,
>   그 측정이 「CAN↔Seer 교차검증」이었다는 성격 규정은 **무효**다 — 1005·1040 **둘 다 `0x6064` 유래**다.
> - 근거 링크는 §10 이 아니라 **§0**(§10 은 값만 유효, 근거 서술은 §11-1 이 반증)을 볼 것.

중대성: field-record `:49` — **Seer 는 리셋되면 복구 시 항상 재호밍 = 조향 물리 이동 동반(안전 직결).**

원문은 이력 보존을 위해 아래에 그대로 남긴다.

---

```
CATL-Ford CAN 릴레이 도킹 프로젝트를 이어서 진행합니다.

현재 상태(2026-07-23 저녁 확정):
- amap-2(현장)에 판다 #2(우리 펌웨어 DEV-26524538: RTR+릴레이분리+SEER_GATE)를 seer↔panda↔모터로 배선 완료, 실통신 활성 확인함.
  [2026-07-27 정정] DEV-26524538 은 구버전이다. 현행 빌드(Tools/Can_Relay/panda-firmware/board/obj/version 참조)에만 부팅 기본 250 kbps(board/drivers/can_common.h:174-176)와 heartbeat fail-open(board/main.c:257-258)이 들어있다(ADR 2026-07-27-panda-boot-bitrate-and-failsafe.md D1·D2). 킷 동봉 panda.bin.signed(2026-07-23)로 재플래시하지 말 것.
- Seer가 정차 중 steer(node3·4)에 target_position(0x607A=7871815/7840086)을 지속 지령함(관측). 작동이상 HOMING 모드 진입 0(검출기로 확인).
  [2026-07-27 정정] 이 목표값이 곧 "현재 위치(home)" 인지는 **미판정 모순**이다 — 따라서 "= 정상 position-hold" 로 단정하지 말 것.
  [2026-08-03 정정] 이 관측이 곧 "구값의 출처" 였다. 여기 적힌 0x607A 목표 7871815/7840086 이 조향 0° 채택 정본이다.
    ⇒ 조향 0° 채택 정본은 [7871815, 7840086] 이다(구 [7871810, 7839894] 폐기 — 그것은 0° 가 아니라 Seer 가 0° 를 가리키지 않던 자세의 raw 판독값이었다).
    ⇒ 2026-08-02 의 "구값은 출처 없는 값" 판정은 철회됐다. 출처는 Seer 의 실시간 0x607A 조향 목표다.
    ⚠ [정정 2026-08-03 15:40] 이 값은 "Seer 좌표계 기준" 이다. 물리적 직진은 미확인이다.
      - "실측 0° 추정 7871816/7840087 과 1 count 이내로 일치" 를 독립 확인으로 인용하지 말 것. 그 추정 자체가 같은 0x6064 에서 나온 항등식 산물이다
        (역산식 0° = CAN + Seer°×57344 는 기울기 ×57344 = 1.0000000 이라 CAN 항이 소거된다 — 바퀴를 어디에 두고 실행해도 같은 값이 나온다).
      - Seer 1005(steer_angles) 와 1040(motor_info.position) 은 둘 다 0x6064 유래다. 독립 앵커가 아니다.
      - ⇒ "CAN 과 Seer 의 독립 경로 교차검증", "2회 독립 실행 일치", "사용자 Seer 0° 확인이 이상적 조건" 은 전부 무효다. 인용 금지.
      - 값 채택 자체는 유효하다(내비게이션이 Seer 좌표계를 쓴다). 무효인 것은 "물리 0° 실측 확정" 이라는 라벨뿐이다.
      - [폐기 — 아래 재정정으로 대체] 물리 직진 앵커는 아직 없다. 필요하면 非-Seer 계측(휠 평면 대 차체 중심선 직접 계측 등)을 먼저 해야 한다.
      ❌ [재정정 2026-08-03 17:30] 바로 위 두 줄은 서로 다른 두 질문을 뭉갰다. "물리 직진 앵커 부재" 는 거짓이다.
        (a) 육안 확인이 "조향 0° = 몇 counts 인가" 역산에 기여하나 → 아니오. 이 판정은 유지된다(역산식이 항등식).
        (b) "Seer 0° 표시 = 바퀴가 물리적으로 직진" 이 성립하나 → 예. Seer 1005/1040 은 EasyDRIVE steerOffset 으로
            교정된 조향각이고(같은 파일 아래 "steerOffset 실값 node3 138.000° / node4 137.250°" 참조),
            사용자가 바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 육안 확인했다(can_relay GUI 로도 동일 확인).
        ⇒ 물리 직진 앵커는 실재한다. 앵커 정밀도는 ≈±1°(±57,344 counts) — 직진 판정에는 충분하고,
           counts 소수 자리(193 c = 0.0034°, ±1° 의 1/297) 판정에는 불충분하다.
        ⇒ counts 정본이 "공학적 채택값" 인 이유는 "앵커 부재" 가 아니라 "앵커 정밀도 부족" 이다.
        ⇒ 非-Seer 계측(휠 평면 대 차체 중심선 직접 계측 등)은 "counts 소수 자리까지 판정하려면" 이라는 조건에서만 필요하다.
    ⚠ 차이 193c = 0.0034° 로 거동상 무의미하다 — 안전 문제가 아니라 정본 정확성 문제다. 과장 금지.
    ⚠ 7882020/7859062 는 홈(0°)이 아니라 펌웨어 GOZERO 상수(호밍 후 정착 목표)다. 값은 수정하지 말고 라벨만 정정할 것. 0° 대비 편차 +0.178°/+0.331° (Seer 좌표계 기준).
    ⚠ "호밍 후 0x6064 리드백을 정본으로 삼으라" 는 과거 권고를 따르지 말 것 — 그 리드백은 GOZERO 정착값이라 정착값을 0° 로 굳힌다. 정본은 config steer_home_counts 다.
    근거: docs/homing/2026-08-03-can-relay-homing-assets.md §0 (§10 은 값만 유효, 근거 서술은 §11-1 이 반증). 코드는 이미 반영됨(docking_drive.py:77 · amap2_monitor.py:114 = {3: 7871815, 4: 7840086}).

  [2026-08-03 15:40 확정 — 호밍 10회 연속 실측] orin_home_experiment.py --repeat 10 (15:33~15:40, 접지, 펌웨어 DEV-cc5e0491-DEBUG)
    - 호밍 10/10 성공, 35.0 s (34.99~35.16, 편차 0.17 s). ENABLE → SET_SPEED → START → WAIT(31.7 s, −리밋 탐색) → RESTORE → GOZERO → GOZERO_W → DONE.
    - 정착값(10회 중앙, σ≈3 counts): node3 7,882,021 / node4 7,859,065. 펌웨어 상수 7882020/7859062 와 1~3 counts 일치.
      ⇒ 호밍은 조향을 0° 가 아니라 이 지점에 놓는다. 결함이 아니라 설계 동작이다.
    - 0x6098 = 1 (Home 1, 음의 리밋). 리밋 스위치 실재 — 10회 모두 DI 0x01→0x09 (bit3 = −Limit).
    - counts/° = 57,344 (지령각→CAN 기울기 실측 1.000000).
    - 09:58 ERR_TIMEOUT 1회 이후 12회 연속 성공, 재현되지 않는다. 실패해도 ERR_TIMEOUT 은 깔끔한 terminal 이라 재시도로 충분하다.
    ⛔ 다음 주장들은 폐기됐다. 인용 금지:
       "Switch on disabled 라 호밍이 막힌다" / "0x6098=0 이라 호밍 비활성" / "RstStart 가 1 로 고착돼 에지 불가"
       "0x6064=0 은 전원 사이클로만 풀리는 래치" / "축이 이미 홈이면 무동작 즉시 완료 → 타임아웃" / "CAN 과 Seer 가 독립 경로라 교차검증 성립"
       편차 표기 +0.334° / +0.332° (→ +0.331° 로 쓸 것)
    ⚠ 펌웨어 WAIT 에 "이미 홈" 종료 조건이 추가돼 있다(SEER_HOME_ATHOME_S, seer_home_athome_mask — safety_seer_gate.h:221,247,410-460).
       무해함은 실증됐으나(10회 정상 동작) 필요성은 미확인이다(10회 전부 발동 0건). "이 수정이 09:58 실패를 고쳤다" 고 쓰지 말 것.

  [2026-08-03 17:00 재정정 — 바로 위 15:40 블록의 수치·판정 정정. 원자료 재계산. 이 항목이 우선한다]
    (원자료: Log/home_experiment_260803_153319_summary.json · …_153319.jsonl · …_091956_summary.json ·
     …_095815_summary.json · Log/homing_edge_260803_144305.json · …_144602.json · …_152520.json ·
     Log/seer_homing_260803_100813.jsonl · Log/steer_two_phase_260803_131305.jsonl ·
     Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h)
    - [E1] "12회 연속 성공" → 12회. 2026-08-03 호밍 개시는 총 13회 = 성공 12 / 실패 1
      (09:58 ERR_TIMEOUT(final_state=6, 120.26 s) 1 · 14:46 1(37.0 s) · 15:25 1(34.91 s) · 15:33~15:40 10).
      fsm_trace 가 빈 14:43 · 14:45×4 · 15:29 은 호밍 미개시라 시도에서 제외.
      "10회 연속 10/10" 자체는 유효. 단 15:33 실행의 baseline 은 호밍이 아니라 레지스터 스냅샷이다
      (Tools/docking_field_kit/orin_home_experiment.py:390 base = snapshot(rig, "baseline")) — 호밍 횟수로 세지 말 것.
    - [E2] "0x6064=0 은 09:58 1회 관측 후 재현 안 됨" 은 거짓이다. 재현된다:
      09:19 baseline pos3/pos4 = 0 · 09:58 baseline·run1_pre·run1_post 전부 0 ·
      10:08 seer_homing_260803_100813.jsonl 의 0x6064 SDO 응답 node3 10,327/10,327 · node4 10,327/10,327 전량 0 ·
      리부팅 뒤 14:43 homing_edge_260803_144305.json node4 pos = 0.
      다만 인과는 미판정이다 — 성공한 15:33 10회 실행 중에도 0x6064 는 0 을 다수 반환했다
      (node3 32,801/38,764 · node4 32,726/38,737). "0 이면 호밍 실패" 도 "0 은 09:58 실패와 무관" 도 양쪽 미판정.
      아래 (4) 의 "debt-036 … 09:58 1회 관측 후 재현 안 됨" 문구도 이 항목으로 대체한다.
    - [E3] "WAIT(31.7 s)" → WAIT 체류는 31.30 s (state4→state8 평균 31.302 / 중앙 31.295, 31.21~31.38).
      31.7 s 는 개시(t=0)~RESTORE(state 8) 절대시각(평균 31.687)이다.
    - [E4] "35.0 s" → 평균 35.07 / 중앙 35.05 (34.99~35.16, 편차 0.17 s 는 정확).
    - [E5] "counts/° = 57,344 (기울기 1.000000)" → node3 57,344.0 (1.0000000) / node4 57,344.3 (1.0000049).
      근거 Log/steer_two_phase_260803_131305.jsonl phase A 5점(−5/−2.5/0/+2.5/+5°) 회귀. "1.000000" 은 node3 에만 해당.
    - [E6] "σ≈3 counts" 는 모표준편차 기준이다 — node3 2.80 / node4 3.21 (표본표준편차는 2.95 / 3.38).
      원자료 정착값은 node3 7,882,021×8 · 7,882,014×2 / node4 7,859,065×8 · 7,859,058×2 의 이산 2값 분포다.
    - [E7] "결함이 아니라 설계 동작이다" 는 과잉 확정이다. 확인된 것은 재현되는 정착 동작뿐이고,
      의도된 사양임을 보이는 벤더·설계 문서 근거는 미확인이다.
      ⇒ "재현되는 정착 동작(상수 적정성은 별건 — debt-016)" 으로 쓸 것.
    - [E8] 조향 0° [7871815, 7840086] 은 값은 유지하되 근거를 강등한다 — "실측 확정" 이 아니라 공학적 채택값이다
      (역산식이 항등식이라 실측이 값을 결정하지 못한다). "실측 확정" 표현 금지.
  근거: docs/verified_facts/2026-07-27.md §B-1 (판다 read node3 ≈ -1,517 · node4 ≈ +1,161 counts vs Seer 1040 encoder -7,871,810/-7,840,091, 바퀴 육안 0° — 조향 노드에서만 7.87M counts(=137°) 어긋남, 어느 쪽인지 미판정),
  [2026-08-03 정정 — 아래 4줄 중 3줄은 죽었거나 철회됐다. 그대로 인용하지 말 것.]
  (1) ❌ 철회+줄번호 사망: docs/ros2_driver/2026-07-09-design-inputs.md:56,81 ("부팅 시 0x6064≈0 이 정상, 홈 상수는 절대 목표 steerOffset 137.3°, 매 기동 시 스윙 필요").
      → 인용 3건 모두 그 파일 안에서 in-place 철회됐고 줄번호도 어긋난다(파일이 계속 append 되어 자체 대응표마저 죽음).
        현재 위치는 줄번호가 아니라 문구로 찾을 것: "부팅 직후 전 노드 0x6064 ≈ 0"=현 :162(바로 아래 :163-167 이 철회),
        "홈 상수는 전원 사이클 불변"=현 :186(:187 재개방 → :215 철회), §7 표 "조향 절대원점 불변성"=현 :272(같은 줄에서 ①②③ 철회).
      → 대체 사실: 조향축은 절대 엔코더다(전원 사이클을 넘어 7.87M 유지, 구동축만 증분 카운터로 리셋 — docs/debt/registry.md debt-036).
        steerOffset 단일값 137.3° 는 없다 — EasyDRIVE 실값 node3 138.000° / node4 137.250°.
  (2) ✅ 유지: verified_facts 사용 규칙 2 (§B 항목은 확정으로 인용하지 않는다).
  (3) ❌ 존재하지 않는 줄: src/Actuators/motor_control/config/tongyi_amr.yaml:91 ("⚠ debt-007 판정 전까지 무비판 신뢰 금지").
      → 그 파일은 총 47줄뿐이다. 실제 위치는 :39 steer_home_counts: [7871815, 7840086] / :40 주석 / :25-38 2026-08-03 정정 블록.
        그리고 이 파일은 사본이다 — 정본은 src/Comm/CAN/can_relay/config/machine/foil_a082.yaml:133 (같은 값).
        debt-007 은 2026-08-02 종결됐다(결론값만 2026-08-03 재정정, 종결 자체는 유지). "판정 전까지 무비판 신뢰 금지" 는 현재 상태가 아니다.
  (4) ✅ 완료(더 이상 과제 아님): 판정에 필요한 측정 = intercept off 상태에서 판다로 조향 0x6064 를 다회 read + 동시각 Seer 1040 encoder 대조.
      → 2026-08-03 11:44 수행 완료. 도구 Tools/docking_field_kit/orin_steer_crosscheck.py, 판다 SAFETY_SILENT·passthrough(제어권 미취득, 송신 0건 AST 검사).
        node3 CAN 7,871,823 / node4 CAN 7,840,052 (Seer 1040 각도 -0.000° / +0.001°). 산출 Log/steer_xcheck_reboot_0deg.jsonl · …_confirm.jsonl.
      → 살아남는 판정: 판다 read 는 오염되지 않았다(0x6064 실값이 판다·Seer 양쪽에서 같은 수로 읽힌다).
      → [정정 2026-08-03 15:40] "두 소스가 같은 자세를 가리킨다 = 교차검증 성립" 은 무효다. Seer 1040 은 판다가 엿듣는 바로 그 0x6064 의 아핀 변환이고
        (디더가 counts 단위로 일치, 기울기 ×57344 = 1.0000000), Seer 1005 도 같은 0x6064 유래다. 같은 프레임을 두 번 읽은 것이지 독립 경로가 아니다.
        "사용자 확인 Seer 0°" · "2회 독립 실행 동일" 도 결과에 기여하지 않는다(추정량이 자세 불변). 이 3개를 근거로 인용하지 말 것.
      → 이 측정을 다시 수행하지 말 것(같은 항등식을 또 돌리는 것뿐이다). 물리 직진을 확정하려면 非-Seer 앵커가 필요하다.
        ❌ [재정정 2026-08-03 17:30] "다시 수행하지 말 것" 은 유효(항등식). 그러나 "물리 직진을 확정하려면" 은 과했다 —
           물리 직진 자체는 이미 확정돼 있다(Seer 1005/1040 = EasyDRIVE steerOffset 교정 조향각 + 사용자 직진 자세
           앞바퀴 2축 0° 육안 확인, can_relay GUI 동일 확인). 非-Seer 앵커는 counts 소수 자리까지 판정할 때만 필요하다.
      → 남은 미판정: debt-036(조향 0x6064 가 0 으로 래치되는 조건 — 09:58 1회 관측 후 재현 안 됨, 추적 우선순위 낮음) + [폐기: 물리 직진 앵커 부재]
        → [재정정 2026-08-03 17:30] "물리 직진 앵커 부재" → "물리 직진 앵커 정밀도 부족(≈±1° = ±57,344 c) — 그 이하의 정렬 오차 미확인".
        ❌ [재정정 2026-08-03 17:00 / E2] "09:58 1회 관측 후 재현 안 됨" 은 거짓이다 — 09:19 · 09:58 · 10:08(전량 10,327/10,327) ·
           리부팅 뒤 14:43(node4) 에서 재현됐다. 다만 성공한 15:33 10회 중에도 0 이 다수 반환돼(node3 32,801/38,764)
           인과는 양쪽 미판정이다. "재현 안 됨" 을 근거로 우선순위를 논하지 말 것.
- amap-1에서 24h 신뢰성 테스트 연속 진행중(Run1·Run2 각 100%, 0실패). 러너 ~/run_reliability_loop.sh 가 24h마다 자동 반복.

먼저 Tools/docking_field_kit/HANDOFF-amap2.md 를 읽고, tailscale로 amap-2(ssh amap@amap-2) 접속해 판다 연결과 실통신을 재확인해줘.

오늘 할 일(현장, attended):
1) amap2_monitor.py monitor 30  — 실 통신 건전성 + Seer 지령 관측 재확인
2) [보류 2026-07-25] amap2_monitor.py gate 30      — intercept 전환이 실차 Seer 리셋 유발(field-record :31,:68-71). 실행 금지.
3) [보류 2026-07-25] amap2_monitor.py gatecheck 30 — 실차에서 Seer write 가 모터측으로 누출됨 = 차단 실패(field-record :33). 실행 금지.
4) [보류 2026-07-25] docking_drive.py 실도킹(take→enable→f 30→도킹→stop→release) — 원인 규명·사용자 승인 전까지 보류(field-record :56, :54). 실행 금지.
   ⇒ 2)~4) 를 진행하려면 먼저 전환 무중단화(커널 can-gw 수준) 확보 + 사용자 승인이 필요하다.

킷: amap-2 ~/Project/T-Robotics/T-Driver-Analysis/tools/docking_field_kit/ (본 저장소 Tools/docking_field_kit/ 와 동기).
   [2026-07-27 정정] 이 경로는 현 로봇 PC(Ford-CATL-orin-nx)에 없다. 실제 사본 = ~/Project/Ford-CATL-AMR/T-Driver-Analysis/tools/docking_field_kit/ · ~/Project/CAN-Relay/docking_field_kit/ (field-record-orin-nx-2026-07-25.md:11). 정본 = 저장소 Big-AMR/Tools/docking_field_kit/.
클론 핀맵 주의: CAN2_H=pin23(pin22 死핀). git 협업 모드 solo.
각 단계 실행·판정 결과를 보고하고, 실모터 구동(4번) 전에는 나에게 확인받고 진행해.
```

---

## 참고 — 이 프롬프트가 가리키는 핵심 파일
- `Tools/docking_field_kit/HANDOFF-amap2.md` — 상태·발견·절차 인수인계
- `Tools/docking_field_kit/amap2_monitor.py` — monitor/gate/gatecheck/seq
- `Tools/docking_field_kit/docking_drive.py` — 실 도킹 드라이버
- `Tools/docking_field_kit/PINMAP.md` · `RUNBOOK.md`
- `docs/can_relay/reliability-24h-results.md` — 24h 결과
