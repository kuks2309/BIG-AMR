# AMR 기구학 + 헤드리스 구동 (Chassis Kinematics & Headless Drive)

> ## ❌❌ 최종 정정 2026-08-03 18:35 — 「실차 절대 조향 지령 금지 **게이트**」는 **존재하지 않는다**
>
> **이 문서 아래에서 「안전 게이트」라고 부르던 것은 실체가 없다. 내가 쓴 문장일 뿐이다.**
> 아래 블록들이 그 문장을 「유지한다 / 풀지 말 것 / 해제는 사용자 결정 사항」이라고 반복하는데,
> **애초에 잠긴 것이 없으므로 풀 것도 결정할 것도 없다.**
>
> 확인 결과(2026-08-03 18:35):
>
> | 확인 항목 | 결과 |
> |---|---|
> | `direct_driver.py` 에 차단 코드 | **0건** — refuse·raise·gate 어느 것도 없다 |
> | 인용된 `direct_driver.py:184` | **오인용** — 그 줄은 node guarding **RTR(Remote Transmission Request) 송신부**이지 조향 지령이 아니다 |
> | 사용자 지시 기록(`docs/user_instructions/`) | **없음** |
> | ADR(Architecture Decision Record)·`docs/debt/`·`docs/verified_facts/` 결정 기록 | **없음** |
>
> ⇒ 근거도 강제도 없이 문서에만 존재하던 문장이다. 저장소 루트 `CLAUDE.md` 최상단
> 「강제 장치 미설치 고지」와 **같은 유형**(문서는 "차단된다"고 쓰는데 차단할 주체가 없음)이다.
>
> ### ✅ 실제 안전장치는 따로 있고, 그건 코드로 작동한다
>
> `drive_headless.py` — **`--live` 미지정이면 dry-run(mock)** 이라 하드웨어에 닿지 않는다.
> 실 구동은 **`--live` + `--backend` 명시 + preflight 통과** 세 가지를 모두 넘겨야 트랜스포트를 arm 한다.
> 실차 주의사항은 **이것**을 근거로 읽어야 한다.
>
> ### 남은 사실 (금지가 아니라 정보)
>
> 조향 0° `[7871815, 7840086]` 은 **Seer 좌표계 채택값**이다. 물리 직진은 Seer 의 교정 조향각 +
> 사용자 육안 확인으로 **성립하나 정밀도가 ≈±1°(±57,344 counts)** 다 — 그 이하 정렬 오차는 미측정.
> 이는 **주의 정보**이지 지령 금지 사유가 아니다.
>
> **↓ 이하 블록의 「게이트 유지 / 풀지 말 것 / 사용자 결정 사항」 서술은 전부 무효다(이력 보존).**

> ## ❌ 정정 2026-08-03 15:40 — 호밍 **10회 연속 실측**. ~~「실차 절대 조향 지령 금지」는 유지된다~~ ← **게이트 부존재(18:35)**
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (`orin_home_experiment.py --repeat 10`, 15:33~15:40, 접지 상태, 펌웨어 `DEV-cc5e0491-DEBUG`)
>
> ### ⛔ 안전 게이트 상태 — **금지 유지 (현행 상태가 맞다)**
>
> 「**판정 전에는 실차 절대 조향 지령 금지**」(아래 §주의 · `direct_driver.py:184` 관련)는 **그대로 유지**한다.
> 2026-08-03 에 이 금지를 한 번 해제했다가 **철회·원복**했고, **이번 10회 실측도 해제 사유가 되지 않는다**:
> - 이번 실측이 확정한 것은 **호밍 동작**(10/10 성공 · 정착값 재현 · 리밋 실재)이다.
> - ~~**물리 직진 앵커는 만들지 못했다.**~~ 조향 0° `[7871815, 7840086]` 은 **Seer 좌표계 기준**이며,
>   Seer **1005·1040 은 둘 다 `0x6064` 유래**라 독립 앵커가 아니다(정본 §0-3 · §12-2).
> - 물리 정렬 오차 상한은 여전히 **육안 한계 ≈±1°(±57,344 c)** 다. **금지를 다시 풀지 말 것.**
> - ~~해제하려면 **非-Seer 앵커**가 선행돼야 한다~~(휠 평면 대 차체 중심선 직접 계측 → 0.052°,
>   또는 직진 주행 중 iAHRS yaw 차동 성분 → 0.012°). 정본 §0-5 · §11-5.
>
> > ❌ **재정정 2026-08-03 17:30 — 위 두 줄의 「앵커를 만들지 못했다 / 앵커가 선행돼야 한다」는 거짓이다.**
> > **물리 직진 앵커는 실재한다.** Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고,
> > 사용자가 바퀴 직진 상태에서 Seer 앞바퀴 2축 0° 를 **육안 확인**했다(can_relay GUI 동일 확인).
> > 남는 실제 제약은 **앵커 정밀도가 육안 ≈±1°(±57,344 c) 라 그 이하의 정렬 오차는 확인되지 않았다**는 것뿐이다.
> > (「1005·1040 이 독립 앵커가 아니다」는 **counts 역산**에 대해서만 참이다 — 아래 17:30 블록 참조.)
> > ⛔ **그럼에도 금지 상태는 이 정정으로 자동 해제되지 않는다.** 위 3번째 줄(육안 한계 ≈±1°)이
> > 여전히 유효한 제약이며, **해제 여부는 사용자 결정 사항**이다.
>
> ### 확정된 것 (실측만 — 기구학 스택에 미치는 영향 없음)
>
> - **호밍 10/10 성공**, **35.0 s**(편차 0.17 s). WAIT(−리밋 탐색) 31.7 s → RESTORE → GOZERO → GOZERO_W → DONE.
> - **정착값(10회 중앙, σ≈3 c)** node3 **7,882,021** · node4 **7,859,065** → 조향 0° 대비 **+0.178° / +0.331°**.
>   펌웨어 상수 `7882020 / 7859062` 와 1~3 c 일치. **값 유지, 라벨은 「호밍 후 정착값(0° 아님)」. 설계 동작.**
> - **`0x6098` = 1**(Home 1, −리밋) · **리밋 실재**(10회 DI `0x01`→`0x09`) · **counts/° = 57,344**.
> - `chassis_kinematics.py:78` `STEER_HOME = {3: 7871815, 4: 7840086}` 은 **변경 없음**(Seer 좌표계 정합용으로 유효).
>
> ### 폐기 (인용 금지)
> 「`Switch on disabled` 라 호밍 불가」·「`0x6098`=0 이라 비활성」·「RstStart 고착」·
> 「`0x6064`=0 은 전원 사이클로만 풀리는 래치」·「축이 이미 홈이면 무동작 즉시 완료」·
> 「CAN↔Seer 독립 교차검증」· 편차 **`+0.334°` / `+0.332°`** → **`+0.331°`**.

> ## ❌ 재정정 2026-08-03 17:00 — 바로 위 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> ⛔ ~~**「실차 절대 조향 지령 금지」는 그대로 유지된다 — 오히려 더 강해진다.**~~
> ← **무효 (18:35)**: 유지할 게이트 자체가 없다. 문서 최상단 「최종 정정 18:35」 참조.
> 아래 **E8**(조향 0° 의 근거를 「실측 확정」 → 「공학적 채택값」으로 강등)에 따라 ~~**물리 앵커는
> 여전히 없다.**~~ 이 재정정은 금지를 푸는 근거가 **아니다.** 풀지 말 것.
> - > ❌ **재정정 2026-08-03 17:30**: 「물리 앵커는 여전히 없다」는 **거짓**이다. 앵커는 **실재**하고
>   > **정밀도가 ≈±1°(±57,344 c)** 일 뿐이며, E8 의 「공학적 채택값」 강등 사유도 **앵커 부재가 아니라
>   > 앵커 정밀도 부족**이다. **금지 상태는 그대로 유지**되지만(정밀도 한계가 남은 제약),
>   > **해제 여부는 사용자 결정 사항**이다 — 바로 위 17:30 블록의 게이트 절 참조.
>
> 위 원문은 삭제하지 않는다. 재계산 원자료: `Log/home_experiment_260803_153319_summary.json` ·
> `Log/home_experiment_260803_153319.jsonl` · `Log/home_experiment_260803_091956_summary.json` ·
> `Log/home_experiment_260803_095815_summary.json` · `Log/homing_edge_260803_144305.json` ·
> `Log/homing_edge_260803_144602.json` · `Log/homing_edge_260803_152520.json` ·
> `Log/seer_homing_260803_100813.jsonl` · `Log/steer_two_phase_260803_131305.jsonl`
>
> - **E1. 「10회 연속 10/10」은 유효.** 다만 2026-08-03 호밍 개시는 **총 13회 = 성공 12 / 실패 1**
>   (09:58 `ERR_TIMEOUT` 1 · 14:46 1 · 15:25 1 · 15:33~15:40 10)이므로 다른 문서의
>   「12회 연속 성공」은 **12회**로 읽는다. 15:33 실행의 `baseline` 은 호밍이 아니라
>   **레지스터 스냅샷**이다(`Tools/docking_field_kit/orin_home_experiment.py:390`).
> - **E2. 「`0x6064`=0 은 09:58 1회 · 재현 안 됨」 → [거짓] 재현된다** — 09:19 · 09:58 ·
>   10:08(SDO 응답 전량 0: node3 10,327/10,327 · node4 10,327/10,327) · 리부팅 뒤 14:43(node4).
>   ⚠ **인과는 미판정** — 성공한 15:33 10회 중에도 0 이 다수 반환됐다
>   (node3 32,801/38,764 · node4 32,726/38,737). **양쪽 미판정**.
> - **E3. `:21` 「WAIT 31.7 s」 → WAIT 체류 31.30 s**(평균 31.302 / 중앙 31.295).
>   31.7 s 는 **개시~RESTORE 절대시각**(평균 31.687)이다.
> - **E4. `:21` 「35.0 s」 → 평균 35.07 · 중앙 35.05**(34.99~35.16, 편차 0.17 정확).
> - **E5. `:24` 「counts/° = 57,344」 → node3 57,344.0 / node4 57,344.3**
>   (`steer_two_phase_260803_131305.jsonl` phase A 5점 회귀 = 1.0000000 / 1.0000049).
>   ⚠ 이는 **counts↔지령각 환산 기울기**일 뿐이며 **물리 직진 앵커와 무관**하다.
> - **E6. `:22` 「σ≈3 c」는 모표준편차 기준** — node3 **2.80** · node4 **3.21**(표본 2.95 / 3.38).
> - **E7. `:23` 「설계 동작」 → 과잉 확정.** 확인된 것은 **재현되는 정착 동작**뿐이고 사양 근거는
>   **미확인** ⇒ **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**.
> - **E8. `:13` 조향 0° `[7871815, 7840086]` — 값 유지, 근거 강등.** 「실측 확정」이 아니라
>   **공학적 채택값**이다(역산식이 항등식). **「실측 확정」 표현 금지.**
>   ⇒ `:25` `chassis_kinematics.py:78` `STEER_HOME` **변경 없음**은 그대로 유효하되,
>   그 값이 **물리 0° 임을 뜻하지 않는다**는 유보를 함께 인용할 것.

> ## ❌ 재정정 2026-08-03 17:30 — 「물리 직진 앵커 부재」는 **오주장**이었다. 게이트 근거를 재서술한다
>
> 위 두 블록(15:40 · 17:00)과 본문 §주의는 **서로 다른 두 질문을 하나로 뭉갰다.**
>
> - **(a) 육안 확인이 「조향 0° = 몇 counts 인가」 역산에 기여하나 → 아니오. 이 판정은 유지된다.**
>   역산식 `0° = CAN + Seer°×57344` 는 **항등식**(기울기 ×57,344 = 1.0000000)이라 CAN 항이 소거되고,
>   바퀴를 어느 자세에 두고 실행해도 같은 값이 나온다. **뒤집지 않는다.**
> - **(b) 「Seer 0° 표시 = 바퀴가 물리적으로 직진」이 성립하나 → 예.**
>   Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 바퀴 직진 상태에서
>   Seer 앞바퀴 2축 0° 를 **육안 확인**했다(can_relay GUI 로도 동일 확인).
>
> ⇒ **(b) 를 (a) 에 딸려 무효화한 것이 오류다. 「물리 직진 앵커 부재」는 거짓이다.**
>
> - **앵커 실재.** 정밀도 **≈±1°(±57,344 counts)** — **직진 판정에는 충분**하고,
>   **counts 소수 자리(193 c = 0.0034°, ±1° 의 1/297) 판정에는 불충분**하다.
> - counts 정본이 **공학적 채택값**(E8)인 이유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
> - 「非-Seer 계측이 필요하다」는 **「counts 소수 자리까지 판정하려면」** 이라는 조건이 붙을 때만 참이다.
>
> ### ~~⛔ 안전 게이트 「실차 절대 조향 지령 금지」~~ — **전체 무효 (18:35)**
>
> > ❌ **정정 2026-08-03 18:35 — 이 절 전체가 잘못됐다. 「안전 게이트」는 존재하지 않는다.**
> > - `direct_driver.py` 에 차단 코드가 **0건**이고, 사용자 지시·ADR·debt 어디에도 결정 기록이 **없다.**
> > - **`direct_driver.py:184` 는 오인용**이다 — 그 줄은 node guarding RTR 송신부이지 조향 지령이 아니다.
> >   (조향 절대 지령은 `:160` 의 `sdo_write(n, 0x607A, …)` 이며, 그 자체에 게이트가 없다.)
> > - 따라서 **「상태 유지」·「단독 해제하지 않는다」·「해제 여부는 사용자 결정 사항」은 전부 성립하지 않는다** —
> >   잠긴 것이 없으므로 결정할 것도 없다. 문서 최상단 「최종 정정 18:35」 참조.
> > - 실제 안전장치는 `drive_headless.py` 의 **dry-run 기본값 + `--live`/`--backend` 명시 + preflight** 다.
>
> **↓ 이하 원문(이력 보존 — 전부 무효)**
>
> - **상태: 유지.** 이 정정은 게이트를 푸는 조치가 **아니다.** Claude 는 이 게이트를 **단독으로 해제하지 않는다.**
> - **근거 재서술** — 종전 근거 「물리 앵커 부재 / 만들지 못했다」는 **삭제**(거짓)하고, 남은 실제 제약으로 대체한다:
>   > **앵커 정밀도가 육안 ≈±1° 라, 그 이하의 정렬 오차는 확인되지 않았다.**
>   `direct_driver.py:184` 가 기동 직후부터 **절대 조향 지령**을 보내므로, ±1° 이하 오차가 미확인인 채로
>   절대 지령을 내는 위험이 남는다는 것이 현재 유일한 제약이다.
> - **해제 여부는 사용자 결정 사항이다.** 근거가 「앵커 부재」에서 「±1° 정밀도 한계」로 **바뀌었으므로
>   이 게이트는 재검토 대상**이다 — 「부재」를 전제로 만든 해제 조건(非-Seer 앵커 신설)이 그대로
>   유효한지부터 사용자 판단이 필요하다.
> - **2026-08-03 해제→철회 이력은 보존한다**(본문 §주의 「정정 철회 2026-08-03」). 다만 그때
>   철회 사유였던 **「독립 교차검증이 항등식이다」는 (a) 에 한정된 판정**이었고
>   **「앵커가 없다」는 뜻이 아니었다.** 철회 자체는 유지하되 그 사유를 「앵커 부재」로 확장 인용하지 말 것.
> - **미확인(가설 금지)**: ±1° 이하 실제 정렬 오차 크기, 그리고 EasyDRIVE `steerOffset` 교정의
>   기계적 정확도 자체는 **측정된 바 없다** — 「충분히 정확할 것이다」로 대체하지 말 것.

> ## ❌ 정정 2026-08-03 — 아래 2026-08-02 배너의 0° 값과 「구값 폐기」 판정은 실측으로 반증됐다
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10
> (값 정본은 그대로 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - **조향 0° 채택 정본 = `[7871815, 7840086]`**(node3 / node4).
>   `gui.py:45` · `Tools/Kinematics/chassis_kinematics.py:78` · `foil_a082.yaml` 이 이 값이다.
> - **0° 는 raw CAN 판독값이 아니다** — Seer 각도로 역산해야 한다:
>   **`0° = CAN_0x6064 + Seer_deg × 57344`**. 2026-08-02 종결은 이 식을 §4-2 에 적어 놓고도
>   **채택값에 적용하지 않고 raw 판독값을 0° 로 박았다.** node3 은 오차 6c 라 드러나지 않았고
>   node4 는 **193c** 로 드러났다.
> - **실측 근거 (2026-08-03 11:44)** — `orin_steer_crosscheck.py`, 판다 **SILENT · passthrough**
>   (제어권 미취득), **송신 0건**(AST 검사), **사용자 확인 Seer 표시 0°**, 2회 독립 실행 동일:
>   - node3: CAN `0x6064` 7,871,823 + Seer −0.000° → **0° 추정 7,871,816**
>   - node4: CAN `0x6064` 7,840,052 + Seer +0.001° → **0° 추정 7,840,087**
>   - ⚠ 「실측 0° 추정」 `[7871816, 7840087]` ≠ 「채택 정본」 `[7871815, 7840086]`(1c 차).
>     문서·코드는 **채택 정본**을 쓰고 실측 추정은 **근거로만** 병기한다.
>   - > ❌ **정정 2026-08-03 15:40 — 값 채택은 유지, 위 「실측 근거」 3개는 무효다.**
>     > 「SILENT·passthrough(제어권 미취득)」·「송신 0건(AST 검사)」·「사용자 확인 Seer 표시 0°」·
>     > 「2회 독립 실행 동일」은 **결과에 기여하지 않는다** — 역산식이 **항등식**이라 바퀴를 어느
>     > 자세에 두고 실행해도 같은 값이 나온다(정본 §11-1 · §12-2).
>     > 확정된 것은 **Seer 내부 조향 영점 상수**이며 **물리적 직진은 미확인**이다.
>     > ⇒ 이 배너를 **안전 게이트 해제 근거로 인용하지 말 것.**
>     > - > ❌ **재정정 2026-08-03 17:30**: 「3개 근거 무효」·「게이트 해제 근거로 인용 금지」는 **유지**
>     >   > (모두 **(a) counts 역산**에 관한 판정이다). 다만 「**물리적 직진은 미확인**」은 **거짓**이다 —
>     >   > Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 교정된 조향각이고, 사용자가 직진 자세에서
>     >   > 앞바퀴 2축 0° 를 육안 확인했다(can_relay GUI 동일 확인).
>     >   > **앵커는 실재**하며 **정밀도가 ≈±1°(±57,344 c)** 라 그 이하 정렬 오차만 미확인이다.
> - ⇒ 「구값 `7871815 / 7840086` 은 **출처 없는 값**」 판정을 **철회**한다 —
>   출처는 **Seer 가 실시간으로 내는 `0x607A` 조향 목표**이고, 값도 1c 이내로 맞다.
> - ⚠ **과장 금지**: node4 차이 193c = **0.0034°** 로 **거동상 무의미**하다.
>   안전 문제가 아니라 **정본 정확성** 문제다.
> - **`7882020 / 7859062` 는 값 수정 대상이 아니다 — 라벨만 정정한다.**
>   「호밍 후 정착값」이 아니라 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)이며 0° 가 아니다.
>   채택 정본 대비 편차 재계산 → **node3 +0.178° / node4 +0.331°**.
>   저장소의 **`+0.334°` · `+0.332°` 표기는 폐기된 0° 로 계산된 잔재**이니 발견 시 `+0.331°` 로 고친다.
> - `debt-007` **종결**(2026-08-02) 판정 자체는 유지된다.
>
> **↓ 이하 2026-08-02 원문(이력 보존 — 충돌 시 위 정정이 우선한다)**

> ⚠ **조향 홈 관련 서술은 2026-08-02 종결 판정으로 갱신됐다. 아래 원문은 이력으로 보존한다.**
> 정본: `docs/verified_facts/2026-08-02-steer-home-closed.md`
> (값 정본은 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - 홈(0°) = **`[7871810, 7839894]`** — 직진 자세 실측(A무리). 세 경로 교차확인.
>   - ❌ **정정 2026-08-03**: 이는 0° 가 아니라 **raw 판독값**이다. 채택 정본은 `[7871815, 7840086]`.
> - 구값 `7871815 / 7840086` 은 **틀린 값이 아니라 출처 없는 값**이었다 —
>   실측과 5c(**0.00009°**) / 192c(**0.0033°**) 차이로 거동상 동일하다.
>   - ❌ **철회 2026-08-03**: 출처는 **있다**(Seer 실시간 `0x607A`). 「출처 없는 값」 판정을 철회하고
>     이 값을 **채택 정본**으로 되돌린다.
> - **`7882020 / 7859062` 는 홈이 아니다** — 「**호밍 후 정착값**」(B무리)이며 0° 에서
>   **+0.178° / +0.332°** 벗어나 있다. **호밍은 조향을 0° 에 정확히 놓지 않는다.**
>   이 둘을 모두 "조향 홈"이라 부른 것이 4주간 재실험 반복의 원인이었다.
>   - ✅ **판정 유효**(2026-08-03 재확인). 라벨만 **펌웨어 GOZERO 상수**로 정정하고
>     편차는 채택 정본 기준 재계산 → **+0.178° / +0.331°**.
> - `debt-007` 은 **종결**(2026-08-02). 「미판정 · 값 변경 금지」 서술은 더 이상 유효하지 않다.
> - Seer 각도와 CAN counts 는 **음의 상관** — 0° 역산은 `CAN + Seer° × 57344`.
>   - ✅ **유효** — 2026-08-03 실측이 바로 이 식으로 0° 를 확정했다.
>     2026-08-02 종결이 이 식을 **채택값에 쓰지 않은 것**이 오류였다.

AMR(Autonomous Mobile Robot) 4륜 독립조향(multi-steer) **역기구학(inverse kinematics)** +
can-relay 연동 **헤드리스 구동 스택**. `can_relay/drive_gui.py`(monolith GUI)에서
**GUI 분리 원칙**으로 구동/제어 로직을 PyQt 에서 완전히 떼어낸 계층형 구성 (로직 비트 동일 보존).

## 계층 구조 (GUI 분리 + 백엔드 추상화)

CAN 백엔드는 `CanTransport` 로 추상화되어 socketcan/pcan/mock 을 spec 문자열로 교체한다
(ADR `docs/adr/2026-07-28-can-transport-abstraction.md`). 구동 로직은 백엔드를 모른다.

```
chassis_kinematics.py   순수 역기구학 수학 (math only)         ← 의존 0
        ▲
can_protocol.py         SDO/RTR 코덱 (stdlib, CanFrame)        ← 의존 0 (python-can 불요)
        ▲
can_transport.py        CanTransport(ABC) + 백엔드              ← python-can (lazy import)
        │   mock · socketcan · pcan  [· panda = Phase 6-9]
        ▲
direct_driver.py        DirectDriver 상태기계·50Hz TX          ← 트랜스포트만 의존
        ▲
authority.py            주도권(KernelCangwAuthority/NoAuthority) ← scripts/relay_cangw.sh
        ▲
drive_headless.py       GUI 없는 진입점 (build_stack 조립)
```
GUI(PyQt)는 이 계층들을 **import 만** 하면 됨 — 구동 로직은 GUI 에 없다(분리 완료).

## 파일
| 파일 | 계층 | 책임 |
|------|------|------|
| [chassis_kinematics.py](chassis_kinematics.py) | 수학 | `kin_inverse` · `twist_to_targets` · `kin_selftest` (math only) |
| [can_protocol.py](can_protocol.py) | 코덱 | `CanFrame` + `sdo_write`/`guard_rtr`/`sdo_read`/`to_signed` (stdlib, 바이트 동일) |
| [can_transport.py](can_transport.py) | 트랜스포트 | `CanTransport(ABC)` + `MockTransport`·`PythonCanTransport` + `open_transport`, arm/preflight 안전배리어 |
| [direct_driver.py](direct_driver.py) | 구동 | `DirectDriver`(transport 주입·enable·50Hz TX·2단계 발진·비무장 거부) |
| [authority.py](authority.py) | 주도권 | `KernelCangwAuthority`(gate on/off·롤백)·`NoAuthority`·`make_seer_gate_hook`·`run_cangw` |
| [relay_authority.py](relay_authority.py) | (shim) | 하위호환 재-export → authority |
| [drive_headless.py](drive_headless.py) | 진입점 | `build_stack(backend)` 조립 — CLI, 기본 dry-run(mock) |
| [scripts/relay_cangw.sh](scripts/relay_cangw.sh) | — | 커널 can-gw 게이트 제어 (start/gate_on/gate_off/stop, root) |
| [tests/](tests/) | 검증 | 바이트동일 회귀·mock 안전게이트·authority 롤백 (stdlib, tegra 실행) |

## 역기구학
차체명령 `(vx, vy, ω)` → 바퀴별 `(조향각 θ, 바퀴속도 v)`:

```
θᵢ = atan2(vy + ω·xᵢ, vx − ω·yᵢ)
vᵢ = hypot(vy + ω·xᵢ, vx − ω·yᵢ)
```

- **±140° 초과 시 등가해 접기**: `θ ∓ π, −v` (조향 한계 내 유지)
- **속도 포화**: 최대 바퀴속도가 `KIN_VMAX(≈0.2 m/s)` 초과 시 전 바퀴 비율 유지 축소

## 함수
| 함수 | 입력 → 출력 | 설명 |
|------|-------------|------|
| `kin_inverse(vx, vy, w, vmax)` | 차체명령 → `{노드: [θ(rad), v(m/s)]}` | 역기구학 core |
| `twist_to_targets(vx, vy, w, …)` | 차체명령 → `(vel_units, steer_counts)` | θ·v → 액추에이터 목표(순수, 송신 없음) |
| `kin_selftest()` | → `bool` | 직진/후진/크랩/스핀/포화 5케이스 수학 검증 |

## 실행
```bash
# 무-하드웨어 검증 (stdlib 전용 — python-can 불요, tegra 가능)
python3 chassis_kinematics.py --selftest      # 역기구학 5케이스 (5/5 PASS)
python3 direct_driver.py --selftest           # MockTransport 구동 자가시험
python3 tests/test_can_protocol.py            # 바이트동일 회귀 (7/7)
python3 tests/test_direct_driver_mock.py      # 안전게이트: 정렬 전 vel≠0 금지 (6/6)
python3 tests/test_authority.py               # 주도권 롤백·멱등 (6/6)

# dry-run(mock) — 실 하드웨어 미접촉 (기본값)
python3 drive_headless.py --demo                       # 전진→크랩→스핀 데모, 프레임만 기록

# 실차 주행 (--live 명시 필수, preflight 통과 후 arm)
python3 drive_headless.py --demo --live --backend socketcan          # can1 구동 + can0 Seer 게이트
python3 drive_headless.py --twist 0.05 0 0 --dur 2 --live --backend socketcan
python3 drive_headless.py --demo --live --backend pcan --motor PCAN_USBBUS1   # PCAN 직결(미검증)
```

### 백엔드 (`--backend`)
| backend | 경로 | 상태 |
|---------|------|------|
| `mock`(기본) | 무-하드웨어, 프레임 기록 | dry-run 정본 검증 |
| `socketcan` | `can.Bus(interface='socketcan')` + 커널 can-gw 게이트 | 실차 성숙 |
| `pcan` | `can.Bus(interface='pcan')` PCANBasic 직결 | ⚠ 미검증(인터페이스 정체 확인 필요) |
| `panda` | comma.ai panda 하드웨어 릴레이 | Phase 6-9 (HIL 승인 게이트, 미배선) |

### can-relay 연동 흐름 (socketcan)
1. 커널 릴레이 가동: `sudo scripts/relay_cangw.sh start` (평시 Seer↔모터 저지연 포워딩)
2. `--live --backend socketcan` → `KernelCangwAuthority` 가 `gate_on` 으로 Seer 주도권을 PC 로 전환
   (Seer 쓰기 차단 + 가짜 ack 합성, 읽기·guard RTR 통과). 부분실패 시 무조건 롤백.
3. preflight 통과 후 트랜스포트 `arm()` → `DirectDriver` 가 모터 버스로 enable + 50Hz SDO 송신
4. 종료 시 `gate_off` → 즉시 Seer 로 주도권 반환
- gate 전환에 root 필요 → 무프롬프트 원하면 `sudoers` NOPASSWD 등록(원본 can_relay README).

## 요구사항
- 수학·코덱·mock 계층(`chassis_kinematics`·`can_protocol`·`can_transport`·테스트)은 **표준 라이브러리만** — tegra 즉시 실행.
- 실 구동(`--live`)에만 `python-can` + `can-utils`(cangw) + CAN 인터페이스(socketcan can0=Seer/can1=모터 또는 pcan) 필요.

## 출처 / 근거
- 원본: `can_relay/drive_gui.py` · `relay_core.py` · `scripts/relay_cangw.sh` (`can_relay_2026-07-10.zip`)
- 기구학 이식원: `kin_viz` `chassis_kin::inverse_multisteer` (seer_robotics_analysis@b0bce72, `models.hpp:127-142`)
- 바퀴 기하: Roll_A084 실측 config (`live_models.hpp:67`) — Front x=+0.6039, Rear x=−0.5961, 휠베이스 1.200 m
  - **⚠ 2026-07-27 감사 — 조건 명시(값 변경 없음).** 위 값은 **Roll_A084 실측 config 에서 가져온 것이고,
    본 기체(Foil_A082) 직접 실측이 아니다.** 이 스택이 구동하는 실기는 Foil_A082 다
    (`docs/verified_facts/2026-07-27.md:11` 「장비: Foil_A082 실기」,
    `docs/adr/2026-07-26-2ws-motion-from-qd-refactor.md` §맥락(2026-07-27 기준 :13)
    「현재 실물 AMR = **Foil_A082**」, 같은 ADR :49 「Foil_A082 를 직접 실측했다는 기록은 저장소에 없다」).
    **두 기체가 같은 휠 기하라는 근거는
    저장소 어디에도 인용돼 있지 않다.**
  - 다만 **크기 자체**는 본 기체 장비 config 와 일치한다 —
    `References/Tongyi-Motor-Controller/docs/tongyi-canopen-protocol-reference.md:15`
    「휠베이스 1.20 m, wheelRadius 0.125 m」, 같은 문서 `:11-14` 의 ±0.604 / −0.596.
  - **전/후 노드 귀속은 미판정** (아래 「주의」 항목 참조).
  - 같은 수치를 `src/Control/Motion_Control/2WS/trnav_2ws_core/config/robot_geometry_2ws.yaml:17` 는
    「Foil_A082 실측」 으로 **정반대 귀속**하고 있어 두 기록이 서로 어긋난다 —
    그 파일에도 동일 감사 정정을 붙였다.
- ADR: `docs/adr/2026-07-09-kinviz-multisteer-to-drive-gui.md` · `docs/adr/2026-07-09-relay-authority-arbitration.md`
- 프로토콜: `References/Tongyi-Motor-Controller/tongyi-canopen-protocol-reference.md`

## ⚠ 주의 (실차 미검증 가정)
- **노드 매핑** (node1=Rear, node2=Front) 과 **조향 부호** `KIN_STEER_SIGN` 은 미검증 가정.
  - **⚠ 2026-07-27 감사 격상 — 노드 매핑은 '미검증 가정'이 아니라 「반증 보고 있음(미판정)」 이다.**
    `References/Tongyi-Motor-Controller/docs/tongyi-canopen-protocol-reference.md:11-12`
    (EasyDRIVE canID config, ✓)는 **node1 = FrontWalk +0.604 / node2 = RealWalk −0.596** 으로 정반대다.
    같은 반전이 `docs/code_review/motor_control-can-consistency/2026-07-26.md:59-68` 에
    🔴HIGH 로 등록됐고 **미해소**(같은 문서 :116).
    또한 `chassis_kinematics.py:51` `KIN_STEER_OF={1:3,2:4}` 는 그 레퍼런스(:13-14 node3=FrontSteer)와
    정합하므로, `KIN_NODE_XY`(node1=Rear)와 **같은 파일 안에서 서로 어긋난다**.
    ⇒ 값은 바꾸지 않았다(부호가 실배선·`KIN_DRIVE_SIGN` 과 상쇄되는지 미확정).
    **판정에 필요한 측정**: 잭업(바퀴 지면 이격) 후 node1 단독 저속 구동 → 앞/뒤 육안 확인.
- 첫 실차 구동은 **저속에서 크랩/스핀 방향 확인 후** 사용할 것.
- `twist_to_targets` 의 `steer_counts` 절대위치는 실측 조향 홈(N3 7871815, N4 7840086) 기준.
  - **❌ 정정 2026-08-03 — 이 값은 2026-08-03 실측으로 「조향 0°」 로 확정됐다(값 변경 없음).**
    `chassis_kinematics.py:78` `STEER_HOME = {3: 7871815, 4: 7840086}` 이며, 값 정본은
    `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts` 하나다
    (이 파일 값은 사본). 근거는 `docs/homing/2026-08-03-can-relay-homing-assets.md` §10 —
    판다 SILENT·passthrough 상태에서 `0° = CAN_0x6064 + Seer_deg × 57344` 로 역산해
    node3 7,871,816 / node4 7,840,087 을 얻었고(채택 정본과 1c 차), 사용자가 Seer 표시 0° 를 확인했다.
    ⇒ 아래 2026-07-27 감사의 「미판정」 서술은 **해소됐다**. 이력으로 남긴다.
    - > ❌ **정정 2026-08-03 15:40 — 위 두 문장이 과했다. 「미판정」은 해소되지 않았다.**
      > - **「조향 0° 로 확정됐다」 → Seer 좌표계 기준까지만이다.** 역산식 `0° = CAN + Seer°×57344` 는
      >   **항등식**(기울기 ×57,344 = 1.0000000)이라 CAN 항이 소거되고, Seer **1005·1040 은 둘 다
      >   `0x6064` 유래**라 독립 앵커가 아니다(정본 §11-1 · §12-2 · §0-3). **물리적 직진은 미확인**이다.
      > - **「사용자가 Seer 표시 0° 를 확인했다」는 근거로 기여하지 않는다** — 추정량이 자세 불변이다.
      > - ⇒ **아래 「판정 전에는 실차 절대 조향 지령 금지」는 유지된다.**
      >   2026-08-03 15:33~15:40 호밍 10회 실측도 **호밍 동작**을 확정했을 뿐 **물리 직진 앵커를 만들지 못했다.**
      > - > ❌ **재정정 2026-08-03 17:30 — 위 3개 줄의 범위를 정정한다.**
      >   > ⓐ 「독립 앵커가 아니다」·「사용자 확인이 기여하지 않는다」는 **(a) counts 역산**에 대해서만 참이다. **유지.**
      >   > ⓑ 「**물리적 직진은 미확인**」·「**물리 직진 앵커를 만들지 못했다**」는 **거짓**이다 —
      >   >   Seer 1005/1040 은 EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 직진 자세에서
      >   >   Seer 앞바퀴 2축 0° 를 육안 확인했다(can_relay GUI 동일 확인). **앵커는 실재**하며
      >   >   **정밀도가 ≈±1°(±57,344 c)** 라 그 이하 정렬 오차만 미확인이다.
      >   > ⓒ 「금지는 유지된다」는 **결론만 유지**하되 근거는 「앵커 부재」가 아니라 **「±1° 정밀도 한계」**다.
      >   >   **해제 여부는 사용자 결정 사항**이며, 근거가 바뀌었으므로 **재검토 대상**이다.
      > - **값 `[7871815, 7840086]` 은 변경 없음** — Seer 좌표계 정합용으로 유효하다.
  - **⚠ 2026-07-27 감사 — 위 문장은 조건이 빠졌다(값 유지, 서술만 정정).**
    이 홈 상수(N3 7871815 / N4 7840086)는 **기동 브링업 스윙을 마친 뒤에만 유효한 기준일 수 있다.**
    `docs/ros2_driver/2026-07-09-design-inputs.md`(2026-07-27 기준 :99, :103)은 「부팅 직후 **전 노드
    `0x6064` ≈ 0**(N3=0, N4=−39 — 홈값 아님, 정상)」, 「조향이 **0에서 홈까지 3.3 s 물리 스윙(+137.3°)**」
    이라고 한다. 즉 홈 상수는 **매 기동 시 스윙으로 도달하는 절대 목표**이지 기동 직후의 현재값이 아니다.
    같은 문서 :160 은 이 항목의 「✓ 해소(전원 사이클 불변)」 판정을 **2026-07-27 재개방(미판정)** 했다.
  - **⚠ 미판정 모순이 등록돼 있다** — `docs/verified_facts/2026-07-27.md` §B-1(안전 직결):
    (❌ **정정 2026-08-03**: 이 모순은 `debt-007` 종결(2026-08-02) + 2026-08-03 조향 0° 실측으로
    **해소됐다**. 아래는 이력이며, 결론은 위 「정정 2026-08-03」 항목이 대체한다.)
    같은 시각 Seer 1040 은 FrontSteer `encoder −7,871,810` 에서 `position ≈0 rad`·`calib=True`,
    운전자 육안도 바퀴 0°(직진)인데, 판다 read 는 node3 ≈ **−1,517 counts** 였다.
    구동 노드는 두 소스가 절댓값 일치하는데 **조향 노드에서만 7.87 M counts(=137°) 어긋난다.**
    가능 설명 (a) 판다 조향 `0x6064` read 오염 / (b) 호밍 후 드라이브가 위치 기준 재설정 — **미판정**.
  - 이 값은 ~~`direct_driver.py:184`~~ → **`:160`** (`sdo_write(n, 0x607A, steer[n], size=4)`,
    DirectDriver 50 Hz TX 루프)가 기동 직후부터 조향 노드에 **절대 지령**한다(같은 파일 :17 「3/4=조향(0x607A)」).
    ⇒ ~~**판정 전에는 실차 절대 조향 지령 금지.** ← **유지된다.**~~
    > ❌ **정정 2026-08-03 18:35** — 줄번호가 밀렸다(`:184` 는 node guarding RTR 송신부, 실제는 `:160`).
    > 그리고 **「금지」라는 게이트는 존재하지 않는다** — 코드에 차단 0건, 사용자 지시·ADR 결정 기록 0건.
    > 절대 지령이라는 **사실은 맞고**, 실차 접촉을 막는 것은 `drive_headless.py` 의
    > **dry-run 기본값 + `--live`/`--backend` 명시 + preflight** 다. 문서 최상단 「최종 정정 18:35」 참조.

    ⚠ **정정 철회 2026-08-03 (적대적 검증 결과)** — 이 금지를 한때 「전제 해소」로 풀었으나
    **되돌린다.** 풀었던 근거(「2026-08-03 실측으로 조향 0° 확정」)가 반증됐다:
    - 그 측정의 역산식 `0° = CAN + Seer°×57344` 는 **항등식**이다. Seer 각도가 같은 `0x6064` 의
      아핀 변환(기울기 ×57344 = 1.0000000, 7자리 일치)이라 CAN 항이 소거되고, **바퀴를 어디에
      두고 실행해도 같은 값**이 나온다. 실제로 0.049°(2,825 c) 떨어진 자세의 08-02 로그도
      소수 4자리까지 같은 값을 낸다.
    - ⇒ 그 측정이 확정한 것은 **Seer 내부 영점 상수**이지 **물리적 직진이 아니다.**
      - ❌ **재정정 2026-08-03 17:30 — 이 철회의 사유는 (a) 에 한정된다.**
        「독립 교차검증이 항등식이다」는 **「그 측정이 counts 값을 결정하지 못한다」**는 뜻이지
        **「물리 직진 앵커가 없다」는 뜻이 아니었다.** 앵커는 **실재**한다 — Seer 1005/1040 은
        EasyDRIVE `steerOffset` 으로 **교정된** 조향각이고, 사용자가 바퀴 직진 상태에서
        Seer 앞바퀴 2축 0° 를 **육안 확인**했다(can_relay GUI 동일 확인).
        **철회 자체(라벨 「물리적 0° 확정」 무효)는 유지**하되, 이 사유를 「앵커 부재」로 확장 인용하지 말 것.
      「CAN 과 Seer 가 독립 경로」라는 서술도 거짓이다 — 판다는 Seer 가 받는 바로 그
      `0x581~0x584` 응답 프레임을 엿듣는다. 같은 프레임을 두 번 읽은 것이다.
    - 현재 물리 정렬 오차의 상한은 여전히 **육안 확인 한계(≈±1° = ±57,344 c)** 이며,
      이번에 정정한 193 c 의 **약 297배**다. 큰 항이 열린 채 작은 항만 좁혔다.
    - ~~**금지를 풀려면 非-Seer 앵커가 필요하다**~~: ① 휠 평면 대 차체 중심선 직접 계측
      (1.1 m 직선자 · 1 mm 판독 → 0.052°, **모션 0 · 지금 즉시 가능**) 또는
      ② 직진 주행 중 iAHRS yaw 변화로 차동 성분 측정(0.012° 분해능).
      - ❌ **재정정 2026-08-03 17:30 — 이 해제 조건은 「앵커 부재」를 전제로 쓴 것이라 근거가 무너졌다.**
        ①·② 는 **앵커를 새로 만드는 수단**이 아니라 **앵커 정밀도를 ±1° → 0.052° / 0.012° 로 높이는
        수단**이다. 즉 이 계측은 **counts 소수 자리(193 c = 0.0034°)까지 판정하려는 경우에** 필요하다.
        **게이트 상태는 유지**하되 **해제 여부는 사용자 결정 사항**이며, 근거가
        「앵커 부재」 → 「±1° 정밀도 한계」로 바뀌었으므로 **이 해제 조건 자체가 재검토 대상**이다.
        Claude 는 이 게이트를 **단독으로 해제하지 않는다.**
    값 `[7871815, 7840086]` 자체는 **Seer 좌표계 정합용으로 유효**하다 — 내비게이션이 Seer
    좌표를 쓰므로 이 값에 맞추는 것은 옳다. 무효인 것은 **「물리적 0° 확정」이라는 라벨**이다.

    ✅ **재확인 2026-08-03 15:40 — 이 철회·원복이 옳다. 금지 유지가 현행 정답이다.**
    - 같은 날 15:33~15:40 **호밍 10회 연속 실측**(10/10 성공, 35.0 s, 정착값 σ≈3 c)은
      **호밍 동작**을 확정했을 뿐 **非-Seer 앵커를 만들지 않았다.**
      (❌ **재정정 2026-08-03 17:30**: 「非-Seer 앵커를 만들지 않았다」는 사실이나, 이를
      **「물리 직진 앵커가 없다」로 읽지 말 것** — 앵커는 실재하고 정밀도가 ≈±1° 일 뿐이다.)
    - 추가로 반증된 것: **Seer 1005 `steer_angles` 도 1040 과 마찬가지로 `0x6064` 유래**다
      (정본 §0-3 · §12-2). ⇒ 「1005 를 독립 앵커로 쓰면 된다」는 우회로도 **성립하지 않는다.**
      (❌ **재정정 2026-08-03 17:30**: 이는 **counts 역산**에 대해서만 참이다. 1005/1040 이 EasyDRIVE
      `steerOffset` 으로 **교정된** 조향각이라는 사실 때문에, **「Seer 0° 표시 = 물리 직진」은 성립**한다.)
    - ⇒ ~~위 ①(휠 평면 대 차체 중심선 직접 계측) 또는 ②(iAHRS yaw) 가 **여전히 유일한 해제 경로**다.~~
      **금지를 다시 풀지 말 것.** 근거: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0-3 · §0-5.
      - ❌ **재정정 2026-08-03 17:30**: 「유일한 해제 경로」 서술은 **「앵커 부재」 전제로 쓴 것이라 무효**다.
        ①·② 는 **앵커 정밀도를 ±1° 이하로 높이는 수단**이다. **금지 상태는 유지**하되
        **해제 여부는 사용자 결정 사항**이고, 근거 변경(앵커 부재 → ±1° 정밀도 한계)에 따라 **재검토 대상**이다.
    - ❌ **재정정 2026-08-03 17:00 — 금지는 유지되고, 오히려 근거가 더 강해진다.**
      ⓐ 「35.0 s」는 **평균 35.07 / 중앙 35.05**, σ≈3 c 는 **모표준편차 2.80 / 3.21**(표본 2.95 / 3.38)
      이다(E4·E6). ⓑ 조향 0° `[7871815, 7840086]` 의 근거는 「실측 확정」이 아니라
      **공학적 채택값**으로 강등됐다(E8) — ~~즉 **물리 앵커는 여전히 없다.**~~
      (❌ **재정정 2026-08-03 17:30**: 강등 사유는 **앵커 부재가 아니라 앵커 정밀도 부족**이다.
      물리 앵커는 **실재**하며 정밀도가 **≈±1°(±57,344 c)** 다 — 193 c = 0.0034° 판정에는 불충분할 뿐이다.)
      ⓒ 「10회 연속 10/10」은 유효하되, 같은 날 호밍 개시는 **총 13회(성공 12 / 실패 1)** 였다(E1).
    **판정에 필요한 측정**: 제어권 미획득(비-intercept) 상태의 판다 `0x6064` read 와
    **동시각** Seer 1040 `encoder` 를 대조하여 (a)/(b) 를 분리.
    (같은 뿌리의 사고 기록: `docs/claude-mistake/2026-07-27-002_node4-unverified-command-damage.md`
    — 조향 node4 가 137° 범위 밖으로 밀려 물리적으로 갇힌 사건.)
- 본 스택은 개발 PC(python-can 미설치)에서 **stdlib 회귀 19/19 PASS**(바이트동일 7 + mock 안전게이트 6 +
  authority 롤백 6)까지 완료. 단, **실 CAN 버스 경로(`PythonCanTransport._to_msg` 바이트 대조)는 미검증**
  — python-can 머신에서 `CanFrame→can.Message` 필드 동일성 1건 대조가 실차 arm 전 게이트(ADR §미해결 M2).
- 실차 승급 순서: `--backend mock`(기본 dry-run) → python-can 머신 `_to_msg` 대조 → `--live --backend socketcan`
  attended 정차(vel 0) → 저속 크랩/스핀 방향부호. 각 승급 전 사용자 명시 승인.
- 실로봇 구동: 안전구역·E-STOP 상비. `direct/기구학` 모드는 **커널 릴레이 gate 전환 후**에만(버스 충돌 방지).
