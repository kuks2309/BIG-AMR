# 이관 — `gui.py` 리팩터링 계획 (2026-07-28 → 다음 세션)

> ## ❌ 정정 2026-08-03 15:40 — 호밍 **10회 연속 실측**으로 확정. 이 블록이 우선한다
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` **§0**
> (아래 「§10」 인용은 §0 으로 갱신됐다 — 충돌 시 §0 이 이긴다)
>
> - **호밍 10/10 성공, 35.0 s**(−리밋 탐색 31.7 s 포함). 09:58 `ERR_TIMEOUT` 1회 뒤
>   **12회 연속 성공**이며 재현되지 않는다 — 실패해도 **재시도로 충분**하다.
> - **호밍 후 정착값(10회, σ≈3 counts)** — node3 **7,882,021** / node4 **7,859,065**.
>   조향 0° 대비 **+0.178° / +0.331°**. 펌웨어 GOZERO 상수 `7882020 / 7859062` 와 1~3 counts 일치.
>   ⇒ **설계 동작이다.** 「호밍이 0° 에 놓지 않는다」는 결함이 아니라 사양이다.
> - ⚠ **「조향 0° = `[7871815, 7840086]`」 은 Seer 좌표계 기준**이고 **물리 직진은 미확인**이다 —
>   Seer 1005·1040 이 **둘 다 `0x6064` 유래**라 非-Seer 앵커가 없다(§0-3 · §11-5 · §14-3).
>   - > ❌ **재정정 2026-08-03 17:30 — 위 줄은 두 질문을 뭉갰다. 「물리 직진 앵커 부재」는 거짓이다.**
>     > - **(a) 육안 확인이 「조향 0° = 몇 counts」 역산에 기여하나 → 아니오. 판정 유지**(역산식이 항등식).
>     > - **(b) 「Seer 0° 표시 = 바퀴 물리적 직진」이 성립하나 → 예.** Seer 1005/1040 은 EasyDRIVE
>     >   `steerOffset` 으로 **교정된** 조향각이고, 사용자가 직진 자세에서 Seer 앞바퀴 2축 0° 를
>     >   **육안 확인**했다(can_relay GUI 동일 확인).
>     > - ⇒ 앵커는 **실재**하며 **정밀도가 ≈±1°(±57,344 counts)** 다 — **직진 판정에는 충분**,
>     >   **counts 소수 자리(193 c = 0.0034° = ±1° 의 1/297)에는 불충분**.
>     > - counts 정본이 **공학적 채택값**인 이유는 「앵커 부재」가 아니라 **「앵커 정밀도 부족」**이다.
>     > - 「非-Seer 계측 필요」는 **「counts 소수 자리까지 판정하려면」** 조건이 붙을 때만 참이다.
>   리팩터에서 값을 옮길 때 이 유보를 함께 옮길 것.
> - **`counts/°` = 57,344**(기울기 1.000000 실측) · **`0x6098` = 1**(Home 1, −리밋) ·
>   **리밋 스위치 실재**(10회 모두 DI `0x01`→`0x09`).
> - **폐기 — 인용 금지**: 「`Switch on disabled` 라 호밍 불가」 · 「`0x6098`=0」 · 「RstStart 1 고착」 ·
>   「`0x6064`=0 은 전원 사이클 래치」 · 「축이 이미 홈이면 무동작 즉시 완료」 ·
>   「CAN↔Seer 독립 교차검증」 · **`+0.334°` / `+0.332°`(→ `+0.331°`)**.
>
> **↓ 이하 2026-08-03 11:44 원문(이력 보존)**

> ## ❌ 재정정 2026-08-03 17:00 — 바로 위 15:40 블록의 수치·판정을 **원자료 재계산**으로 정정한다
>
> 위 원문은 삭제하지 않는다. 재계산 원자료: `Log/home_experiment_260803_153319_summary.json` ·
> `Log/home_experiment_260803_153319.jsonl` · `Log/home_experiment_260803_091956_summary.json` ·
> `Log/home_experiment_260803_095815_summary.json` · `Log/homing_edge_260803_144305.json` ·
> `Log/homing_edge_260803_144602.json` · `Log/homing_edge_260803_152520.json` ·
> `Log/seer_homing_260803_100813.jsonl` · `Log/steer_two_phase_260803_131305.jsonl` ·
> `Tools/Can_Relay/panda-firmware/board/safety/safety_seer_gate.h`
>
> - **E1. `:9` 「12회 연속 성공」 → 12회.** 2026-08-03 호밍 개시는 **총 13회 = 성공 12 / 실패 1**
>   (09:58 `ERR_TIMEOUT`(`final_state`=6, 120.26 s) 1 · 14:46 1 · 15:25 1 · 15:33~15:40 10).
>   ⚠ **「10회 연속 10/10」 은 유효.** 15:33 실행의 `baseline` 은 호밍이 아니라 **레지스터 스냅샷**이다
>   (`Tools/docking_field_kit/orin_home_experiment.py:390`).
> - **E2. 「`0x6064`=0 은 09:58 1회 · 재현 안 됨」 → [거짓] 재현된다** — 09:19 · 09:58 · 10:08
>   (10:08 은 SDO 응답 전량 0: node3 10,327/10,327 · node4 10,327/10,327) · 리부팅 뒤 14:43(node4).
>   ⚠ **인과는 미판정** — 성공한 15:33 10회 중에도 0 이 다수 반환됐다(node3 32,801/38,764 ·
>   node4 32,726/38,737). **양쪽 미판정**이다.
> - **E3. `:8` 「−리밋 탐색 31.7 s」 → WAIT 체류 31.30 s**(평균 31.302 / 중앙 31.295, 31.21~31.38).
>   31.7 s 는 **개시~RESTORE 절대시각**(평균 31.687)이다.
> - **E4. `:8` 「35.0 s」 → 평균 35.07 · 중앙 35.05**(34.99~35.16, 편차 0.17 정확).
> - **E5. `:16` 「counts/° = 57,344, 기울기 1.000000」 → node3 57,344.0(1.0000000) /
>   node4 57,344.3(1.0000049)** — 「1.000000」은 node3 에만 해당한다.
> - **E6. `:10` 「σ≈3 counts」는 모표준편차 기준** — node3 **2.80** · node4 **3.21**(표본 2.95 / 3.38).
> - **E7. `:12` 「설계 동작이다 / 결함이 아니라 사양이다」 → 과잉 확정.** 확인된 것은
>   **재현되는 정착 동작**뿐이고 사양 근거는 **미확인**이다 ⇒
>   **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**. 리팩터에서 이 유보도 함께 옮길 것.
> - **E8. `:13` 조향 0° — 값 유지, 근거 강등.** 「실측 확정」이 아니라 **공학적 채택값**이다.
>   **「실측 확정」 표현 금지** — `:230` 의 「2026-08-03 실측으로 확정됐다」도 같은 강등 대상이다.
> - **E9. `seer_home_cancel_frames()` = `safety_seer_gate.h:312-316`** — `:202` 표기는 **이미 정확**하다.

> ## ❌ 정정 2026-08-03 — 아래 2026-08-02 배너의 0° 값과 「구값 폐기」 판정은 실측으로 반증됐다
>
> 정본: `docs/homing/2026-08-03-can-relay-homing-assets.md` §10
> (값 정본은 그대로 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` 의 `steer_home_counts`)
>
> - **조향 0° 채택 정본 = `[7871815, 7840086]`**(node3 / node4).
>   `gui.py:45` · `Tools/Kinematics/chassis_kinematics.py:78` · `foil_a082.yaml` 이 이 값이다.
> - **0° 는 raw CAN 판독값이 아니다** — Seer 각도로 역산해야 한다:
>   **`0° = CAN_0x6064 + Seer_deg × 57344`**. 2026-08-02 종결은 이 식을 §4-2 에 적어 놓고도
>   **채택값에 적용하지 않고 raw 판독값을 0° 로 박았다.** node3 은 오차 6c 라 드러나지 않았고
>   node4 는 **193c** 로 드러났다.
> - **실측 근거 (2026-08-03 11:44)** — `orin_steer_crosscheck.py`, 판다 **SILENT · passthrough**
>   (제어권 미취득), **송신 0건**(AST 검사), **사용자 확인 Seer 표시 0°**, 2회 독립 실행 동일:
>   - node3: CAN `0x6064` 7,871,823 + Seer −0.000° → **0° 추정 7,871,816**
>   - node4: CAN `0x6064` 7,840,052 + Seer +0.001° → **0° 추정 7,840,087**
>   - ⚠ 「실측 0° 추정」 `[7871816, 7840087]` ≠ 「채택 정본」 `[7871815, 7840086]`(1c 차).
>     문서·코드는 **채택 정본**을 쓰고 실측 추정은 **근거로만** 병기한다.
> - ⇒ 「구값 `7871815 / 7840086` 은 **출처 없는 값**」 판정을 **철회**한다 —
>   출처는 **Seer 가 실시간으로 내는 `0x607A` 조향 목표**이고, 값도 1c 이내로 맞다.
> - ⚠ **과장 금지**: node4 차이 193c = **0.0034°** 로 **거동상 무의미**하다.
>   안전 문제가 아니라 **정본 정확성** 문제다.
> - **`7882020 / 7859062` 는 값 수정 대상이 아니다 — 라벨만 정정한다.**
>   「호밍 후 정착값」이 아니라 **펌웨어 GOZERO 상수**(호밍 후 정착 목표)이며 0° 가 아니다.
>   채택 정본 대비 편차 재계산 → **node3 +0.178° / node4 +0.331°**.
>   저장소의 **`+0.334°` · `+0.332°` 표기는 폐기된 0° 로 계산된 잔재**이니 발견 시 `+0.331°` 로 고친다.
> - `debt-007` **종결**(2026-08-02) 판정 자체는 유지된다.
>
> **↓ 이하 2026-08-02 원문(이력 보존 — 충돌 시 위 정정이 우선한다)**

> ⚠ **조향 홈 정본**: `docs/homing/2026-08-03-can-relay-homing-assets.md` §0 — 조향 0° = **`[7871815, 7840086]`**(Seer 좌표계 기준).
>     2026-08-02 종결이 이 식을 **채택값에 쓰지 않은 것**이 오류였다.

- 작성: 2026-07-28 (KST) · sess 56a709a5 · 사용자 지시로 세션 중단하며 인계
- 대상: [`Tools/amr_test_gui/gui.py`](gui.py) — Tongyi 4축 AMR 구동 테스트 GUI (실기 전용)
- 사용자 요청 원문: *"연동후 스파케티 코드 없애고 리펙토링 필요할 것 같음"* (2026-07-28 20:5x)

> **이 문서만 읽고 시작해도 되게 썼다.** 배경은 [README.md](README.md),
> 구 패키지 폐기 경위는 [ADR 2026-07-28-old-gui-removal](../../docs/adr/2026-07-28-old-gui-removal.md).

## 1. 지금 상태 (측정값, 2026-07-28 21:2x)

| 항목 | 값 |
| --- | --- |
| `gui.py` | **1,142 줄**, 단일 파일 |
| `MainWindow` 메서드 | **46 개** (+ `WheelView` 5, 모듈 함수 5) |
| 테스트 | 5 파일 · 59 함수 → **88 케이스 통과** (하드웨어·화면 없이 실행) |

`MainWindow` 하나가 **네 가지 책임**을 함께 진다:

| 책임 | 해당 메서드 |
| --- | --- |
| ① 위젯 구성 | `_build_connect` `_build_jog` `_build_wheel_adj` `_build_settings` `_build_motors` `_build_seer` `_build_status` `_build_wheel` `_build_log` `_motor_table` |
| ② CAN 프로토콜·지령 | `_sdo_write` `_drive` `_steer_axis` `_steer_to` `_jog_run` `_homing_run` `_wait_homed` `_wait_settle` `_loop` |
| ③ Seer 네트워크 | `_seer_loop` `_on_seer_data` `_on_seer_status` `_fmt_alarm` `_on_clear_fatal` `_set_alarm_color` |
| ④ 스레드·수명주기 | `scan` `_on_usb` `_on_take` `safe_release` `closeEvent` (+ 모듈 `main` 의 4 종료경로 배선) |

### 왜 고쳐야 하는가 (근거 — 이번 세션의 실제 사고 2건)

1. **주석 오염** — 20 에이전트 line-by-line 감사에서 확정 18건. 삭제된 파일을 가리키는 인용,
   폐기된 절차를 현재 동작으로 쓴 서술, 코드가 보장하지 않는 과장이 뒤섞여 있었다.
   책임이 안 갈려 있으니 "어디를 봐야 하는지" 가 코드에 드러나지 않았다.
2. **슬라이더 먹통** — 원인은 UI 레이아웃(78×20 px)이었는데, CAN 계층에서 네 번 오진했다
   ([claude-mistake 2026-07-28-014](../../docs/claude-mistake/2026-07-28-014_ui-diagnosed-without-looking-at-screen.md)).
   UI 와 프로토콜이 한 클래스에 있으니 증상이 어느 층인지 가리지 못했다.

## 2. 목표 구조 (제안 — 확정 아님, 다음 세션에서 재검토할 것)

```
Tools/amr_test_gui/
  gui.py              MainWindow — 위젯 배치와 시그널 배선만
  tongyi_can.py       TongyiCan   — SDO 인코딩·조향/구동 지령·호밍 시퀀스·폴링 (Qt 무의존)
  seer_status.py      SeerStatus  — 1040/1050 폴링·알람 (네트워크 전담, Qt 무의존)
```

**분할 기준은 "테스트가 이미 가리키는 경계"** 다. 지금 테스트가 무엇을 잡고 있는지 보면
경계가 드러난다 — `test_gui_math.py`(순수 환산)·`test_homing.py`(SDO 프레임)는 이미
Qt 없이 도는 로직을 시험하고 있고, 이것이 곧 `TongyiCan` 의 표면이다.

### 각 모듈이 가져갈 것

- **`TongyiCan`** — `_sdo_write` `_drive` `_steer_axis` `_steer_to` `_wait_settle`
  `_homing_run` `_wait_homed` `_loop`, 모듈 함수 `steer_counts` `drive_units`,
  상수 `COUNTS_PER_DEG` `STEER_HOME` `VEL_*` `STEER_LIMIT_DEG` `_ABORT`.
  판다 핸들을 주입받고, 결과는 콜백 또는 큐로 낸다(**Qt 시그널을 알지 못해야 한다**).
- **`SeerStatus`** — `_seer_loop` `_fmt_alarm` `_on_clear_fatal` 의 네트워크 부분,
  상수 `SEER_IP` `SEER_GUI`.
- **`MainWindow`** — `_build_*`, `_on_*` 핸들러, 시그널 정의, `safe_release`·`closeEvent`.

## 3. 이관 순서 (안전한 순서 — 이대로 할 것)

한 번에 다 옮기지 말 것. **각 단계마다 88건이 통과해야 다음으로 간다.**

1. **`TongyiCan` 추출** — 가장 안전하다. 순수 환산(`steer_counts`·`drive_units`)이 이미
   모듈 함수이고 테스트가 붙어 있다. `_sdo_write`·`_steer_axis`·`_drive` 를 옮기고
   `MainWindow` 은 위임만 한다. 이 단계에서 **CAN 바이트가 바뀌면 안 된다** — `test_homing.py`
   가 프레임을 바이트로 고정하고 있으니 어긋나면 즉시 실패한다.
2. **`SeerStatus` 추출** — 네트워크는 이미 독립적이다. 창 파괴 뒤 emit 하던 경쟁을 이번에
   고쳤으니(`RuntimeError` 조용한 종료) 그 처리를 **콜백 경계로 옮기는 것**이 핵심이다.
3. **`MainWindow` 정리** — 남은 것은 위젯과 배선뿐. `_build_*` 가 10 개라 그룹별로 묶을지
   그대로 둘지는 그때 판단.
4. **실기 재검증** — 리팩터 후 반드시 실기에서 ① 제어권 획득 ② 슬라이더 조향 ③ 조그 구동
   ④ 호밍까지 한 번씩 돌린다. **테스트 88건 통과는 실기 검증을 대신하지 못한다.**

## 4. 절대 바꾸지 말 것 (실기로 확인된 것)

바꾸면 로봇이 다치거나 동작이 죽는다. 리팩터는 **위치만 옮기고 값·순서는 보존**한다.

| 항목 | 값 / 순서 | 확인 |
| --- | --- | --- |
| 조향 지령 프레임 | `0x607A`(4B) + `0x6040=0x3F`(2B) — Seer 프레임과 바이트 동일 | 2026-07-28 캡처 대조 |
| 조향 counts | `STEER_HOME[n] + deg × 57344`, **±90° 클램프** | `test_gui_math.py` |
| `STEER_HOME` 값 | N3 `7871815` / N4 `7840086` — **리팩터에서 값을 옮기되 바꾸지 않는다.** 정본은 `src/Comm/CAN/can_relay/config/machine/foil_a082.yaml` `steer_home_counts` 이고 `gui.py:45` 는 사본이다 (❌ 정정 2026-08-03 추가) | 2026-08-03 실측 (`docs/homing/2026-08-03-can-relay-homing-assets.md` §10 → ❌ **정정 2026-08-03 15:40**: 정본은 **§0**. ⚠ 이 0° 는 **Seer 좌표계 기준**이고 **물리 직진은 미확인** — 값을 옮길 때 이 유보도 함께 옮길 것. ❌ **재정정 2026-08-03 17:30**: 「물리 직진 미확인」은 **거짓** — 물리 직진 앵커는 **실재**한다(Seer 1005/1040 = EasyDRIVE `steerOffset` 교정 조향각 + 사용자 직진 자세 육안 확인, can_relay GUI 동일 확인). 함께 옮길 유보는 **「앵커 정밀도 ≈±1°(±57,344 c) — counts 소수 자리 판정에는 불충분」**이다) |
| 호밍 | 조향축 3·4 만 · `0x6040=0x86` → `0x6099:00=2500` → `0x60FB:04=1` | 실기 캡처 |
| 호밍 완료 판정 | `0x6041` bit15 의 **0→1 전이**(1 만 보면 오판) | `test_homing.py` |
| `0x6098` | **쓰지 않는다** — 덮으면 리셋 모드가 꺼진다 | Handbook + 캡처 (❌ **정정 2026-08-03 15:40**: 실측 `0x6098` = **1**(Home 1, −리밋). 「=0 이라 호밍 비활성」 서술은 폐기. **「쓰지 않는다」 규칙은 유효**) |
| crab 순서 | 구동 0 → 조향 → **양축** 정착 확인 → 구동 | `_jog_run` |
| 호밍 중 `0x6064` | 실위치가 아니라 0 — 각도 갱신을 멈춘다 | 실기 캡처 |
| 종료 4경로 | 전부 `safe_release()` 로 모인다(멱등) | `test_safe_release.py` |
| 슬라이더 | **실측을 되쓰지 않는다.** 되먹이면 먹통이 된다 | `test_slider.py` |
| 슬라이더 크기 | 폭 ≥240 px · 높이 ≥24 px · 1 px ≤1° | `test_slider.py` |
| 판다 | **1 PC 1대 원칙** — 선택 UI 를 두지 않는다 | 사용자 결정 |
| 소프트 E-STOP | **두지 않는다** — 하드웨어 E-STOP 이 권위 | 사용자 결정 |
| dry-run·시뮬레이터 | **없다** — 실기 전용 | 사용자 결정 |

> ❌ **정정 2026-08-03 15:40** — 위 표의 호밍 관련 행 3개에 실측을 덧붙인다
> (정본 `docs/homing/2026-08-03-can-relay-homing-assets.md` §0)
>
> - **호밍 행**: 시퀀스 `0x6040=0x86` → `0x6099:00=2500` → `0x60FB:04=1` 은 그대로 유효하다.
>   덧붙일 것 — **`0x60FB:04` 에 `0` 을 쓰면 호밍이 취소된다.** 저장소에 취소 경로가 셋 있다:
>   펌웨어 `safety_seer_gate.h:312-316` `seer_home_cancel_frames()` ·
>   USB `usb_comms.h:411-414` `0xea` wValue=0 · ROS2 `driver_node.py:194-195,311` `~/home_cancel`.
>   **`gui.py` 에만 취소가 미구현**이다(불가능한 것이 아니다). 리팩터 시 `TongyiCan` 에
>   `cancel_homing()` 을 둘 수 있고, `gui.py:958` 과 같은 SDO 호출로 값만 `0` 이면 된다.
> - **호밍 소요**: 펌웨어 시퀀서 **10회 실측 35.0 s**(−리밋 탐색 31.7 s), **성공률 10/10**.
>   `gui.py` 의 완료 대기 90 s 타임아웃은 이 값에 비추어 충분하다.
> - **호밍 도달 지점**: 조향 0° 가 아니라 펌웨어 GOZERO 상수 **7,882,020 / 7,859,062**
>   (10회 정착 실측 **7,882,021 / 7,859,065**, σ≈3c). 조향 0° 대비 **+0.178° / +0.331°** — **설계 동작**.
>   리팩터에서 「호밍 후 = 0°」로 가정하는 코드를 만들지 말 것.
> - ❌ **재정정 2026-08-03 17:00** — 위 두 항목의 수치·판정 정정:
>   ⓐ 소요는 **평균 35.07 / 중앙 35.05 s**(34.99~35.16), **−리밋 탐색(WAIT) 체류는 31.30 s**,
>   31.7 s 는 **개시~RESTORE 절대시각**이다(E3·E4). 90 s 타임아웃이 충분하다는 판단은 그대로 유효.
>   ⓑ σ≈3c 는 **모표준편차 2.80 / 3.21**(표본 2.95 / 3.38)이다(E6).
>   ⓒ 「**설계 동작**」은 **과잉 확정** — **「재현되는 정착 동작(상수 적정성은 별건 — debt-016)」**(E7).
>   「호밍 후 = 0° 로 가정 금지」는 그대로 유효하다.

## 5. 함께 정리할 것 (선택)

- **주석 규율** — 이번에 세운 기준을 유지한다: 코드 주석에 외부 인용(파일:줄·캡처 이름·
  mistake id)을 쌓지 않는다. 근거는 [README.md](README.md) 가 든다. 저장소가 바뀔 때마다
  코드가 같이 썩는 것이 이번 오염의 원인이었다.
- **`_build_*` 10 개** — 위젯 구성이 `MainWindow` 을 길게 만드는 주범이지만, 쪼개면
  오히려 배치를 읽기 어려워질 수 있다. 쪼갤지 말지는 실제로 옮겨본 뒤 판단할 것.

## 6. 미해결 부채 (리팩터와 별개, 그대로 이관)

| id | 내용 |
| --- | --- |
| ~~debt-007~~ | ~~`STEER_HOME` 기준계 미판정 — **값은 실측 없이 바꾸지 말 것**~~ → ❌ **정정 2026-08-03**: **종결**(2026-08-02). 조향 0° = `[7871815, 7840086]` 이 2026-08-03 실측으로 확정됐다. **미해결 부채가 아니므로 리팩터 이관 대상에서 뺀다.** 값 정본은 `foil_a082.yaml` `steer_home_counts` 하나 — 위 §4 표 참조 |
| debt-010 | 조향 추종 실패 시 FAULT 래치 없음 — 그 회차 구동만 취소되고 재시도가 안 막힌다 |
| debt-011 | 실기 상호작용(실제 CAN 왕복·Seer 응답) 자동 검증 없음 |
| debt-012 | Node Guarding RTR 을 PC 가 못 보낸다 — Seer guard 가 대신 만족시킨다는 가정 미확인 |

**미검증 항목 하나 더** — `_redraw_wheel` 이 제어권 없을 때 쓰는 **Seer position 의 부호**가
판다 실측과 같은 방향인지 확인되지 않았다. 두 축 모두 0° 인 상태에서만 대조했다.
Seer 로 한쪽 조향을 틀어 두 표의 부호를 대조할 것.

> ❌ **정정 2026-08-03 15:40** — 근거가 생겼다(다만 GUI 화면 자체는 여전히 미대조).
> 2026-08-03 30 s 동시 샘플링에서 **1040 `motor_info.position` 은 `0x6064` 의 아핀 변환**임이 확정됐다
> (6-counts 디더가 counts 단위로 일치, 기울기 × 57,344 = 1.0000000 — §12-2). **양의 상관**이므로
> 판다 직독과 **같은 방향**이다. ⇒ 「1040 은 독립 경로」라는 전제로 교차검증을 주장하지 말 것.
> 반면 Seer **표시 조향각**(1005 `steer_angles`)은 CAN counts 와 **음의 상관**이며
> (역산식 `0° = CAN + Seer°×57344` 의 `+` 가 그 부호다), **1005 도 `0x6064` 유래**로 확정됐다(§14-3).

## 7. 이번 세션에서 배운 함정 (다음 세션이 같은 데 빠지지 않도록)

1. **UI 증상은 화면을 먼저 캡처한다.** 코드만 보고 네 번 오진했다. 캡처는 수 초,
   내가 택한 우회는 25 만 프레임 파싱 + 계측 삽입 + 재시작 3회였다.
   `~/.claude/capture_screen.py --mode window --window-id <id>` 로 즉시 볼 수 있다.
2. **같은 증상을 두 번째로 들으면 진단 수단을 바꾼다.** 같은 층을 더 깊게 파지 말 것.
3. **위젯 크기는 눈이 아니라 `geometry()` 로 잰다.** 이 세션에서만 같은 종류의 레이아웃
   결함이 2건 나왔다(버튼 겹침 · 슬라이더 78 px).
4. **`pkill -f 'gui[.]py'` 는 반드시 단독 호출로.** 다른 명령과 묶으면 패턴이 자기
   명령줄에 걸려 셸을 죽인다(이 세션에서 2회 발생).
5. **공유 워킹트리에서 `git checkout -b` 금지** — HEAD 가 전역 이동해 다른 세션을 끌고 간다.
   `git worktree add` 로 별도 트리에서 커밋할 것
   ([2026-07-28-009](../../docs/claude-mistake/2026-07-28-009_shared-tree-head-moved-by-checkout.md)).

## 8. 시작 전 확인

```bash
cd Tools/amr_test_gui
QT_QPA_PLATFORM=offscreen python3 -m pytest test/ -q      # 88 passed 확인 후 착수
can_relay                                                  # 실기 기동 (~/.bashrc alias)
```

⚠ 리팩터 중에도 **다른 세션이 같은 워킹트리에서 `gui.py` 를 편집할 수 있다.**
이 세션에서 실제로 다른 세션이 `safe_release` 를 이 파일에 추가했다. 착수 전
`git status` 로 확인하고, 커밋은 세션 브랜치에 격리할 것.
