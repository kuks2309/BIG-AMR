# 이 줄을 지우고 아래에 tailscale auth key 를 붙여넣으세요 (tskey-auth-…).
# 발급: https://login.tailscale.com/admin/settings/keys  (Reusable + 만료기간 권장)
# 이 파일에 키가 없으면 setup_orin_dev_pc.sh 는 수동 인증(sudo tailscale up)을 안내합니다.
