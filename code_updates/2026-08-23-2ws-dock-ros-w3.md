# 2026-08-23 — 2WS 도킹 W3: 액션 서버 조립 (빌드·스모크 OK, 폐루프 SIL 대기)

> 약어: IDL(Interface Definition Language) · SIL(Software-In-the-Loop)

- 사전승인: `docs/adr/2026-08-23-2ws-dock-ros-w3.md`
- 사전조사(실측): mux `source_40 "dock"`(`/motion/wheel_cmd/dock`, timeout 200 ms) 기예약,
  전환은 `/select_motion_source`(line_follow 선례), 지령은 `WheelSetArray{velocity, steering}×2`.

## 신설

- `trnav_2ws_interfaces/action/AMRMotionDockApproach.action` (공개표면) — goal 목표
  자세(스테이션 프레임)·접근축(base_link deg)·허용오차(d/lat/yaw)·timeout,
  feedback 페이즈+3축 오차, result stop_reason 7종
- `trnav_2ws_dock_ros/` — `dock_approach_action_server.cpp`(약 460줄):
  - 관측: `/wall_pose` → `wallPoseToDockObs`, 신선도 상한 0.2 s + 유예 1.0 s
    (유예 중 steer-hold 정지 — 원위치 판정의 대기 규약 승계), yaw runaway 는
    `/wall_pose` yaw 를 `imuAccumStep`/`imuRunaway` 로 누적 감시
  - 페이즈: PRE_ALIGN(`geomEntryTranslateNeed`>0 일 때 도크면 평행 crab)
    → APPROACH(`distPidStep`+`phase4Vcap`+`geomEntryDeltaBiased`+속도차 yaw
    `composePhase4Wheels`) → VERIFY(정지 30사이클 평균 판정) → REAPPROACH(1회 한도)
  - mux: goal 수락 시 40 전환(응답은 루프에서 비블로킹 확인 — 승인 전 지령 금지),
    **모든 종료 경로에서 정지 지령 → 복원(기본 0 joystick)** 순서
- config(게인·arm=0.6039 초기값 — **재튜닝 대상 명기**) · launch

## 검증

- `trnav_2ws_interfaces`·`trnav_2ws_dock_ros` colcon 빌드 성공
- 스모크: 액션 `/amr_motion_dock_approach` 노출·파라미터 로드 확인.
  **goal 은 보내지 않았다** — 실기 mux 가 살아 있는 시스템에서 전환을 유발하지 않기 위함.
  폐루프 검증은 SIL 로 한다(아래).

## 남은 일

1. **폐루프 SIL**: 시뮬레이터가 `/motion/wheel_cmd/dock` 을 소비해 기구학 적분으로
   가상 로봇을 움직이고 벽 스캔을 재생성 → wall_localizer → dock 서버 전 체인 검증
   + 게인 초기 확인 (wall_localizer sim 킷 확장)
2. HIL(실기): 접근축·조향 부호 검증(사용자 입회) → 게인 튜닝 → 도킹 반복 정밀도 측정
3. 함수표 상세(노드 콜백 표) 확정 — 인벤토리 §1-2 앵커의 실줄 갱신
