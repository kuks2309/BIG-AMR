# 조향각(steering angle)은 어디서 오고 어떻게 요청하는가 — 2026-08-03

- 사용자 지시: "이거 스티어링 각도 보는부분 어디에있음? 어떻게 값 요청하면 받을수있는지 알려줘" / "이게 그 스티어링 이라해야하나.. 그 모터 각도 받아오는게 맞지?"
- 대상: 원본 RoboshopPro 의 Map/Control 탭, 조그 패드 오른쪽의 빨강·초록 반원 다이얼 2개 (0.00° 표기)
- 근거 등급: **`[동작]`** — 이 프로젝트에 이미 구현·배선되어 있는 코드 경로를 읽은 결과. 벤더 API 필드 정의는 `[문서]` 로 따로 표시.

---

## 0. 한 줄 요약

**SEER API(Application Programming Interface) 1005 (Query Robot Speed) · 포트 19204** 의 `steer_angles` / `steer` 필드가 그 값이다. 단위는 **라디안**, 배열 순서는 **모델 파일 순서**(듀얼 조향이면 `[front, rear]`). 이 프로젝트는 이미 매 fast 틱마다 이걸 폴링해 스냅샷으로 흘려보내고 있으므로, 프론트엔드는 **새로 API 를 부를 필요 없이** `snapshot.steer_angles` 만 읽으면 된다.

---

## 1. "모터 각도"가 맞나 — 무엇의 각도인가

맞다. 다만 **구동 모터의 회전량이 아니라 조향 축(steer axis)의 각도**다. 셋을 구분해야 한다.

| 구분 | API | 무엇 | 단위 |
|---|---|---|---|
| **조향각** | **1005** `steer_angles` / `steer` | 조향륜이 **향하고 있는 방향** — 화면의 다이얼이 이것 | rad |
| 구동 회전량 | 1018 (`encoder()`) | 모터 엔코더 펄스 | pulse |
| 모터 드라이버 상태 | 1040 (`motor()`) | per-motor CAN(Controller Area Network)/드라이버 상태 | — |

1500 모델 파일에는 **조향 전용 모터가 따로** 있다 — `FrontSteer`, `RearSteer` (구동륜은 `FrontWalk`, `RearWalk`). Foil_A082 모델로 실측 확인된 사실이다 ([model.py:125-134](../../core/seer_core/model.py#L125-L134)). 그러니 "조향 모터 각도"라는 표현은 물리적으로 맞다.

> `[미확인]` — 1005 가 돌려주는 값이 조향 모터 엔코더를 **그대로** 읽은 것인지, 아니면 감속비 보정·융합을 거친 값인지는 벤더 문서에 나오지 않는다. 문서는 "조향각"이라고만 한다. 캘리브레이션 목적으로 원시 엔코더가 필요하면 1018 을 따로 봐야 한다.

**다이얼이 2개인 이유**: 이 로봇이 듀얼 스티어(`multiSteers`)라서다. 배열 원소 하나당 다이얼 하나 — 앞바퀴 조향각, 뒷바퀴 조향각.

---

## 2. 로봇에게 값 요청하는 법

### 2.1 API 제원

| 항목 | 값 |
|---|---|
| API 번호 | **1005** (Query Robot Speed) |
| 포트 | **19204** (Robot Status 대역 1000~1999) |
| 요청 바디 | **없음** (length = 0) |
| 응답 번호 | 11005 (요청 번호 + 10000) |

포트 대역은 로봇이 **강제**한다. 같은 요청이라도 제 포트에서는 `ret_code=0`, 다른 포트에서는 `ret_code=60000 "error api type"` 이 온다 ([client.py:315-329](../../core/seer_core/client.py#L315-L329)).

### 2.2 프레임 포맷

16바이트 헤더 + JSON(JavaScript Object Notation) 바디. 1005 는 바디가 없다.

```
struct 포맷: ">BBHIH6s"   (빅엔디안, 16 bytes)

  0x5A        sync
  0x01        version (RBK(Robokit) 3.4)
  serial      uint16, 요청마다 증가
  0x00000000  body length (1005 는 0)
  0x03ED      api number = 1005
  00*6        reserved
```

([client.py:21-24](../../core/seer_core/client.py#L21-L24))

### 2.3 응답에서 꺼낼 필드

| 필드 | 의미 | 비고 |
|---|---|---|
| `steer_angles` | 다조향륜 각도 **배열** (rad) | **모델 파일 순서** → 듀얼이면 `[front, rear]` `[문서]` |
| `steer` | 단일 조향륜 각도 **스칼라** (rad) | 조향륜 1개짜리 섀시 `[문서]` |
| `vx` / `vy` / `w` | 실측 섀시 속도 (m/s, m/s, rad/s) | 같은 응답에 함께 실려 온다 |

차동(differential)·옴니(omni) 섀시는 조향각 필드가 **아예 없다**. 물리적으로 조향축이 없으니 당연하다.

`[문서]` 표시 항목의 근거: `REF/robot-status-api/005-query-robot-speed.md` L33(`steer`), L40(`steer_angles`). RE 대조 결과 우리 구현과 **불일치 없음** ([_re대조_01_api.md:80](_re대조_01_api.md)).

### 2.4 직접 확인

```python
from core.seer_core.client import RobokitClient

c = RobokitClient("192.168.44.82")
print(c.robot_speed())
# {'vx': 0.0, 'vy': 0.0, 'w': 0.0, 'steer_angles': [0.0, 0.0], 'ret_code': 0}
```

메서드 정의: [client.py:424-432 `robot_speed()`](../../core/seer_core/client.py#L424-L432) → `self.call("status", API_ROBOT_SPEED)`.

---

## 3. 이 프로젝트에서의 전체 경로

```
로봇 (19204)
  │  1005 Query Robot Speed
  ▼
worker.py  _poll_fast()          ← 매 fast 틱, 위치(1004)·내비(1020)와 같은 틱
  │  speed.get("steer_angles") or [speed["steer"]]
  ▼
self._live["steer_angles"]       ← 스냅샷 필드
  │
  ▼
브리지 스냅샷 스트림
  │
  ▼
robotSlice  selectSnapshot
  │  snap.steer_angles  (rad)
  ▼
SteerGauges.tsx                  ← rad → deg 변환 후 다이얼 렌더
  │
  ▼
JogControl.tsx L318              ← 조그 패드 오른쪽에 배치
```

### 3.1 폴링 — [worker.py:1133-1165](../../core/seer_web_bridge/worker.py#L1133-L1165)

```python
def _poll_fast(self):
    loc  = self._client.call("status", 1004)
    nav  = self._client.call("status", seer.API_NAV_STATUS)   # 1020
    speed = self._client.robot_speed()                        # 1005
    ...
    steer_angles=(list(speed.get("steer_angles") or [])
                  or ([speed["steer"]] if "steer" in speed else [])),
```

**설계 결정 2가지:**

1. **같은 틱에 읽는다.** 자세(1004)와 조향각(1005)을 같은 틱에 읽어야 UI(User Interface)가 "이번 사이클 자세 + 지난 사이클 바퀴각" 을 섞어 그리지 않는다.
2. **배열 하나로 통일한다.** `steer_angles`(다조향륜)가 있으면 그대로, 없고 `steer`(단일)만 있으면 `[steer]` 로 감싼다. 섀시 종류가 무엇이든 UI 는 **모양 하나만** 그리면 된다.

푸시 스트림이 끊겨 개별 폴링으로 전환될 때도 1005 는 폴백 목록에 들어 있다 ([worker.py:1258](../../core/seer_web_bridge/worker.py#L1258), ADR-004).

### 3.2 표시 여부 판정 — [worker.py:1046-1058](../../core/seer_web_bridge/worker.py#L1046-L1058)

1500 모델에서 섀시 종류를 읽어 `chassis_mode` 로 발행한다.

```python
self._live["chassis_mode"] = chassis_mode(model)
# "steer" / "multiSteers" / "differential" / "omni" / None
```

`chassis_mode` 는 모델의 chassis 드롭다운에서 선택된 `childKey` 를 읽는다 ([model.py:59-63](../../core/seer_core/model.py#L59-L63)).

### 3.3 렌더 — [SteerGauges.tsx](../../src/features/nav/SteerGauges.tsx)

```tsx
const STEERING_CHASSIS = new Set(["steer", "multiSteers"]);
const SWEEP_DEG = 90;

if (!mode || !STEERING_CHASSIS.has(mode) || angles.length === 0) return null;

{angles.map((rad, i) => <Dial key={i} deg={(rad * 180) / Math.PI} />)}
```

| 항목 | 값 | 이유 |
|---|---|---|
| 표시 조건 | `chassis_mode ∈ {steer, multiSteers}` **AND** `steer_angles.length > 0` | 둘 다(OR 아님). 조향 섀시인데 아직 각도가 안 온 상태에서 빈 행이 나타났다 사라지는 걸 막는다 |
| 단위 변환 | `deg = rad * 180 / π` | 로봇은 rad, 화면은 ° |
| 스윕 | ±90° 클램프 | 넘는 각도가 **감기면 반대편 락(lock)으로 읽힌다**. 끝단에 붙여 두는 게 안전 |
| 색 | 왼쪽 반원 초록 `#22c55e`, 오른쪽 반원 빨강 `#ef4444` | 벤더 툴 색을 그대로 |
| 배경 | 없음 | 조작부가 아니라 **읽기 전용 표시**라, 지도 위에 그대로 얹어 스캔을 가리지 않는다 |

타입 정의: [types.ts:72-77](../../src/api/types.ts#L72-L77)

```ts
chassis_mode?: string | null;
/** Live steering angles in RADIANS, model-file order (dual steer =
 *  [front, rear]). Empty on a chassis that does not steer. */
steer_angles?: number[];
```

---

## 4. 프론트엔드에서 쓰는 법

새로 API 를 부를 필요 없다. 스냅샷에 이미 실려 있다.

```tsx
import { useAppSelector } from "@store/hooks";
import { selectSnapshot } from "@store/robotSlice";

const snap = useAppSelector(selectSnapshot);
const anglesRad = snap?.steer_angles ?? [];        // [front, rear] (rad)
const anglesDeg = anglesRad.map(r => r * 180 / Math.PI);
const mode = snap?.chassis_mode;                    // "multiSteers" 등
```

`selectSnapshot`: [robotSlice.ts:121](../../src/store/robotSlice.ts#L121)

---

## 5. 관련 파일 색인

| 파일 | 역할 |
|---|---|
| [core/seer_core/client.py](../../core/seer_core/client.py) | `API_ROBOT_SPEED = 1005` (L51), `robot_speed()` (L424), 포트/프레임 정의 (L21-29) |
| [core/seer_web_bridge/worker.py](../../core/seer_web_bridge/worker.py) | `_poll_fast()` 1005 폴링 (L1133), `_load_geometry()` chassis_mode (L1046) |
| [core/seer_core/model.py](../../core/seer_core/model.py) | `chassis_mode()` (L59), `wheels_from_model()` — FrontSteer/RearSteer (L125) |
| [src/features/nav/SteerGauges.tsx](../../src/features/nav/SteerGauges.tsx) | 다이얼 렌더 |
| [src/features/nav/JogControl.tsx](../../src/features/nav/JogControl.tsx) | L318 배치 |
| [src/api/types.ts](../../src/api/types.ts) | `Snapshot.steer_angles` / `chassis_mode` 타입 (L72-77) |
| [docs/analysis/_re대조_01_api.md](_re대조_01_api.md) | L80 — 1005 벤더 문서 대조 결과 (불일치 없음) |
